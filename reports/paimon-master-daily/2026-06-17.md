# Apache Paimon Master Daily Report - 2026-06-17

## Summary

范围：`cd5460321378175dfb9aa704e40093761a7bab59..c28080796cbbe7c54ad18f0c4fad6ecc5b1219c0`，共 3 个 first-parent 提交。当前 Apache upstream `master` head 是 `c28080796cbb`（2026-06-16 22:18:58 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次变化集中在三个方向：global index 从单列扩展到多列索引框架，Flink PostgreSQL CDC 修复 `timestamp(n<=3)` 被误识别为 `BIGINT` 的问题，Spark DataEvolution 增加 raw BLOB 列的 `MERGE INTO UPDATE` 支持并顺带修正 dedicated blob/vector 文件的 row-id 覆盖判断。对 `release-1.4` 最明确的 bugfix 是 PostgreSQL CDC timestamp 映射，release 分支的 `PostgresRecordParser` 仍缺少 `Timestamp.SCHEMA_NAME -> TIMESTAMP(3)` 分支，建议优先评估 backport。Spark raw BLOB update 相关代码在 release 分支已有部分 DataEvolution 路径但缺少关键实现，是否 backport 取决于 release-1.4 是否需要暴露 Spark raw BLOB update 能力。

本地 `git fetch` 仍受沙箱网络限制：`git clone/fetch` GitHub DNS 失败，提升审批连续超时；本次真实 upstream 数据通过 GitHub connector 的 compare/commit/file 接口获取，并逐个读取 commit diff。`release-1.4` 影响判断基于 GitHub commit search、release 分支关键生产文件内容和实际 hunk 对照。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Global index framework | 1 | 新增 `ConstantGlobalIndexReader`、`GlobalIndexMultiColumnWriter`；`GlobalIndexerFactory` 支持 primary field + extra fields；index meta 保存 extra field ids；scanner 能从 extra field 命中多列索引并为短覆盖范围补 all-hit bitmap；Flink/Spark create/drop global index procedure 支持逗号分隔多列。 | release-1.4 仍是单列 global index 路径，缺少新增 reader/writer 和 extra field meta。偏新功能，非必须 backport。 |
| Flink PostgreSQL CDC | 1 | `PostgresRecordParser.extractFieldType` 在 int64 分支新增 Debezium `io.debezium.time.Timestamp` 到 `TIMESTAMP(3)` 的映射，并新增单测覆盖 millis timestamp、micro timestamp 和普通 int64。 | release-1.4 生产文件存在且仍将 millis timestamp fall through 到 `BIGINT`。这是明确 bugfix，建议 backport。 |
| Spark/DataEvolution raw BLOB update | 1 | Spark `MERGE INTO` 支持更新 raw BLOB 列：引入 marker columns、raw blob fallback row、partial write 多文件 first-row-id 分配；`ConflictDetection` 对 dedicated blob/vector 文件使用 range 覆盖判断；`BlobFallbackRecordReader` 全 placeholder 时返回 null row；新增各 Spark 版本 blob update 测试。 | release-1.4 有 Spark DataEvolution 基础路径，但没有 raw BLOB marker/fallback、`containsExactly`、dedicated vector 排除等关键 hunk；部分生产文件缺失。按功能暴露情况评估 backport。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | Touched production files on release-1.4 | Likely release-1.4 impact |
| --- | --- | --- | --- | --- |
| `40c57b9f59` | PostgreSQL CDC `timestamp(n<=3)` 的 Debezium logical type `io.debezium.time.Timestamp` 现在映射为 `TIMESTAMP(3)`，避免 schema path 是 timestamp、record parser path 是 `BIGINT` 导致 CDC job 启动后持续重启。 | GitHub commit search 只找到 master commit `#8222`；release 文件 hunk 对照未发现等价修复。 | `paimon-flink/paimon-flink-cdc/src/main/java/org/apache/paimon/flink/action/cdc/postgres/PostgresRecordParser.java` 存在，int64 分支只有 `MicroTimestamp` 和 `MicroTime`，缺少 `Timestamp.SCHEMA_NAME`。 | 高。PostgreSQL CDC 同步 millisecond precision timestamp 时会直接影响可用性，建议 backport 到 release-1.4。 |
| `c28080796cb` | Spark DataEvolution `MERGE INTO UPDATE` 支持 raw BLOB 列，避免过去直接拒绝或无法正确写回 blob placeholder/null；同时 dedicated blob/vector 文件 row-id 覆盖判断更严格，避免 adjacent data files 被误当成同一覆盖区间。 | GitHub commit search 只找到 master commit `#8175`；release hunk 对照未发现 marker/fallback、`containsExactly`、dedicated vector 排除等关键实现。 | `MergeIntoPaimonDataEvolutionTable.scala`、`DataEvolutionTableDataWrite.scala`、`RowRangeIndex.java`、`ConflictDetection.java` 存在；`BlobFallbackRecordReader.java` 在 release-1.4 未找到。 | 中。若 release-1.4 需要 Spark raw BLOB update 或 dedicated blob/vector partial update，建议拆分评估 backport；否则偏新增能力。 |
| `c48daf6580` | 多列 global index 的 reader/writer/meta/scanner/procedure 框架；其中短 range index padding 修正了多列/extra field 查询时 AND 组合可能丢掉长 range 命中的问题。 | GitHub commit search 只找到 master commit `#7933`；release hunk 对照未发现新增 reader、multi-column writer、extra field meta。 | release-1.4 存在单列 `GlobalIndexScanner` 和 `GlobalIndexerFactory`，但 `ConstantGlobalIndexReader.java` 不存在，factory 仍只接受单个 `DataField`。 | 低到中。主要是新功能；只有计划在 release-1.4 支持多列 global index 时才需要 backport。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `c48daf6580` | Global index framework | 新增 `ConstantGlobalIndexReader` 用固定 bitmap padding 短覆盖范围；新增 `GlobalIndexMultiColumnWriter`；`GlobalIndexerFactory#create` 支持 `indexField + extraFields`；`GlobalIndexMeta`/`IndexManifestFileHandler` 保存并展示 extra field ids；`GlobalIndexScanner` 按 primary/extra field 建 reader，extra field 查询能复用多列索引；Spark/Flink create/drop index procedure 接受多列并按完整 indexed field ids 删除；Spark builder 过滤 schema 中缺少索引列的旧文件。 | release-1.4 只有单列 global index 实现，缺少新增 class 和 meta 语义。偏 feature，不建议无需求 backport。 |
| 2 | `40c57b9f59` | Flink PostgreSQL CDC | `PostgresRecordParser` 的 int64 分支新增 `Timestamp.SCHEMA_NAME` 检查并返回 `DataTypes.TIMESTAMP(3)`；新增 `PostgresRecordParserTest` 构造 Debezium JSON，验证 millis timestamp -> `TIMESTAMP(3)`、micro timestamp -> `TIMESTAMP(6)`、plain int64 -> `BIGINT`。 | release-relevant bugfix。release-1.4 同一文件仍缺少该分支，建议 backport。 |
| 3 | `c28080796cb` | Spark/DataEvolution raw BLOB update | `RowRangeIndex` 新增不合并相邻 range 的 create overload 和 `containsExactly`；`ConflictDetection` 对 normal file 要求 exact range，对 blob/vector dedicated file 要求单个 normal file 覆盖；Spark MergeInto 识别 raw BLOB update columns，生成不冲突 marker columns，避免从 target 读取 raw blob；DataEvolution writer 用 `FallbackMappingRow` 写入 `BlobPlaceholder` 或 null，并允许一次 partial write 产生 normal/blob/vector 多文件；各 Spark 版本新增 `BlobUpdateTest`。 | 部分 release 文件存在但缺少关键 hunk；`BlobFallbackRecordReader` 缺失。若业务依赖 Spark raw BLOB update，应进一步拆分 backport。 |

## Checks Run

| Check | Result |
| --- | --- |
| GitHub compare `cd5460321378175dfb9aa704e40093761a7bab59..master` | `ahead_by=3`，current head `c28080796cbbe7c54ad18f0c4fad6ecc5b1219c0`。 |
| GitHub commit fetch for all 3 commits | 已逐个读取 `c48daf6580`、`40c57b9f59`、`c28080796cb` 的 diff/files，并按实际 patch 归类。 |
| GitHub commit search for cherry/equivalent commits | `#7933`、`#8222`、`#8175` 均只定位到 master 对应 commit；未发现 release-1.4 cherry-pick 提交。 |
| GitHub release-1.4 file hunk checks | 检查了 `PostgresRecordParser.java`、`RowRangeIndex.java`、`ConflictDetection.java`、`MergeIntoPaimonDataEvolutionTable.scala`、`DataEvolutionTableDataWrite.scala`、global-index scanner/factory 等关键文件；未发现关键修复 hunk。 |
| Local shell `git clone/fetch` | 受沙箱网络限制失败或审批超时；未能执行真实本地 `git fetch upstream master release-1.4`，已用 GitHub connector 补足数据和远端提交。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `c28080796cbbe7c54ad18f0c4fad6ecc5b1219c0`
- `last_processed_commit_date`: `2026-06-16 22:18:58 +0800`
- `last_report_date`: `2026-06-17`
- `last_report_path`: `reports/paimon-master-daily/2026-06-17.md`

# Apache Paimon Master Daily Report - 2026-07-09

## Summary

范围：`5b797ef63fba5e98bf4fd285fb5a34877c91ee81..9c201b724f7f3b4577a892124d8c729358b66eaa`，共 18 个 first-parent 提交。当前 Apache upstream `master` head 是 `9c201b724f7f`（2026-07-09 13:23:03 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 111 个文件，`+8738/-619`。

本次主线集中在两个大功能块：data-evolution compaction 支持物化删除并重写 row id，以及 Flink unaware-bucket append table 支持可选的 JobManager-side coordinator commit。其他变更包括 PyPaimon projection/filter 正确性修复、Ray data-evolution merge source column pruning、Spark global-index build options 修复、Spark MAP selected-key pushdown 读类型准备、vector global index 训练采样，以及 API/type/hash/default-size 小修复。

`git cherry -v upstream/release-1.4 upstream/master 5b797ef63fba...` 显示本范围 18 个提交均为 `+`，没有等价 patch 已进入 `upstream/release-1.4`。较明确的 1.4 直接 bugfix 候选是 `4f4df2e74`、`8cc7724ba`、`01284b9aa`、`8033b4763` 和 `0f8d3c9af`；`1ea6e749e` 修的是 master 新增的外部 `partition_predicate` 入参路径，`release-1.4` 尚无该入口，不是直接回补项。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时不展开 hunk，又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch。release-1.4 判断使用了 range/per-commit `git cherry`、stable patch-id、`git grep` 和 `git show upstream/release-1.4:<path>`。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Flink sink coordinator commit | 1 | `9c201b724` 新增 `sink.coordinator-commit.enabled`，将 unaware-bucket append streaming commit 移到 writer `OperatorCoordinator`，新增 writer pending committable state、coordinator state serializer、watermark alignment、restore/recommit 逻辑和 IT/unit tests。 | 1.4 没有 `sink.coordinator-commit.enabled` 或 coordinator commit 类；默认关闭的新功能，非 bugfix。 |
| Core data-evolution compaction | 1 | `85351acdf` 新增 `data-evolution.compaction.rewrite-row-ids`，拆分 normal/blob/materialize-deletion compact task，物化 DV 时重写 row id、drop affected global indexes，并让 Flink/Spark compaction 固定在同一 snapshot。 | 1.4 有 data-evolution compaction 基础路径，但这是新能力；除非产品决定引入 rewrite-row-ids，否则不按 bugfix 回补。 |
| PyPaimon read / Ray merge | 3 | `8033b4763` 为 projection 外过滤列内部拓宽 read schema 并最终投影回输出列；`1ea6e749e` 重写外部 partition predicate 到 partition-row index；`cd290a22a` 只读取 Ray merge source 需要的 join/update/condition 列。 | `8033b4763` 是直接 1.4 正确性候选；`1ea6e749e` 的外部 partition predicate 入口 1.4 不存在；`cd290a22a` 是性能/IO 优化。 |
| Spark global index / MAP pushdown | 2 | `0f8d3c9af` 让 Spark global-index build 使用 merged build options 中的 `global-index.row-count-per-shard` 与 `global-index.build.max-parallelism`；`5c3d4bcf` 增加 MAP selected-key metadata、Spark optimizer rule 和 read type rewrite，但 rule 仍 TODO 未启用。 | `0f8d3c9af` 是直接 1.4 候选；MAP selected-key 是 master 新能力准备，1.4 无路径。 |
| API / type / Arrow / build metadata | 6 | 修复 `MultisetType.equalsIgnoreFieldId/isPrunedFrom`、`FunctionChange.UpdateDefinition.hashCode`、composite type `defaultSize()` int overflow；同时修正 Arrow 错误信息、benchmark label 和 bundle 注释中的 lz4 groupId。 | 前三个 API/type 修复是直接 1.4 候选；Arrow/benchmark/bundle 变更不改变核心运行语义。 |
| Vector global index | 1 | `0e22fa885` 升级 native vector index 到 0.2.0，新增 `train.sample-ratio`，训练阶段按采样比例和批次向 native trainer 送样本，避免一次性大数组训练。 | `release-1.4` 没有同名 vector module/path；新功能，不直接适用。 |
| Docs | 4 | 修正文档里的 Flink procedure partition OR wording、`compact_database` mode 默认值、`sink.writer-coordinator.cache-memory` 默认值，并新增 Flink lineage 文档。 | 文档类变更，不进入 1.4 生产 bugfix 判断。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `a16880edd` | benchmark 名称/注释 copy-paste 修正。 | patch-id `75c8dc457578`; cherry `+`。 | 只改 micro-benchmark test/source label。 | 不需要回补。 |
| `a4255cca8` | bundle POM 注释里的 lz4-java groupId 修正。 | patch-id `39cd6a6a4c9b`; cherry `+`。 | 只改注释中的 shade include 示例。 | 不需要回补。 |
| `4f4df2e74` | `MultisetType` 比较/裁剪判断需要递归 element type，否则 ROW element 的 field id 差异不会被正确忽略或裁剪。 | patch-id `26b2dcb0a42b`; cherry `+`。 | 1.4 `MultisetType` 只有 `equals`，没有 `equalsIgnoreFieldId` / `isPrunedFrom` override。 | 直接 bugfix 候选。 |
| `8cc7724ba` | `FunctionChange.UpdateDefinition.hashCode` 漏掉 name，导致不同函数同 definition 的 hash 与 equals 不一致。 | patch-id `65a086187665`; cherry `+`。 | 1.4 仍是 `Objects.hash(definition, definition)`。 | 直接 bugfix 候选。 |
| `01284b9aa` | `MapType`、`MultisetType`、`RowType` default size 累加可能 int overflow。 | patch-id `97d27e6f301c`; cherry `+`。 | 1.4 仍直接用 `+` / `sum()`，没有 `MathUtils.addSafely`。 | 直接 bugfix 候选。 |
| `356b8bb3c` | Arrow writer 失败信息不应写成 ORC file。 | patch-id `08e69a1ed6ec`; cherry `+`。 | 1.4 Arrow writer 路径存在；这是诊断信息修正。 | 可选低优先级回补。 |
| `151344e51` | Flink procedure 文档 partition filter 示例应描述 OR。 | patch-id `8635bec42c8c`; cherry `+`。 | 文档修复。 | 可选文档回补。 |
| `b8c9de7a8` | Flink procedure 文档中 `compact_database` 默认 mode 从 combined 修正为 divided。 | patch-id `2bc5bbcd6bd0`; cherry `+`。 | 文档修复。 | 可选文档回补。 |
| `f2ae9f680` | 写性能文档中 `sink.writer-coordinator.cache-memory` 默认值修正为 2GB。 | patch-id `dfee2b2fc3c6`; cherry `+`。 | 文档修复。 | 可选文档回补。 |
| `8033b4763` | PyPaimon projection 不包含过滤列时，旧逻辑会丢弃 reader filter 或无法安全执行字符串 filter；补丁内部读取 predicate-only columns，row-level fallback 后再投影输出。 | patch-id `d60896056cad`; cherry `+`。 | 1.4 `SplitRead` 明确“Only apply filter when all predicate columns are in read schema”，`TableRead` 无 scan/read output projection 分离。 | 直接正确性候选。 |
| `1ea6e749e` | 外部 partition predicate 可能带 full-schema index，需要 trim/rewrite 到 partition-row layout。 | patch-id `3a4ece0ae4f4`; cherry `+`。 | 1.4 `FileScanner.__init__` 没有 `partition_predicate` 入参，只从 `predicate` 派生 partition predicate。 | 不直接适用。 |
| `0f8d3c9af` | Spark global-index build 忽略 action/build options，且 build parallelism 固定为 task count。 | patch-id `5bd970c86d17`; cherry `+`。 | 1.4 已有 `GLOBAL_INDEX_BUILD_MAX_PARALLELISM`，但 builder 仍从 table options 取 rows-per-shard 并 `parallelize(taskList, taskList.size())`。 | 直接 bugfix 候选。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `0e22fa885` | Vector global index | 7 files，`+375/-27`。新增 `train.sample-ratio` option，native vector index 版本到 0.2.0；训练改为 `VectorIndexTrainer` 分批添加样本，按均匀采样选择训练向量，并限制单个 float array 大小。 | 新功能；1.4 无对应 vector module/path。 |
| 2 | `a16880edd` | Benchmark | 2 files，`+2/-2`。Arrow write benchmark 名称从 read 改 write，lookup reader benchmark 注释从 writing 改 reading。 | 非生产路径。 |
| 3 | `a4255cca8` | Bundle | 1 file，`+1/-1`。注释中的 lz4-java groupId 从 `org.lz4` 改 `at.yawk.lz4`。 | 注释修复。 |
| 4 | `4f4df2e74` | API types | 2 files，`+106/-0`。`MultisetType` 新增 `equalsIgnoreFieldId` 和 `isPrunedFrom`，递归委托 element type，并补充 multiset row element 测试。 | 直接 1.4 候选。 |
| 5 | `8cc7724ba` | API function | 2 files，`+59/-1`。`UpdateDefinition.hashCode` 从重复 definition 改为 `Objects.hash(name, definition)`，测试覆盖 equals/hash 一致性。 | 直接 1.4 候选。 |
| 6 | `01284b9aa` | API types | 4 files，`+81/-3`。`MapType`、`MultisetType`、`RowType` defaultSize 改用 `MathUtils.addSafely`，新增 overflow 饱和到 `Integer.MAX_VALUE` 的测试。 | 直接 1.4 候选。 |
| 7 | `356b8bb3c` | Arrow | 1 file，`+1/-1`。`ArrowBundleWriter` 二次写失败时异常信息改为 writing arrow record。 | 低优先级诊断修复。 |
| 8 | `151344e51` | Docs | 1 file，`+1/-1`。Flink procedure partition filter 示例把两个 partition 的关系描述为 OR。 | 文档修复。 |
| 9 | `b8c9de7a8` | Docs | 1 file，`+2/-2`。`compact_database` mode 文档标注 `divided` 为默认，`combined` 为非默认。 | 文档修复。 |
| 10 | `f2ae9f680` | Docs | 1 file，`+1/-1`。写性能文档中 writer coordinator cache memory 默认值改为 2GB。 | 文档修复。 |
| 11 | `8033b4763` | PyPaimon read | 16 files，`+413/-128`。新增 predicate field discovery、arrow unsafe string predicate fallback、wrapper metadata adoption、predicate-extra internal read schema、最终 output projection，以及 projection/filter/data-evolution/parallel tests。 | 直接 1.4 候选。 |
| 12 | `1ea6e749e` | PyPaimon scanner | 2 files，`+80/-4`。外部 partition predicate 先 drop non-partition leaves，再按 partition fields 重写 index，避免 reordered partition keys 静默过滤错误数据。 | 1.4 缺外部入参，不直接适用。 |
| 13 | `cd290a22a` | PyPaimon Ray | 3 files，`+179/-6`。Ray data-evolution merge 根据 join key、source refs 和 condition 中的 source columns 选择 source projection，matched update/not-matched insert 先 `select_columns` 再 rename。 | 性能/IO 优化；可按需评估。 |
| 14 | `0f8d3c9af` | Spark global index | 2 files，`+102/-9`。`DefaultGlobalIndexTopoBuilder` 使用传入 `Options` 读取 rows-per-shard 和 max parallelism，并校验正数；Spark parallelize 并发度变为 `min(taskCount, maxParallelism)`。 | 直接 1.4 候选。 |
| 15 | `85351acdf` | Core data-evolution | 27 files，`+1618/-426`。新增 rewrite-row-ids option、blob/materialize/normal compact task、commit preparation、global-index dropper、DV range 删除能力；Flink/Spark compaction 源和 sink 传入同一 latest snapshot。 | 新能力；不按 bugfix 直接回补。 |
| 16 | `5c3d4bcf` | Spark MAP selected-key | 16 files，`+928/-4`。新增 selected-key metadata 编码/解析、Spark 3.2-4 rule shell、common optimizer rule、`BaseScan` read type rewrite 和 scan description；extension 里保留 TODO，尚未启用 rule。 | 新能力准备；1.4 无路径。 |
| 17 | `fc09fc740` | Docs | 2 files，`+40/-1`。新增 Flink lineage 文档并加入 sidebar，说明 FLIP-314、OpenLineage 配置和 Flink 2.x 限制。 | 文档新增。 |
| 18 | `9c201b724` | Flink sink | 23 files，`+4749/-2`。新增 coordinator commit option、writer operator pending state、coordinator event/state/watermark/restore commit 逻辑、discarding sink path和大量 IT/unit tests；仅 streaming unaware-bucket append、checkpoint enabled、write-only、无 precommit compact、单并发 checkpoint 时允许启用。 | 默认关闭新功能；1.4 无路径。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | sandbox DNS 失败后用联网权限重跑成功。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | sandbox DNS 失败后用联网权限重跑成功；`upstream/master` 前进到 `9c201b724f7f`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | sandbox DNS 失败后用联网权限重跑成功；`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse 5b797ef63fba..upstream/master` | 18 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat 5b797ef63fba..upstream/master` | 111 files changed, 8738 insertions(+), 619 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <18 commits>` | 对 18 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <18 commits>` | 对 18 个 commit 均已执行并检查实际 patch hunks；大提交按关键生产文件补充拆看。 |
| `git cherry -v upstream/release-1.4 ...` | range-level 全部为 `+`；对 bugfix 候选逐个执行后也均为 `+`。 |
| `git show <commit> \| git patch-id --stable` | 对 bugfix/修复类提交计算 stable patch-id，并在影响表记录。 |
| release-1.4 targeted code checks | 检查 API types/function、PyPaimon read/scanner、Spark global-index builder、data-evolution compaction、coordinator commit、vector/MAP 路径存在性和旧代码片段。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `9c201b724f7f3b4577a892124d8c729358b66eaa`
- `last_processed_commit_date`: `2026-07-09 13:23:03 +0800`
- `last_report_date`: `2026-07-09`
- `last_report_path`: `reports/paimon-master-daily/2026-07-09.md`

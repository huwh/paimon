# Apache Paimon Master Daily Report - 2026-07-05

## Summary

范围：`f2de0ee193e7991575f4418db44343f35fef92bb..9ea8120590944a7a31af0a046ad7ec0db0ae5021`，共 7 个 first-parent 提交。当前 Apache upstream `master` head 是 `9ea812059094`（2026-07-04 22:29:58 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线集中在 common bitmap range-list 性能、core scan TopN 正确性、PyPaimon multimodal Ray 读、Daft 写失败 abort、global-index 搜索过滤删除行、nested partial-update null-key 策略，以及 Flink BLOB descriptor 写入容错。最值得 release-1.4 关注的是 `c77405ea` 的非分区过滤下 TopN pushdown 正确性修复；`077788e1` 是 global-index + deletion-vector 搜索正确性候选，但 1.4 代码结构不同，需要适配；`d5644175` 落在 1.4 不存在的 Daft 路径；`9ea81205` 是 BLOB descriptor fetch failure 的 opt-in 容错能力，生产路径存在但配置和前置 plumbing 需要一起评估。

`git cherry -v upstream/release-1.4 upstream/master f2de0ee193e7991575f4418db44343f35fef92bb` 显示本次 7 个提交均为 `+`，未发现等价 patch 已 cherry-pick 到 release-1.4。所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时不展开 hunks，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 并检查实际补丁。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Common bitmap utility | 1 | `f4282276` 将 `RoaringNavigableMap64.toRangeList()` 从 boxed iterator 路径改为 dense bitmap 下基于 `select` 的 range 探测，并保留 sparse iterator 路径。 | 性能优化；1.4 有同一方法但不是正确性 bugfix。 |
| Core scan / query correctness | 1 | `c77405ea` 在 `DataTableBatchScan.applyPushDownTopN()` 遇到 non-partition filter 时跳过 split-level TopN pruning，避免先按 stats 剪 split 后再 read-time 过滤导致漏行。 | 直接 1.4 候选。 |
| PyPaimon multimodal / Ray | 1 | `865b2c96` 为 multimodal `scan().to_ray()` 和 `map_with_blobs()` 增加 Ray Dataset BLOB descriptor 读与 worker-side payload fetch。 | 新 Python API；1.4 只有基础 Ray datasource，没有 multimodal 路径。 |
| PyPaimon Daft sink | 1 | `d5644175` 在 Daft write 失败时调用 `table_write.abort()`，只在 `prepare_commit()` 成功后 close，避免失败任务遗留未提交数据文件。 | Daft 路径在 1.4 缺失，非直接适用。 |
| Global index search / deletion vectors | 1 | `077788e1` 增加 `GlobalIndexLiveRowFilter`，将 deletion-vector live rows 合入 vector / full-text include-row filters，Java、Python、Tantivy 路径和测试同步更新。 | 1.4 有 vector/full-text global-index 和 DV，但实现结构较旧；属于需适配的正确性候选。 |
| Nested aggregation | 1 | `ce9817b9` 为 nested partial-update aggregation 增加 `nested-key-null-strategy=<merge|ignore|error>`，默认 `merge` 保持旧语义。 | 新配置；非普通 bugfix。 |
| BLOB descriptor writes / Flink | 1 | `9ea81205` 增加 `blob-write-null-on-fetch-failure`，并让 Flink descriptor exists-check、`BlobFormatWriter`、HTTP 错误解析和测试覆盖 invalid URI / HTTP 400 / rate-limit。 | 生产路径存在；是 opt-in 容错能力，1.4 需连同配置和 descriptor null-write plumbing 评估。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `c77405ea` | TopN pushdown 原来只排除了 limit、PK、DV 等场景；当 WHERE 是非分区过滤时，split stats 剪枝发生在 read-time filter 前，可能丢掉真实 post-filter top-N 所在 split。补丁新增 `snapshotReader.hasNonPartitionFilter()` guard，并保留 partition-only filter 的 TopN pushdown。 | stable patch-id `c78c5ef64cf7`; cherry `+`。 | 1.4 `DataTableBatchScan.applyPushDownLimit()` 已检查 `snapshotReader.hasNonPartitionFilter()`，但 `applyPushDownTopN()` 没有；TopN pushdown 和相关 scan path 存在。 | 高。普通 batch scan 的 `ORDER BY ... LIMIT` 加非分区 WHERE 可能返回漏行/错行；建议优先评估回补。 |
| `d5644175` | Daft sink 失败路径从 unconditional `close()` 改为 exception 时 `abort()`，避免前面 micropartition 已写出的文件在后面 schema mismatch 后遗留。 | stable patch-id `771ca9625c62`; cherry `+`。 | `paimon-python/pypaimon/daft/daft_datasink.py` 在 release-1.4 不存在。 | 不直接适用。若 1.4 没有 Daft sink，则无运行时路径。 |
| `077788e1` | global-index vector/full-text search 原来不会剔除 deletion-vector 删除的 row id，旧 index 文件仍可能命中已删除行；补丁从当前 snapshot 计算 live rows，并传入 include-row filters。 | stable patch-id `6213a95f2f5f`; cherry `+`。 | 1.4 有 `VectorReadImpl` / `FullTextReadImpl`、`VectorSearch.includeRowIds`、`OffsetGlobalIndexReader`、deletion vectors；但 master 的 `AbstractVectorRead` / structured full-text query 形态在 1.4 不同。 | 中高，需适配。开启 DV 且使用 global-index vector/full-text search 时，1.4 可能返回已删除行；建议单独做 1.4 形态的 backport 评估。 |
| `9ea81205` | BLOB descriptor 写入时，invalid URI、HTTP 400/420 等非 404 fetch failure 默认会失败；补丁新增 opt-in 配置，允许写 NULL，并修复 HTTP error response close / 状态码识别。 | stable patch-id `3a5eae2c8eff`; cherry `+`。 | 1.4 有 Flink `path_to_descriptor`、`FlinkRowWrapper`、`BlobFormatWriter` 和 BLOB descriptor write 路径；但没有 `blob-write-null-on-missing-file` / `blob-write-null-on-fetch-failure` 配置链。 | 条件影响。若 1.4 需要容忍外部 BLOB fetch failure，可回补为新 opt-in 能力；否则不是默认行为 bugfix。 |
| `f4282276` | `toRangeList()` 性能优化，dense bitmap 避免逐个 row id 迭代。 | stable patch-id `068ca6f430cd`; cherry `+`。 | 1.4 仍是 TODO iterator 实现。 | 性能候选，不是正确性 bugfix；可按大范围 DV/global-index 使用压力决定。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `f4282276` | Common bitmap | `RoaringNavigableMap64.toRangeList()` 新增 dense 判断、`select` 指数/二分探测 range end、primitive `LongIterator` sparse fallback；测试覆盖高 32-bit 边界、大连续区间和 `Long.MAX_VALUE`/`Long.MIN_VALUE` wrap-around 不合并。 | 性能优化；1.4 有相同旧实现，可选回补。 |
| 2 | `c77405ea` | Core scan TopN | `DataTableBatchScan.applyPushDownTopN()` 新增 `snapshotReader.hasNonPartitionFilter()` guard；`TableScanTest` 验证非分区 filter 保留全部 splits，分区 filter 仍可剪到 3 个 splits。 | 直接高优先级候选。 |
| 3 | `865b2c96` | PyPaimon Ray multimodal | 抽出 `fetch_blob_bodies()`；`ScanQuery.to_ray()` 以 `blob-as-descriptor=true` 读 Ray Dataset 并挂载 FileIO/BLOB metadata；`map_with_blobs()` 在 Ray worker batch 中 fetch payload，并拒绝非 BLOB / 未知 descriptor 列；文档和 Ray tests 覆盖。 | 新 Python API；1.4 缺 multimodal scan/table 形态，非直接 bugfix。 |
| 4 | `d5644175` | PyPaimon Daft sink | `PaimonDataSink.write()` 将 `table_write.close()` 移出 `finally`，异常时先尝试 `table_write.abort()` 再 re-raise；测试构造 valid micropartition 后 schema-mismatched micropartition，断言无 parquet/orc/avro 文件残留。 | 1.4 路径缺失，非直接适用。 |
| 5 | `077788e1` | Core/Python/Tantivy global-index | Java 新增 `GlobalIndexLiveRowFilter`，DV 增加 `forEachDeletedPosition`；vector/full-text read 将 live rows 与 scalar matched rows 求交后传 include-row ids；FullTextSearch/OffsetGlobalIndexReader/Tantivy Java+Rust+Python 同步处理 include filters；测试覆盖删除 indexed rows 不返回。 | 正确性候选，但需按 1.4 `VectorReadImpl`/`FullTextReadImpl` 旧结构适配。 |
| 6 | `ce9817b9` | Core nested aggregation | `FieldNestedPartialUpdateAgg` 接入 `CoreOptions.NestedKeyNullStrategy`，null nested key 可 `merge`、`ignore` 或 `error`；factory 读取 field option；文档和 aggregator tests 覆盖 first aggregation 与后续 merge。 | 新配置；默认 `merge` 兼容。 |
| 7 | `9ea81205` | Core/Flink BLOB descriptor writes | 新增 `CoreOptions.BLOB_WRITE_NULL_ON_FETCH_FAILURE`；`HttpClientUtils` 关闭非 200 response 并识别 HTTP 状态 / invalid URI；`FlinkRowWrapper` 在配置开启时延后非 404 exists-check failure；`BlobFormatWriter` 对 404 走 missing-file、对非 404/invalid URI/fetch failure 写 NULL；Flink IT 和 format tests 覆盖。 | 条件候选，需连同 BLOB descriptor null-write 配置链评估。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净且本地跟踪 `origin/codex/paimon-master-daily-report`。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | sandbox DNS 失败后提权重跑成功。 |
| `git fetch upstream master release-1.4` | sandbox DNS 失败后提权重跑成功；`upstream/master` 前进到 `9ea812059094`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git rev-list --first-parent --reverse f2de0ee193e7991575f4418db44343f35fef92bb..upstream/master` | 7 个 first-parent commit，均已纳入报告。 |
| `git diff --stat f2de0ee193e7991575f4418db44343f35fef92bb..upstream/master` | 范围共 57 个文件，2982 insertions / 168 deletions。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <7 commits>` | 对 7 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <7 commits>` | 对 7 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master f2de0ee193e7991575f4418db44343f35fef92bb` | 7 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对 7 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 TopN scan、global-index vector/full-text、deletion vectors、Daft/Ray/multimodal 路径、BLOB descriptor write、nested aggregation 和 bitmap utility。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `9ea8120590944a7a31af0a046ad7ec0db0ae5021`
- `last_processed_commit_date`: `2026-07-04 22:29:58 +0800`
- `last_report_date`: `2026-07-05`
- `last_report_path`: `reports/paimon-master-daily/2026-07-05.md`

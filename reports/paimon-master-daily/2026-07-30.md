# Apache Paimon Master Daily Report - 2026-07-30

## Summary

范围：`a4bfec1bd2242847510c09ff7faf017a9ee2efbb..27f4656bb033e8fdb9b9574559914fdcc09d5485`，共 19 个 first-parent 提交，92 个文件变更，`+4641/-896`。本轮 `upstream/master` 前进到 `27f4656bb033`（2026-07-30 13:19:27 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

主要变化集中在 format table 对提交暂存目录的读写隔离、data-evolution split read 下推与 row-id assignment 优化、REST/Python dependency read context、Flink Blob dedicated split read、paimon-vindex 0.3.0、Spark compact 内存优化、filesystem shaded jar 修复，以及若干 Python 依赖/错误信息修正。按实际 patch、`git cherry` 和 release-1.4 代码路径判断，本轮最值得关注的 1.4 候选是 format table staging 修复（`6602ec963`、`cf2eb3fff`）、shared `_temporary` 清理修复（`9bcbbc4a6`）、`ParallelExecution.close()` 等待读者清理（`a578bada1`）、filesystem impl jar nested `META-INF` 修复（`e23b7e116`）、Iceberg table-location metadata 路径修复（`6365ffeba`）、PostgreSQL UUID CDC 映射（`d42625a5d`）、Spark compact driver 侧反序列化内存优化（`cccc4456a`）和 Flink Blob dedicated split read（`c6964a7a7`）。REST dependency read context 与 Python 聚合/DataSketches patch 在 release-1.4 当前路径缺失或能力不一致，需单独评估。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Format table / FileIO staging safety | 3 | `FormatTableScan` 改为按层级 `listStatus`，跳过 `_temporary`、`__magic*`、`.hive-staging*` 等 staging tree；overwrite 清理旧数据时只删 committed data file；`RenamingTwoPhaseOutputStream.clean()` 不再删除共享 `_temporary` 目录。 | release-1.4 同路径仍用 recursive `listFiles` 和文件名判断，且 `clean()` 删除整个 `_temporary`；是直接候选。 |
| Data evolution read / row-id assignment | 3 | `DataEvolutionSplitRead` 支持 file index、bitmap index 和安全 predicate pushdown；row-id assignment planner 增加大连续区间跳过、manifest row-range projection、collector 压缩/排序优化。 | release-1.4 有 `DataEvolutionSplitRead` 的 TODO 下推缺口；但 row-id assignment planner 类缺失，`27f4656bb` 不可原样回摘。 |
| REST / dependency reads | 3 | Java/Python REST dependency read 增加 `X-Paimon-Read-Via` context，并修复 custom REST metastore 被覆盖；Blob dependency table 加载使用 dependency context。 | release-1.4 有 `CatalogEnvironment`，但无 `READ_VIA_HEADER`、`BLOB_DESCRIPTOR_SOURCE_TABLE` grep 命中；不是直接小 patch。 |
| Python / PyPaimon | 5 | Python CI 使用 `pypaimon-rust==0.3.0`；聚合 unsupported message 更新；DataSketches 从基础依赖拆成 `theta-sketch` extra，常规聚合不再强依赖。 | release-1.4 Python 聚合实现未命中 `theta_sketch`/`_datasketches` 路径；多为 master Python 能力。 |
| Filesystem packaging | 1 | 各 filesystem impl 解包 shaded jar 时排除 jar 自身 `META-INF/**`，license checker 新增 nested `META-INF/versions/*/META-INF` 检查，避免 JaCoCo/扫描工具遇到重复类元数据。 | release-1.4 同 poms 仍有 `META-INF/versions/11/META-INF/maven/**` 特例，缺少通用排除和 checker；直接候选。 |
| Iceberg metadata | 1 | `IcebergCommitCallback.catalogDatabasePath` 只在 `catalog-storage` 下要求 `<db>.db` warehouse layout，`table-location` 允许非 warehouse database location。 | release-1.4 同路径存在且旧逻辑先统一检查 `<db>.db`；直接候选。 |
| Flink CDC / source | 2 | PostgreSQL `uuid` 映射为 `STRING`；Flink dedicated split read path 透传 read type/blob-as-descriptor，用 `FlinkRowDataWithBlob` 处理 BLOB 字段。 | release-1.4 同 CDC/Blob/Flink source 路径存在，均为可达生产路径。 |
| Spark | 2 | Compact procedure 反序列化 commit messages 时边反序列化边释放 serialized byte[]；Spark E2E 过滤 shutdown 噪声。 | compact 内存优化适用于 release-1.4；E2E patch 为测试稳定性。 |
| Vector index | 1 | paimon-vindex 升到 0.3.0，替换/新增 `ivf-sq`、`ivf-rq`、`diskann`，搜索参数改为 `VectorSearchParams`/automatic；删除自带 JNI loader。 | release-1.4 是 `paimon-lumina` 路径而非 `paimon-vector`，不是同路径 bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `6602ec963` | format table 读分区时不再递归进入 committer staging tree，避免把 `__magic*/_temporary/.hive-staging*` 里的未提交 `part-*` 文件当 committed data 读，尤其可避免 0-byte magic placeholder 触发 Parquet "length is too low"。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `FormatTableScan` 仍有 `isDataFileName`，`createSplits` 用 `fileIO.listFiles(path, true)` 后仅按文件名过滤。 | 高；format table 生产读路径可达，建议优先 backport。 |
| `cf2eb3fff` | format table overwrite 清理旧数据时复用 `listDataFiles`，只删除 committed data file；对静态分区前缀保留其他 writer 的 staging tree，同时处理 value-only 默认分区名例外。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `FormatTableCommit.deletePreviousDataFile` 仍递归 `listFiles(partitionPath, true)`，再按文件名删除。 | 高；影响并发写/overwrite 安全，需结合 `6602ec963` 回补。 |
| `9bcbbc4a6` | `RenamingTwoPhaseOutputStream.Committer.clean()` 只删除本 writer 暂存文件，不再删除共享 `_temporary` 目录，避免破坏其他 writer 正在打开或即将写入的 staging 目录。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `RenamingTwoPhaseOutputStream` 仍调用 `fileIO.deleteDirectoryQuietly(tempPath.getParent())`。 | 高；common FileIO commit path，建议 backport。 |
| `a578bada1` | `ParallelExecution.close()` 在所有 reader 已完成时用 `shutdown()` 并 `awaitTermination`，确保 reader close 结束后再返回；未完成时仍 `shutdownNow()`，并移除测试中的 sleep。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `ParallelExecution.close()` 仍直接 `shutdownNow()`；`IndexBootstrapTest` 依赖 sleep 的现象与 patch 描述一致。 | 中高；避免资源泄漏/flaky close，common 路径可回补。 |
| `e23b7e116` | filesystem impl shaded jar 解包排除嵌套 `META-INF/**`，license checker 明确拒绝 nested multi-release `META-INF`，避免 JaCoCo/扫描工具重复类名或异常。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 filesystem poms 仍只特例排除 `META-INF/versions/11/META-INF/maven/**`；checker 仍有 `isJavaxManifest` 特例。 | 中高；构建/发布 artifact 质量问题，适合回补。 |
| `6365ffeba` | Iceberg metadata `table-location` 不再要求数据库路径是 `<db>.db`；只有 `catalog-storage` 仍需要 warehouse layout，并给出明确异常信息。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `IcebergCommitCallback.catalogDatabasePath` 在读取 `storageLocation` 前统一检查 `dbPath.endsWith(".db")`。 | 中高；外部/跨账号 catalog database location 下 table-location 可达。 |
| `d42625a5d` | PostgreSQL CDC `uuid` 类型映射为 Paimon `STRING`，保留 Debezium `io.debezium.data.Uuid` 的 canonical 文本。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `PostgresTypeUtils` 存在但无 `PG_UUID` 分支。 | 中；Postgres sync 遇到 uuid 列会受影响，patch 小。 |
| `cccc4456a` | Spark compact collect serialized commit messages 后，反序列化时将 byte[] 置空释放引用，降低 driver 同时持有 serialized 与 deserialized messages 的峰值内存。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 `CompactProcedure` 三处仍 `collect()` 后遍历反序列化并保留 serialized list。 | 中；大 compact 任务内存风险，属于性能/稳定性回补候选。 |
| `c6964a7a7` | Flink `MonitorSource -> ReadOperator` dedicated split path 透传 read type 和 `blob-as-descriptor`，读 BLOB 字段时使用 `FlinkRowDataWithBlob`，修复 dedicated split generation 下 BLOB 投影/descriptor 语义。 | `git cherry` 为 `+`，未等价合入 release-1.4。 | release-1.4 有 `BLOB_AS_DESCRIPTOR`、`FlinkRowDataWithBlob`，但 `MonitorSource` 仍 `new ReadOperator(readBuilder::newRead, ...)`，`ReadOperator` 未处理 BLOB 字段。 | 中；BLOB + dedicated split generation 可达，建议按 1.4 Flink API 验证后回补。 |
| `1a67a4e10` / `1b57cea5b` | REST dependency read header/context，并保留 custom REST metastore。 | `git cherry` 均为 `+`。 | release-1.4 未命中 `READ_VIA_HEADER`、`BLOB_DESCRIPTOR_SOURCE_TABLE`；只有基础 `CatalogEnvironment`。 | 低/需设计；不是同路径小修。 |
| `8335a3cef` | `DataEvolutionSplitRead` file-index/predicate pushdown，含系统字段、缺失列、被覆盖列、bitmap + deletion vector 等正确性边界。 | `git cherry` 为 `+`。 | release-1.4 同类 `withFilter` 仍是 TODO，说明功能缺口存在。 | 中；性能优化为主，语义边界复杂，回补需完整测试而非摘一小段。 |
| `27f4656bb` | data-evolution row-id assignment 优化：大连续逻辑 row-id run 可跳过、manifest row-range projection、collector radix sort/chunk compression、APPEND manifest merge rebound。 | `git cherry` 为 `+`。 | release-1.4 只有 compact task，无 `DataEvolutionRowIdAssignmentPlanner`/`DataEvolutionRowIdReassigner` 同路径类。 | 低；master-only 架构，不直接影响 1.4。 |
| `bca8fd9c1` / `e4ef362d8` / `81f7bbe71` | Python DataSketches optional、聚合 unsupported 文案、Python REST dependency context。 | `git cherry` 均为 `+`。 | release-1.4 未命中 Python `theta_sketch`/`_datasketches` 聚合路径，也未命中 dependency read header。 | 低；master Python 能力，需按 1.4 Python 实现另行判断。 |
| `319e09c3d` / `376ccd3cb` | Python CI 版本与 Spark E2E 输出过滤。 | `git cherry` 均为 `+`。 | 只改 workflow/test。 | 无生产影响。 |
| `d4920972a` | paimon-vindex 0.3.0 新 index types/search params/native API 适配。 | `git cherry` 为 `+`。 | release-1.4 无 `paimon-vector` module，仍是 `paimon-lumina` 路径。 | 低；非同路径 bugfix。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `319e09c3d` | Python CI | `.github/workflows/paimon-python-checks.yml` 将 Python 3.10 的 `pypaimon-rust` 测试版本从 `0.2.0` 提到 `0.3.0`。 | CI-only，无生产影响。 |
| 2 | `1a67a4e10` | REST / catalog | Java REST API 新增 `X-Paimon-Read-Via` header；`CatalogEnvironment.dependencyReadContext()` 为 REST dependency read 注入 outer table identifier；append data-evolution read 和 blob descriptor source table 加载改用该 context。 | release-1.4 缺少对应 header/source-table 路径，需单独设计。 |
| 3 | `6602ec963` | Format table read | `FormatTableScan` 新增 `listDataFiles`，按目录层级跳过 hidden/staging tree；`SplitEnumerator` 不再 recursive `listFiles` 后仅按 leaf name 判断；测试覆盖 magic/_temporary/hive staging。 | 高影响 1.4 候选。 |
| 4 | `1b57cea5b` | REST / catalog | `dependencyReadContext()` 在已有 custom REST metastore 时不再强制覆盖为 `rest`，新增 custom-rest 测试。 | 依赖 `1a67a4e10` 的新 context；1.4 不是直接小修。 |
| 5 | `a578bada1` | Common parallel read | `ParallelExecution.close()` 根据 latch 状态选择 `shutdown`/`shutdownNow` 并最多等待 1 分钟；新增 reader close wait 测试，删除 sleep。 | 中高 1.4 候选。 |
| 6 | `8335a3cef` | Core data evolution read | `DataEvolutionSplitRead.withFilter` 接入 file index/format predicate pushdown；单文件路径可用 file/bitmap index，merge group 只用 file index 跳过整组；过滤系统字段、缺失列、被旧文件覆盖列，bitmap selection 与 deletion vector 合成。 | release-1.4 有同类 TODO；功能复杂，需完整回补验证。 |
| 7 | `e4ef362d8` | Python aggregation | 更新 PyPaimon aggregation unsupported 文案，反映当前支持 aggregator 扩展后仅 `hll_sketch`/`rbm64` 延后。 | release-1.4 聚合实现不匹配，低影响。 |
| 8 | `81f7bbe71` | Python REST / catalog | PyPaimon 增加 `dependency_read_context`、`CatalogFactory.create_from_context`，blob view lookup 通过 dependency context 读取 REST dependency table。 | release-1.4 未命中 header/context 相关路径，低影响。 |
| 9 | `e23b7e116` | Filesystem packaging | Azure/COSN/GS/OSS/S3 impl 解包时排除 `META-INF/**`；CI jar checker 新增 nested multi-release META-INF 检测，并移除 javax manifest 特例。 | 中高 1.4 候选。 |
| 10 | `6365ffeba` | Iceberg metadata | `<db>.db` warehouse layout 检查移动到 `CATALOG_STORAGE` 分支；`TABLE_LOCATION` 允许任意 database location，并新增正反测试。 | 中高 1.4 候选。 |
| 11 | `d42625a5d` | Flink CDC | `PostgresTypeUtils` 为 `uuid` 增加 `STRING` 映射，IT case 和 unit test 覆盖 uuid 值和 null。 | 中 1.4 候选。 |
| 12 | `cf2eb3fff` | Format table overwrite | `FormatTableCommit.deletePreviousDataFile` 使用 partition level-aware `listDataFiles`；静态分区前缀 overwrite 不删其他 writer staging，value-only 默认分区名仍可清理。 | 高影响 1.4 候选，需与 `6602ec963` 一起看。 |
| 13 | `bca8fd9c1` | Python dependencies | 从基础 requirements 移除 `datasketches`，将 theta sketch 依赖放入 `pypaimon[theta-sketch]` extra；`theta_sketch` aggregator 在使用时懒加载并给出安装提示。 | release-1.4 未命中同聚合路径，低影响。 |
| 14 | `9bcbbc4a6` | Common FileIO staging | `RenamingTwoPhaseOutputStream.clean()` 不再删除 `_temporary` 目录，只删除自身 staged temp file；测试覆盖其他 writer 文件、空 staging dir、未 commit clean。 | 高影响 1.4 候选。 |
| 15 | `cccc4456a` | Spark compact | 三处 compact commit message 反序列化抽出 helper，遍历时 `serializedMessages.set(i, null)` 释放 byte[] 引用。 | 中 1.4 稳定性候选。 |
| 16 | `376ccd3cb` | Spark E2E | Spark E2E stdout 校验前剥离 `dispatcher-CoarseGrainedScheduler` shutdown 噪声，降低测试误报。 | 测试-only。 |
| 17 | `c6964a7a7` | Flink Blob source | `FlinkSourceBuilder` 复用 read builder 并向 `MonitorSource/ReadOperator` 传 read type 与 `blob-as-descriptor`；`ReadOperator` 根据 BLOB 字段选择 `FlinkRowDataWithBlob`。 | 中 1.4 候选。 |
| 18 | `d4920972a` | Vector index | 升级 paimon-vindex 0.3.0；Java/Python 支持 `ivf-sq`、`ivf-rq`、`diskann`，搜索参数改为 automatic/ivf/diskann，新增训练 sample ratio/expected vector count，删除 native loader。 | release-1.4 是 `paimon-lumina`，非同路径 bugfix。 |
| 19 | `27f4656bb` | Core data evolution row-id | 新增 `data-evolution.reassign.skip-contiguous-row-count`；row-id planner 使用 manifest min/max 和 row-range projection 减少重扫；`LiveFileRowIdRangeCollector` 增加 abort、radix sort、fixed chunk compression；APPEND manifest merge 后重新绑定 current manifest。 | release-1.4 缺少 row-id reassignment planner，同路径低影响。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，本轮开始工作区干净且与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功，fork 自动化分支已刷新。 |
| `git fetch upstream master release-1.4` | 成功，`upstream/master=27f4656bb033e8fdb9b9574559914fdcc09d5485`；`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git log --first-parent --reverse a4bfec1bd224..upstream/master` | 19 个 first-parent commit。 |
| `git diff --shortstat a4bfec1bd224..upstream/master` | 92 files，`+4641/-896`。 |
| `git show --stat --name-status <commit>` / `git show --patch --stat <commit>` | 已为 19 个提交逐个检查文件状态、统计和实际 patch；merge commit 下将 name-status 与 patch 分开读取。 |
| `git cherry -v upstream/release-1.4 upstream/master a4bfec1bd224` | 19 个提交全部为 `+`，未发现 release-1.4 等价 patch。 |
| release-1.4 targeted `git grep` / `git show upstream/release-1.4:<path>` | 已核验 format table、temporary clean、ParallelExecution、Iceberg、CDC、DataEvolutionSplitRead、Flink Blob、Spark Compact、filesystem poms/checker、Python/REST/vector 路径存在性和旧逻辑。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报、推进 state 并发布自动化分支。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `27f4656bb033e8fdb9b9574559914fdcc09d5485`
- `last_processed_commit_date`: `2026-07-30 13:19:27 +0800`
- `last_report_date`: `2026-07-30`
- `last_report_path`: `reports/paimon-master-daily/2026-07-30.md`

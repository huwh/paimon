# Apache Paimon Master Daily Report - 2026-06-19

## Summary

范围：`c28080796cbbe7c54ad18f0c4fad6ecc5b1219c0..04282834bb4962387e476f8f789739b98aa70aa9`，共 18 个 first-parent 提交。当前 Apache upstream `master` head 是 `04282834bb49`（2026-06-19 11:49:14 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次变化是一次较大的 multimodal/global-index 推进：从 multi-vector/hybrid/batch vector search 到 bitmap global index、sorted build 复用、vector read API 重构、Lumina per-field options，并同步补 Spark/Python 入口和文档。另有 Parquet per-column compression、Mosaic 依赖升级、write-only 主键表 sequence 初始化优化，以及两个明确 bug/test fix。

`release-1.4` 最需要关注的是 `b1a68a57d2e`：release 分支已有 row tracking、global index、`visibility-callback.enabled`，但 callback 仍只覆盖 compaction，且只在主键表 postpone/deletion-vector 场景挂载；master 修复把 row-tracking global-index 可见性纳入等待。`54545a9ae5f` 的 Python Ray nested projection 是明确生产 bugfix，但 release-1.4 缺少 master 侧 nested projection / split provider 基础设施，不能直接小 cherry-pick。`04282834bb49` 的 VectorScan 多列 filter index 修复在 release 分支有同类旧判断和 `extraFieldIds` 元数据，但 public create_global_index 仍是单列，属于条件风险。

本次本地 git 路径可用，但需要受控提权写 `.git` 元数据：已 clone `huwh/paimon` 的 `codex/paimon-master-daily-report`，添加 `upstream https://github.com/apache/paimon.git`，并 fetch `upstream master release-1.4`。所有 commit 均先执行 `git show --stat --name-status --patch` 取得文件状态，再用 `git show --patch --stat --no-ext-diff` 读取实际 hunks 做分类；`--name-status` 在本 Git 版本会覆盖 patch format，所以 hunk 检查单独保存。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Multimodal / global index core | 10 | 新增 multi-vector ranker、hybrid search、batch vector search、bitmap global index；重命名并抽象 sorted-index reader/build path；统一 single-column writer；vector scan 修正 multi-column filter index 匹配；visibility callback 等待 global-index build。 | `b1a68a57d2e` 是明确 release 风险；`04282834bb49` 取决于 release 是否会生成多列 index meta；bitmap/hybrid/batch vector 多为新能力。 |
| Spark search integration | 5 | Spark scan/TVF 接入 multi-vector/hybrid/full-text query operator；create_global_index 适配 sorted/bitmap builder；metric 单测按 command metric ids 找 SQL execution。 | `a333bdc71ed` 是功能补齐；`e897bbba3f1` 只改测试；不建议作为生产修复优先 backport。 |
| Python / pypaimon | 4 | Python 支持 global index column update action、hybrid/vector API 调整、Ray nested projection 修复。 | `54545a9ae5f` 是生产 bugfix，但 release 缺少 master 的 nested projection 基础，需要成组评估。 |
| Parquet / format | 1 | Parquet writer 支持按列配置 compression codec，引入 column compression page store / record writer，并补读写测试。 | 新能力，未见 release bugfix 信号。 |
| Lumina / Mosaic / docs | 4 | Lumina 支持 per-field vector index options；Mosaic 升级到 0.2.0；global-index 文档拆分并新增 bitmap/hybrid/vector/full-text 页面。 | 以新能力和文档整理为主。 |
| Core write path | 1 | write-only 主键表新增 lightweight sequence initialization，避免为初始化 max sequence 扫全表。 | 性能/初始化优化，非明确 correctness bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | Touched production files on release-1.4 | Likely release-1.4 impact |
| --- | --- | --- | --- | --- |
| `b1a68a57d2e` | `visibility-callback.enabled` 现在等待 row-tracking data 被已有 global indexes 覆盖，避免 batch/bounded write 在 global index 追上前返回。 | `git cherry -v upstream/release-1.4 upstream/master c280807...` 显示 `+`，未被 cherry-pick。 | `AbstractFileStore.java`、`VisibilityWaitCallback.java`、`CoreOptions.java` 存在；release 只在主键 postpone/DV 场景挂 callback，callback 只等 compaction。 | 高。release-1.4 已有 row tracking 和 global index，开启 visibility callback 的 row-tracking/global-index 表可能读到 index 未覆盖的新数据；建议优先评估 backport。 |
| `54545a9ae5f` | Python Ray worker 现在转发 `nested_name_paths`，避免 `read_paimon(... projection=['payload.a'])` 这类 nested-leaf projection 在 worker 侧被当成缺失顶层列并读成 NULL。 | `git cherry` 显示 `+`，未被 cherry-pick。 | `ray_datasource.py`、`table_read.py` 存在；release 没有 `split_provider.py`，`ReadBuilder` 也没有 master 的 nested projection / `_nested_name_paths` 逻辑。 | 中，且依赖前置能力。若 release-1.4 要支持或已私有 backport nested projection，应连同该修复一起处理；单独 cherry-pick 不足。 |
| `04282834bb49` | `VectorScanImpl` 对 filter scalar index 改为匹配 `indexFieldId + extraFieldIds`，但 vector/full-text 主 search column 仍必须是 primary field，避免误用或漏用 multi-column filter index。 | `git cherry` 显示 `+`，未被 cherry-pick。 | `VectorScanImpl.java` 和 `GlobalIndexMeta.extraFieldIds` 存在；但 release public `create_global_index` 仍只接受单个 `index_column`。 | 中到低。release 代码有同类旧判断，但常规公开路径难以生成多列 index；若计划把多列 global index 带到 release-1.4，需要同时带这个修复。 |
| `e897bbba3f1` | Spark `PaimonMetricTest` 不再读取最后一个 SQL execution，而是等待 listener 并按 command metric ids 定位 execution，降低异步事件导致的单测不稳定。 | `git cherry` 显示 `+`，未被 cherry-pick。 | 只改 `paimon-spark-ut/.../PaimonMetricTest.scala` 测试文件；release 同测仍用 `lastExecutionMetrics`。 | 低。测试稳定性修复；如 release CI 有相同 flaky，可单独 backport。 |
| `a333bdc71ed` | Spark `full_text_search` TVF 支持可选 `query_operator`，与 core full-text/hybrid 的 `or/and` 能力对齐。 | `git cherry` 显示 `+`，未被 cherry-pick。 | release `PaimonTableValuedFunctions.scala` 只接受 `table, column, query_text, limit`；release `FullTextSearch` 也没有 `queryOperator`。 | 低到中。不是回归修复，是功能补齐；除非 release-1.4 需要 Spark full-text `and` 查询，否则不建议优先 backport。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `a81d1297029` | Core/Spark multi-vector | 新增 multi-vector search predicate/table/builder/ranker，Spark TVF 支持多路 vector routes，ranker 汇总 scored row ids，并补 Spark SQL / core tests。 | 新能力，非 release bugfix。 |
| 2 | `b1a68a57d2` | Core visibility | `AbstractFileStore` 改为 row-tracking 时也挂 `VisibilityWaitCallback`；新增 `GlobalIndexVisibilityChecker` 扫 index manifest 的 row ranges，callback 同时等待 compaction 和 global index visibility。 | 高，建议评估 backport。 |
| 3 | `548169cbe8` | Mosaic format | Mosaic 依赖升级到 0.2.0，测试按新 reader/writer 行为调整。 | 依赖升级，未见 release correctness 风险。 |
| 4 | `9af77b0a7c` | Lumina vector | `LuminaVectorIndexOptions` 支持 per-field options，例如不同 field 的 metric/dimension；factory/test 覆盖字段级解析。 | 新能力。 |
| 5 | `36caecf49e` | Python write/update | pypaimon 增加 `global-index-column-update-action` option，并在 update checker / options utils / tests 中暴露该行为。 | 新能力，对 release backport 取决于 Python update 能力边界。 |
| 6 | `cabb0beb58` | Core/Spark/Python hybrid search | 将 multi-vector 语义重命名/扩展为 hybrid search；新增 vector/full-text route、RRF ranker、Spark `hybrid_search` TVF、Python builder，并删除旧 pypaimon global-index doc。 | 新能力，release 不需默认 backport。 |
| 7 | `494a633877` | Docs | 将 global-index 文档拆成 btree/full-text/hybrid/vector 多页面，更新 redirects/sidebar。 | 文档整理。 |
| 8 | `3bee09f21f` | Global index writer API | 删除 singleton/parallel writer 分裂，统一为 `GlobalIndexSingleColumnWriter`；BTree、Tantivy、Vector、Lumina、Flink/Spark builder 和测试同步改接口。 | API 重构，为后续 bitmap/sorted 复用铺路，非独立 bugfix。 |
| 9 | `f7d77e77d9` | Batch vector search | 新增 `BatchVectorSearch`、batch vector read/search builder，reader 支持一次多 query vectors 返回多个 scored results；Python/vector/lumina 测试覆盖。 | 新能力。 |
| 10 | `06ff91b15d` | Vector read APIs | 抽象 `AbstractVectorRead`，分离 `BatchVectorRead` 与 `VectorReadImpl`，Python builder 对齐 sibling read/result 结构。 | API 整理和能力完善，非 release bugfix。 |
| 11 | `a8074ed60c` | Parquet format | 新增 per-column Parquet compression 配置；引入 `ColumnCompressionCodecs/PageWriteStore/RecordWriter` 并改 `RowDataParquetBuilder`。 | 新能力。 |
| 12 | `a333bdc71e` | Spark full-text TVF | `full_text_search` 支持第五个 `query_operator` 参数，默认仍为 `or`；测试覆盖 `and` 和非法 operator。 | 功能补齐，低优先级。 |
| 13 | `5fde0cd20c` | Bitmap global index | 新增 bitmap index format/options/writer/reader/factory 和 lazy filtered bitmap reader；抽象 sorted file reader/meta selector；文档新增 bitmap 页面和 core 表测试。 | 大型新能力，非小 backport。 |
| 14 | `54545a9ae5` | Python Ray read | Ray datasource 从 split provider 获取并转发 nested projection paths，`TableRead.to_ray` 也传递该信息；新增 `read_paimon` 和 `to_ray` nested projection 回归测试。 | 中，依赖 nested projection 基础能力。 |
| 15 | `e435a774e2` | Core write sequence | 新增 `SequenceSnapshotProperties`，write-only 主键表可从 snapshot property 初始化 max sequence，减少全表扫描；commit/write path 和 tests 更新。 | 性能优化，非明确 release bugfix。 |
| 16 | `7f4134c2a7` | Sorted/bitmap index build | 将 BTree build path 泛化为 sorted global index build，bitmap 复用 sorted builder；Flink/Spark create index procedure 通过 type 选择 builder。 | 支撑 bitmap 新能力，非独立 bugfix。 |
| 17 | `e897bbba3f` | Spark test stability | `PaimonMetricTest` 等待 Spark listener，并按当前 command 的 metric ids 找 execution metrics，替代 last execution。 | 测试-only；release CI flaky 时可 backport。 |
| 18 | `04282834bb` | Core vector scan | `VectorScanImpl` 用 `containsField(indexFieldId + extraFieldIds)` 匹配 filter index，同时要求 vector/full-text search column 必须是 primary field；新增 vector/full-text 回归测试。 | 条件风险；多列 global index backport 时应一并带上。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git clone --branch codex/paimon-master-daily-report https://github.com/huwh/paimon.git .` | 成功；当前分支 `codex/paimon-master-daily-report` 跟踪 `origin/codex/paimon-master-daily-report`。 |
| `git remote add upstream https://github.com/apache/paimon.git` | 成功；本地原先只有 fork `origin`。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=04282834bb4962387e476f8f789739b98aa70aa9`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git log --first-parent --reverse c28080796cbbe7c54ad18f0c4fad6ecc5b1219c0..upstream/master` | 18 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch` / `git show --patch --stat --no-ext-diff` | 对 18 个 commit 均已执行并检查实际 hunks；patch 输出保存在 `/private/tmp/paimon-master-daily-2026-06-19-patch-hunks`。 |
| `git cherry -v upstream/release-1.4 upstream/master c28080796cbbe7c54ad18f0c4fad6ecc5b1219c0` | 18 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted file checks | 检查 `AbstractFileStore.java`、`VisibilityWaitCallback.java`、`VectorScanImpl.java`、`GlobalIndexMeta.java`、Python Ray/TableRead、Spark TVF、Spark metric test 等关键文件。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `04282834bb4962387e476f8f789739b98aa70aa9`
- `last_processed_commit_date`: `2026-06-19 11:49:14 +0800`
- `last_report_date`: `2026-06-19`
- `last_report_path`: `reports/paimon-master-daily/2026-06-19.md`

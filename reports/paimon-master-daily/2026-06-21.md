# Apache Paimon Master Daily Report - 2026-06-21

## Summary

范围：`cca01af0fdda277171212a27c16ba2311850428d..9cdb9026164d9516a6f631198bb0c348972587de`，共 9 个 first-parent 提交。当前 Apache upstream `master` head 是 `9cdb9026164d`（2026-06-20 20:52:22 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次主线变化集中在 Iceberg 兼容性、REST 客户端、global-index/hybrid search、Flink source split reader 可靠性、Spark 测试稳定性和 GitHub Actions 并发控制。明确的 runtime bugfix 包括 Iceberg REST committer 创建表时缺少 location、float/double comparator 对 NaN 排序错误、REST catalog 请求体缺少 JSON Content-Type、Flink split reader close/recycle 资源释放不可靠、Iceberg manifest 对复杂类型生成 bounds。

`release-1.4` 方面，9 个提交经 `git cherry -v upstream/release-1.4 <commit> <commit>^` 检查均为 `+`，未发现等价 patch 已 cherry-pick。优先关注 `df3dd39c9c`、`34e9b9f313`、`439ff3ce71`、`bda726b82f`、`9cdb902616`：release-1.4 仍存在对应生产代码旧逻辑。`c63da64049` 属于 master-only hybrid search 路径，release-1.4 未见相关文件；`feecdfe064` 是 global-index search mode 新能力，默认行为仍为 fast，是否回补取决于 release 是否需要该能力。

所有 commit 都用 `git show --stat --name-status --patch` 查看文件状态，并补跑 `git show --stat --patch` 读取实际 hunks，因为当前 Git 输出在带 `--name-status` 时未显示 patch hunks。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Iceberg compatibility | 2 | REST committer 建表失败后带 Paimon metadata location 重试，并将 `s3a://`/`s3n://` 归一到 `s3://`；Iceberg manifest 对 ARRAY/MAP/ROW/MULTISET 不再生成 lower/upper bounds。 | 两个生产路径在 release-1.4 均存在旧逻辑，是明确回补候选。 |
| REST / codegen core | 2 | Record comparator 对 FLOAT/DOUBLE 改用 `Float.compare`/`Double.compare`；REST POST/DELETE 请求体设置 JSON Content-Type，simple client 按 body 类型设置 form 或 JSON。 | release-1.4 仍用 primitive comparison 和无 Content-Type 的 `StringEntity`，建议评估 backport。 |
| Global-index and hybrid search | 2 | `weighted_score` 改为 route 内 min-max 归一化后加权；新增 `global-index.search-mode=fast/full/detail`，full/detail 可补扫未建索引的 row-id 范围。 | hybrid search 文件在 release-1.4 不存在；search-mode 是新能力，不是小型 bugfix。 |
| Flink source reader | 1 | close 时释放 `currentFirstBatch`；`releaseBatch` 用 finally 回收 pooled iterator；CDC reader 无 split fetch 改为明确失败。 | release-1.4 两个 split reader 仍有旧释放逻辑，高优先级可靠性 backport 候选。 |
| Spark tests | 1 | `MigrateTableProcedureTest` 在不同 file format iteration 中使用 format 后缀隔离 hive/target 表名。 | test-only；release-1.4 测试仍有旧命名，按 CI 需要决定。 |
| CI workflows | 1 | 多个 GitHub Actions workflow 增加或修正 pull_request concurrency 和 cancel-in-progress。 | CI-only，非 runtime。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | Touched production files on release-1.4 | Likely release-1.4 impact |
| --- | --- | --- | --- | --- |
| `df3dd39c9c` | Iceberg REST committer 原先直接 `createTable` 且不传 location；新逻辑失败后用 Paimon metadata location 重试，并把 `s3a://`/`s3n://` 转成 REST catalog 接受的 `s3://`。 | `+`，未 cherry-pick。 | release-1.4 `IcebergRestMetadataCommitter` 仍在两条分支直接调用 `restCatalog.createTable(...)`，没有 `toRestLocation`。 | 高。AWS Glue Iceberg REST 等不自动分配 location 的 catalog 会受影响，建议 backport。 |
| `34e9b9f313` | FLOAT/DOUBLE comparator 原先用 `>`/`<`，NaN 和 -0.0/0.0 场景不符合 Java total order；新逻辑使用 `Float.compare`/`Double.compare`。 | `+`，未 cherry-pick。 | release-1.4 `GenerateUtils.scala` 仍把 FLOAT/DOUBLE 放在 primitive comparison case。 | 高。涉及排序、比较器生成和含 NaN 浮点列的结果正确性，建议 backport。 |
| `c63da64049` | hybrid search `weighted_score` 对不同 score scale 的 route 做 min-max 归一化，避免 BM25 等大数值 route 淹没权重。 | `+`，未 cherry-pick。 | release-1.4 未见 `HybridSearchRanker`、Python `hybrid_search_builder.py` 或 Spark `HybridSearchTest`。 | 低。看起来是 master-only global-index/hybrid search 能力，不构成直接 release-1.4 bugfix。 |
| `439ff3ce71` | REST catalog POST/DELETE 请求体显式设置 JSON Content-Type；simple client 对 Map body 使用 form，其他 body 使用 JSON。 | `+`，未 cherry-pick。 | release-1.4 `HttpClient` 和 `SimpleHttpClient` 仍使用无 Content-Type 的 `new StringEntity(encodedBody)`。 | 高。严格 REST server 可能在处理前拒绝请求，建议 backport。 |
| `feecdfe064` | 新增 `global-index.search-mode`：`fast` 保持只查索引；`full`/`detail` 检测未索引 row-id 并补扫 raw data。 | `+`，未 cherry-pick。 | release-1.4 有 `GlobalIndexScanner`，但没有 `global-index.search-mode` 和相关覆盖/补扫逻辑。 | 低到中。偏新能力和行为扩展；若 release 用户需要索引 freshness，可专项评估。 |
| `bda726b82f` | Flink source split reader close 时释放已抓取的 `currentFirstBatch`；`releaseBatch` 即使底层 release 抛异常也回收 iterator；CDC 无 split 时明确抛错。 | `+`，未 cherry-pick。 | release-1.4 `FileStoreSourceSplitReader` 和 `CDCSourceSplitReader` 仍只关闭 lazy reader，`releaseBatch` 先 release 再 recycle，缺少 finally。 | 高。资源泄漏或 iterator pool 丢失可能导致后续 fetch 阻塞，建议 backport。 |
| `c71fa3a01b` | GitHub Actions pull_request 并发组和 cancel-in-progress 配置。 | `+`，未 cherry-pick。 | release-1.4 workflow 可单独检查，但不影响 runtime。 | 低。CI-only。 |
| `68dd708052` | Spark migrate table procedure test 在 format 参数化循环中隔离表名，避免同一随机名跨 format 迭代冲突。 | `+`，未 cherry-pick。 | release-1.4 `MigrateTableProcedureTest` 仍使用 `hive_tbl_$random` 和 `target_$random`。 | 低。test-only；若 release CI 偶发失败，可 backport。 |
| `9cdb902616` | Iceberg manifest 不再为 ARRAY/MAP/ROW/MULTISET 复杂类型生成 lower/upper bounds，避免不适合写入 Iceberg bounds 的类型被转换。 | `+`，未 cherry-pick。 | release-1.4 `IcebergDataFileMeta` 仍对所有非 null min/max 调用 `IcebergConversions.toByteBuffer` 并写入 bounds。 | 高。含复杂类型列的 Iceberg 兼容 manifest 可能失败或产生不合法 metadata，建议 backport。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `df3dd39c9c` | Iceberg REST committer | `IcebergRestMetadataCommitter` 将两处 `restCatalog.createTable` 改为私有 `createTable` helper：先让 catalog 自行分配 location，失败后用 `toRestLocation(newMetadata.location())` 重试；测试覆盖 `s3a://`、`s3n://`、`s3://`、file 和 null。 | 高优先级 bugfix。release-1.4 有相同旧 create path。 |
| 2 | `34e9b9f313` | Codegen comparator | `GenerateUtils.genComp` 将 FLOAT/DOUBLE 从 primitive comparison case 拆出，生成 `java.lang.Float.compare` 和 `java.lang.Double.compare`；新增 comparator total order 和 NaN sort 测试。 | 高优先级 bugfix。release-1.4 有旧 comparator。 |
| 3 | `c63da64049` | Hybrid search ranking | Java `HybridSearchRanker` 和 Python builder 都在 weighted score 前对每个 route 做 min-max normalization；单 hit 或全 tie 映射为 1.0；docs 和 Spark/Python tests 同步更新。 | release-1.4 未见相关 hybrid search 文件，低。 |
| 4 | `439ff3ce71` | REST client | `HttpClient.post/delete` 使用 `StringEntity(encodedBody, ContentType.APPLICATION_JSON)`；`SimpleHttpClient` 按 body 是否为 Map 选择 form 或 JSON；新增 mock server 检查 Content-Type。 | 高优先级 bugfix。release-1.4 有旧 REST client path。 |
| 5 | `feecdfe064` | Global-index search mode | 新增 `CoreOptions.GLOBAL_INDEX_SEARCH_MODE`；`GlobalIndexScanner` 记录 index coverage，并在 full/detail 模式把 unindexed ranges 加回结果；detail 模式按 data file row-id range 精确计算；新增 predicate field id helper 和 BTree tests。 | 新能力，非小型 bugfix。 |
| 6 | `bda726b82f` | Flink source split reader | common 与 CDC split reader 的 close 都释放 `currentFirstBatch`；pooled iterator release 用 finally recycle；CDC 无 split fetch 从 silent return 改为 `IOException`；新增失败 release、close release、no split tests。 | 高优先级可靠性 bugfix。release-1.4 有旧代码。 |
| 7 | `c71fa3a01b` | CI workflows | e2e/unit/file-size workflow 增加 concurrency；多数 workflow 只在 pull_request cancel in progress，避免非 PR run 被取消。 | CI-only。 |
| 8 | `68dd708052` | Spark test stability | `MigrateTableProcedureTest` 在 hive table 和 target table 名中追加 `$format`，避免 parquet/orc 等参数化 case 共用同名表。 | test-only；可按 release CI 需要回补。 |
| 9 | `9cdb902616` | Iceberg manifest bounds | `IcebergDataFileMeta` 在写 lower/upper bounds 前检查 `DataTypeRoot`，跳过 ARRAY、MAP、ROW、MULTISET，标量类型保持原逻辑。 | 高优先级 bugfix。release-1.4 仍对复杂类型写 bounds。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 沙箱 DNS 失败后升级执行成功。 |
| `git fetch origin` | 成功，本地跟踪分支与 fork 对齐。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=9cdb9026164d9516a6f631198bb0c348972587de`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git merge-base --is-ancestor cca01af0fdda277171212a27c16ba2311850428d upstream/master` | 成功，状态文件中的 last processed commit 是 upstream/master 祖先。 |
| `git log --first-parent --reverse cca01af0fdda277171212a27c16ba2311850428d..upstream/master` | 9 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 9 个 commit 均已执行，用于文件状态和 patch 检查入口。 |
| `git show --stat --patch <commit>` | 对 9 个 commit 均补跑并检查实际 hunks。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 9 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted code checks | 检查 Iceberg REST committer、GenerateUtils、REST clients、Flink split readers、IcebergDataFileMeta、hybrid search 文件存在性。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `9cdb9026164d9516a6f631198bb0c348972587de`
- `last_processed_commit_date`: `2026-06-20 20:52:22 +0800`
- `last_report_date`: `2026-06-21`
- `last_report_path`: `reports/paimon-master-daily/2026-06-21.md`

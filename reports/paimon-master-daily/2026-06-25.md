# Apache Paimon Master Daily Report - 2026-06-25

## Summary

范围：`50f28639d8e6036473b2d595f873c66b554f1804..7e22e56330af73c3de7747f760fb6a4a8af42d1d`，共 13 个 first-parent 提交。当前 Apache upstream `master` head 是 `7e22e56330af`（2026-06-25 10:42:19 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次主线重点集中在：core data-evolution manifest sort、JDBC Catalog view 持久化、Spark external table metadata、PyPaimon tag TTL / vindex batch search、ORC/Parquet format metadata、global-index hybrid ranker top-k、docs 和依赖锁文件。`a5eb81e1269a` 引入的 Arrow schema metadata 方案已被 `c35ef892ecbe` 回滚，最终由 `7e22e56330af` 以不引入 Arrow runtime dependency 的 FlatBuffers metadata 方案替代。

`release-1.4` 方面，13 个提交经 `git cherry -v upstream/release-1.4 upstream/master 50f28639d8e6036473b2d595f873c66b554f1804` 检查均为 `+`，未发现等价 patch 已 cherry-pick。优先关注 2 个直接行为修复：Spark external table type 暴露、normalized options immutability 误判。其他候选多数是 master 新能力，或在 `release-1.4` 缺少对应代码路径。

所有 commit 都用 `git show --stat --name-status --patch` 查看文件状态，并补跑 `git show --stat --patch` 读取实际 hunks，因为当前 Git 输出在带 `--name-status` 时未显示 patch hunks。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core schema / manifest / JDBC catalog | 4 | `8f47383d8` 让 data-evolution RowID manifest sort 尊重 `manifest-sort.partition-field`；`1d64355a3` 为 JDBC Catalog 新增 persistent view CRUD 和 `paimon_views` 表；`1bb9dacae` 跳过 primary-key/partition normalized options 的 false-positive immutability check；`b782f24a2` 移除 `ObjectsFile` 无用 `loadFilter` 参数。 | `1bb9dacae` 是直接 bugfix 候选；JDBC view 是新能力；`ManifestFileSorter` 在 1.4 不存在。 |
| Spark | 1 | `5d3752ce8` 在 `PaimonSparkTableBase.properties()` 中根据 `table.type=EXTERNAL` 暴露 `TableCatalog.PROP_EXTERNAL`，测试覆盖 `DESC FORMATTED`。 | 1.4 同文件没有 `PROP_EXTERNAL` 映射，是直接 bugfix 候选。 |
| PyPaimon | 2 | `ce76984da` 支持 FileSystemCatalog tag `time_retained`，并按 Java tag JSON 形状持久化 `tagCreateTime` / `tagTimeRetained`；`812bca965` 为 vindex reader 增加 batch vector search，优先使用 native `search_batch`。 | 主要是 SDK 新能力；1.4 缺 vindex reader，tag catalog surface 也不同。 |
| Format ORC / Parquet | 3 | `a5eb81e12` 先引入 Arrow schema metadata；`c35ef892e` 回滚该方案以避免 Arrow dependency；`7e22e5633` 新增 `SupportsWriterMetadata` / `SupportsReaderFieldMetadata`、Arrow IPC metadata 编解码、ORC/Parquet footer 读写和兼容测试。 | 新 format metadata 能力；不是 1.4 现有 bugfix。 |
| Global index | 1 | `ad4e32bf6` 将 hybrid ranker top-k 从全量排序改成 bounded heap，并保持 score desc / rowId asc tie-break。 | 1.4 缺 `HybridSearchRanker` 路径，不直接适用。 |
| Docs / dependency | 2 | `b894b48b0` 升级 docs `http-proxy-middleware` lockfile 到 2.0.10；`7b1c1a17d` 补充 Lumina vector index 文档。 | 1.4 无 `docs/yarn.lock`；Lumina 文档是 master 新能力说明。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `1bb9dacae` | 修复 Spark 4.x 等路径把 normalized `primary-key` / `partition` 重新带入 dynamic options 后，误判 immutable option 变更并抛错的问题。 | patch-id `4d051fe56eb2`; cherry `+`。 | 1.4 `SchemaManager.commitChanges` 和 `AbstractFileStoreTable.checkImmutability/copyInternal` 仍直接比较 old/new options，没有 `isUnchangedNormalizedKey`。 | 高。非空表或 Spark scan/copy 场景可能误报 `Change 'primary-key' is not supported yet.`，建议评估回补。 |
| `5d3752ce8` | Spark-facing metadata 暴露 external table type，避免 Paimon external table 在 `DESC FORMATTED` 等 Spark 元数据中显示为 MANAGED。 | patch-id `ca772c869e57`; cherry `+`。 | 1.4 `PaimonSparkTableBase.properties()` 只设置 provider/comment/location，不设置 `TableCatalog.PROP_EXTERNAL`。 | 中。影响 Spark 用户可见表类型和依赖 Spark reserved property 的命令。 |
| `8f47383d8` | data-evolution RowID manifest sort 使用配置的 partition field，否则仍使用完整 partition row；新增非法字段校验。 | patch-id `fe01d6f59952`; cherry `+`。 | 1.4 没有 `ManifestFileSorter.java`；full-compaction 仍在 `ManifestFileMerger`，未出现相同 `manifest-sort.partition-field` 路径。 | 低/不直接适用。更像 master 新实现上的能力修正。 |
| `b894b48b0` | docs 依赖 `http-proxy-middleware` 从 2.0.9 升到 2.0.10。 | patch-id `bede60dd6860`; cherry `+`。 | 1.4 没有 `docs/yarn.lock`。 | 低/不直接适用。仅 docs build chain。 |
| `ce76984da` | PyPaimon FileSystemCatalog tag TTL 从拒绝/不可见改为可持久化和可读取。 | patch-id `85db7d720bc0`; cherry `+`。 | 1.4 有 Python `TagManager`，但 catalog tag surface 与 master 不同，`Tag` 仍只继承 `Snapshot`。 | 中低。属于 SDK feature gap；是否回补取决于 1.4 Python tag TTL 需求。 |
| `812bca965` / `ad4e32bf6` | vindex batch search 和 hybrid ranker top-k 性能/边界改进。 | patch-ids `eba1232e7dc3`, `bd3252905f1e`; cherry `+`。 | 1.4 缺 `vindex_vector_global_index_reader.py` 和 `HybridSearchRanker.java`。 | 低/不直接适用。需前置能力进入。 |
| `c35ef892e` / `7e22e5633` | 回滚 Arrow dependency 方案，并以 FlatBuffers metadata 方案支持 ORC/Parquet field metadata。 | `7e22e5633` patch-id `1184ccfd18cd`; cherry `+`。 | 1.4 未包含被回滚的 `a5eb81e12`，也没有新的 metadata interfaces。 | 低。新能力，不是 1.4 既有回归修复。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `8f47383d8` | Core manifest sort | `ManifestFileSorter` 抽出 `createPartitionComparator`，RowID sort 在配置 `manifest-sort.partition-field` 时只比较指定 partition field；测试构造多分区 RowID manifest 验证排序顺序。 | 1.4 无同文件/同路径，低优先级。 |
| 2 | `ce76984da` | PyPaimon tag TTL | 移除 `create_tag(time_retained=...)` 的 `NotImplementedError`；新增 duration/local datetime codec、Tag create-time/retention 字段、`$tags` 系统表输出和 Java-compatible JSON 测试。 | SDK 新能力，1.4 需按 Python tag API 现状单独评估。 |
| 3 | `5d3752ce8` | Spark metadata | 当底层 Paimon table option `table.type=EXTERNAL` 时设置 Spark `TableCatalog.PROP_EXTERNAL=true`，测试 external/managed `DESC FORMATTED`。 | 直接 bugfix 候选。 |
| 4 | `1d64355a3` | JDBC Catalog views | 自动创建 `paimon_views`，实现 get/create/drop/list/rename/alter views，维护 table/view 单命名空间，补 docs、core tests 和 Flink IT。 | 新能力；1.4 JDBC Catalog 没有 view 支持。 |
| 5 | `1bb9dacae` | Core options/schema | 新增 `SchemaManager.isUnchangedNormalizedKey`，在 schema commit、table copy 和 dynamic options merge 中识别 normalized primary-key/partition 未变更。 | 直接 bugfix 候选。 |
| 6 | `812bca965` | PyPaimon vindex | 新增 `visit_batch_vector_search`，统一 `_run_search` 校验维度/filter/effective_k，优先走 native `search_batch`，否则逐条 fallback。 | 1.4 缺 vindex reader，不直接适用。 |
| 7 | `b894b48b0` | Docs dependency | 只更新 `docs/yarn.lock` 中 `http-proxy-middleware` 2.0.9 -> 2.0.10。 | 1.4 无对应 lockfile。 |
| 8 | `7b1c1a17d` | Docs vector index | 文档加入 Lumina index type、build/search options、field-level option 说明和 x86_64 native library 限制。 | docs-only。 |
| 9 | `a5eb81e12` | Format metadata | 引入 Arrow schema metadata 读写接口和 ORC/Parquet footer 支持，但新增 Arrow runtime dependency。 | 已被下一条 revert。 |
| 10 | `c35ef892e` | Format metadata revert | 完整回滚 `a5eb81e12`，原因是避免向 `paimon-format` / bundle 引入 Arrow dependency。 | 1.4 未包含被回滚提交。 |
| 11 | `b782f24a2` | Core ObjectsFile | 删除 `ObjectsFile.read/readWithIOException` 未使用的 `loadFilter` 参数，调用点同步简化。 | 内部清理，无行为变化。 |
| 12 | `ad4e32bf6` | Global-index ranker | `topK` 改用 size-limited heap，保留同分按更小 rowId 优先；测试覆盖边界 tie。 | 1.4 缺该 ranker。 |
| 13 | `7e22e5633` | ORC/Parquet format metadata | 新增 FlatBuffers-based Arrow IPC metadata encoder/decoder、format metadata interfaces、ORC/Parquet writer/reader metadata 读写、Arrow compatibility tests。 | 新 shared-shredding 支撑能力，非 1.4 bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 成功；本地自动化分支与 `origin/codex/paimon-master-daily-report` 一致。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=7e22e56330af73c3de7747f760fb6a4a8af42d1d`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git rev-list --first-parent --reverse 50f28639d8e6036473b2d595f873c66b554f1804..upstream/master` | 13 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 13 个 commit 均已执行，用于文件状态检查入口。 |
| `git show --stat --patch <commit>` | 对 13 个 commit 均补跑并检查实际 hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master 50f28639d8e6036473b2d595f873c66b554f1804` | 13 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show <commit> \| git patch-id --stable` | 对 bugfix/条件候选计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 normalized options、Spark external metadata、ManifestFileSorter/JDBC views/Python vindex/docs yarn/hybrid ranker 等触点。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `7e22e56330af73c3de7747f760fb6a4a8af42d1d`
- `last_processed_commit_date`: `2026-06-25 10:42:19 +0800`
- `last_report_date`: `2026-06-25`
- `last_report_path`: `reports/paimon-master-daily/2026-06-25.md`

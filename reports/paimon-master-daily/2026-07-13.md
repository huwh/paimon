# Apache Paimon Master Daily Report - 2026-07-13

## Summary

范围：`fb3484df6e0053a94ed281f43e2dad09d04cf704..ec1d8ee8ac9c67972ac148e132f8e6c2952afee0`，共 31 个 first-parent 提交。当前 Apache upstream `master` head 是 `ec1d8ee8ac9c`（2026-07-13 13:06:33 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 147 个文件，`+10531/-553`。

本次主线有两块：一是 primary-key vector search 继续从 bucket search、按物理位置读回、Core search plan、Spark/Flink 入口推进到异步 ANN compaction；二是较多跨模块修复，覆盖 Iceberg multiset metadata、aborted commit 残留 index file、不可变 table option、Hive column delimiter、Spark NaN filter、PyPaimon Ray HASH_FIXED primary-key 写入、binary map equality、floating-point equaliser、Parquet narrowing read，以及 primary-key table `LIMIT` 读路径。

`git cherry -v upstream/release-1.4 upstream/master fb3484df6e0053a94ed281f43e2dad09d04cf704` 显示 31 个提交均为 `+`，没有 patch-id 等价提交已进入 `release-1.4`。其中 primary-key vector search、Spark V2 delta row-level ops、BLOB fetch metrics、Avro array optimization 属于 master 新功能或优化；明确建议评估 `release-1.4` 的直接 bugfix 候选是 Iceberg multiset、aborted index cleanup、不可变 option、filesystem security/metadata、Hive delimiter、Spark NaN filter、common/codegen equality、Parquet narrowing 和 primary-key table `LIMIT`。

所有 commit 都先执行 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在带 `--name-status` 时不展开 hunk，又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch。release-1.4 判断使用 `git cherry`、`git ls-tree`、`git grep` 和 targeted `git show upstream/release-1.4:<path>` 对照生产路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Primary-key vector search / ANN | 5 | `cd0f9251`, `d41ec735`, `a50a36ff`, `3d463ba`, `4a2d409f` 串起 bucket-local ANN/exact search、按物理位置读回、Core search plan、Spark/Flink 入口、Score reader 和异步 ANN compaction。 | 1.4 只有旧 vector field/global vector path，缺 `pk-vector` 配置和 `pkvector` 包；vector 部分不是直接候选。 |
| Core read/write correctness | 5 | `f2185284` 清理 aborted commit 的新 index files；`3cfaa512` 禁止改 table `type`；`ee50c951` 修正 binary map key equality/hash；`23da7422` 把 primary-key table `LIMIT` 移到 table-read 层；`ec1d8ee8` 保留 `ScoreRecordIterator` 元数据。 | 多数生产路径在 1.4 存在且未 cherry；`LIMIT` 修复需按 1.4 无 `LimitRecordReader` 的状态适配。 |
| Spark | 4 | `f797982e` 将 Spark V2 `EqualTo(NaN)` 转为 `isNaN`；`8490f4d5` 拒绝 `ALTER TABLE ... SET LOCATION`；`fa62f98a` 新增 DV append table 的 V2 delta row-level ops；`3d463ba` 增加 PK vector search Spark reader。 | `f797982e`、`8490f4d5` 是直接候选；delta row-level ops 和 PK vector search 是新能力。 |
| Format / Iceberg / BLOB / Avro | 4 | `df71005c` 支持 Iceberg multiset metadata；`6509987c` 用 `AvroBytesArray` 优化 Avro array 读写；`1379eefb` 支持 Parquet INT64->INT、DOUBLE->FLOAT narrowing vectorized read；`d7e49334` 增加 BLOB HTTP fetch metrics。 | `df71005c`、`1379eefb` 是直接候选；Avro 是性能优化，BLOB metrics 是观测增强。 |
| PyPaimon / REST / Ray | 3 | `8393d28b` 阻止有 snapshot 后修改 immutable options 并修正 latest snapshot hint；`2b2904d2` 修正 mock REST server snapshot serialization；`450a993e` 把 HASH_FIXED primary-key Ray group 写入放到 `map_groups` UDF 内提交。 | `8393d28b` 直接影响 PyPaimon；`450a993e` 在 1.4 路径不完全一致，需适配验证；`2b2904d2` 主要是测试 mock。 |
| Filesystem / shading | 7 | `73d38b6d` 强制 hadoop-shaded 中 CVE-patched `commons-lang3` 和 `snappy-java`；`1b43de29` 让 GSFileIO 返回真实 access time/owner；`208764a0` 添加 COSN loader serialVersionUID；`1b821f59` 降低 COSN config log 级别；其余是 shade POM 注释修正。 | `73d38b6d`、`1b43de29` 是直接候选；`208764a0`/`1b821f59` 低风险可选；注释修正不需要回补。 |
| Docs / tests / loader comments | 3 | `aa41699e` 修正 config doc generator 对 `&` 的转义；`5cfb2cf6` 稳定 Flink CDC schema-change IT；`a7fa5594` 修正 stale dependency comments。 | docs generator 可选；Flink CDC 和 loader comments 不影响 release runtime。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `df71005c` | Iceberg metadata 遇到 `MULTISET` 不再抛 `UnsupportedOperationException`，映射为 Iceberg map element->int count。 | `git cherry` 为 `+`。 | 1.4 `IcebergDataField` 有 `MAP` case，但无 `MULTISET` case，会落入 unsupported。 | 直接候选；影响含 multiset column 的 Iceberg metadata/read path。 |
| `f2185284` | `FileStoreCommitImpl.abort` 清理 data/changelog 外，也清理 data/compact increment 的 `newIndexFiles`，避免 aborted commit 遗留 index files。 | `+`。 | 1.4 `abort` 只收集 `newFiles/changelog/compactAfter` 并 `deleteQuietly` data path，未处理 `newIndexFiles`。 | 直接候选；影响 index file-in-data-dir 或 external index file 的失败清理。 |
| `8393d28b` | PyPaimon 阻止表已有 snapshots 后修改 immutable options，并把 `LATEST` 视为 hint 而不是唯一真相。 | `+`。 | 1.4 有 PyPaimon schema/snapshot manager，但无 `IMMUTABLE_OPTIONS` 和 latest-hint reconcile。 | 直接候选；影响 PyPaimon schema alter 防护。 |
| `3cfaa512` | Java/Flink 侧把 table `type` 标为 immutable，并拒绝在表上改/删 `type`。 | `+`。 | 1.4 `CoreOptions.TYPE` 未按该补丁处理，`SchemaManager` 无 type 特判。 | 直接候选；防止同一数据路径切换 table kind。 |
| `73d38b6d` | hadoop-shaded 强制使用 patched `commons-lang3 3.18.0` 和 `${snappy.version}`。 | `+`。 | 1.4 `paimon-hadoop-shaded/pom.xml` 只有 `commons-beanutils` dependencyManagement，无这两个 override。 | 直接安全候选；需确认 1.4 依赖树和 NOTICE。 |
| `1b43de29` | GSFileIO `FileStatus` 返回真实 access time 和 owner。 | `+`。 | 1.4 `HadoopCompliantFileIO` 未 override `getAccessTime/getOwner`。 | 直接候选；影响 GCS file metadata。 |
| `bb085744` | Hive reader 读取正确的 `column.name.delimiter` key。 | `+`。 | 1.4 仍是拼写错误 `"column.name.delimite"`。 | 直接候选；影响非逗号 delimiter 的 Hive column projection。 |
| `f797982e` | Spark V2 filter 把 `EqualTo(NaN)` 转成 Paimon `isNaN`，避免 NaN 文件被 stats/equality 误剪枝。 | `+`。 | 1.4 `SparkV2FilterConverter` 仍有 `TODO deal with isNaN` 并走 `equal`。 | 直接候选；影响 Spark NaN 查询正确性。 |
| `450a993e` | PyPaimon Ray HASH_FIXED primary-key `map_groups` 内完成写入并只返回 commit messages，避免输出 block split 后多 writer 产生重叠 sequence。 | `+`。 | 1.4 有 `TableWrite.write_ray` 直接 `dataset.write_datasink(PaimonDatasink)`，但缺 master 的 high-level `pypaimon.ray`/shuffle 结构。 | 条件候选；需按 1.4 Ray API 适配并验证 HASH_FIXED PK 写入。 |
| `8490f4d5` | Spark analysis 阶段拒绝 Paimon 表 `ALTER TABLE ... SET LOCATION`，避免静默通过。 | `+`。 | 1.4 `PaimonAnalysis` 无 `SetTableLocation` guard。 | 直接候选；属于 DDL 误操作防护。 |
| `ee50c951` | `InternalRowUtils.equals/hash` 对 `MAP<BINARY,...>` 使用 typed equality，不再依赖 Java `Map.containsKey` 的 byte-array identity。 | `+`。 | 1.4 common path存在；codegen map path也仍用 `containsKey`。 | 直接候选；影响 binary key map 的 equality/hash。 |
| `8b51ac5b` | codegen equaliser 使用 `Float.compare/Double.compare`，禁用含 floating point 字段的 binary-row raw equals，并修正嵌套 map/array/row 浮点比较。 | `+`。 | 1.4 equaliser 对 primitive 仍走 `==`，map codegen 仍有 `containsKey`。 | 直接候选；影响 NaN、-0.0 和嵌套浮点字段比较语义。 |
| `1379eefb` | Parquet vectorized reader 在文件物理类型更宽时支持 `INT64->INT`、`DOUBLE->FLOAT` narrowing，含 dictionary decoding。 | `+`。 | 1.4 `ParquetVectorUpdaterFactory` 对 `INT`/`FLOAT` 仍固定 `IntegerUpdater`/`FloatUpdater`。 | 直接候选；影响外部 Parquet 文件兼容读取。 |
| `23da7422` | primary-key table `LIMIT` 从 split/raw-read 层提升到 table-read 层，避免多个 L0 文件 merge read 时每个文件先 limit 导致全局 limit 错误。 | `+`。 | 1.4 `KeyValueTableRead.withLimit` 仍把 limit 传给 initialized split read；无 `LimitRecordReader`。 | 直接候选；需要 backport `LimitRecordReader` 或在 1.4 读路径等价实现。 |
| `ec1d8ee8` | `LimitRecordReader` 包装 `ScoreRecordIterator` 时保留 score/rowId。 | `+`。 | 1.4 无 `LimitRecordReader`；只有在回补 `23da7422` 或类似 reader wrapper 时相关。 | 跟随 `23da7422` 的配套修复。 |
| `aa41699e` | config doc generator 转义 `&`，避免生成 HTML 破损。 | `+`。 | 1.4 docs util 需按路径确认后可回补。 | 文档生成候选，非 runtime。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `cd0f9251` | Core pk-vector | 新增 `VectorSearchMetric`、exact searcher、bucket search 和 search result；ANN result 与 exact fallback 按 distance/file/position 合并。 | 1.4 缺 `pkvector` 包；新功能，非直接。 |
| 2 | `d41ec735` | Core pk-vector read | 新增 `PrimaryKeyIndexedSplitRead`、`PrimaryKeyVectorPositionReader`，把 IndexedSplit row ranges 当物理位置读回并保留 score。 | 新功能；1.4 无 primary-key vector result materialization。 |
| 3 | `a50a36ff` | Core pk-vector scan | 新增 bucket split、`PrimaryKeyVectorRead/Scan/Result`，DataEvolution vector read/scan rename，batch scan 可接 GlobalIndexSplitResult。 | 新功能；1.4 无 pk-vector planner。 |
| 4 | `3d463ba` | Spark/Flink pk-vector | 增加 docs、Flink procedure IT、Spark primary-key vector reader 和 SQL 测试；Spark metadata score 支持 `ScoreRecordReader`。 | 新功能；1.4 缺 pk-vector search API。 |
| 5 | `df71005c` | Iceberg | `IcebergDataField` 支持 Paimon `MultisetType` 到 Iceberg map 类型转换，并加测试。 | 直接 1.4 候选。 |
| 6 | `960cb6a8` | Filesystem docs | 修正 hadoop-uber shade POM 注释：OBS -> Hadoop。 | 注释，无需回补。 |
| 7 | `f2185284` | Core commit cleanup | `abort` 使用 `IndexFilePathFactories` 删除 data/compact increment 的新 index files，并保留 deleted index files。 | 直接 1.4 候选。 |
| 8 | `6509987c` | Avro format | 新增 `AvroBytesArray`，BinaryDecoder 下懒解码 array element bytes，BinaryEncoder 可原样写回 raw elements；带 micro benchmark 和测试。 | 性能优化；可选，不是 bugfix。 |
| 9 | `8393d28b` | PyPaimon schema | 增加 immutable options 集合、snapshot hint reconcile、alter option guard，并更新 Ray row-tracking 测试预期。 | 直接 PyPaimon 候选。 |
| 10 | `3cfaa512` | Core schema/Flink | `type` 标记 immutable；SchemaManager 和 Flink dynamic options 把默认/normalized type 视为 no-op，但拒绝真实改变。 | 直接 1.4 候选。 |
| 11 | `73d38b6d` | Filesystem security | hadoop-shaded dependencyManagement 强制 CVE-patched `commons-lang3` 和 `snappy-java`。 | 直接安全候选。 |
| 12 | `1b43de29` | GCS filesystem | `HadoopCompliantFileIO` status wrapper 返回 access time 和 owner。 | 直接候选。 |
| 13 | `ee61b54e` | Filesystem docs | 修正 hadoop-shaded-3.4 POM 注释 `jeysey-json` 拼写。 | 注释，无需回补。 |
| 14 | `208764a0` | COSN | 为 COSNLoader 声明 serialVersionUID。 | 低风险可选。 |
| 15 | `1b821f59` | COSN | COSN Hadoop config entry 日志从 warn 降到 debug。 | 可选，降低日志噪音。 |
| 16 | `aa41699e` | Docs generator | `escapeCharacters` 先转义 `&`，并保留 `<wbr>` placeholder 行为。 | 文档生成候选。 |
| 17 | `2b2904d2` | PyPaimon REST tests | mock REST server 用 `JSON.to_json(snapshot)` 写 snapshot，新增多次 row-tracking commit 测试。 | 测试 mock 修复，非 runtime。 |
| 18 | `bb085744` | Hive | Hive `PaimonRecordReader` 修正 `column.name.delimiter` option key。 | 直接 1.4 候选。 |
| 19 | `f797982e` | Spark filter | `EqualTo(NaN)` 生成 `isNaN` predicate，增加 float/double NaN scan 测试。 | 直接 1.4 候选。 |
| 20 | `450a993e` | PyPaimon Ray | HASH_FIXED PK `map_groups` group 内写入并返回序列化 commit messages，driver 统一 commit；文档说明 concurrency/remote args。 | 条件候选，需适配 1.4 Ray 写路径。 |
| 21 | `8490f4d5` | Spark DDL | Spark analysis rule 拒绝 Paimon 表 `SET LOCATION` 并加 DDL 测试。 | 直接候选。 |
| 22 | `fa62f98a` | Spark row-level ops | 为 deletion-vector append tables 新增 V2 delta UPDATE/DELETE/MERGE path、delta writer、Spark 4 rewrite branch 和 metrics。 | 新能力，非直接 1.4 bugfix。 |
| 23 | `ee50c951` | Common equality | `InternalRowUtils` map equality/hash 改为遍历 key/value arrays，用 typed equality 匹配 binary keys。 | 直接候选。 |
| 24 | `8b51ac5b` | Codegen equality | equaliser/codegen 修正 float/double、嵌套 floating point、map key equality，并避免含浮点字段走 BinaryRow raw equals。 | 直接候选。 |
| 25 | `1379eefb` | Parquet format | vectorized updater 按 Parquet primitive type 选择 `IntegerFromLongUpdater` / `FloatFromDoubleUpdater`，支持 dictionary narrowing。 | 直接候选。 |
| 26 | `d7e49334` | BLOB metrics | 新增 BlobFetch metrics group、reporter、writer hooks 和 metrics docs，统计 success/null/failure/http code。 | 观测新能力，非 bugfix。 |
| 27 | `5cfb2cf6` | Flink CDC tests | schema change IT 在 ALTER 前 sleep 等待新增表 snapshot，降低负载下 flaky。 | 测试稳定性，不影响 runtime。 |
| 28 | `a7fa5594` | Codegen loader docs | 修正 POM dependency comment 中 stale module 名称。 | 注释，无需回补。 |
| 29 | `23da7422` | Core read limit | 新增 common `LimitRecordReader`，FormatTable/KeyValue table read 在 table-read 层统一 limit；增加 PK multi-L0、auth filter、Flink IT。 | 直接候选，需适配。 |
| 30 | `4a2d409f` | Core pk-vector ANN compaction | ANN build 改为异步 executor，支持 waitCompaction、LSM-like levels、stale source filtering、search mode fallback 和 compaction options。 | pk-vector 新功能，非直接。 |
| 31 | `ec1d8ee8` | Limit hotfix | `LimitRecordReader` 检测 `ScoreRecordIterator` 并包装为 `LimitScoreRecordIterator`，保留 returnedScore/rowId。 | 跟随 `23da7422`。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master:refs/remotes/upstream/master release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/master` 前进到 `ec1d8ee8ac9c`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse fb3484df6e0053a94ed281f43e2dad09d04cf704..upstream/master` | 31 个 first-parent commit，均已纳入报告。 |
| `git diff --stat fb3484df6e0053a94ed281f43e2dad09d04cf704..upstream/master` | 147 files changed, 10531 insertions(+), 553 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <31 commits>` | 对 31 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <31 commits>` | 对 31 个 commit 均已执行并检查实际 patch hunks；大 patch 使用 targeted file checks 补充。 |
| `git cherry -v upstream/release-1.4 upstream/master fb3484df6e0053a94ed281f43e2dad09d04cf704` | 31 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted checks | `git ls-tree` / `git grep` 检查候选路径：Iceberg multiset、abort index cleanup、Hive delimiter、Spark NaN、binary map equality、floating equaliser、Parquet narrowing、KeyValueTableRead limit 等均未包含对应修复。 |
| Build / unit tests | 未运行 Maven 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `ec1d8ee8ac9c67972ac148e132f8e6c2952afee0`
- `last_processed_commit_date`: `2026-07-13 13:06:33 +0800`
- `last_report_date`: `2026-07-13`
- `last_report_path`: `reports/paimon-master-daily/2026-07-13.md`

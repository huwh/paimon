# Apache Paimon Master Daily Report - 2026-07-14

## Summary

范围：`ec1d8ee8ac9c67972ac148e132f8e6c2952afee0..4fe3cfdd131af7799259d6c4307d89a884c4b011`，共 13 个 first-parent 提交。当前 Apache upstream `master` head 是 `4fe3cfdd131a`（2026-07-14 13:18:12 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 125 个文件，`+10447/-857`。

本次主线集中在 source-backed primary-key index：先补齐 primary-key vector search 的 exact rerank 和 postpone-bucket 支持，再把 vector index 的 source metadata / write lifecycle 泛化为 primary-key index 基础设施，随后新增 BTree / Bitmap 标量主键索引定义、payload 构建、bucket 维护、写入接线，最后接入 primary-key batch scan 和 vector search 的 sorted-index pruning。其他变化包括 Variant storage 文档、`$partitions` 系统表新增 `deleted_record_count`，以及 Flink/Spark `rewrite_file_index` no-op rewrite 的冲突修复。

`git cherry -v upstream/release-1.4 upstream/master ec1d8ee8ac9c67972ac148e132f8e6c2952afee0` 显示 13 个提交均为 `+`，没有 patch-id 等价提交已进入 `release-1.4`。本次明确的 release-1.4 bugfix 候选是 `0376c8bb`，因为 1.4 已有 Flink/Spark rewrite file index 生产路径且仍会为 no-op rewrite 构造 compact commit message；`55e4358d` 只改新测试同步方式，没有生产影响。主键向量/排序索引功能栈依赖 master 近期新增的 `pkvector` / `pksorted` / source-backed index 代码，通常不作为 1.4 直接 bugfix 回补。

所有 commit 都先执行 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在带 `--name-status` 时不展开 hunk，又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch。release-1.4 判断使用 `git cherry`、`git patch-id --stable`、`git ls-tree`、`git log -- <path>` 和 targeted `git show upstream/release-1.4:<path>` 对照生产路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Primary-key vector search | 2 | `7e396cff` 增加 exact rerank：ANN 候选和 exact fallback 分流、`VectorSearchRefineOptions`、Spark driver rerank；`26f415a1` 允许 `bucket=-2` postpone table 使用 PK-vector，并让 Flink/Spark compact 同时处理 postpone bucket 和 real Level-0 files。 | 1.4 缺 master 的 primary-key vector index 栈；不是直接 bugfix。 |
| Primary-key index infrastructure | 3 | `f87fe0fb` 将 `PkVectorSource*` 泛化为 `PrimaryKeyIndexSource*`；`9107d0a` 新增 `BucketedPrimaryKeyIndexMaintainer` 协调 source-backed index lifecycle；`f31f199e` 增加 `pk-btree.index.columns` / `pk-bitmap.index.columns`、定义解析和 schema 校验。 | 新功能基础设施，依赖 master index 栈。 |
| Source-backed sorted index write/read | 4 | `c8a87e9c` 构建 BTree/Bitmap payload builder；`be80abff` 维护 bucket-local sorted payload；`4dddb86a` 接入写入并把 source-backed payload 排除出普通 global row-id scanner；`4fe3cfdd` 让 primary-key batch scan / vector search 使用 sorted index pruning。 | 新功能；1.4 无 `pksorted` 和 source-backed scan planning。 |
| System tables / PyPaimon | 1 | `cad04196` 为 Java 和 PyPaimon `$partitions` 增加 `deleted_record_count`，按 deletion-vector cardinality 聚合，legacy cardinality 缺失返回 `NULL`。 | 功能增强，可选回补但不是 bugfix。 |
| Docs | 1 | `32e10321` 新增 Variant storage 文档，说明 Parquet Variant encoding、shredding、pushdown 和限制。 | 文档新增，不影响 runtime。 |
| Bugfix / test stability | 2 | `55e4358d` 修正新 sorted-index 单测同步方式；`0376c8bb` 在 Flink/Spark rewrite file index 中跳过 `entry.file().equals(indexedFile)` 的 no-op rewrite，并验证 snapshot 不前进。 | `0376c8bb` 是直接 1.4 候选；`55e4358d` 测试-only。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `55e4358d` | `BucketedSortedIndexMaintainerTest` 不再用额外 latch 判断 executor task 完成，改为提交空 task 等待前序任务完成。 | `git cherry -v upstream/release-1.4 55e4358d 55e4358d^` 为 `+`；patch-id `286f940eec0a...`。 | 1.4 没有本次新增的 `pksorted/BucketedSortedIndexMaintainerTest`。 | 测试稳定性，无 release runtime 影响。 |
| `0376c8bb` | Flink `RewriteFileIndexSink` 和 Spark `RewriteFileIndexProcedure` 在 `FileIndexProcessor.process` 返回原文件时跳过 commit message，避免 no-op rewrite 仍提交 compact before/after 相同文件。 | `git cherry -v upstream/release-1.4 0376c8bb 0376c8bb^` 为 `+`；patch-id `9749e7df9f28...`；`release-1.4` 聚焦路径历史无等价修复。 | `release-1.4` 存在两个生产文件，且代码仍无 `entry.file().equals(indexedFile)` guard，会继续构造 `CompactIncrement(Collections.singletonList(entry.file()), Collections.singletonList(indexedFile), ...)`。 | 直接候选；重复调用 `rewrite_file_index` 或文件索引已无需重写时，可能触发冲突错误或不必要 snapshot。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `7e396cff` | PK-vector search | 将 bucket search 拆成 indexed/exact candidate streams，新增 refine/rerank option 解析和 search-limit overflow 校验；Spark distributed search 返回 serialized `SearchResult` 后在 driver rerank。 | PK-vector 新能力，非直接。 |
| 2 | `26f415a1` | PK-vector postpone bucket | `FileSystemWriteRestore` 支持 pinned snapshot；`PostponeUtils` 增加 snapshot-aware bucket/row-count/Level-0 枚举；Flink/Spark compact 用固定 snapshot 同时处理 postpone bucket 与 real L0，并发布 ANN index。 | 新功能，非直接。 |
| 3 | `f87fe0fb` | PK index metadata | `PkVectorSourceFile/Meta/Policy` 重命名并泛化到 `org.apache.paimon.index.pk`，vector maintainer/searcher 改用通用 source metadata。 | 重构/新功能基础，非直接。 |
| 4 | `32e10321` | Docs | 新增 `docs/docs/multimodal-table/variant.mdx`，覆盖 Variant vs JSON、plain/shredded layout、schema inference、Spark pushdown 和限制。 | 文档新增。 |
| 5 | `9107d0a` | PK index write lifecycle | 新增 `BucketedPrimaryKeyIndexMaintainer` 包装 vector maintainer；`AbstractFileStoreWrite` 从 vector-specific factory 改为 primary-key index coordinator，并把 restore payload 命名为 source index payloads。 | 新功能基础，非直接。 |
| 6 | `f31f199e` | BTree/Bitmap definitions | CoreOptions 增加 `pk-btree.index.columns` / `pk-bitmap.index.columns` 和 per-field JSON option 解析；schema validation 拒绝重复列、跨 family 重复、非 PK/DV/不支持类型以及改/drop indexed column。 | 新功能，非直接。 |
| 7 | `c8a87e9c` | Sorted payload build | 新增 `SortedSingleColumnIndexWriter`、`PkSortedDataFileReader`、`PkSortedIndexBuilder/File/Group`，从 compact data file 读取索引列并排序生成 BTree/Bitmap payload。 | 新功能，非直接。 |
| 8 | `be80abff` | Sorted payload maintenance | 新增 `BucketedSortedIndexMaintainer` 和 bucket state：识别 active source files、剔除 stale payload、异步 build/retry、失败清理和恢复期 rejected payload。 | 新功能，非直接。 |
| 9 | `4dddb86a` | Write wiring | `KeyValueFileStore` 按所有 PK index definitions 创建 coordinator；sorted/vector commits 合并到 append/compact increments；`GlobalIndexScanner` / coverage 跳过含 `sourceMeta` 的 source-backed payload。 | 新功能，非直接。 |
| 10 | `55e4358d` | Test hotfix | sorted-index maintainer 单测用 executor barrier 代替 task 内 completed latch，避免异步完成判断不稳定。 | 测试-only。 |
| 11 | `cad04196` | System table | Java/PyPaimon `$partitions` 新增 nullable `deleted_record_count`，只在投影或谓词需要该列时扫描 DV index，按 partition 聚合 cardinality。 | 功能增强，可选。 |
| 12 | `0376c8bb` | Flink/Spark file index rewrite | Flink operator `return`、Spark iterator `continue` 跳过 no-op rewrite；新增 Flink/Spark 测试确认重复 rewrite 不创建新 snapshot。 | 直接 1.4 bugfix 候选。 |
| 13 | `4fe3cfdd` | Sorted index pruning | 将 batch scan 拆为 append/primary-key scan；新增 `PrimaryKeySortedIndexScan/Result`，用 source-backed BTree/Bitmap 产出 file-local `IndexedSplit` ranges，并让 vector ANN/exact search 复用 row ranges。 | 新功能，非直接。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master` 前进到 `4fe3cfdd131a`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse ec1d8ee8ac9c67972ac148e132f8e6c2952afee0..upstream/master` | 13 个 first-parent commit，均已纳入报告。 |
| `git diff --stat ec1d8ee8ac9c67972ac148e132f8e6c2952afee0..upstream/master` | 125 files changed, 10447 insertions(+), 857 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <13 commits>` | 对 13 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <13 commits>` | 对 13 个 commit 均已执行并检查实际 patch hunks；大 patch 使用 targeted file checks 补充。 |
| `git cherry -v upstream/release-1.4 upstream/master ec1d8ee8ac9c67972ac148e132f8e6c2952afee0` | 13 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted checks | `git ls-tree` / `git log -- <path>` / `git show upstream/release-1.4:<path>` 确认 `rewrite_file_index` 两个生产路径存在且缺 no-op guard。 |
| Build / unit tests | 未运行 Maven 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `4fe3cfdd131af7799259d6c4307d89a884c4b011`
- `last_processed_commit_date`: `2026-07-14 13:18:12 +0800`
- `last_report_date`: `2026-07-14`
- `last_report_path`: `reports/paimon-master-daily/2026-07-14.md`

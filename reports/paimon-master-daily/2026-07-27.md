# Apache Paimon Master Daily Report - 2026-07-27

## Summary

范围：`a998338ec62f2cae5c0649e797eda4d02be5de36..45cf5ffb003368ea2efb0a6a92fed8f27dc6dd0b`，共 17 个 first-parent 提交，161 个文件变更，`+14065/-1045`。本轮 `upstream/master` 前进到 `45cf5ffb0033`（2026-07-26 23:12:28 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

主要变化集中在 PyPaimon 读写正确性、data evolution/global index、postpone bucket merge-on-read、Format Table 扫描并行度、Variant/MAP shredding、自适应/向量检索以及 Iceberg VARIANT 元数据转换。按 patch 和 release-1.4 实际代码路径判断，本轮最值得关注的 1.4 候选是 `e069a756c`（data evolution 类型变更列 stats pruning）和 `0a8d04260`（scored global index 多 split 合并后再 topK）。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| PyPaimon read / write / aggregation | 4 | Parquet VARIANT row-group 有界读取、Daft PK 分布式写协调、theta_sketch、Java 对齐的 sum/product 与 Decimal 支持。 | 多数依赖 1.5/master 才有的 PyPaimon 结构；未见可直接套到 1.4 的聚合器/Daft 文件。 |
| Core scan / data evolution / global index | 6 | catalog-managed Format Table 分区并行 list；索引族独立 search-mode；data evolution 类型变更列禁用错误 stats pruning；scored result materialize；chain table key-range split；coverage constructor hotfix。 | `e069a756c` 与 `0a8d04260` 在 1.4 有相近生产路径，建议单独 backport 评估。 |
| Postpone bucket merge-on-read | 2 | 新增 `postpone.merge-on-read`，核心 `PostponeMergePlan/Read/ReadBuilder`，real bucket 与 postpone records 合并读取；后续补齐 Flink/Spark snapshot-bound、reroute 和并行度逻辑。 | 1.4 不具备该 MOR 架构，属于 master-only 功能。 |
| Spark connector | 4 | lateral vector search LIMIT 输入补 shuffle；postpone MOR Spark scan/exec；scored vector read 合并逻辑；Spark 3/4 shim 分布。 | `0a8d04260` 的 scored 合并问题在 1.4 旧 vector/full-text read 中有类似路径；其余多为 master 功能。 |
| Flink connector | 2 | data evolution action search-mode 参数切换；postpone MOR batch source、禁用 lookup/dynamic filtering 不兼容路径。 | 1.4 无 postpone MOR；search-mode family options 也未见同名配置入口。 |
| Format / Iceberg / docs dependencies | 3 | inline blob 超大读取显式失败；Iceberg VARIANT metadata 转换但发布时阻止；docs postcss/nanoid lockfile bump。 | blob reader 类与 docs yarn lock 在 1.4 不同或缺失；Iceberg VARIANT 属 master 新类型路径。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `20377469c` | PyPaimon Parquet VARIANT 改为 row-group 有界读取，并递归组装嵌套 shredded VARIANT。 | `git cherry` 为 `+`，未在 1.4 发现等价 patch。 | 1.4 有 `FormatPyArrowReader`/`split_read.py`，但缺少 `variant_shredding.py` 与本 patch 的 shredded VARIANT 读路径。 | 低；看起来是 master PyPaimon shredding 路径问题，不建议直接回摘。 |
| `e069a756c` | data evolution 统计裁剪遇到列类型变化时不再沿用旧文件 min/max/null stats。 | `git cherry` 为 `+`。 | 1.4 的 `DataEvolutionFileStoreScan.evolutionStats` 仍有同一 TODO，命中字段 id 后直接 `rowOffsets[j] = i`，类型不匹配时可能错误裁剪 pre-ALTER 文件。 | 高；建议为 release-1.4 做适配 backport，并带 `testTypeChangedColumnMustNotPrunePreAlterFiles` 等价测试。 |
| `1ad95324a` | inline BLOB 声明长度超过 `Integer.MAX_VALUE` 时显式抛错，避免 `(int) length` 静默截断。 | `git cherry` 为 `+`。 | 1.4 没有 `AbstractBlobElementReader`，blob reader 文件集合不同。 | 低；无直接同构代码路径。 |
| `0a8d04260` | scored global index 合并时先 materialize 分数，再合并/topK，避免大量 lazy `or()` 链在 topK 时重复/深链访问。 | `git cherry` 为 `+`。 | 1.4 有 `ScoredGlobalIndexResult.or`、`UnionGlobalIndexReader.visitVectorSearch`，以及 `VectorReadImpl`/`FullTextReadImpl` 中逐步 `result = result.or(next.get())` 再 `topK(limit)` 的旧路径。 | 中高；建议对 1.4 旧类名做语义回摘，尤其是 vector/full-text 多 split 查询。 |
| `d6b44a0ce` | Daft PK 分布式写新增单 worker 限制、commit-message 二阶段提交、动态 bucket 路由与 Java/Python E2E。 | `git cherry` 为 `+`。 | 1.4 有基础 Py write/dynamic bucket 文件，但缺少 `pypaimon/daft/*` 和新增 `pypaimon/index/dynamic_bucket.py`。 | 低；Daft 路径 master-only，除非 1.4 ToB 分支另行引入 Daft。 |
| `bce7744d9` | PyPaimon `FieldSumAgg` 对齐 Java：整型溢出、DECIMAL 精度/scale、retract，并新增 `product`。 | `git cherry` 为 `+`。 | 1.4 未发现 `FieldSumAgg` / `aggregators.py` / `merge_engine_support.py` 路径。 | 低；无直接 1.4 生产路径。 |
| `679af6914` | docs `postcss` 8.5.15 -> 8.5.23，连带 `nanoid`。 | `git cherry` 为 `+`。 | 1.4 docs 结构未见 `docs/yarn.lock` 中对应锁定项。 | 低；若 1.4 docs 另有 Node lockfile，需要单独做依赖审计。 |
| `45cf5ffb0` | 移除 `DataEvolutionGlobalIndexCoverage` 无用构造器并修正测试调用。 | `git cherry` 为 `+`。 | 1.4 未发现 `DataEvolutionGlobalIndexCoverage` 类。 | 无直接影响。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `20377469c` | PyPaimon Parquet / VARIANT | `FormatPyArrowReader` 新增 `_bounded_variant_read`，用 `pyarrow.parquet.ParquetFile.iter_batches(row_groups=...)` 分 row group 读取；补 predicate 字段列读取、嵌套路径选择、递归 `_assemble_variant_column`，并新增 434 行 row-group VARIANT 测试。 | master shredding 路径为主，1.4 缺失关键文件，低影响。 |
| 2 | `75d6dfb49` | Format Table scan | 新增 `format-table.scan.list-parallelism`；抽出 `SplitEnumerator`、`CatalogSplitEnumerator`、`FileSystemSplitEnumerator`；catalog-managed partition list 使用 bounded executor 并对缺失/腐坏分区给出告警。 | 性能/重构类，未作为 1.4 bugfix。 |
| 3 | `3f84bbdc7` | Global index search mode | `global-index.search-mode` 改为无默认 fallback；新增 `scalar-index.search-mode=full`、`vector-index.search-mode=fast`、`full-text-index.search-mode=fast`；Java/Python/Flink/Spark 调用点切到 family-specific option。 | 1.4 未见同名配置入口，属于 master 行为演进。 |
| 4 | `e069a756c` | Data evolution scan | 在 `evolutionStats` 中记录 `typeMismatchedFieldIds`，类型变化字段保持 `rowOffsets=-1`，避免 predicate 使用旧类型 stats 裁剪历史文件；新增复现测试。 | 高影响候选，见上表。 |
| 5 | `1ad95324a` | Blob format | `AbstractBlobElementReader.read` 在 inline blob 长度超过 int 上限时抛出清晰错误，避免静默截断；新增 overflow 测试。 | 1.4 无同构 reader 类，低影响。 |
| 6 | `4badbf69d` | Chain table | 新增 `chain-table.split.key-range-enabled`；batch chain scan 使用 snapshot 分支 key comparator，经 `IntervalPartition` 保证同 key 的 snapshot/delta 文件同 split，再按大小 pack。 | 性能功能，未作为 1.4 bugfix。 |
| 7 | `999df993c` | Variant / MAP shredding | 新增 Variant shredding adaptive inference mode、adaptive buffer/retention 配置、`VariantShreddingInferenceSession`；MAP shared shredding 改为跨文件根据完成文件 stats 计算下一文件列数；writer factory 增加 file completion 回调。 | master shredding 功能，未作为 1.4 bugfix。 |
| 8 | `0a8d04260` | Scored global index | 新增 `ScoredGlobalIndexResult.merge(List)`，合并时把 rowId->score materialize；Union reader、core vector/full-text、Spark vector read 均改为收集结果后 merge/topK；新增 5000 split 测试。 | 中高影响候选，见上表。 |
| 9 | `1f3040a87` | Spark vector search | 新增 `vector-search.lateral-join.parallelism` 和 `RepartitionLateralVectorSearchInput` optimizer rule；Spark physical plan 对 LIMIT 输入声明 clustered distribution，恢复 lateral vector search 并行度。 | Spark 性能/执行计划优化，未作为 1.4 bugfix。 |
| 10 | `39de52c52` | Iceberg compatibility | Iceberg metadata 层支持 `variant` 类型序列化/解析；commit callback 在发布 schema 时阻止 VARIANT，因为 Iceberg v3 row lineage 要求当前不满足；修正 array element required 判断。 | VARIANT/Iceberg master 新路径，低影响。 |
| 11 | `cb51c6813` | Postpone MOR / Spark | 新增 `postpone.merge-on-read`、`PostponeMergePlan/Read/ReadBuilder`、postpone 文件按 writer 分组、bucket router、real+postpone merge reader，以及 Spark scan/exec/AQE 相关实现和测试。 | master-only 功能。 |
| 12 | `d6b44a0ce` | PyPaimon Daft write | Daft direct PK 写增加安全检查；新增 grouped write UDF、commit-only sink、dynamic bucket helper、commit conflict handling 和 Java/Python E2E。 | master Daft 路径，低影响。 |
| 13 | `3ed6fa676` | PyPaimon aggregation | 引入 `datasketches` 依赖并注册 `theta_sketch` 聚合器，读取两侧 compact sketch 后 union。 | 新功能，未作为 1.4 bugfix。 |
| 14 | `bce7744d9` | PyPaimon aggregation | 新增 Python `Decimal` 类型工具；`sum` 增加整型溢出检查、DECIMAL 精度/scale、retract；新增 `product` 聚合器和大量单测。 | 1.4 缺少对应聚合器路径，低影响。 |
| 15 | `679af6914` | Docs dependency | `docs/yarn.lock` 中 `postcss` 升到 8.5.23、`nanoid` 升到 3.3.16。 | 1.4 docs lockfile 不同，低影响。 |
| 16 | `930fdfaaf` | Flink / Spark postpone MOR | 补齐 Flink batch source 的 postpone MOR 自定义 source、snapshot-bound builder、lookup/dynamic filtering 禁用、并行度推断与 reroute；Spark MOR scan/exec 继续适配。 | master-only 功能。 |
| 17 | `45cf5ffb0` | Data evolution hotfix | 删除 `DataEvolutionGlobalIndexCoverage` 无用构造器，测试改为显式传 `scalarIndexSearchMode()`。 | 1.4 无该类，无直接影响。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，工作区进入本轮时干净。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功；fork 自动化分支已刷新。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=45cf5ffb0033`，`upstream/release-1.4=05d1ba75a97c`。 |
| `git log --first-parent --reverse ... a998338e..upstream/master` | 17 个 first-parent commit。 |
| `git diff --stat/--name-status a998338e..upstream/master` | 161 files，`+14065/-1045`。 |
| `git show --stat --name-status --patch <commit>` / `git show --patch --stat <commit>` | 已为 17 个提交生成并检查 patch；因 `--name-status` 会压制 patch 展示，另用 `--patch --stat` 核验实际 hunk。 |
| `git cherry -v upstream/release-1.4 upstream/master a998338e` | 17 个提交全部为 `+`，release-1.4 无 patch-id 等价提交。 |
| release-1.4 targeted `git cat-file` / `git grep` / `git show` | 已核验候选 bugfix 的生产文件存在性和旧分支代码路径。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报和推进 state。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `45cf5ffb003368ea2efb0a6a92fed8f27dc6dd0b`
- `last_processed_commit_date`: `2026-07-26 23:12:28 +0800`
- `last_report_date`: `2026-07-27`
- `last_report_path`: `reports/paimon-master-daily/2026-07-27.md`

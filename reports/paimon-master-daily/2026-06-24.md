# Apache Paimon Master Daily Report - 2026-06-24

## Summary

范围：`eae7d22ff4c7ad7e5885bf02d421aff340f79178..50f28639d8e6036473b2d595f873c66b554f1804`，共 24 个 first-parent 提交。当前 Apache upstream `master` head 是 `50f28639d8e6`（2026-06-24 09:25:10 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次主线变化较分散，但重点集中在 Spark 写入/SQL/procedure/vector search、Core snapshot/catalog/manifest/compaction、Flink sink/compaction、Common search/security/file-index/shared-shredding、Parquet/ORC format reader、Iceberg REST metadata、OSS BYOK、PyPaimon CLI，以及少量文档和测试覆盖。

`release-1.4` 方面，24 个提交经 `git cherry -v upstream/release-1.4 upstream/master eae7d22ff4c7ad7e5885bf02d421aff340f79178` 检查均为 `+`，未发现等价 patch 已 cherry-pick。优先关注 8 个直接生产修复：Spark `REPLACE COLUMNS` silent corruption、Spark streaming checkpoint resume、Hive/JDBC metastore table path missing drop、format table empty option validation、Hadoop Kerberos config legality、file-index predicate duplicate traversal、Parquet/ORC case-insensitive read、Iceberg REST schema drift crash。`e01147795` 的 remote lookup overwrite 修复在 `release-1.4` 旧实现上不直接复现；hybrid route 修复缺少 `HybridSearchRoute` 路径；RowID manifest sort 依赖 master 的 `ManifestFileSorter`，release-1.4 不直接适用。

所有 commit 都用 `git show --stat --name-status --patch` 查看文件状态，并补跑 `git show --stat --patch` 读取实际 hunks，因为当前 Git 输出在带 `--name-status` 时未显示 patch hunks。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Spark SQL / write / procedures / vector | 6 | `0e8ce7664` 禁止 Spark `ALTER TABLE ... REPLACE COLUMNS`；`e2b5eb9d8` 给 snapshot 记录逻辑 operation；`1d2cf8478` 增强 SQL UDF body analysis；`031fb354f` 修复 streaming checkpoint resume；`c96f22b79` 新增 `compact_chain_table`；`ffaebaec5` 支持 lateral inner join vector search。 | `0e8ce7664`、`031fb354f` 是明确 bugfix 候选；其余多为新能力或元数据增强。 |
| Core catalog / snapshot / compaction / manifest | 6 | `e2b5eb9d8` 扩展 `Snapshot.Operation`；`d886309b1` 修复 metastore 表存在但目录缺失时 drop；`efed7dbb0` compaction 可跳过已过期分区；`67978a6b9` format table 空选项前置校验；`e01147795` 修正 master overwrite + remote lookup file；`50f28639d` data-evolution manifest sort 支持 RowID。 | `d886309b1`、`67978a6b9` 是直接候选；`e01147795` 在 release-1.4 旧实现不直接复现；`50f28639d` 依赖 release 缺失的 manifest-sort 实现。 |
| Flink sink / compaction | 2 | `8fb1bc5fe` 将 committer context 从 operator-only 状态中解耦；`efed7dbb0` 在 compactor source 过滤已过期 values-time 分区。 | 主要是重构和功能增强；不是紧急 bugfix。 |
| Common search / global-index / file-index / security | 6 | `1b6f6cbe9` 拒绝非有限 hybrid route weight；`8168aad14` 缺 Kerberos keytab/principal 时不包装 secured FS；`522e2ea10` 拒绝混用 vector/full-text builder settings；`c44b9e7df` 修复 file-index predicate 重复 traversal；`8aeeb1599` btree prefix range scan；`0644a727f` MAP shared-shredding write helpers。 | `8168aad14`、`c44b9e7df` 是直接候选；hybrid route 相关路径在 release-1.4 不直接存在。 |
| Format readers | 1 | `0e65589ef` 将 catalog `case-sensitive=false` 传递到 format reader，并为 Parquet/ORC 做 case-insensitive field matching。 | release-1.4 同路径仍精确大小写匹配，是明确候选。 |
| Iceberg REST | 1 | `0023c3512` 对 REST schema dedup/remap/renumber，处理 option-only schema version drift，并避免空 snapshot table 反复 drop/create。 | release-1.4 是 shift-only schema id 逻辑，是高优先级候选。 |
| Filesystems / OSS | 1 | `7a675c956` 通过 OSS SDK internal operation swap 支持 customer CMK SSE-KMS key id。 | 新 OSS 能力；是否回补取决于 BYOK 需求。 |
| PyPaimon / docs / tests | 3 | `815990c90` 增加 tag CLI；`ef9c31b1c` 更新 Spark compatibility matrix；`1ef01ddc2` 仅补 dedicated-format fallback 测试。 | 新 CLI / docs / test-only，无紧急 runtime 回补压力。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `0e8ce7664` | Spark 拒绝 `ALTER TABLE ... REPLACE COLUMNS`，避免 Spark drop+add 重新分配 field id 后把同名旧数据读成 null。 | patch-id `ea34f1798edc`; cherry `+`。 | release-1.4 的 `PaimonAnalysis.scala` 只有 `PaimonDropPartitions` case，没有 `ReplaceColumns` guard。 | 高。Spark 用户可触发 silent data corruption，建议回补。 |
| `031fb354f` | Spark streaming restart 只在 checkpointed snapshot 已过期时回退 `initOffset`，避免 latest-full 重启后重扫整表。 | patch-id `1f33d23b1473`; cherry `+`。 | release-1.4 仍是 `if (startOffset0.compareTo(initOffset) < 0) initOffset`。 | 高。影响 checkpoint 恢复语义。 |
| `d886309b1` | metastore 有表但表目录丢失时，Hive/JDBC catalog 仍可 drop metastore metadata。 | patch-id `db79318f0e5a`; cherry `+`。 | release-1.4 `AbstractCatalog.dropTable` 先 `getTable`，目录缺失会抛 `TableNotExistException` 并不执行 `dropTableImpl`；Hive/JDBC 无 tableExists override。 | 高。避免残留 HMS/JDBC metadata 和人工清理。 |
| `67978a6b9` | format table 创建时前置拒绝空 CSV/JSON/Text delimiter/quote/escape，避免 INSERT/SELECT 时 `StringIndexOutOfBoundsException`。 | patch-id `45f898eb1d0b`; cherry `+`。 | release-1.4 format table validation 只检查 primary key 和 data-token/engine 条件。 | 中到高。错误更早暴露，回补集中。 |
| `8168aad14` | 缺 keytab 或 principal 时 `SecurityConfiguration.isLegal()` 返回 false，避免无 Kerberos 配置也包装 secured Hadoop FS。 | patch-id `4465f263a371`; cherry `+`。 | release-1.4 在 keytab/principal 都为空时返回 true。 | 高。影响 Hadoop FileSystem 初始化和外部 UGI 场景。 |
| `c44b9e7df` | `FileIndexPredicate.getRequiredNames` 改用 `PredicateVisitor.collectFieldNames`，避免 compound child 被 visit 两次。 | patch-id `81b6ff2f8887`; cherry `+`。 | release-1.4 同方法里先 `child.visit(this)` 又 `result.addAll(child.visit(this))`。 | 中。通常是额外遍历/副作用风险，修复很小。 |
| `0e65589ef` | Parquet/ORC reader 在 catalog `case-sensitive=false` 时按大小写不敏感匹配列，并处理 Parquet duplicate ambiguity。 | patch-id `16f73286f5d8`; cherry `+`。 | release-1.4 `FormatReadBuilder` 只用 table options，`ParquetReaderFactory` 精确 `containsField/getType`，ORC 未传 `IS_SCHEMA_EVOLUTION_CASE_SENSITIVE`。 | 中到高。影响外部 mixed-case Parquet/ORC format table 读取。 |
| `0023c3512` | Iceberg REST sync 对 option-only schema changes 做 schema dedup/remap/renumber，避免 `Cannot set current schema to unknown schema`。 | patch-id `ce0e84e87fcc`; cherry `+`。 | release-1.4 `adjustMetadataForRest` 仅 schema id +1，随后 `addSchema`/`setCurrentSchema`；无 duplicate schema remap。 | 高。Iceberg REST + option-only schema version drift 会崩溃。 |
| `e01147795` | master 上修正 overwrite 场景仍创建 remote lookup file，避免 lookup remote file 缺失。 | patch-id `ece38aef7c83`; cherry `+`。 | release-1.4 旧 `MergeTreeCompactManagerFactory` 没有 master 新增的 `lookupEnabled = !ignorePreviousFiles && needLookup` 参数，仍走 `options.needLookup()`。 | 低/不直接适用。旧 release 路径看起来未引入同一回归。 |
| `1b6f6cbe9` / `522e2ea10` | hybrid route 拒绝非有限 weight 和混用 vector/full-text builder settings。 | patch-ids `3e07eea00e9b`, `a28e54cc430a`; cherry `+`。 | release-1.4 缺 `HybridSearchRoute.java`、`HybridSearchRanker.java`，仅有较早 global-index/vector/full-text 路径。 | 低/不直接适用。需等 hybrid route 前置能力进入。 |
| `50f28639d` | data-evolution manifest sort 按 RowID 排序，partitioned 表先 partition 后 RowID。 | patch-id `8c789aa7f2ec`; cherry `+`。 | release-1.4 有 data evolution，但没有 `ManifestFileSorter` / `manifest-sort.*` 选项。 | 低/不直接适用。不是当前 release 代码路径。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `0e8ce7664` | Spark schema evolution | 在 `PaimonAnalysis` 中对 resolved Paimon `ReplaceColumns` 直接抛 `UnsupportedOperationException`，测试覆盖 Spark REPLACE COLUMNS。 | 直接修复候选。 |
| 2 | `e2b5eb9d8` | Snapshot / Spark write | `Snapshot` 增加 nullable `operation` enum，`FileStoreCommit` / `BatchTableCommit` 可设置 operation，Spark INSERT/OVERWRITE/UPDATE/DELETE/MERGE/CTAS/RTAS/TRUNCATE 写入对应 operation。 | 元数据增强；旧 snapshot 兼容，非紧急 bugfix。 |
| 3 | `815990c90` | PyPaimon CLI | 新增 `paimon tag create/list/get/delete`，支持 prefix/json/snapshot-id/ignore-if-exists，并补 CLI 集成测试和文档。 | 新 CLI 能力。 |
| 4 | `8fb1bc5fe` | Flink sink | `Committer.Context` 改用抽象 `StateStore` 和普通 `MetricGroup`，新增 `OperatorBackendStateStore`，snapshot state 移到 committer 显式方法。 | 纯重构，为 coordinator 承载做准备。 |
| 5 | `1d2cf8478` | Spark SQL UDF | Spark 4 common 新增完整 `CreatePaimonSQLFunctionCommand`，分析函数 body、推导 return type、校验 deterministic/aggregate/window/cyclic/temp object；Spark 4.0 轻量实现保留。 | 功能/校验增强，release 是否需要取决于 SQL UDF 支持范围。 |
| 6 | `1ef01ddc2` | Core tests | 增加 dedicated-format bundle fallback 覆盖，验证缺少 dedicated encoder 时 fallback writer 正常写 vector 数据。 | test-only。 |
| 7 | `031fb354f` | Spark streaming | `PaimonMicroBatchStream.planInputPartitions` 不再用 `compareTo(initOffset)` 盲目 clamp，而是只在 checkpointed snapshot 早于 earliest snapshot 时回退并记录 warning。 | 直接修复候选。 |
| 8 | `d886309b1` | Catalog drop | `AbstractCatalog.dropTable` 先按 catalog 判断存在性，只有文件系统表存在时读取 external paths；Hive/JDBC override 用 metastore 判断存在性。 | 直接修复候选。 |
| 9 | `efed7dbb0` | Core/Flink compaction | 新增 `compaction.skip-expired-partitions`；Flink `CompactorSourceBuilder` 用 values-time partition expiration 策略过滤已过期 partitions。 | 功能增强，降低无效 compaction IO。 |
| 10 | `c96f22b79` | Spark procedure / chain table | 新增 `CompactChainTableProcedure`，支持单 partition 合并 chain table snapshot/delta 到 snapshot branch，并扩展 `ChainGroupReadTable` batch scan helpers。 | 新 procedure。 |
| 11 | `ef9c31b1c` | Docs | Spark compatibility matrix 从 3.2-4.0 更新到 3.2-4.1。 | docs-only。 |
| 12 | `67978a6b9` | Format table validation | `CatalogUtils` 为 CSV/JSON/Text format table 校验 delimiter/quote/escape/line delimiter 非空。 | 直接修复候选。 |
| 13 | `1b6f6cbe9` | Hybrid search | Java/Python/Spark route weight 和 `HybridSearchRanker.WeightedResult` 均拒绝 NaN/Infinity/非正数。 | release-1.4 缺 hybrid route 直接路径。 |
| 14 | `8168aad14` | Hadoop security | `SecurityConfiguration.isLegal()` 改为 keytab/principal 任一缺失都非法，且 keytab 必须存在可读；测试覆盖空配置和单 keytab。 | 直接修复候选。 |
| 15 | `e01147795` | Remote lookup file | master 上把 compact manager 参数语义从 `lookupEnabled` 改为 `ignorePreviousFiles`，lookup 仍按 `options.needLookup()` 创建；overwrite 不再跳过 remote lookup file。 | release-1.4 旧实现不直接复现。 |
| 16 | `0644a727f` | Common shared-shredding | 新增 MAP shared-shredding allocator/context/field dict/row converter，为后续 MAP write path 做准备。 | 新内部能力，尚未接入完整写路径。 |
| 17 | `7a675c956` | OSS filesystem | 新增 `fs.oss.server-side-encryption-key-id`，通过反射替换 OSS object/multipart operations，为 PutObject/CopyObject/InitiateMultipartUpload 打 SSE-KMS CMK header。 | 新 BYOK 能力。 |
| 18 | `ffaebaec5` | Spark vector search | 支持 lateral inner join vector search，新增 dynamic vector relation、optimizer filter pushdown、physical exec batching，以及 `vector-search.lateral-join.batch-size`。 | 新 Spark vector 能力。 |
| 19 | `522e2ea10` | Hybrid route builder | `HybridSearchRoute.Builder` 显式记录 route type，混用 vector/full-text settings 时报错；非法 full-text query 不污染 builder state。 | release-1.4 缺直接路径。 |
| 20 | `0e65589ef` | Parquet/ORC format reader | `FormatReadBuilder` 合并 catalog case-sensitive；ORC 设置 schema evolution case flag；Parquet 增加大小写不敏感 field matching 和 duplicate 检测。 | 直接修复候选。 |
| 21 | `c44b9e7df` | File-index predicate | `getRequiredNames` 复用通用 field-name collector，避免 compound children 被 visit 两次。 | 直接修复候选。 |
| 22 | `8aeeb1599` | BTree global index | `BTreeIndexReader.visitStartsWith` 用 prefix upper bound 做 range query，避免 prefix 查询扫描全 segment。 | 性能/能力增强；release 有 btree 但不是 bugfix。 |
| 23 | `0023c3512` | Iceberg REST metadata | REST commit 对空 snapshot table 按新表更新；schema dedup、remap并顺序重编号；properties 只在变化时 set。 | 直接修复候选。 |
| 24 | `50f28639d` | Core manifest sort | `ManifestFileSorter` 抽象 `ManifestSortKey`，data-evolution manifest sort 在有 RowID range 时按 partition + RowID 排序，并校验相关 schema 条件。 | release-1.4 缺 manifest-sort 路径，不直接适用。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 成功；远端 fork 分支仍停在 2026-06-22，本地有 2026-06-23 未推送提交。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=50f28639d8e6036473b2d595f873c66b554f1804`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git rev-list --first-parent --reverse eae7d22ff4c7ad7e5885bf02d421aff340f79178..upstream/master` | 24 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 24 个 commit 均已执行，用于文件状态和 patch 检查入口。 |
| `git show --stat --patch <commit>` | 对 24 个 commit 均补跑并检查实际 hunks。 |
| `git show <commit> \| git patch-id --stable` | 对 bugfix/条件候选计算 stable patch-id。 |
| `git cherry -v upstream/release-1.4 upstream/master eae7d22ff4c7ad7e5885bf02d421aff340f79178` | 24 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted code checks | 检查 Spark analysis/streaming、Hive/JDBC drop、format validation、Kerberos security、remote lookup、file-index、Parquet/ORC reader、Iceberg REST、hybrid route、manifest sort 等触点。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `50f28639d8e6036473b2d595f873c66b554f1804`
- `last_processed_commit_date`: `2026-06-24 09:25:10 +0800`
- `last_report_date`: `2026-06-24`
- `last_report_path`: `reports/paimon-master-daily/2026-06-24.md`

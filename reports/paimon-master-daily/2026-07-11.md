# Apache Paimon Master Daily Report - 2026-07-11

## Summary

范围：`9c201b724f7f3b4577a892124d8c729358b66eaa..1886d2f4e297cbb3e2f72b041387037488aebba7`，共 11 个 first-parent 提交。当前 Apache upstream `master` head 是 `1886d2f4e297`（2026-07-11 13:05:59 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 77 个文件，`+5993/-1267`。

本次主线集中在三块：Variant/Parquet 读取重构和 vector refine 读优化，PyPaimon 写入/Daft/time-travel/update-by-row-id/BLOB 读取改动，以及 Spark/Flink chain-table compact 与 Spark V2 delete metrics。最新提交 `1886d2f4e` 还重写了 data-evolution row-id reassign 的 retry 规划，使提交冲突后可以复用相对 row-id mapping 并推进到新 snapshot。

`git cherry -v upstream/release-1.4 <commit> <commit>^` 显示本范围 11 个提交均为 `+`，没有等价 patch 已进入 `upstream/release-1.4`。较明确的 1.4 直接 bugfix 候选只有 `1d9e9f332`（PyPaimon write-cols schema type validation）；`25a3b5a96` 的 BLOB fallback batch-size/row-id gap 修复触及 1.4 的 BLOB/data-evolution 读风险，但 master 的 `BlobFallbackBatchReader` 在 1.4 不存在，需要按旧 `FormatBlobReader` / `DataEvolutionMergeReader` 路径适配。`51fd07c21`、`e462cf84d`、`1886d2f4e` 都修复真实行为，但对应 master-only 或 1.4 缺失的 runtime path，不是直接 cherry-pick 项。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时不展开 hunk，又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch。release-1.4 判断使用了 per-commit `git cherry`、stable patch-id、`git ls-tree`、`git grep` 和 `git show upstream/release-1.4:<path>`。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core format / Variant / vector | 2 | `d9383ae9` 把 Variant Parquet read 从 Parquet column-vector 内部逻辑迁到通用 `ShreddingReadPlanFactory` / `ShreddingFormatReader`，并把 readable vector utilities 提到 common；`005dbff9` 优化 vector refine raw read，只读 vector/filter/row-id 必要列、候选 bitmap 内打分、batch query 共享 raw vector 读取。 | 1.4 有旧 Variant Parquet 和旧 `VectorReadImpl`，但没有 shared shredding read plan、`AbstractVectorRead` 或 batch refine 结构；偏重构/性能，非直接 bugfix。 |
| PyPaimon write / Daft / Ray / BLOB | 4 | `1d9e9f3` 修复 column-subset write 只比列名不比类型的问题；`a60d5fd` 为 Daft `read_paimon` 增加 timestamp time travel；`51fd07c` 调整 Ray `update_by_row_id` commit/abort/close 语义；`25a3b5a` 让 BLOB fallback reader 按 batch_size 流式读取并处理 row-id gap、DV 和多版本覆盖。 | `1d9e9f3` 是直接 1.4 正确性候选；Daft/Ray update path 在 1.4 缺失；BLOB 修复需适配 1.4 旧 reader 结构。 |
| Spark / Flink chain-table compact | 2 | `e462cf8` 让 Spark `compact_chain_table` 即使表配置关闭 dynamic overwrite，也在 procedure 内固定使用 dynamic partition overwrite，避免 group partition 下误删同 partial partition 的其它数据；`aef8121` 新增 Flink `compact_chain_table` action/procedure、source skip target snapshot preload 和 IT。 | 1.4 没有 Spark/Flink `CompactChainTableProcedure`；若回补 chain-table compact 功能，需要一并带 dynamic overwrite 保护。 |
| Spark metrics | 1 | `b04db5a` 为 Spark V2 copy-on-write DELETE 添加 `deletedRecords` custom metric，并在 commit driver metrics 中用 deleted files row count 减 rewritten files row count 计算。 | 1.4 有 metric 框架但没有 master 的 `PaimonBatchWriteBase` 抽象；这是观测增强，不是 correctness bugfix。 |
| Core data-evolution row-id reassign | 1 | `1886d2f4` 将 row-id reassignment 拆成相对 mapping + assignment plan，最多 3 次提交重试；冲突后读取新 snapshot、合并新 manifest/index ranges、保留 null partition grouping，并补大量 retry/data-integrity/index tests。 | 1.4 没有 `DataEvolutionRowIdReassigner` / `RowRangeMappingIndex` 路径；不直接适用。 |
| Docs | 1 | `a8e4069` 把 C++ Paimon 文档、clone URL 和 API links 从 Alibaba 地址改到 Apache 地址。 | 文档更新，可选同步。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `1d9e9f332` | PyPaimon column-subset write 不能只比较 `data_schema.names == write_cols`；否则 `int64` 输入可写入 `int32` 表列。补丁为 full schema 与 write-cols schema 都执行类型兼容检查，同时保留 binary/fixed-size-binary 互通。 | stable patch-id `e0c480fbd845`; cherry `+`。 | 1.4 `table_write.py` 仍先 `if data_schema.names == self.file_store_write.write_cols: return`，只在 full schema 分支做 binary-family 类型检查。 | 直接正确性候选，建议回补。 |
| `51fd07c21` | Ray `update_by_row_id` 生成 commit messages 后，如果 `new_commit()` 失败应 abort pending messages；commit 已开始后失败则不再 abort，close 失败只 warning。 | stable patch-id `574b9b47f841`; cherry `+`。 | 1.4 没有 `pypaimon/ray/update_by_row_id.py`；旧路径是 `pypaimon/write/table_update_by_row_id.py` 并由 `table_update.py` 调用。 | 不直接适用；旧 update-by-row-id 如需相同行为，需要单独审查旧 writer/commit lifecycle。 |
| `25a3b5a96` | BLOB fallback reader 不应一次性物化所有目标 row ids；补丁按 `batch_size` 读取、复用每个 blob reader、跳过 batch 外 file state、处理 row-id gap 和 DV。 | stable patch-id `03858d46984f`; cherry `+`。 | 1.4 有 BLOB descriptor 和 data-evolution merge reader，但没有 master 的 `BlobFallbackBatchReader`；旧路径在 `FormatBlobReader`、`DataFileBatchReader` 和 `DataEvolutionMergeReader`。 | 需要适配的候选；关注大 BLOB 表内存和 sparse row-id gap 读取正确性。 |
| `e462cf84` | Spark chain-table compact overwrite 必须强制 dynamic partition overwrite，否则按部分 chain partition compact 时可能删除同一 partial partition 下没有 delta 的其它 region/group 数据。 | stable patch-id `92784495e083`; cherry `+`。 | 1.4 没有 `paimon-spark/.../CompactChainTableProcedure.java`。 | 不直接适用；如果 1.4 引入 chain-table compact，则应一并带上。 |
| `1886d2f4` | data-evolution row-id reassign 在提交冲突后应基于新 snapshot 重用相对 mapping 重试，而不是直接失败或重新分配导致 index/range 不一致；同时处理 null partition grouping 和新增/删除 manifest/index。 | stable patch-id `2349d277b903`; cherry `+`。 | 1.4 没有 `DataEvolutionRowIdReassigner.java` 和 `RowRangeMappingIndex.java`。 | 不直接适用；master-only data-evolution maintenance path。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `d9383ae9` | Core format / Variant | 38 files，`+1693/-691`。新增 `BatchColumnarRowIterator`、`ColumnVectorUtils`、`ShreddingReadPlan*` 和 `ShreddingFormatReader`；Parquet Variant read 改由 `VariantShreddingReadPlanFactory` 生成 physical read schema 和 assembler；field metadata 从 reader capability 移到 format-level `SupportsFieldMetadata`；测试覆盖 Arrow metadata、old Variant file 读新增列和 read plan factory。 | cherry `+`，patch-id `56783f314048`。1.4 有旧 Variant Parquet internals，但缺 shared read plan；重构/新架构，不按直接 bugfix 回补。 |
| 2 | `005dbff9` | Core/Python vector search | 6 files，`+573/-79`。Java/Python raw refine read 改为只投影 vector、filter fields 和 `_ROW_ID`；candidate bitmap 作为打分边界；batch refine 对多 query 先 union candidates，只读一次 raw vectors，再按 query 各自 topK rerank。 | cherry `+`，patch-id `c3ee71b92722`。1.4 只有旧 `VectorReadImpl`，无 `AbstractVectorRead`/batch refine raw read path；偏性能优化。 |
| 3 | `1d9e9f3` | PyPaimon write | 2 files，`+72/-15`。抽出 `_is_compatible_pyarrow_schema` 和 `_write_cols_pyarrow_schema`；write-cols 模式按列名和类型共同校验；新增 int64 写 int32 column subset 必须拒绝、binary family 仍允许的测试。 | 直接 1.4 正确性候选。 |
| 4 | `a60d5fd` | PyPaimon Daft | 3 files，`+141/-24`。`read_paimon` / `explain_paimon_scan` 支持 `timestamp`，int/datetime 映射 `scan.timestamp-millis`，string 映射 `scan.timestamp`；`snapshot_id`、`tag_name`、`timestamp` 三者互斥。 | 1.4 没有 `pypaimon/daft/daft_paimon.py`；新功能。 |
| 5 | `51fd07c` | PyPaimon Ray update | 2 files，`+208/-5`。新增 `_commit_update_messages` 和 `_abort_pending_update_messages`，区分 commit 未开始/已开始失败，close/abort 失败写 warning；测试覆盖 new_commit、commit、close failure。 | 1.4 旧 update path 不同，不直接适用。 |
| 6 | `25a3b5a` | PyPaimon BLOB read | 4 files，`+459/-61`。新增 `_BlobFileState`；`BlobFallbackBatchReader` 按 target ranges 生成下一批 row ids，复用 per-file reader，根据 selected range 计算 blob offset position，多版本按 sequence 覆盖；`DataEvolutionSplitRead` 传入 batch_size；测试覆盖 batch_size、reader reuse、row-id gap、sparse range 和避免全量 row-id 物化。 | 1.4 有 BLOB/data-evolution 读风险但缺同名 reader；需适配旧路径。 |
| 7 | `e462cf8` | Spark chain-table compact | 2 files，`+109/-70`。Spark procedure 写 snapshot branch 前复制 table options 并强制 `dynamic-partition-overwrite=true`；partition existence 检查改用 `listPartitions()`；测试在表级 dynamic overwrite true/false 两种情况下确认未 compact 的 group/region 数据保留。 | 1.4 缺 Spark compact_chain_table procedure；非直接。 |
| 8 | `aef8121` | Flink chain-table compact | 11 files，`+993/-5`。新增 Flink action/factory/procedure，要求 batch mode、chain table、单 partition；存在 snapshot partition 且未 overwrite 时构造 empty pipeline；overwrite 时 source 跳过 preload target snapshot 并 sink 到 snapshot branch dynamic overwrite；注册 factory 并补 IT。 | 1.4 缺 Flink compact_chain_table；新功能。 |
| 9 | `b04db5a` | Spark metrics | 4 files，`+48/-5`。新增 `deletedRecords` metric；copy-on-write DELETE 时用 deleted file rows 减 added rewritten rows 作为 driver metric；仅 DELETE 暴露，不对 UPDATE/MERGE 推导。 | 观测增强；1.4 可适配但不是 bugfix。 |
| 10 | `a8e4069` | Docs | 1 file，`+8/-10`。C++ Paimon repo、clone URL 和文档 links 改为 `apache/paimon-cpp` / `paimon.apache.org/docs/cpp`，并清理 Alibaba community wording。 | 文档同步项。 |
| 11 | `1886d2f4` | Core data-evolution row-id reassign | 4 files，`+1689/-302`。reassign plan 从绝对 row-id assignment 改为 relative mapping，commit 前写 manifest/index replacement；冲突后读取 intervening snapshots、更新要重写的 manifest metas、处理新增/删除/compaction/index ranges，并最多重试 3 次；补 null partition grouping、data integrity、stale index、compaction conflict 等测试。 | 1.4 缺该 maintenance path，不直接适用。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/master` 前进到 `1886d2f4e297`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse 9c201b724f7f..upstream/master` | 11 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat 9c201b724f7f..upstream/master` | 77 files changed, 5993 insertions(+), 1267 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <11 commits>` | 对 11 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <11 commits>` | 对 11 个 commit 均已执行并检查实际 patch hunks；大提交按关键生产路径补充 `git grep` / `git show`。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 11 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show <commit> \| git patch-id --stable` | 对 11 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 PyPaimon `table_write.py`、BLOB/data-evolution reader、Ray/update-by-row-id、Daft、Spark/Flink compact_chain_table、Spark metrics、Variant/vector/data-evolution row-id reassign 等路径存在性和旧代码片段。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `1886d2f4e297cbb3e2f72b041387037488aebba7`
- `last_processed_commit_date`: `2026-07-11 13:05:59 +0800`
- `last_report_date`: `2026-07-11`
- `last_report_path`: `reports/paimon-master-daily/2026-07-11.md`

# Apache Paimon Master Daily Report - 2026-07-17

## Summary

范围：`eb797e11119be30b56d975c1c3618c707eb3e7c3..a26016e99977e34affdca0a156b13c2736afffc8`，共 13 个 first-parent 提交。当前 Apache upstream `master` head 是 `a26016e99977`（2026-07-17 11:17:35 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 97 个文件，`+7349/-324`。

本次主线集中在 PyPaimon 和存储格式能力：Daft reader 修复 data-evolution split 的 fallback 路由，并新增 BLOB 表非 BLOB 列 native parquet 读取；Python read/write 侧新增单进程 auto parallelism、`write_pandas` 的 `write_cols` 语义、DuckDB parallelism 参数和 BLOB 写入性能优化；Python primary-key global index 查询开始对齐 Java 的 source-backed scalar-index 路径；core/format 侧加入 MAP shared-shredding 的 append read/write。跨模块 bugfix 包括 Flink postpone bucket incremental split、Spark `remove_unexisting_files` 空表、Flink 大 OR/AND 谓词栈溢出，以及 Arrow 缺失复杂列的 all-null vector 转换。

`git cherry -v upstream/release-1.4 upstream/master eb797e11119be30b56d975c1c3618c707eb3e7c3` 显示 13 个提交均为 `+`，没有 patch-id 等价提交已进入 `release-1.4`。明确的 1.4 直接候选包括 `fe493497`（Flink postpone bucket incremental split）、`7298f93e`（Spark empty table procedure）、`4ac91ffa`（Flink / common predicate 大 OR/AND 栈溢出）、`296b2e8f`（PyPaimon `write_pandas` respect `write_cols`）和 `8e23d1ea`（Arrow missing complex column）。`67d24eb2`、`ef013535` 是可选性能候选；Daft、primary-key global index、MAP shared-shredding 在 `release-1.4` 缺对应运行路径，不直接适用。

所有 commit 都先执行 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；同时对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch hunks。release-1.4 判断使用 `git cherry`、`git patch-id --stable`、`git ls-tree`、`git grep` 和 targeted `git show upstream/release-1.4:<path>` 对照生产路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| PyPaimon Daft / read / write / BLOB | 5 | Daft data-evolution fallback 改为要求 `raw_convertible`；BLOB 表非 BLOB projection 可走 native parquet；Python read auto parallelism；`write_pandas` 使用 `write_cols` 子 schema；BLOB CRC / Arrow grouping 写入性能优化。 | `296b2e8f` 是直接候选；Daft 路径在 1.4 不存在；BLOB 写入优化可选但不是 correctness bugfix。 |
| PyPaimon primary-key global index | 1 | 新增 PK index definitions/source meta、BTREE/BITMAP/FULL_TEXT/VECTOR 查询和 split-result 解释，global index meta 增加 `source_meta`。 | 1.4 缺 `paimon-python/pypaimon/index/pk` 与 primary-key source scan 路径，不直接适用。 |
| Core writer / tag initialization | 2 | `TagAutoCreation` 初始化只读最新 auto tag；writer 获取 latest snapshot 改走 filesystem fresh read。 | `TagAutoCreation` 是可选性能候选；writer snapshot 优化在 1.4 文件结构不一致，非直接 bugfix。 |
| Flink source and predicates | 2 | IncrementalSplit 的 postpone bucket 用 write id 选 task；common PredicateBuilder 改为 balanced binary tree，Flink converter 迭代 flatten AND/OR。 | `fe493497`、`4ac91ffa` 均为直接 1.4 correctness 候选。 |
| Spark procedure | 1 | `remove_unexisting_files` 在无 partition 时直接返回 empty RDD，避免 0 分区 parallelize。 | `7298f93e` 是直接 1.4 候选。 |
| Arrow / schema evolution | 1 | 新增 `AllNullColumnVector`，Arrow writer 遇全空缺失复杂列直接 set null，避免复杂类型 writer cast 失败。 | `8e23d1ea` 是直接 1.4 候选；1.4 仍用 `index -> true` all-null lambda。 |
| Core / format MAP shared-shredding | 1 | 新增 MAP shared-shredding read plan，接入 append writer、ORC/Parquet read plan、schema validation 和大量 e2e tests。 | 1.4 缺 shared-shredding 目录和 read plan，属 master 新功能。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `1e059d4b` | Daft native reader 只有在 `raw_convertible` 时才接管；非 PK data-evolution split 否则 fallback 到 PyPaimon merge reader，避免把同一 row-id range 的 column fragments 当独立物理行读出。 | patch-id `78d1f7b2`; `git cherry` 为 `+`。 | 1.4 没有 `paimon-python/pypaimon/daft/daft_datasource.py`。 | master Daft 路径 bugfix；不直接适用 1.4。 |
| `99661104` | BLOB 表在 projection 不含 BLOB/vector 列且 parquet 覆盖 row-id range 时，Daft 可只读 covering parquet files；否则保守 fallback。 | patch-id `640d816a`; `git cherry` 为 `+`。 | 1.4 缺 Daft datasource 路径。 | master Daft 性能/读取路径扩展；不直接适用。 |
| `67d24eb2` | `TagAutoCreation` 初始化从读所有 tag 文件改为 list tag names 后只读最新 tag。 | patch-id `7e54657b`; `git cherry` 为 `+`。 | 1.4 `TagAutoCreation` 仍调用 `tagManager.tags(...)` 读取全部 tag；`TagManager` 无 `tagNames` helper。 | 可选性能候选；影响大量 auto-created tags 的 committer 初始化耗时。 |
| `fe493497` | `IncrementalSplit` 的 `POSTPONE_BUCKET` 根据 after files 或 delete-only 的 before files 提取 write id，再映射到 source task；同时校验 suggested task 在并行度范围内。 | patch-id `0951f3df`; `git cherry` 为 `+`。 | 1.4 `ContinuousFileSplitEnumerator.assignSuggestedTask(IncrementalSplit)` 仍有 postpone bucket TODO。 | 直接候选；影响 streaming incremental split assignment，错误 task 会导致 split stranded 或分配异常。 |
| `7298f93e` | Spark `remove_unexisting_files` 对空表直接返回 `emptyRDD[String]`。 | patch-id `0b727661`; `git cherry` 为 `+`。 | 1.4 `SparkRemoveUnexistingFiles.buildRDD` 仍对 empty `binaryPartitions` 计算 `realParallelism=0` 后 parallelize。 | 直接候选；影响空表调用 procedure。 |
| `4ac91ffa` | common `PredicateBuilder.and/or` 构建 balanced binary tree；Flink `PredicateConverter` 迭代 flatten 嵌套 AND/OR，避免大 IN/OR 表达式递归栈溢出。 | patch-id `b8570ffa`; `git cherry` 为 `+`。 | 1.4 `PredicateBuilder` 仍 stream reduce 成左深树；Flink converter 仍递归 `children.get(0).accept(this)`。 | 直接候选；影响 Flink 复杂谓词 pushdown / 大 IN 条件稳定性。 |
| `5ad505ca` | writer 取 latest snapshot 改用 `latestSnapshotFromFileSystem()`，避免缓存 snapshot 影响 writer 判断。 | patch-id `a712cffd`; `git cherry` 为 `+`。 | 1.4 `AbstractFileStoreWrite` 对应逻辑结构不同，未发现相同 hunk 位置。 | master writer 性能/一致性优化；非直接 1.4 bugfix。 |
| `8dc60733` | Python `read.parallelism` 默认改为 auto，即 `min(split count, cpu count)`；仍可用 option/runtime 参数强制串行或指定并行度。 | patch-id `64127575`; `git cherry` 为 `+`。 | 1.4 `TableRead.to_arrow` 仍是串行实现，未发现 `_resolve_parallelism` / `read.parallelism` 路径。 | master Python reader 行为扩展；不直接适用。 |
| `296b2e8f` | `write_pandas` 在 `with_write_type(write_cols)` 时按子 schema 构造 RecordBatch，允许 DataFrame 只包含写入列；`to_duckdb` 透传 parallelism。 | patch-id `67363ecf`; `git cherry` 为 `+`。 | 1.4 `TableWrite.write_pandas` 仍用全表 schema，`with_write_type` 已存在且设置 `file_store_write.write_cols`。 | 直接候选；影响 PyPaimon append-only subset-column pandas writes。 |
| `ef013535` | Python BLOB writer 优先用 `isal_zlib.crc32`；partition/bucket 分组写入可复用整 batch 或 zero-copy slice，减少 Arrow `take`。 | patch-id `55bc6508`; `git cherry` 为 `+`。 | 1.4 `blob_format_writer.py` 仍直接用 `zlib.crc32`，`TableWrite.write_arrow_batch` 总是 `pa.compute.take`。 | 可选性能候选；需评估新增 `isal` 依赖策略。 |
| `8e23d1ea` | 缺失复杂列不再用普通 `ColumnVector` lambda 代表 all-null；新增 typed-independent `AllNullColumnVector`，Arrow writer 直接填 null，避免 MAP/ARRAY/ROW/VECTOR/VARIANT cast 失败。 | patch-id `8812bedb`; `git cherry` 为 `+`。 | 1.4 `VectorMappingUtils.createMappedVectors` 对缺失列仍返回 `index -> true`；`ArrowFieldWriter.write` 直接调用 typed `doWrite`。 | 直接候选；影响 schema evolution 下缺失复杂列的 Arrow/vectorized 转换。 |
| `1ce18953` | Python primary-key global index 查询支持 source-backed scalar-index path，BTREE/BITMAP 结果按 source file physical positions 解释。 | patch-id `d15a42c1`; `git cherry` 为 `+`。 | 1.4 缺 `pypaimon/index/pk`、primary-key vector/full-text/sorted scan 等路径。 | master 新功能/正确性路径；不直接适用。 |
| `a26016e9` | MAP shared-shredding append read/write 支持，新增 read plan、format plan、ORC/Parquet physical read row 到 logical MAP 组装。 | patch-id `f2c483f3`; `git cherry` 为 `+`。 | 1.4 缺 `paimon-common/src/main/java/org/apache/paimon/data/shredding` 和 `format/shredding` 路径。 | master 新功能；不直接适用。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `1e059d4b` | PyPaimon Daft | `PaimonDataSource._reader_routing` 去掉“非 PK 即可 native”的条件，要求所有 native parquet route 都必须 `raw_convertible`；新增 data-evolution fallback reason 和 integration test 证明 column fragments 被 merge 成 logical rows。 | 不直接适用，1.4 缺 Daft datasource。 |
| 2 | `99661104` | PyPaimon Daft / BLOB | 新增 `_blob_native_covering_files`，按 projected columns、write_cols、row-id range 判断 BLOB 表可否走 covering parquet native reader；explain 统计只计实际读取 parquet 文件。 | 不直接适用，1.4 缺 Daft datasource。 |
| 3 | `67d24eb2` | Core tag | `TagAutoCreation` 用 `TagManager.tagNames` 找最新 auto tag，只读一个 tag 文件恢复 next snapshot/tag；避免保留大量 tag 时初始化 N 次文件读取。 | 可选性能候选。 |
| 4 | `fe493497` | Flink source | `assignSuggestedTask(IncrementalSplit)` 对 postpone bucket 从 after/before files 的文件名解析 write id；新增 delete-only diff、real bucket 和 out-of-range 校验测试。 | 直接候选。 |
| 5 | `7298f93e` | Spark procedure | `SparkRemoveUnexistingFiles.buildRDD` 遇空 partition list 返回 empty RDD，并补空表 dry-run / real-run 测试。 | 直接候选。 |
| 6 | `4ac91ffa` | Common predicate / Flink | `PredicateBuilder.and/or` 改 balanced tree；Flink converter 用 stack flatten 同类 AND/OR；更新 JSON/toString 预期并新增大谓词 source 测试。 | 直接候选。 |
| 7 | `5ad505ca` | Core writer | `AbstractFileStoreWrite` 读 latest snapshot 改用 filesystem fresh read；REST token 相关测试异常类型适配。 | master 优化，非直接 1.4 bugfix。 |
| 8 | `8dc60733` | PyPaimon read | `read.parallelism` option 改为 no default，runtime/option unset 时按 split count 与 CPU auto；文档说明 limit 下并行读结果非确定 subset。 | Python reader 行为扩展；1.4 无对应并行读路径。 |
| 9 | `296b2e8f` | PyPaimon write/read | `write_pandas` 尊重 `write_cols` 子 schema；`to_duckdb` 增加 parallelism 参数并透传给 `to_arrow`。 | `write_pandas` 是直接候选；DuckDB parallelism 依赖 master 并行读。 |
| 10 | `ef013535` | PyPaimon BLOB write | 引入 optional `isal` CRC backend；Arrow batch 写入对单 group 复用原 batch、连续 group 用 zero-copy slice、非连续保留 `take`。 | 可选性能候选；需依赖评估。 |
| 11 | `8e23d1ea` | Arrow / common vector | 新增 `AllNullColumnVector`，Arrow reader / `VectorMappingUtils` 复用该 sentinel；Arrow writer 对 sentinel 和 empty batch 直接处理，覆盖 MAP/ARRAY/ROW/VECTOR/VARIANT。 | 直接候选。 |
| 12 | `1ce18953` | PyPaimon PK global index | 新增 primary-key index definitions/source meta、source-backed sorted/vector/full-text read/scan、manifest source_meta serde、file scanner index filtering和 golden tests。 | 新功能栈，1.4 不直接适用。 |
| 13 | `a26016e9` | Core / format MAP shared-shredding | 新增 `MapSharedShreddingReadPlan` 和 factory，接入 append writer、schema validation、ORC/Parquet read path 和 e2e tests，覆盖 projection/data evolution/null/overflow/complex values。 | 新功能栈，1.4 不直接适用。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | 成功；`upstream/master` 前进到 `a26016e99977`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功；`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse eb797e11119be30b56d975c1c3618c707eb3e7c3..upstream/master` | 13 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat eb797e11119be30b56d975c1c3618c707eb3e7c3..upstream/master` | 97 files changed, 7349 insertions(+), 324 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <13 commits>` | 对 13 个 commit 均已执行，输出缓存到 `/private/tmp/paimon-daily-2026-07-17-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <13 commits>` | 对 13 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master eb797e11119be30b56d975c1c3618c707eb3e7c3` | 13 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git patch-id --stable` | 对 13 个 commit 均记录 stable patch-id，用于 release-impact 对照。 |
| release-1.4 targeted checks | `git ls-tree` / `git grep` / `git show upstream/release-1.4:<path>` 确认 Flink postpone bucket、Spark empty table procedure、Predicate/Flink converter、PyPaimon `write_pandas`、Arrow missing complex column 等候选路径存在且缺对应修复；确认 Daft、PK index、MAP shared-shredding 路径缺失。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `a26016e99977e34affdca0a156b13c2736afffc8`
- `last_processed_commit_date`: `2026-07-17 11:17:35 +0800`
- `last_report_date`: `2026-07-17`
- `last_report_path`: `reports/paimon-master-daily/2026-07-17.md`

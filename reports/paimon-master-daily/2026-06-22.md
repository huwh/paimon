# Apache Paimon Master Daily Report - 2026-06-22

## Summary

范围：`9cdb9026164d9516a6f631198bb0c348972587de..84d5acbea9716ba9fa3e0edd87c7175f650b2be0`，共 5 个 first-parent 提交。当前 Apache upstream `master` head 是 `84d5acbea971`（2026-06-22 12:00:06 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次主线变化集中在 global-index/vector search 的 raw fallback、PyPaimon 对同一 fallback 行为的补齐、Spark 4 SQL-defined UDF 支持，以及 full-text search 迁移到 JSON DSL。另有一个 Lance 向量检索测试因 Hadoop 依赖问题被禁用。

`release-1.4` 方面，5 个提交经 `git cherry -v upstream/release-1.4 <commit> <commit>^` 检查均为 `+`，未发现等价 patch 已 cherry-pick。但这些提交大多是 master 新能力或 API 对齐，不是小型 runtime bugfix。需要关注的条件性风险是 `89dab0ef6c`：release-1.4 已有部分 vector/global-index 搜索路径，但缺少 `global-index.search-mode` 前置能力，不能直接小范围回补。`84d5acbea971` 触及 full-text 查询语义和 scan 正确性，但同时引入新的 JSON DSL/API，release 回补成本较高。

所有 commit 都用 `git show --stat --name-status --patch` 查看文件状态，并补跑 `git show --stat --patch` 读取实际 hunks，因为当前 Git 输出在带 `--name-status` 时未显示 patch hunks。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Global-index / vector search | 2 | Java core/Spark/vector index 和 PyPaimon 增加 `global-index.search-mode=fast/full/detail` 的 raw fallback：索引覆盖范围外的 row-id 可在 full/detail 模式下回扫原始数据；拆分 index/raw vector split，并补充 Spark raw split 执行路径。 | patch-id 未在 release-1.4；release 有部分 vector/global-index 搜索路径，但缺少 search-mode 和若干新类，属于条件性回补而非直接 bugfix。 |
| Lance vector test | 1 | 禁用 `LanceVectorSearchTest.testVectorSearchFullModeScansUnindexedData`，原因是该测试需要 Hadoop 依赖。 | test-only；无 runtime 影响。 |
| Spark SQL functions | 1 | Spark 4 支持 `CREATE FUNCTION ... RETURN ...` SQL-defined scalar UDF；新增 SQL/function converter、parser rewrite、shim 方法、默认参数 metadata 和测试。 | master 新能力；release-1.4 有 V1 function 基础代码，但 Spark 4 SQL UDF 不是 release bugfix。 |
| Full-text / hybrid search | 1 | Java/Python/Spark/Tantivy 将 full-text search 公共接口对齐到 JSON DSL，新增 `FullTextQuery`，支持 match、phrase、boost、multi_match、boolean，并更新 hybrid route full-text 参数。 | patch-id 未在 release-1.4；release 仍是旧 query text API，Tantivy/PyPaimon 多个新路径缺失，回补会改变 API 面。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | Touched production files on release-1.4 | Likely release-1.4 impact |
| --- | --- | --- | --- | --- |
| `89dab0ef6c` | `global-index.search-mode=full/detail` 下，vector search 可把索引未覆盖的 raw rows 加入搜索结果，避免新写入但尚未建索引的数据不可见。默认 `fast` 仍只查索引。 | `+`，patch-id `41619d38131864ab02387d0ac6198122f85ac317` 未 cherry-pick。 | release-1.4 存在 `GlobalIndexScanner`、`VectorReadImpl`、`VectorScanImpl` 等部分路径，但缺少 `GlobalIndexCoverage`、`IndexVectorSearchSplit`、`RawVectorSearchSplit`、Spark vector read path 和 `global-index.search-mode`。 | 中，条件性。它修复/扩展的是索引 freshness 语义，但依赖 master 的 search-mode 设计；不建议当作小 patch 直接回补。 |
| `fb3a95a2b3` | 禁用 Lance full-mode raw fallback 测试，避免缺 Hadoop 依赖时测试失败。 | `+`，patch-id `773244adb69a672271dae8919aaa7438b225ed6e` 未 cherry-pick。 | 仅测试文件；过滤生产路径后无文件。 | 低。test-only；只有 release CI 需要时才考虑。 |
| `3ab3a2c65f` | PyPaimon 增加与 Java raw fallback 对齐的 global-index coverage、search-mode、raw vector split/read/scan 行为。 | `+`，patch-id `bf2ebdfa7d170fbc03d29a058e5ff2d8aef31d77` 未 cherry-pick。 | release-1.4 有部分 PyPaimon global-index/read 基础文件，但 `pypaimon/table/source/vector_search_*` 等关键路径缺失。 | 低到中，条件性。主要是 Python 对 master 新能力的补齐；release 是否需要取决于是否计划同步 search-mode。 |
| `b686cd7d84` | Spark 4 支持 SQL-defined scalar UDF，并让 Paimon catalog 持久化 SQL function definition。 | `+`，patch-id `c9bea96fc43eb7c33f2d1b7a2ac7ce60973430ab` 未 cherry-pick。 | release-1.4 有 V1 function registry/support 基础代码，但缺部分 Spark 4-specific 源路径和新增 converters。 | 低。功能新增，不是 bugfix；回补会扩大 Spark function API 行为。 |
| `84d5acbea9` | Full-text search 改用 JSON DSL，并补充 Java/Python/Spark/Tantivy 解析、校验、搜索和 hybrid route 逻辑；旧 column/query/operator 模型被替换。 | `+`，patch-id `b06101078c5f5bb74eb5c0d320a1fcc08c3353c9` 未 cherry-pick。 | release-1.4 有 core full-text builder/read/scan 和 Spark TVF 旧路径，但缺 `FullTextQuery`、多处 PyPaimon full-text source、Tantivy JNI 相关路径。 | 中，偏 API/能力迁移。若 release 发现具体 full-text correctness bug，应拆出最小修复；整 commit 不适合直接回补。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `89dab0ef6c` | Global-index / vector search | 新增 `VectorGlobalIndexer` 和 `GlobalIndexCoverage`；`GlobalIndexScanner` 记录索引覆盖范围并暴露 `unindexedRows`；`DataEvolutionBatchScan` 合并索引结果和未索引行；`VectorScanImpl` 拆出 index/raw split；`VectorReadImpl`/Spark reader 对 raw row ranges 做原始数据扫描并按 metric 计算 top-k。 | 条件性风险。release-1.4 有部分旧代码路径，但缺 search-mode 前置能力和多个新类，需专项评估。 |
| 2 | `fb3a95a2b3` | Lance vector test | `LanceVectorSearchTest` 覆盖父类方法并用 `@Disabled("Requires Hadoop dependencies")` 禁用 full-mode raw fallback 测试。 | test-only，低。 |
| 3 | `3ab3a2c65f` | PyPaimon global-index / vector search | Python 增加 `GlobalIndexSearchMode`、`global_index_coverage.py`、scanner unindexed rows、table scan global-index result 合并、vector raw split/read/scan；混合 Java/Python E2E 脚本新增 raw fallback 场景。 | 条件性。release-1.4 部分 Python 基础存在，但缺主要 vector table/source 新路径。 |
| 4 | `b686cd7d84` | Spark SQL UDF | 新增 Spark 4 SQL function converter 和 parser rewrite；`SupportV1Function.createV1Function` 改收 Paimon `Function`；文件型 V1 function converter 重命名为 `FileFunctionConverter`；Spark 3 shim 对 SQL UDF 明确不支持，Spark 4 shim 解析 SQL function expression。 | 功能新增，低。 |
| 5 | `84d5acbea9` | Full-text / hybrid search | 新增 Java `FullTextQuery` DSL；`FullTextSearch`、builder/read/scan/split 改为 query JSON；PyPaimon 增加 full-text query DSL；Spark `full_text_search` 和 hybrid route 参数改为 JSON query；Tantivy JNI 增加 `searchJson` 并解析 match/phrase/boost/boolean。 | 中。release-1.4 有旧 full-text API；整 commit 回补会改变 API 和多语言实现，建议只按明确 bug 拆分。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=84d5acbea9716ba9fa3e0edd87c7175f650b2be0`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git log --first-parent --reverse 9cdb9026164d9516a6f631198bb0c348972587de..upstream/master` | 5 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 5 个 commit 均已执行，用于文件状态和 patch 检查入口。 |
| `git show --stat --patch <commit>` | 对 5 个 commit 均补跑并检查实际 hunks。 |
| `git patch-id --stable` | 已记录 5 个 commit 的 stable patch-id。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 5 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted code checks | 检查 global-index/vector、PyPaimon、Spark V1 function、full-text/Tantivy 关键生产路径存在性。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `84d5acbea9716ba9fa3e0edd87c7175f650b2be0`
- `last_processed_commit_date`: `2026-06-22 12:00:06 +0800`
- `last_report_date`: `2026-06-22`
- `last_report_path`: `reports/paimon-master-daily/2026-06-22.md`

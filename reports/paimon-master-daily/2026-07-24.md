# Apache Paimon Master Daily Report - 2026-07-24

## Summary

范围：`85a01158ca9681cbc3988d72546ec0b9eee37fab..a998338ec62f2cae5c0649e797eda4d02be5de36`，共 27 个 first-parent 提交，125 个文件变更，`+5460/-319`。本轮 `upstream/master` 推进到 `a998338ec62f`（2026-07-24 11:52:29 +0800），`upstream/release-1.4` 仍是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

已逐个提交检查 `git show --stat --name-status --patch --find-renames --format=fuller` 和单独 patch-only 输出，patch 缓存在 `/private/tmp/paimon-daily-2026-07-24-patches`。主要变化集中在 core 写入/compaction/系统表修复、Paimon API/REST/JDBC catalog、对象存储 FileIO、Arrow/Parquet/Hive/Vector 读写、Flink data-evolution/CDC、PyPaimon 聚合器和分支 schema 管理。

`release-1.4` 影响上，本轮 27 个 commit 对 `upstream/release-1.4` 均为 `git cherry +`，没有等价 patch。直接值得评估的 1.4 bugfix 候选包括：JDBC view 分页/LIKE 过滤、TIMESTAMP_LTZ normalized key、Azure/COSN 按 authority 缓存 FileSystem、VectorType prune 长度比较、`$audit_log` option 污染、Parquet timestamp predicate schema 校验、query-auth fallback/index-bootstrap、Arrow 嵌套 nullability、full compaction 后 DV index cleanup、VECTOR-as-ARRAY 读取、PyPaimon OSS legacy mode、Hive timestamp 精度、PyPaimon branch schema 隔离、Postgres numeric/time 映射、REST warehouse 双重编码。Map shared-shredding、Flink data-evolution delete、row-count rolling 等依赖 master-only 或新增功能栈，不能按简单 bugfix 处理。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core table storage / schema / write / compaction | 6 | 修复 `$audit_log` 构造时污染原表 options；新增多静态分区 overwrite；迁移/删除 map shared-shredding 字段级 options；query-auth fallback/index-bootstrap 修复；full compaction 回传 DV cleanup；新增 `target-file-row-num` 行数滚动。 | 多个直接候选：audit log、query auth/fallback、DV cleanup；map shared-shredding 和 row-count rolling 属于 master-only/功能新增。 |
| Catalog / API / REST / JDBC | 3 | JDBC catalog `listViewsPaged` 改为 SQL 级 LIKE + cursor 分页；`VectorType#isPrunedFrom` 加长度比较；REST `/v1/config` 的 `warehouse` query 不再预编码。 | 3 个都是 1.4 同路径旧逻辑，均可做 1.4 bugfix 候选。 |
| Format / codegen / connector read-write types | 5 | TIMESTAMP_LTZ 支持 normalized key；Parquet timestamp predicate pushdown 校验文件 logical type/unit/UTC；Arrow writer 使用嵌套字段自身 nullability；VECTOR 可作为 ARRAY 读取；Hive timestamp 映射保留默认精度。 | 5 个均有 1.4 同路径旧逻辑，属于直接候选。 |
| Filesystem / object storage | 3 | Azure/COSN FileSystem cache 从单实例改为按 authority 分桶；PyPaimon OSS legacy bucket-in-endpoint 避免 `create_dir` 误触发 CreateBucket，并对 list_status 给出明确错误。 | Azure/COSN 和 Python OSS 路径在 1.4 仍是旧逻辑，直接候选。 |
| Flink action / query service / CDC | 4 | 新增 data-evolution delete action；query service 增加 partial lookup metrics；Postgres numeric 越界 fallback 到 STRING；Postgres `time(n)` Debezium/JDBC 映射收敛并允许 TIME 精度扩宽。 | CDC 两个是直接候选；data-evolution delete 和 metrics 是功能新增。 |
| PyPaimon merge/read/catalog | 3 | 新增 `merge_map`、`nested_partial_update` 聚合器；filesystem/rest catalog 的 schema manager 按 branch 读取/修改。 | branch schema 隔离是直接候选；聚合器是功能补齐。 |
| Docs / test-only stability | 3 | lookup metrics 文档更新；Spark/Hive/CDC flaky test 修正；Spark Hive metastore 测试端口拆开。 | docs/test-only，不影响 1.4 runtime。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `ea1aa7c22` | JDBC `listViewsPaged` 从一次性 `listViews` 改为 SQL 级 `LIKE` + cursor + `LIMIT maxResults+1`，并让 JDBC override 在 catalog-wide pattern flag 仍为 false 时也能直接支持 view pattern。 | patch-id `0599de785c1a`；`git cherry` 为 `+`。 | 1.4 `JdbcCatalog` 未 override `listViewsPaged`，只有 `Catalog` 默认实现；`JdbcUtils` 没有 `LIST_VIEWS_PAGED_SQL`。 | 直接候选；影响 JDBC catalog 大量 view 下的分页正确性和 pattern 过滤。 |
| `c65d2550f` | normalized key codegen 把 `TIMESTAMP_WITH_LOCAL_TIME_ZONE` 纳入 Timestamp key 类型、compact 判断和 key 长度计算。 | patch-id `af30d691b714`；`git cherry` 为 `+`。 | 1.4 `SortCodeGenerator` 只处理 `TIMESTAMP_WITHOUT_TIME_ZONE`，`GenerateUtils` 已有 LTZ runtime 支持但 sort key 漏掉。 | 直接候选；影响 TIMESTAMP_LTZ 排序/normalized-key 路径。 |
| `0e80c0e70` | Azure FileIO 的 Hadoop `FileSystem` 缓存从单个 `fs` 改为 `Map<authority, FileSystem>`。 | patch-id `1d1405ad2664`；`git cherry` 为 `+`。 | 1.4 Azure `HadoopCompliantFileIO` 仍是 `protected transient volatile FileSystem fs`。 | 直接候选；同一个 FileIO 访问多 authority 时可避免复用错误 FS。 |
| `2d178e130` | COSN FileIO 同样按 authority 缓存 Hadoop `FileSystem`。 | patch-id `0e7b8081f077`；`git cherry` 为 `+`。 | 1.4 COSN `HadoopCompliantFileIO` 仍是单 `fs`。 | 直接候选；对象存储多 authority 场景风险同 Azure。 |
| `926ffb1df` | `VectorType#isPrunedFrom` 除 element type 外还比较 vector length。 | patch-id `1281a381beaa`；`git cherry` 为 `+`。 | 1.4 `VectorType.isPrunedFrom` 仍只返回 `elementType.isPrunedFrom(...)`，未比较 `length`。 | 直接候选；避免不同维度 VECTOR 被错误认为可投影/裁剪兼容。 |
| `93e047b16` | `$audit_log` 需要 sequence number 时改用 `copyWithoutTimeTravel` 注入动态 option，不再修改共享 `baseTable.options()`。 | patch-id `8d228314e393`；`git cherry` 为 `+`。 | 1.4 `AuditLogTable` 构造函数仍先 `this.wrapped = wrapped`，再 `this.wrapped.options().put(KEY_VALUE_SEQUENCE_NUMBER_ENABLED, true)`。 | 直接候选；查询 `$audit_log` 后可能污染原表读 schema，导致后续查询结果错位。 |
| `e4045f710` | Parquet timestamp predicate pushdown 前校验文件物理类型、timestamp logical type、time unit 和 adjustedToUTC，schema 不兼容时禁用 pushdown。 | patch-id `04d9055c16f7`；`git cherry` 为 `+`。 | 1.4 `ParquetFilters` 对 timestamp 只按读类型 precision 生成 long predicate，没有 `timestampPrimitiveType`/`TimestampLogicalTypeAnnotation` 校验。 | 直接候选；避免错误 predicate pushdown 过滤掉正确数据。 |
| `513966a47` | rename/drop MAP 字段时迁移或清理 map shared-shredding 字段级 options。 | patch-id `14fbd00943a0`；`git cherry` 为 `+`。 | 1.4 `CoreOptions`/schema 路径未找到 `MAP_STORAGE_LAYOUT`、`MAP_SHARED_SHREDDING_*`。 | 不适用；1.4 缺少 shared-shredding map 代码路径。 |
| `f52dde235` | index bootstrap 内部读强制 `scan.mode=latest` 且关闭 query auth；fallback scan/read 透传 `withReadType`，并正确 unwrap `FallbackSplit`/`QueryAuthSplit` 后回退主读。 | patch-id `5494f307a023`；`git cherry` 为 `+`。 | 1.4 `IndexBootstrap` 只设置 `SCAN_MODE`；`FallbackReadFileStoreTable.plan` 仍强转 `DataSplit`；`createReader` 回退时把 `FallbackSplit` 直接交给 mainRead。 | 直接候选；影响 query-auth 启用下的 index bootstrap 和 fallback branch 读取。 |
| `ca8c2ba6d` | Arrow writer 创建子 writer 时使用嵌套字段/元素/value 自身 nullability，map key 固定 non-null。 | patch-id `4ff6b8a34316`；`git cherry` 为 `+`。 | 1.4 `ArrowUtils` 使用外层 `rowType.isNullable()`；map key/value writer 也继承外层 nullable。 | 直接候选；避免嵌套 nullable/not-null 字段写入错误或静默生成非法 Arrow 数据。 |
| `df8592930` | `FileRewriteCompactTask` 在 full rewrite compact 后把 `compactDfSupplier.get()` 放入 `CompactResult`，使消费掉的 DV index entries 被清理。 | patch-id `446f5e3d76e4`；`git cherry` 为 `+`。 | 1.4 `MergeTreeCompactManager` 传入 `compactDfSupplier` 给普通 compact task，但 `FileRewriteCompactTask` 构造器无 supplier，full rewrite 路径不 `setDeletionFile`。 | 直接候选；write-only + standalone full compaction 后可能残留孤儿 DV index。 |
| `5df8b9846` | VECTOR 作为 ARRAY 暴露时，common columnar 和 Spark nested array 读取改走 `VecColumnVector`/`VectorType`，避免 ClassCastException。 | patch-id `ff02f71a0523`；`git cherry` 为 `+`。 | 1.4 `ColumnarArray.getArray` 和 `VectorizedColumnBatch.getArray` 仍强转 `ArrayColumnVector`；Spark `getArray` 仍把元素类型强转 `PaimonArrayType`。 | 直接候选；影响 VECTOR 经 Spark/Flink ARRAY 映射或嵌套读取。 |
| `179abba9b` | PyPaimon OSS bucket-in-endpoint legacy 模式跳过 `create_dir`，只探测真实 bucket；`list_status` 对该模式 fail fast；锁状态支持 pickle。 | patch-id `c9b586a972be`；`git cherry` 为 `+`。 | 1.4 `pyarrow_file_io.py` 在 `mkdirs` 中仍直接 `filesystem.create_dir(path_str, recursive=True)`，无 `_legacy_oss_mode`。 | 直接候选；PyArrow <16 OSS 写入可能误创建/污染 key 前缀对应 bucket。 |
| `1ba6d371e` | Hive `timestamp` -> Paimon `TIMESTAMP()`，不再降为 `TIMESTAMP_MILLIS()`。 | patch-id `ea96e08aa41b`；`git cherry` 为 `+`。 | 1.4 `HiveTypeUtils.toPaimonType("timestamp")` 仍返回 `DataTypes.TIMESTAMP_MILLIS()`。 | 直接候选；Hive schema 推断/迁移会截断亚毫秒精度。 |
| `69a9472a6` | PyPaimon filesystem/rest catalog 获取和修改 schema 时使用 identifier branch，`FileStoreTable.schema_manager` 使用 `current_branch()`。 | patch-id `159bcd376722`；`git cherry` 为 `+`。 | 1.4 `FileSystemCatalog.get_table_schema`/`alter_table` 未把 branch 传给 `SchemaManager`；`FileStoreTable` 从 options 取 branch，而测试需要 identifier branch。 | 直接候选；branch 表 alter/get 可能落到 main schema。 |
| `d57bc442a` | Spark ITCase Hive metastore 端口从 9092 改 9094，避免两个测试类端口冲突。 | patch-id `64837c9a3c05`；`git cherry` 为 `+`。 | 只改测试。 | test-only；不作为 runtime backport。 |
| `ccb794b6e` | Postgres `numeric/_numeric` precision/scale 超出 Paimon DECIMAL 范围时 fallback 到 STRING/ARRAY<STRING>。 | patch-id `1ae06d321ccc`；`git cherry` 为 `+`。 | 1.4 `PostgresTypeUtils` 对 precision > 0 直接构造 `DataTypes.DECIMAL(precision, scale)`。 | 直接候选；避免 PG numeric(>38) 或 PG15 scale 异常导致 CDC 建表/演进崩溃。 |
| `1f0121906` | Debezium `io.debezium.time.Time` int32 映射为 `TIME(3)`；schema evolution 允许 TIME 精度扩宽。 | patch-id `77062cd5ea9e`；`git cherry` 为 `+`。 | 1.4 只识别 `MicroTime.SCHEMA_NAME`，未处理 `Time.SCHEMA_NAME`；`UpdatedDataFieldsProcessFunctionBase` 无 TIME 精度 convert 逻辑。 | 直接候选；Postgres time(0..3) CDC 可从 INT/TIME 冲突变为稳定 TIME。 |
| `f6e3e3155` | RESTApi 构造 `/v1/config` query 时传原始 `warehouse`，避免 HTTP 层再编码造成双重编码。 | patch-id `9887a8fe5888`；`git cherry` 为 `+`。 | 1.4 `RESTApi` 仍使用 `RESTUtil.encodeString(warehouse)`。 | 直接候选；`file:///...` 等 warehouse 在 REST catalog 初始化时可能被服务端收到编码残留。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `ea1aa7c22` | JDBC catalog | `JdbcCatalog.listViewsPaged` 新增 database existence 检查、SQL LIKE、cursor (`view_name > ?`) 和 `LIMIT maxResults+1`；`JdbcUtils` 新增 view paging SQL。 | 直接候选。 |
| 2 | `c65d2550f` | Codegen / sort | `SortCodeGenerator` 对 `LocalZonedTimestampType` 使用 `getPrecision`，在 normalized key 类型、prefix 判断、key 字节数中与普通 timestamp 统一。 | 直接候选。 |
| 3 | `0e80c0e70` | Azure filesystem | Azure Hadoop FileIO 用 `ConcurrentHashMap<authority, FileSystem>` 替代单 `fs`，authority 为空时用 `DEFAULT`。 | 直接候选。 |
| 4 | `2d178e130` | COSN filesystem | COSN Hadoop FileIO 做同样 authority 级缓存。 | 直接候选。 |
| 5 | `926ffb1df` | API VectorType | `isPrunedFrom` 加 `length == vectorType.length`；测试覆盖同/不同维度。 | 直接候选。 |
| 6 | `93e047b16` | Core audit log | `$audit_log` 需要 sequence number 时复制 wrapped table 并注入动态 option，不再改原表 options map；测试覆盖先读 audit 后读 base table。 | 直接候选。 |
| 7 | `e4045f710` | Parquet filter | timestamp predicate 前新增 file schema primitive/logical 校验，unit 和 adjustedToUTC 不匹配则禁用 pushdown。 | 直接候选。 |
| 8 | `2d42fb8da` | Core overwrite | `FileStoreCommit`/`TableCommitImpl` 支持 `withOverwriteStaticPartitions(List<BinaryRow>)`，可一次覆盖多个静态分区。 | 功能新增，非 bugfix。 |
| 9 | `513966a47` | Core schema map shredding | rename/drop MAP 字段时迁移或删除 `fields.<name>.map.*` shared-shredding options。 | 1.4 无 shared-shredding map 路径，不适用。 |
| 10 | `f52dde235` | Core query auth / fallback | index bootstrap 关闭 query auth；fallback scan/read 处理 `QueryAuthSplit`/`FallbackSplit` 和 `withReadType`。 | 直接候选。 |
| 11 | `ca8c2ba6d` | Arrow writer | 嵌套 array/vector/map/row writer 改用子字段 nullability，map key 强制 non-null；测试覆盖嵌套 null。 | 直接候选。 |
| 12 | `df8592930` | Core compaction / DV | FileRewrite compact 结果回传 deletion file cleanup；新增 write-only + standalone compact 复现测试。 | 直接候选。 |
| 13 | `5df8b9846` | Common / Spark vector read | `ColumnarArray`、`VectorizedColumnBatch`、Spark `SparkArrayData` 支持 VECTOR-as-ARRAY 读取。 | 直接候选。 |
| 14 | `2c30419e2` | Docs metrics | 更新 lookup metrics 文档，加入 partial lookup metrics。 | docs-only。 |
| 15 | `5fb075cd9` | PyPaimon aggregator | 新增 `merge_map` 聚合器，支持 dict 和 pyarrow map list 形态，retract 按 key 删除。 | 功能新增。 |
| 16 | `809be2512` | Flink data evolution | 新增 `DataEvolutionDelete` action/operator，按 row-tracking snapshot 生成 deletion vectors；扩展 delete action factory/docs/tests。 | 功能新增，依赖 master data-evolution 栈。 |
| 17 | `179abba9b` | PyPaimon OSS FileIO | legacy OSS 模式 `mkdirs` 不再 create_dir，改探测 bucket；`list_status` 给出 pyarrow>=16/jindo 建议；pickle 重建 lock。 | 直接候选。 |
| 18 | `1ba6d371e` | Hive type mapping | Hive timestamp 映射从 `TIMESTAMP_MILLIS()` 改为 `TIMESTAMP()`，保留默认精度。 | 直接候选。 |
| 19 | `69a9472a6` | PyPaimon branch schema | filesystem catalog 的 get/alter schema 和 REST table schema manager 使用 identifier/current branch。 | 直接候选。 |
| 20 | `d57bc442a` | Spark test infra | `SparkDataEvolutionITCase` Hive metastore port 改 9094，避免并行测试端口冲突。 | test-only。 |
| 21 | `c5884b622` | Flink query service metrics | `QueryExecutorOperator` 给 LocalTableQuery 注入 `PartialLookupMetrics`，测试验证 remote/local lookup 计数。 | 功能/可观测性新增。 |
| 22 | `ccb794b6e` | Flink CDC Postgres | Postgres numeric precision/scale 越界 fallback 到 STRING；新增 PostgresTypeUtils 单测。 | 直接候选。 |
| 23 | `1f0121906` | Flink CDC Postgres | Debezium `Time.SCHEMA_NAME` 映射 `TIME(3)`；TIME 类型 schema evolution 支持精度扩宽。 | 直接候选。 |
| 24 | `f0143c98b` | PyPaimon aggregator | 新增 `nested_partial_update` 聚合器，按 nested-key 合并非空字段，支持 null-key 策略。 | 功能新增。 |
| 25 | `f6e3e3155` | REST API | `/v1/config` 的 `warehouse` query param 不再手动 encode；新增 MockWebServer 测试防双重编码。 | 直接候选。 |
| 26 | `9f0e06d0b` | Test stability | 修 Aliyun CDC、Hive metastore/procedure、Spark ITCase 的不稳定测试等待/端口/断言。 | test-only。 |
| 27 | `a998338ec` | Core row-count rolling | 新增 `target-file-row-num`，Rolling/Format/Dedicated writers 支持按行数滚动；PyPaimon 暂不支持时 fail fast；Spark/Python/schema/docs 同步 option。 | 功能新增，不作为 1.4 bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功；fork 分支已刷新。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | 成功；`upstream/master` 从 `85a01158ca96` 推进到 `a998338ec62f`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 第一次 HTTPS 连接 GitHub 超时，非沙箱重试成功；`upstream/release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse --format=... 85a01158..upstream/master` | 列出并核对 27 个 first-parent commit。 |
| `git diff --stat 85a01158..upstream/master` | 125 files changed, `5460 insertions(+), 319 deletions(-)`。 |
| `git cherry -v upstream/release-1.4 upstream/master 85a01158...` | 本轮 27 个 commit 均为 `+`，未发现等价合入 1.4。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <commit>` | 已对 27 个 commit 执行，combined patch 缓存在 `/private/tmp/paimon-daily-2026-07-24-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 已对 27 个 commit 执行，patch-only 输出同样缓存。 |
| `git patch-id --stable` | 已记录候选 bugfix stable patch-id。 |
| `git grep` / `git show upstream/release-1.4:<path>` | 已核对候选生产路径，包括 JDBC view paging、SortCodeGenerator、Azure/COSN FileIO、VectorType、AuditLogTable、ParquetFilters、IndexBootstrap/FallbackRead、Arrow writer、DV compact、VECTOR columnar/Spark、PyPaimon OSS/branch、HiveTypeUtils、PostgresTypeUtils/PostgresRecordParser、RESTApi、row-count rolling。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只新增日报和 state 游标更新。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `a998338ec62f2cae5c0649e797eda4d02be5de36`
- `last_processed_commit_date`: `2026-07-24 11:52:29 +0800`
- `last_report_date`: `2026-07-24`
- `last_report_path`: `reports/paimon-master-daily/2026-07-24.md`

# Apache Paimon Master Daily Report - 2026-07-23

## Summary

范围：`b52f55f7801e16cdfd64b2fc935b3d3f14257af8..85a01158ca9681cbc3988d72546ec0b9eee37fab`，共 9 个 first-parent 提交，78 个文件变更，`+745/-488`。本轮 `upstream/master` 推进到 `85a01158ca96`（2026-07-22 21:25:32 +0800），`upstream/release-1.4` 仍是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

已逐个提交检查 `git show --stat --name-status --patch --find-renames --format=fuller` 和单独 patch-only 输出，patch 缓存在 `/private/tmp/paimon-daily-2026-07-23-patches`。主要变化集中在 data-evolution/global-index 读写语义、Spark data-evolution raw BLOB merge、full-text 发布依赖、vector vectored-read buffer 复用，以及 docs `yarn.lock` 依赖升级。

`release-1.4` 影响上，本轮 9 个 commit 对 `upstream/release-1.4` 均为 `git cherry +`，没有等价 patch。未发现可直接 cherry-pick 的 1.4 生产 bugfix：`b9915d4` 修复的 source-backed index 过滤在 1.4 旧 scanner 中不存在；`67435d2` 的 unsupported predicate fallback 命中 master 的异步 reader/`IS_NAN`/`ConstantGlobalIndexReader` 路径，1.4 仍是同步 reader 且没有同一访问器；`d637d65` 依赖 master 之后的 raw-blob marker/fallback 架构，1.4 需要前置功能栈适配。`fb02b3c` 的 `IGNORE` 是可选行为新增，若 1.4 用户需要接受短期 stale global index，可作为功能型 backport 评估。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Data evolution / global index | 4 | 新增 `global-index.column-update-action=IGNORE`；data-evolution global-index scanner/coverage 改为读取 source-backed index 并重命名为 `DataEvolution*`；unsupported scalar predicate fallback 补齐；vector read 类继续 DataEvolution 命名整理。 | 1.4 没有等价 patch；`IGNORE` 是可选新行为，source-backed 过滤回归不在 1.4 旧 scanner 中，unsupported predicate patch 需要 1.4-specific 设计。 |
| Spark data-evolution / raw BLOB merge | 1 | 允许 Spark MERGE 更新 raw-data `ARRAY<BLOB>` / `MAP<*, BLOB>`；引入 array/map blob placeholder fallback；filler row 只复制非 blob 字段；补充 raw/descriptor array/map blob 与 deletion-vector gap 测试。 | 1.4 的 Spark data-evolution writer 仍是早期连续 row-id/单文件写法，没有 master raw-blob fallback 参数；不是直接 cherry-pick 候选。 |
| Full-text / Python / CI dependency | 1 | `paimon.full-text-index.version` 从 `0.1.0-SNAPSHOT` 切到 `0.1.0`；删除 CI 中拉源码安装 full-text 的脚本；Python 增加 `pypaimon[full-text]` / `paimon-ftindex==0.1.0` extras 和错误提示。 | 1.4 没有 `paimon-full-text` 模块和 Python full-text 包路径，非直接。 |
| Vector / filesystem vectored read | 1 | `FileRange` 支持 caller-provided buffer；`VectoredReadUtils` fallback/single/combined read 写入提供的 buffer；native vector reader 避免二次分配和拷贝。 | 1.4 有 `FileRange`/`VectoredReadUtils`，但缺少 `paimon-vector/NativeVectorGlobalIndexReader` 主消费者；非直接生产修复。 |
| Docs dependencies | 2 | docs `yarn.lock` 升级 `svgo` 3.3.3 -> 3.3.4、`fast-uri` 3.1.2 -> 3.1.4。 | docs/site 依赖维护，不影响 1.4 runtime。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `fb02b3cf0` | 新增 `GlobalIndexColumnUpdateAction.IGNORE`，让 Flink/Spark/Python data-evolution 更新 indexed column 时可跳过 index 删除/报错，保留已有 index 文件并接受 stale index。 | patch-id `08eebdf52a51f79168cbf826ce26d4da2ec4bc66`；`git cherry` 为 `+`。 | 1.4 已有 `global-index.column-update-action`，但 enum 只有 `THROW_ERROR` / `DROP_PARTITION_INDEX`；Flink/Spark 检查逻辑会扫描 index 后再报错或删除。 | 可选功能型 backport；不是默认行为 bugfix，需要明确接受 stale index 语义。 |
| `b9915d492` | data-evolution global-index scan 应读取 source-backed index；master patch 删除 `sourceMeta != null` 过滤并重命名 scanner/coverage。 | patch-id `7dec59e4fb3f3ca277488575d1142e6fa94b4c2c`；`git cherry` 为 `+`。 | 1.4 `GlobalIndexScanner.create` 中没有 `sourceMeta` 过滤，`DataEvolutionBatchScan` 直接调用该 scanner；`git grep sourceMeta` 未发现同类过滤。 | 1.4 当前旧实现未命中该 master 回归；不建议直接 backport。 |
| `67435d2a4` | unsupported global-index predicates 应安全 fallback；master 为 `IS_NAN` / `BETWEEN` / `NOT BETWEEN` 增加默认 empty result，并补齐 Offset/Union/Constant reader。 | patch-id `6c00f73ed745918394bf06e92b46c3a7a59d7643`；`git cherry` 为 `+`。 | 1.4 global-index reader 是同步 `Optional` 版本；`FunctionVisitor` 没有 `visitIsNaN`；`BETWEEN` / `NOT BETWEEN` 仍通过默认组合比较谓词实现，且 1.4 没有 `ConstantGlobalIndexReader` / native vector reader 同一路径。 | 不可直接套 patch；若 1.4 有具体 unsupported predicate 复现，应按旧同步 reader 单独修。 |
| `d637d6507` | Spark data-evolution MERGE 支持更新 raw array/map blob，并修正 filler row 对 blob 字段的处理。 | patch-id `a273bc7f30d060b3c1711867698fb525484e41ae`；`git cherry` 为 `+`。 | 1.4 `DataEvolutionTableDataWrite` 没有 raw-blob marker/fallback 参数，仍按连续 row-id 写入单个 data file；master hunk 依赖后续 blob/data-evolution 架构。 | 问题域相关但非直接；需要前置 BLOB partial-write 功能栈适配。 |
| `d3b62affb` | full-text 依赖从源码/SNAPSHOT 切到发布版 `0.1.0`，并改 CI/Python 安装路径。 | patch-id `4ad6b9b2a67e6364dd355d451b717289b0495258`；`git cherry` 为 `+`。 | 1.4 没有 `paimon-full-text` 模块，也没有 `pypaimon/globalindex/full_text` 路径。 | 非 1.4 runtime/backport 候选。 |
| `14d3dee2f` | vectored read 写入 caller-provided buffer，避免 native vector index callback buffer 再分配和二次拷贝。 | patch-id `698b298736d470d77cfa7184a3bb28707421c797`；`git cherry` 为 `+`。 | 1.4 有 `FileRange`/`VectoredReadUtils` 的 `new byte[]` 路径，但没有 `paimon-vector/NativeVectorGlobalIndexReader` 主调用方。 | 性能优化可选；没有直接 1.4 生产 bugfix 证据。 |
| `d6ae6a397` | docs `svgo` 依赖升级。 | patch-id `9d984567885a5ff47a9d7909cc79a532ef277f58`；`git cherry` 为 `+`。 | 只改 `docs/yarn.lock`。 | docs/site 依赖维护，非 runtime。 |
| `85a01158c` | docs `fast-uri` 依赖升级。 | patch-id `7da4e8a1068e8cc821ff7c8d7aa73423586ef5fe`；`git cherry` 为 `+`。 | 只改 `docs/yarn.lock`。 | docs/site 依赖维护，非 runtime。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `fb02b3cf0` | Core / Flink / Spark / Python global index | `CoreOptions.GlobalIndexColumnUpdateAction` 新增 `IGNORE`；Flink `MergeIntoUpdateChecker`、Spark `checkUpdateResult`、Python `apply_global_index_update_action` 在 `IGNORE` 下直接返回，不扫描/删除 index；文档说明 stale index 风险；Java/Scala/Python 测试覆盖 ignore。 | 1.4 有同一 option 但缺少 `IGNORE`，可作为显式功能 backport，非默认 bugfix。 |
| 2 | `b9915d492` | Core / Python data-evolution global index | Java/Python `GlobalIndexScanner` / `GlobalIndexCoverage` 重命名为 `DataEvolutionGlobalIndexScanner` / `DataEvolutionGlobalIndexCoverage`；删除对 source-backed index 的过滤；data-evolution full-text/vector/scalar read 改用新类；补 source-backed/mixed coverage 测试。 | 1.4 旧 scanner 没有该过滤，非直接。 |
| 3 | `67435d2a4` | Common global-index evaluator | `GlobalIndexReader` 增加 `IS_NAN`、`BETWEEN`、`NOT BETWEEN` fallback；`OffsetGlobalIndexReader`、`UnionGlobalIndexReader`、`ConstantGlobalIndexReader` 补 `NOT BETWEEN`；测试覆盖 unsupported fallback、offset/union 传递、constant reader 固定结果。 | 1.4 同步 reader/谓词接口不同，需单独复现后适配。 |
| 4 | `abaf053ae` | Core data-evolution vector naming | `AbstractVectorRead` -> `AbstractDataEvolutionVectorRead`，`BatchVectorReadImpl` -> `DataEvolutionBatchVectorRead`；builder 和测试改引用；primary-key vector/full-text scan 构造继续透传 filter。 | 重命名/结构整理，无直接 1.4 bugfix。 |
| 5 | `d637d6507` | Spark data-evolution BLOB | 删除 Spark MERGE 对 raw array/map blob 更新的拒绝；`DataEvolutionTableDataWrite` 用 `BlobArrayPlaceholder` / `BlobMapPlaceholder` 和非 blob field copier 生成 filler row；测试覆盖 raw array/map、descriptor array/map、自 merge、DV gap 后 blob 更新。 | 1.4 缺 master raw-blob partial-write 前置结构，非直接 cherry-pick。 |
| 6 | `d3b62affb` | Full-text / Python / CI | 删除 `.github/workflows/install-paimon-full-text.sh`；CI 不再 clone/build full-text 源码；POM 依赖改为 `0.1.0`；Python dev/setup 增加 `paimon-ftindex==0.1.0`；错误提示改为安装 release 包。 | 1.4 无 full-text 模块，非直接。 |
| 7 | `14d3dee2f` | FS vectored read / vector index | `FileRange.createFileRange(offset, byte[])` 支持外部 buffer；`VectoredReadUtils` 在 sequential/single/combined range 中复用该 buffer；native vector reader 直接传 callback buffer 并只等待 future 完成；测试断言同一 buffer 被填充。 | 1.4 缺 native vector reader 主消费者；性能优化可选。 |
| 8 | `d6ae6a397` | Docs deps | `docs/yarn.lock` 将 `svgo` 从 3.3.3 升级到 3.3.4，更新 resolved/integrity。 | docs-only。 |
| 9 | `85a01158c` | Docs deps | `docs/yarn.lock` 将 `fast-uri` 从 3.1.2 升级到 3.1.4，更新 resolved/integrity。 | docs-only。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | 成功；`upstream/master` 从 `b52f55f7801e` 推进到 `85a01158ca96`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/release-1.4` 保持 `05d1ba75a97c`。 |
| `git fetch --no-tags origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功；本地分支与 origin 对齐。 |
| `git rev-parse upstream/master upstream/release-1.4 <state-last-processed>` | 确认 `master=85a01158ca9681cbc3988d72546ec0b9eee37fab`，`release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git rev-list --count --first-parent b52f55f7801e..upstream/master` | 9。 |
| `git rev-list --reverse --first-parent --format=... b52f55f7801e..upstream/master` | 列出并核对 9 个 first-parent commit。 |
| `git diff --shortstat b52f55f7801e..upstream/master` | 78 files changed, `745 insertions(+), 488 deletions(-)`。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <commit>` | 已对 9 个 commit 执行，combined patch 缓存在 `/private/tmp/paimon-daily-2026-07-23-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 已对 9 个 commit 执行，patch-only 输出同样缓存。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 本轮 9 个 commit 均为 `+`，未发现等价合入 1.4。 |
| `git patch-id --stable` | 已记录全部 9 个 commit 的 stable patch-id。 |
| `git cat-file -e upstream/release-1.4:<path>` / `git grep` / `git show upstream/release-1.4:<path>` | 已核对候选生产路径和旧实现，包括 global-index reader、source-backed scanner、Spark data-evolution writer、full-text/vector 模块、`FileRange`/`VectoredReadUtils`、docs lockfile。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只新增日报和 state 游标更新。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `85a01158ca9681cbc3988d72546ec0b9eee37fab`
- `last_processed_commit_date`: `2026-07-22 21:25:32 +0800`
- `last_report_date`: `2026-07-23`
- `last_report_path`: `reports/paimon-master-daily/2026-07-23.md`

# Apache Paimon Master Daily Report - 2026-07-22

## Summary

范围：`cd39bd70567eb27909142523c0b4faace2c8b540..b52f55f7801e16cdfd64b2fc935b3d3f14257af8`，共 31 个 first-parent 提交，257 个文件变更，`+20457/-1510`。本轮 `upstream/master` 推进到 `b52f55f7801e`（2026-07-22 11:38:02 +0800），`upstream/release-1.4` 仍是 `05d1ba75a97c`。

已逐个提交缓存并检查 `git show --stat --name-status --patch --find-renames --format=fuller` 和单独 patch-only 输出，patch 缓存在 `/private/tmp/paimon-daily-2026-07-22-patches`。主要变化集中在 BLOB 写读/descriptor/Map-Blob 格式、Format Table catalog-managed partitions 及 REST/Spark 接入、Common/API/Codegen correctness 修复、Iceberg metadata 兼容性、PyPaimon/Daft/aggregation/global-index、Flink coordinator-commit idle watermark，以及 docs 依赖和 Jindo/Lumina Maven 仓库 URL。

`release-1.4` 影响上，本轮 31 个 commit 对 `upstream/release-1.4` 均为 `git cherry +`，没有等价 patch。明确直接候选包括 Iceberg metadata bounds/null-count、pre-epoch TIME cast、timestamp truncate 精度、MAP comparator value type、SchemaChange 数组 hash、从过期 snapshot 的 tag 创建 branch、REST/URI/配置脱敏、PyPaimon index scan 最新 snapshot、Iceberg incremental delete visibility、Jindo/Lumina Maven 仓库 URL。`8d26890f1` 的 data-evolution row-id 冲突检测命中 1.4 相关类，但 1.4 的冲突检测结构较旧，需适配验证；`f4ed84d88` 是 PyPaimon `to_range_list` 内存优化候选；`4d365cae7` 因 1.4 缺少被修改的 `top_k` 方法，不列为直接候选。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| BLOB / multimodal format | 3 | `blob.copy-buffer-size` 贯穿 Java/Python BLOB writer；`BlobDescriptorReaderFactory` 支持从 source table FileIO 解析 descriptor；BLOB format 抽出 element serializer 并支持 `Map<X, Blob>`、Java/Python 读写和 placeholder。 | 1.4 有基础 BLOB writer，但 Map-Blob/source-table descriptor 是 master 功能栈；copy-buffer 是可选性能/调优 backport，不是 correctness bugfix。 |
| Format Table / REST / Spark partitions | 4 | Format Table 新增 catalog-managed partition manager；REST catalog 支持注册/列举/按 filter 分页；Spark 支持 ADD/DROP/MSCK 和 partition sink strategy；scan 对 catalog filter 做粗粒度下推并本地复验。 | 1.4 缺少 `FormatTablePartitionManager` 和 REST `ListPartitionsByFilterRequest` 等核心路径，按功能栈处理，不是直接 bugfix。 |
| Common / API / Codegen correctness | 6 | 修复 pre-epoch timestamp -> TIME、timestamp truncate leading-zero fractional precision、MAP comparator value type、SchemaChange 数组 hash；新增 non-compact timestamp normalized key；新增敏感配置/URI/错误消息脱敏。 | 多个直接 1.4 候选；normalized key 是功能/性能支持，不是 bugfix。 |
| Iceberg compatibility | 2 | `IcebergDataFileMeta` 跳过 unknown/null stats 和无 bounds 类型；Iceberg manifest delete entry 记录 deleting snapshot，并在 manifest compaction 中保留本次删除。 | 两个都是直接 1.4 correctness 候选，生产路径存在且旧实现仍保留。 |
| PyPaimon / Daft / global index / aggregation | 5 | Daft filter pushdown 从 stateful `push_filters` 改为 `Pushdowns` planning；Python scored top-k tie-break 调整；新增 `merge_map_with_keytime`；index scan 默认 snapshot 修正；RoaringBitmap64 range list 避免物化全部 row id。 | `e94c39ae1` 是直接 1.4 候选；`f4ed84d88` 是性能/内存候选；Daft/aggregation/top-k 多数为 master 功能或 1.4 缺少同一方法。 |
| Flink sink coordinator | 1 | writer 在 checkpoint committables 中携带 idle 状态，coordinator 通过 `WatermarkAligner` 复现 Flink valve 语义，避免 idle subtask 拖低 coordinator-commit watermark。 | Flink sink 路径存在但这是新能力/语义增强；未列为直接 bugfix。 |
| Core scan / memory / branch / conflict detection | 3 | Branch-from-tag 在 snapshot 文件过期时从 tag JSON 写回 branch snapshot；manifest/cache 扫描在分区/桶枚举时边读边转换降低内存；data-evolution compaction 与 row-id reassignment 冲突检测增强。 | `94af1b2cc` 是直接 1.4 候选；`077f05863` 是内存优化；`8d26890f1` 需按 1.4 冲突检测结构适配。 |
| Docs / build deps / filesystem repos | 7 | docs `yarn.lock` 更新 `brace-expansion`、`body-parser`、`webpack-dev-server`、`shell-quote`；Jindo/JindoDLS/Lumina Maven repo 切到 `oss-accelerate`；`FileChannelManagerImpl` 注释修正。 | docs 依赖是站点构建/供应链维护；`09eb34077` 是直接 1.4 build-stability 候选；注释无运行影响。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `345526e26` | Iceberg data file stats：unknown null count 不应发布为 0；required 字段 unknown min/max 不应读出垃圾 bounds；ARRAY/MAP/ROW/MULTISET/VARIANT/VECTOR/BLOB 不发布 bounds。 | patch-id `bb1e38023255bd5190523138d6446cd77b4e1fd9`；`git cherry` 为 `+`。 | 1.4 存在 `IcebergDataFileMeta.java`，仍直接 `stats.nullCounts().getLong(idx)` 且用非 nullable field getter 读 bounds。 | 直接 1.4 Iceberg correctness 候选，建议优先评估。 |
| `9c0432797` | pre-epoch `TIMESTAMP` cast 到 `TIME` 使用 `floorMod`，避免负的 time-of-day。 | patch-id `7624498ec3c2fc586a46dc25f77bc403d925e349`；`git cherry` 为 `+`。 | 1.4 `TimestampToTimeCastRule.java` 仍使用 `value.getMillisecond() % MILLIS_PER_DAY`。 | 直接 1.4 correctness 候选。 |
| `501e33211` | `DateTimeUtils.truncate` 保留 leading-zero fractional digits 后再判断 significant precision，避免 `.000000009` 等值错误保留纳秒。 | patch-id `958f3cd17de94ce06e671eef948e57917d472ce6`；`git cherry` 为 `+`。 | 1.4 `DateTimeUtils.java` 仍用 `Integer.toString(ts.toLocalDateTime().getNano())`。 | 直接 1.4 correctness 候选。 |
| `ce76ab95e` | 生成 MAP comparator 时，value array 应使用 `mapType.getValueType`，否则 `MAP<INT, STRING>` 等会用 key type 比较 value。 | patch-id `b297010e342e9bf6b83022a2034ff68ae7649e4f`；`git cherry` 为 `+`。 | 1.4 `GenerateUtils.scala` 仍有 `valueArrayType = new ArrayType(mapType.getKeyType)`。 | 直接 1.4 correctness 候选。 |
| `b3b397b8e` | `SchemaChange` 多个 nested field change 的 `hashCode` 应按数组内容计算，避免 equals 相同但 hash 不同。 | patch-id `8d8e921d558929fdb64d12cf849e48b2e2b6ef9d`；`git cherry` 为 `+`。 | 1.4 `SchemaChange.java` 多处仍用 `Objects.hashCode(fieldNames)`，仅部分类已用 `Arrays.hashCode`。 | 直接 1.4 API correctness 候选。 |
| `94af1b2cc` | 从 tag 创建 branch 时，若 tag 保留的 snapshot 文件已过期，应从 tag 内容写 branch snapshot，而不是 copy 不存在的 snapshot file。 | patch-id `14c0d9d4c2d5a70a67870333aa98cf0e3529200e`；`git cherry` 为 `+`。 | 1.4 `FileSystemBranchManager.java` 仍无 `snapshotExists` 判断，直接 copy `snapshotPath(snapshot.id())`。 | 直接 1.4 运维/branch 功能候选。 |
| `ee55502a0` | REST/HTTP/URI/Hadoop config 日志和异常增加敏感信息脱敏，避免 signed URL、token file 内容、AK/SK、query string、raw URI 泄漏。 | patch-id `2d161e0fbb1b4aa4d48c26b8c05d90074698b12d`；`git cherry` 为 `+`。 | 1.4 缺少 `SensitiveConfigUtils`；`HttpClientUtils`/`SimpleHttpClient`/`LoggingInterceptor`/DLF token loader 仍构造 raw request 或拼接 `e.getMessage()`。 | 直接 1.4 security-hardening 候选，建议高优先级评估。 |
| `e94c39ae1` | PyPaimon index scan 未指定 snapshot 时应调用 `get_latest_snapshot()`。 | patch-id `1164489484424cf3150940a25946fae35f137059`；`git cherry` 为 `+`。 | 1.4 `index_file_handler.py` 仍调用 `latest_snapshot()`，而 `snapshot_manager.py` 只暴露 `get_latest_snapshot()`。 | 直接 1.4 PyPaimon correctness 候选。 |
| `39937ccc9` | Iceberg incremental scans 需要看到本次删除：delete entry 的 snapshot id 应记录 deleting snapshot，manifest compaction 也要按 `snapshotId == currentSnapshotId` 保留当前删除。 | patch-id `e97b11f286427f6d323cf0750cea281fb5105d97`；`git cherry` 为 `+`。 | 1.4 `IcebergCommitCallback.java` 删除 entry 仍保留旧 `entry.snapshotId()`，compact 时只看 `fileSequenceNumber() == currentSnapshotId` 或 `EXISTING`。 | 直接 1.4 Iceberg incremental correctness 候选。 |
| `09eb34077` | Jindo/JindoDLS/Lumina POM 仓库 URL 从地域 OSS 切到 `oss-accelerate`，缓解 JNI/Jindo Maven repo timeout。 | patch-id `fb0662cde873202e66b95f4d18734a70afa3d8bf`；`git cherry` 为 `+`。 | 1.4 三个 POM 仍使用 `oss-cn-shanghai` URL。 | 直接 1.4 build-stability 候选。 |
| `8d26890f1` | data-evolution compaction 与 row-id reassignment 并发时，检测 stale compaction 输出把 row id range 回退；retry 时遇到 overwrite 不复用增量 base cache。 | patch-id `8b94ebba4dd119c2f7360cff83b484e6d19faa75`；`git cherry` 为 `+`。 | 1.4 有 `ConflictDetection` / `FileStoreCommitImpl` 和 data-evolution row-id 检查，但缺少 master 当前 `checkRowIdExistence` scaffold 与 `hasOverwriteSinceLastAttempt`。 | 需 1.4-specific 适配与 UT 验证；不是直接 cherry-pick 级别。 |
| `f4ed84d88` | PyPaimon `RoaringBitmap64.to_range_list` 用 iterator 流式构造 range，避免 `list(self._data)` 物化全部 row id。 | patch-id `2eb4571419b49fd97e548b8a348fc53535186888`；`git cherry` 为 `+`。 | 1.4 `roaring_bitmap.py` 仍先 `sorted_values = list(self._data)`。 | 性能/内存候选，非 correctness 优先级。 |
| `4d365cae7` | Python scored global-index top-k tie-break：同分边界按 row id 稳定选择。 | patch-id `9e090e1d8c067728b910ea216d54a5557b776ec8`；`git cherry` 为 `+`。 | 1.4 有 `vector_search_result.py`，但没有本补丁修改的 `top_k` 方法；`git grep top_k` 无结果。 | 非直接 1.4 路径。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `b52d83bb8` | BLOB writer | 新增 `blob.copy-buffer-size`，校验 1 byte 到 `Integer.MAX_VALUE`；Java `BlobFormatWriter` 和 PK externalizer、Python writer 都改为用可配置 buffer，默认仍 4 KiB。 | 性能/调优功能；1.4 可选，非 bugfix。 |
| 2 | `b4a950bce` | PyPaimon / Daft | 移除 Daft source 内部持久化的 pushed/remaining filter 状态；explain 和 scan 统一从 `Pushdowns.filters` 转成 Paimon predicate，并允许可规划 predicate 下推到 scan planning。 | Daft 功能增强，非直接 1.4 bugfix。 |
| 3 | `5a70451c8` | MAP shared-shredding | 新增 `map.shared-shredding.column-placement-policy`，实现 plain/sequential/LRU 三种 allocator，并在 schema validation 中校验该字段级配置。 | master 功能；1.4 缺少 shared-shredding 相关路径，非直接。 |
| 4 | `345526e26` | Iceberg metadata | `IcebergDataFileMeta` 不再把 unknown null count 写成 0；复杂/无 bounds 类型跳过；required 字段用 nullable getter 防止 unknown stats 变成垃圾 bounds。 | 直接 1.4 correctness 候选。 |
| 5 | `983e745ef` | Format Table / REST | `FormatTable` 可携带 partition manager；catalog-managed partition 由 catalog 决定可见性，write 后注册分区，scan 从 catalog list 分区并防御 corrupt spec/路径逃逸；REST catalog 对 managed partition drop/list 做 metadata-only 或 fallback 区分。 | 功能栈；1.4 缺少 manager，非直接。 |
| 6 | `9c0432797` | Common cast | `TimestampToTimeCastRule` 对无时区 timestamp 使用 `Math.floorMod`，新增 pre-epoch cast 测试。 | 直接 1.4 correctness 候选。 |
| 7 | `501e33211` | Common DateTime | `DateTimeUtils.truncate` 用 9 位 padding 并去 trailing zero 计算有效小数位，新增纳秒/微秒/idempotent/max precision 测试。 | 直接 1.4 correctness 候选。 |
| 8 | `ce76ab95e` | Codegen | MAP comparator 的 value array type 从 key type 改为 value type，新增 `MAP<INT, STRING>` comparator 回归测试。 | 直接 1.4 correctness 候选。 |
| 9 | `ae249ece1` | Core comment | `FileChannelManagerImpl` shutdown-hook 相关误导性注释改为说明目录由 manager close 清理。 | 注释-only，无 1.4 运行影响。 |
| 10 | `b1e486221` | Codegen / sort | non-compact timestamp normalized key 改为支持任意 precision，key length 8 -> 12，并在 nano-of-millisecond 上追加 unsigned key。 | 功能/性能支持；非 bugfix。 |
| 11 | `b3b397b8e` | API schema change | `AddColumn`、`RenameColumn`、`DropColumn`、`UpdateColumnType`、`UpdateColumnDefaultValue` 等 hashCode 改用 `Arrays.hashCode(fieldNames)`，新增等价对象 hash 测试。 | 直接 1.4 API correctness 候选。 |
| 12 | `94af1b2cc` | Core branch/tag | 创建 branch from tag 时若原 snapshot file 已过期，则从 tag snapshot JSON 写入 branch snapshot；新增过期后创建 branch 测试。 | 直接 1.4 运维功能候选。 |
| 13 | `4d365cae7` | Python global index | scored top-k heap item 从 `(score, row_id)` 改为 `(score, -row_id)`，同分边界稳定保留较小 row id。 | 1.4 无 `top_k` 方法，非直接。 |
| 14 | `077f05863` | Core scan memory | `ObjectsCache` 读取 segments 时边读边转换，`AbstractFileStoreScan` 在列 partition/bucket 时只构造 `PartitionEntry`/`BucketEntry`，降低全量 manifest entry 物化。 | 内存优化，可选。 |
| 15 | `87aeb1355` | PyPaimon aggregation | 新增 `merge_map_with_keytime` aggregator，支持 MAP value ROW 按 timestamp 合并、null 删除、无 timestamp 忽略；支持 `fields.<f>.ts-field`。 | PyPaimon 功能增强，非直接 bugfix。 |
| 16 | `ee55502a0` | Security / logging | 新增 `SensitiveConfigUtils`；REST request 构造/HTTP execute/LoggingInterceptor/DLF token parse/Hadoop config/URI parse 都改为脱敏或 fail-closed。 | 直接 1.4 security-hardening 候选。 |
| 17 | `1851acf78` | Flink coordinator commit | writer 记录 checkpoint barrier 时的 watermark idle 状态；checkpoint committables serializer 扩展 idle bit；coordinator 用 `WatermarkAligner` 聚合每个 checkpoint 的 watermark。 | 新能力/语义增强，非直接 bugfix。 |
| 18 | `162fae394` | BLOB descriptor source FileIO | `UriReaderFactory` 支持以提供的 FileIO 解析非 HTTP URI；新增 `BlobDescriptorReaderFactory` 从 source table 初始化 FileIO；Flink/Spark write path 传递 reader factory 并在序列化后重挂 BLOB reader。 | BLOB 功能栈；非直接 1.4 bugfix。 |
| 19 | `d7cd09b9a` | Docs deps | docs `yarn.lock` 将 `brace-expansion` 1.1.14 -> 1.1.16。 | docs/site 依赖维护。 |
| 20 | `e65d04425` | Docs deps | docs `yarn.lock` 将 `body-parser` 1.20.5 -> 1.20.6。 | docs/site 依赖维护。 |
| 21 | `4837aff52` | Docs deps | docs `yarn.lock` 将 `webpack-dev-server` 5.2.5 -> 5.2.6。 | docs/site 依赖维护。 |
| 22 | `ed714f274` | Docs deps | docs `yarn.lock` 将 `shell-quote` 1.8.4 -> 1.10.0。 | docs/site 依赖维护。 |
| 23 | `e94c39ae1` | PyPaimon index | `IndexFileHandler.scan(None)` 默认 snapshot 从 `latest_snapshot()` 改为 `get_latest_snapshot()`，新增 mock 回归测试。 | 直接 1.4 PyPaimon correctness 候选。 |
| 24 | `8d26890f1` | Core data evolution | 对 compaction commit 也做 row-id existence 检查，校验 compaction 输出范围仍被当前 base 覆盖；retry 时检测期间出现 overwrite 就放弃复用 incremental base cache。 | 1.4 需适配验证。 |
| 25 | `d4d1c0115` | Spark Format Table DDL | 新增 Format Table partition ADD/DROP/MSCK physical exec，支持 partial DROP、metadata-only unregister 后删除目录、cache refresh、repair diff；parser rewrite 识别 catalog-managed Format Table。 | 依赖 master Format Table manager，非直接。 |
| 26 | `09eb34077` | Filesystem / Lumina build | Jindo/JindoDLS/Lumina POM 仓库从 `oss-cn-shanghai` 切到 `oss-accelerate`。 | 直接 1.4 build-stability 候选。 |
| 27 | `319067fe0` | BLOB file format | `BlobType.isBlobFileField` 支持 MAP value BLOB；新增 `BlobMapPlaceholder`、MapBlobElementSerializer、Java/Python Map-Blob 读写、index validation、key serializer 和大量 BLOB/Map 测试。 | 大功能栈；非直接 1.4 bugfix。 |
| 28 | `260fcc757` | Spark write distribution | Format Table writer 实现 `RequiresDistributionAndOrdering`，复用 `PaimonWriteRequirement` 让 partition sink strategy 对 Format Table 生效。 | 功能增强，非直接。 |
| 29 | `39937ccc9` | Iceberg incremental | 删除 entry 记录当前 deleting snapshot；manifest compact 读取时保留 `snapshotId == currentSnapshotId` 的 deletion；新增 delete entry 和 compaction survive 测试。 | 直接 1.4 Iceberg correctness 候选。 |
| 30 | `f4ed84d88` | PyPaimon bitmap | `RoaringBitmap64.to_range_list` 改为 iterator 流式遍历，避免构造完整 `list(self._data)`；新增空/单值/多 range/32-bit 边界测试。 | 性能/内存候选。 |
| 31 | `b52f55f78` | REST Format Table filter | REST API 新增 `listPartitionsByFilter` endpoint；Catalog 接口新增按 predicate 分页默认 fallback；FormatTableScan 将分区 predicate 和 leading equality prefix 传给 manager 并本地复验。 | 依赖 master manager/request，1.4 非直接。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch --no-tags origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功。 |
| `git fetch --no-tags upstream refs/heads/master:refs/remotes/upstream/master` | 成功；`upstream/master` 从 `cd39bd70567e` 推进到 `b52f55f7801e`。 |
| `git fetch --no-tags upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/release-1.4` 保持 `05d1ba75a97c`。 |
| `git rev-parse upstream/master` / `git rev-parse upstream/release-1.4` | 确认 `master=b52f55f7801e16cdfd64b2fc935b3d3f14257af8`，`release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git rev-list --first-parent --reverse cd39bd70567e..upstream/master` | 列出 31 个 first-parent commit。 |
| `git log --first-parent --reverse --format=... cd39bd70567e..upstream/master` | 获取 31 个 commit 的日期、作者、标题。 |
| `git diff --stat cd39bd70567e..upstream/master` | 257 files changed, `20457 insertions(+), 1510 deletions(-)`。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <commit>` | 已对 31 个 commit 执行，combined patch 缓存在 `/private/tmp/paimon-daily-2026-07-22-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 已对 31 个 commit 执行，patch-only 输出同样缓存。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 本轮 31 个 commit 均为 `+`，未发现等价合入 1.4。 |
| `git patch-id --stable` | 已记录关键候选 patch-id，用于后续 backport/等价检查。 |
| `git cat-file -e upstream/release-1.4:<path>` / `git grep` / `git show upstream/release-1.4:<path>` | 已核对候选生产路径和旧实现，包括 Iceberg、DateTime、Codegen、SchemaChange、BranchManager、REST/URI 脱敏、PyPaimon index、ConflictDetection、Jindo/Lumina POM、RoaringBitmap、Format Table REST manager。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只新增日报和 state 游标更新。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `b52f55f7801e16cdfd64b2fc935b3d3f14257af8`
- `last_processed_commit_date`: `2026-07-22 11:38:02 +0800`
- `last_report_date`: `2026-07-22`
- `last_report_path`: `reports/paimon-master-daily/2026-07-22.md`

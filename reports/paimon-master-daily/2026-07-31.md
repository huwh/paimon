# Apache Paimon Master Daily Report - 2026-07-31

## Summary

范围：`27f4656bb033e8fdb9b9574559914fdcc09d5485..526b91f095a7683a3d5bc315550758b2520cb24f`，共 32 个 first-parent 提交，265 个文件变更，`+10821/-976`。本轮 `upstream/master` 前进到 `526b91f095`（2026-07-31 12:18:10 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

主要变化集中在 snapshot/base UUID commit 校验、PyPaimon row-id 并发 compaction 恢复、BLOB/BlobRef 读写、chain table deletion vectors、MAP storage layout 切换、global-index/vector 正确性、GS/hadoop-shaded filesystem 修复、release workflow/文档、以及 PyPaimon Ray join/metadata cache。按实际 patch、`git cherry` 和 release-1.4 代码路径判断，本轮最值得关注的 1.4 候选是 `4142b4292` single writer open 失败清理、`4926d60ab` empty scored global-index result 序列化、`2665f44c0` chain table DV read、`72a577669` GS config key 映射、`4e9991a6f` hadoop-shaded-3.4 CVE dependency pin、`4adce1d41` BLOB 未转义本地路径、`bebc564d5` SimpleCounter 线程安全、`a8931fe0a` Glue column comment 换行清理、`990d30b99` Hive multiset object inspector，以及 PyPaimon row-id 并发 compaction 恢复 `6464ef301`。`d113d293f`、`a1fcfe109`、`87e20cc5c`、`5b39ec21f`、`555749f92`、`93bcff41a`、`526b91f09` 等依赖 master-only 架构或 1.4 缺少同路径，不能按小 patch 直接回摘。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core commit / metadata compatibility | 4 | Snapshot 增加 UUID，catalog/REST commit 带 base snapshot UUID；single writer open 失败只清理本 writer 创建的资源；index manifest/commit message 版本提升以携带 global-index sourceMeta；SimpleCounter 改 AtomicLong。 | Snapshot UUID/sourceMeta 在 1.4 缺失；writer cleanup 和 SimpleCounter 是同路径候选。 |
| Global-index / vector / data evolution | 6 | empty scored result 序列化 V2；live-row filter 改成先 union row-id range 再扣 DV；materialize-deletion compaction 扫 latest index；DV vector-search read pin 到 plan snapshot；IVF-PQ batch reuse 加入后又被 revert。 | 1.4 有 V1 GlobalIndexResultSerializer 和 scored empty 使用场景；data-evolution vector/dropper/GlobalIndexLiveRowFilter 同路径不足。 |
| PyPaimon data evolution and read | 3 | row-id update 在并发 compaction 后可按当前文件边界重写；Ray 新增 range_join 并抽 join_common；PyArrow Dataset metadata 进程内缓存。 | 1.4 有 PyPaimon row-id 检查路径，row-id 恢复值得单独适配；range_join 是新增能力，metadata cache 是性能优化。 |
| BLOB / format | 2 | BlobRef 连续复制复用源流并强化 EOF/seek/close 失败语义；UriReaderFactory 对未转义本地 file path fallback 到 Path URI，同时 HTTP(S) 非法 URI 仍报错。 | 1.4 有 BLOB 和 `URI.create` 旧路径；未转义路径是直接候选，源流复用需要按 1.4 旧 writer 结构适配。 |
| Chain table / MAP layout | 2 | ChainSplit 序列化 deletion files，chain reader 暴露 DV delete 为 DELETE row；MAP default/shared-shredding 支持切换并兼容 selected-key fallback reads。 | 1.4 有 chain table 和 DV 基础但 ChainSplit 不携带 deletionFiles；MAP shared-shredding 1.4 缺少同路径。 |
| Hive / Spark / procedures | 5 | Glue column comments 去除 CR/LF；Hive multiset 映射为 `map<element,int>` inspector；新增 repair_earliest_snapshot Flink/Spark procedure；Spark Lambda 删除无效 try/catch；Flink IT compaction wait 加 timeout。 | Glue comment、multiset、SimpleCounter 等为同路径候选；procedure 是新增运维能力，Spark try/catch 行为影响低，IT-only 无生产影响。 |
| Filesystem / packaging / security | 3 | `paimon-gs` dependency scope/optional 对齐；GS `gs.*` 配置映射为 Hadoop `fs.gs.*`；hadoop-shaded-3.4 pin commons-lang3 3.18.0 和 snappy-java。 | 1.4 同路径存在，GS 映射和 CVE pin 是直接候选；scope 调整属于 artifact packaging 候选。 |
| Release automation / docs / version | 5 | 新增 release Java/PyPaimon GitHub workflows 和 RM/verification docs；PyPI publish 改 repository secrets；文档说明只发布 tag-only RC；版本推进到 2.1-SNAPSHOT。 | release-1.4 当前无这些 release workflows；非生产 bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `4142b4292` | `SingleFileWriter` / `FormatTableSingleFileWriter` open 失败时不再调用 overridable `abort()`，且不会误删已经存在的目标文件；避免 orphan temp file、误删和 subclass 字段未初始化 NPE。 | `git cherry` 为 `+`，未等价合入。 | release-1.4 同类 catch 中仍在 `out != null` 后调用 `abort()`。 | 高；同路径小修，建议优先 backport。 |
| `4926d60ab` | GlobalIndexResultSerializer V2 显式记录 scored/unscored kind，empty scored result 不再反序列化成 unscored result，同时保留 V1 读取。 | `git cherry` 为 `+`。 | release-1.4 serializer 仍为 V1，`FullTextReadImpl` / `VectorReadImpl` 会创建 empty scored result。 | 高；影响向量/全文 topK 空结果语义，patch 较集中。 |
| `2665f44c0` | Chain table split 携带 deletion files，chain reader 将 DV 删除暴露为 DELETE row，并修正 lookup merge wrapper。 | `git cherry` 为 `+`。 | release-1.4 有 chain table 和 DV 基础，但 `ChainSplit` 不 override `deletionFiles()`，序列化也不保存 deletion files。 | 中高；影响 chain+DV 正确性，但回补规模较大。 |
| `72a577669` | GS catalog option `gs.*` 被映射成 Hadoop 期望的 `fs.gs.*`，避免原样 `gs.*` 配置无法生效。 | `git cherry` 为 `+`。 | release-1.4 `GSFileIO.configure` 直接把 `gs.*` 放入 Hadoop config。 | 中高；GCS 认证/项目配置可达，patch 小。 |
| `4e9991a6f` | hadoop-shaded-3.4 强制 commons-lang3 3.18.0 和当前 snappy-java，并同步 NOTICE，覆盖 CVE dependency 版本。 | `git cherry` 为 `+`。 | release-1.4 NOTICE 仍列 commons-lang3 3.12.0，pom 只 pin commons-beanutils。 | 高；安全/发行物质量候选。 |
| `4adce1d41` | BLOB file path 支持空格/Unicode 等未转义本地路径；HTTP(S) 非法 URI 不 fallback 到文件 reader。 | `git cherry` 为 `+`。 | release-1.4 `UriReaderFactory` 直接 `URI.create(input)`，BLOB writer/read path 已存在。 | 中高；BLOB 外部文件路径可达，patch 较小。 |
| `bebc564d5` | `SimpleCounter` 从 plain `long` 改为 `AtomicLong`，避免并发 metric update 丢计数。 | `git cherry` 为 `+`。 | release-1.4 `SimpleCounter` 仍是非线程安全 `long count`。 | 中；common metrics 路径，patch 小。 |
| `a8931fe0a` | Iceberg Hive metadata 同步 Glue column comment 前移除 `\n`/`\r`，再按 255 长度截断。 | `git cherry` 为 `+`。 | release-1.4 同 `IcebergHiveMetadataCommitter.normalizeColumnComment` 只处理长度。 | 中；Glue/Hive metadata 兼容候选。 |
| `990d30b99` | Hive ObjectInspector 支持 Paimon `MULTISET`，按 `map<element,int>` 暴露给 Hive 读取。 | `git cherry` 为 `+`。 | release-1.4 `PaimonObjectInspectorFactory` 无 `case MULTISET`。 | 中；含 multiset 表的 Hive read 会受影响。 |
| `6464ef301` | PyPaimon row-id update 在 concurrent compaction 改写文件边界后可自动按当前 snapshot 重写 stale update，并用大小上限保护。 | `git cherry` 为 `+`。 | release-1.4 PyPaimon 已有 row-id conflict check 和 `TableUpdateByRowId`，但无 rewriter/max-size option。 | 中；PyPaimon data-evolution 正确性候选，需按 1.4 Python API 适配。 |
| `5f3c5387d` | BLOB writer 连续复制 BlobRef 时复用同一源流，并保证短读 EOF、seek/close 失败不会被错误写 NULL 或泄漏源流。 | `git cherry` 为 `+`。 | release-1.4 `BlobFormatWriter` 每个 BlobRef 都 `blob.newInputStream()`，且旧结构无 master 的抽象 writer 分层。 | 中；性能/资源稳定性候选，但不是机械回摘。 |
| `191a50f7b` | `paimon-gs` 把 `paimon-common` 设 provided、`paimon-gs-impl` 设 optional runtime，和其他 FS wrapper 对齐。 | `git cherry` 为 `+`。 | release-1.4 `paimon-gs/pom.xml` 缺这两个 scope/optional 标记。 | 中；artifact dependency surface 候选。 |
| `190c47634` | 全局配置 `deletion-vectors.merge-on-read=true` 不再让非 DV 表 schema validation 失败，并只在 DV 表的 PK vector/fulltext 校验中约束。 | `git cherry` 为 `+`。 | release-1.4 未命中 `deletion-vectors.merge-on-read` 配置和对应 PK vector/fulltext 校验。 | 低/无直接影响；1.4 无同路径。 |
| `d113d293f` | Snapshot 增加 UUID，REST/catalog commit 传 base snapshot UUID 以检测 stale base。 | `git cherry` 为 `+`。 | release-1.4 `Snapshot` 无 UUID，`Catalog.commitSnapshot`/REST request 无 baseSnapshotUuid。 | 低/需设计；不是小 patch。 |
| `a1fcfe109` | `CachingFileIO` 反序列化后恢复本地 cache，并共享 JVM-local limit/namespace。 | `git cherry` 为 `+`。 | release-1.4 没有 `paimon-common/fs/cache/CachingFileIO`。 | 无直接 1.4 影响。 |
| `87e20cc5c` | global-index live-row filter 先 union 所有 row-id range 再扣 DV，避免重叠 partial-column 文件把已删行重加回来。 | `git cherry` 为 `+`。 | release-1.4 未命中 `GlobalIndexLiveRowFilter` 同路径；仍使用 lumina/旧 vector/fulltext read。 | 低/需另行重现。 |
| `93bcff41a` | full-text native reader 初始化/close 失败时关闭所有资源并保留 suppressed failure。 | `git cherry` 为 `+`。 | release-1.4 无 `paimon-full-text` module / `NativeFullTextGlobalIndexReader`。 | 无直接 1.4 影响。 |
| `5b39ec21f` / `555749f92` | data-evolution global-index dropper 扫 latest；DV vector-search read pin 到 plan snapshot，避免并发/快照不一致。 | `git cherry` 均为 `+`。 | release-1.4 缺少 `DataEvolutionCompactGlobalIndexDropper`、`DataEvolutionVectorScan` 等同路径。 | 低/架构不一致。 |
| `526b91f09` | global-index sourceMeta 进入 manifest/commit message 新版本，同时兼容旧 V11 commit message。 | `git cherry` 为 `+`。 | release-1.4 `GlobalIndexMeta` 没有 sourceMeta，`IndexManifestEntrySerializer` 仍 version 1。 | 低/无当前路径；除非先回补 sourceMeta。 |
| `96bbf065d` / `ee1515fe5` | Spark Lambda 删除无效 try/catch；Flink IT compaction wait 加 timeout。 | `git cherry` 均为 `+`。 | 前者 release-1.4 同旧 try/catch 但行为基本等价；后者测试-only。 | 低/无生产影响。 |
| `bbe084067` / `0dd0bcc31` / `b6bc5910b` / release 系列 | MAP layout switching、Ray range_join、PyArrow metadata cache、release workflow/docs/version。 | `git cherry` 均为 `+`。 | 1.4 缺 MAP shared-shredding同路径；range_join 是新增能力；metadata cache 是性能优化；release workflow 非生产路径。 | 低或按需求评估。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `190c47634` | Core schema validation | 删除 DV disabled 时对 `deletion-vectors.merge-on-read` 的硬失败；PK vector/fulltext 校验只在 DV enabled 时禁止 merge-on-read。 | 1.4 无该 option/校验路径，低影响。 |
| 2 | `d113d293f` | Core / REST / Python commit | `Snapshot` 增 `uuid`；Java/Python `SnapshotCommit` 和 REST `CommitTableRequest` 传 `baseSnapshotUuid`；FileStoreCommit 以 latest snapshot UUID 作为 base。 | 1.4 无 UUID/baseSnapshotUuid，同路径低。 |
| 3 | `6464ef301` | PyPaimon data evolution | 新增 `data-evolution.row-id-conflict-rewrite.max-size`，RowIdExistenceConflict 专用异常和 rewriter；commit loop 可重写 stale row-id update。 | 中；1.4 有 row-id conflict/update 路径，需适配。 |
| 4 | `a1fcfe109` | Common local cache | `CachingFileIO` 记录 cache config/namespace，反序列化后 lazy restore 并引用计数释放共享 cache；disk cache key 加入 size/mtime。 | 1.4 无 CachingFileIO，低。 |
| 5 | `46ec02c3e` | Vector | 临时升级 vector-index 0.4.0-SNAPSHOT 并透传 `ivf_pq.batch_table_reuse`。 | 被 #21 revert，最终无净影响。 |
| 6 | `4926d60ab` | Global index serde | Serializer V2 新增 result kind byte；V1 继续兼容但无法识别 empty scored。 | 高；1.4 同 V1 且有 empty scored 使用。 |
| 7 | `4142b4292` | Core writers | writer 构造时用 `opened` 标记，失败时只 close/delete 自己创建的资源，不调用 subclass `abort()`。 | 高；1.4 同旧失败路径。 |
| 8 | `a8931fe0a` | Hive / Iceberg metadata | Glue column comment 先替换 CR/LF 为空格，再执行 255 字符截断。 | 中；1.4 同路径候选。 |
| 9 | `96bbf065d` | Spark | `LambdaScalarFunction` 去掉包住字段赋值和 bind 的无效 try/catch。 | 低；行为基本等价。 |
| 10 | `e28adfa81` | Snapshot procedures | `SnapshotManager.repairEarliestSnapshot` 校验连续 snapshot suffix 并写 earliest hint；Flink/Spark 注册 `repair_earliest_snapshot`。 | 运维新增能力，非 1.4 bugfix。 |
| 11 | `87e20cc5c` | Global-index live rows | live-row filter 改成两阶段 union/subtract，Python 同步改为 in-place remove。 | 1.4 无同类 class，低/需另查旧路径。 |
| 12 | `5f3c5387d` | BLOB format | 新增 `ReusingBlobRefStreamProvider`；Blob writer 对已知长度 BlobRef 复用源流并强化 EOF/close/seek 失败处理。 | 中；1.4 有 BLOB 但 writer 结构不同。 |
| 13 | `2665f44c0` | Chain table DV | ChainSplit V2 序列化 deletion files；chain reader 用 `ExposeDeletionKeyValueReader` 把 DV 标记转 DELETE。 | 中高；1.4 chain+DV 路径缺失 deletionFiles 传递。 |
| 14 | `bbe084067` | MAP storage layout | 删除 schema manager 中禁止 MAP layout 变更的校验；read plan 可从 default-layout map materialize selected-key ROW。 | 1.4 无 shared-shredding 同路径，低。 |
| 15 | `ee1515fe5` | Test | `PrimaryKeyFileStoreTableITCase` 等待 level-0 compaction 加 deadline 和明确 TimeoutException。 | test-only。 |
| 16 | `bebc564d5` | Metrics | `SimpleCounter` 改 `AtomicLong`。 | 中；1.4 同路径候选。 |
| 17 | `191a50f7b` | GS packaging | `paimon-gs` wrapper 依赖 scope/optional 与其他 filesystem module 对齐。 | 中；1.4 同 pom 候选。 |
| 18 | `72a577669` | GS FileIO | `gs.*` 和 `fs.gs.*` 均写入 Hadoop `fs.gs.*` key；日志降为 debug 且不打印值。 | 中高；1.4 同旧逻辑。 |
| 19 | `4e9991a6f` | Hadoop shaded security | 在 hadoop-shaded-3.4 dependencyManagement pin commons-lang3 3.18.0 和 snappy-java，并更新 NOTICE。 | 高；1.4 同旧版本。 |
| 20 | `4adce1d41` | BLOB URI | `UriReaderFactory.parseUri` 在 `URI.create` 失败后对非 HTTP(S) 使用 `new Path(input).toUri()` fallback。 | 中高；1.4 同旧 `URI.create`。 |
| 21 | `8cd760d64` | Vector revert | 完整 revert `46ec02c3e`，恢复 vector-index 0.3.0 和旧 search params。 | 抵消 #5。 |
| 22 | `dab9cfe1e` | Release workflow/docs | 新增 Java/Python release workflows、RM 创建 release 和候选校验文档，并挂到 docs sidebar。 | 非生产路径。 |
| 23 | `93bcff41a` | Full-text native reader | 初始化失败时 close input 并保留 suppressed；close 时尝试 reader/input 全部关闭并传播首个异常。 | 1.4 无 paimon-full-text 同路径。 |
| 24 | `990d30b99` | Hive read | `PaimonObjectInspectorFactory` 为 `MULTISET` 返回 `PaimonMapObjectInspector(element, INT)`。 | 中；1.4 同路径缺 case。 |
| 25 | `5b39ec21f` | Data-evolution global index | materialize-deletion compaction dropper 扫 latest snapshot，删除并发构建且仍指向旧 row-id 的 global index。 | 1.4 缺同路径，低。 |
| 26 | `555749f92` | Vector search snapshot pin | Data-evolution vector scan plan 暴露 snapshot；read/live-row/scalar pre-filter 使用 plan snapshot。 | 1.4 缺同路径，低。 |
| 27 | `0dd0bcc31` | PyPaimon Ray | 新增 shuffle-free `range_join`，按 manifest/footer key range 分片；bucket_join 共享 join_common。 | 新增能力，1.4 低。 |
| 28 | `b6bc5910b` | PyPaimon read performance | `FormatPyArrowReader` 进程内缓存 PyArrow Dataset metadata，支持 max-size、LRU、并发 coalesce、fork 后重置。 | 性能优化，可按需求评估。 |
| 29 | `9d5cda075` | Release version | 全模块版本从 2.0-SNAPSHOT 推到 2.1-SNAPSHOT，并更新 outputTimestamp。 | release-only。 |
| 30 | `afd8d311d` | Release workflow | PyPaimon publish reusable workflow 改为接收 repository secrets，并更新创建 release 文档。 | release-only。 |
| 31 | `21f466d09` | Release docs | 文档明确 RC 只 push signed tag，不 push 同名 RC branch。 | docs-only。 |
| 32 | `526b91f09` | Index metadata compatibility | `GlobalIndexMeta` equality 纳入 sourceMeta；index manifest version 2；commit message version 12；新增 V11 index meta deserializer。 | 1.4 无 sourceMeta，低。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，开始和写报告前工作区均干净，并与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功，fork 自动化分支已刷新。 |
| `git fetch upstream master:refs/remotes/upstream/master release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/master=526b91f095a7683a3d5bc315550758b2520cb24f`，`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git log --first-parent --reverse 27f4656bb033..upstream/master` | 32 个 first-parent commit。 |
| `git diff --shortstat 27f4656bb033..upstream/master` | 265 files，`+10821/-976`。 |
| `git cherry -v upstream/release-1.4 upstream/master 27f4656bb033` | 32 个提交全部为 `+`，未发现 release-1.4 等价 patch。 |
| `git show --name-status --stat <commit>` | 已为 32 个提交逐个检查 name-status/stat。 |
| `git show --patch --stat <commit>` | 已为 32 个提交逐个检查实际 patch；本地 Git 同时带 `--name-status` 时会抑制 patch 输出，因此 name-status 与 patch 分开读取。 |
| release-1.4 targeted `git grep` / `git show upstream/release-1.4:<path>` | 已核验 writer cleanup、global-index serde、chain split/DV、GS/hadoop-shaded、BLOB URI、Hive metadata/multiset、SimpleCounter、PyPaimon row-id、snapshot UUID、cache、full-text、data-evolution vector、sourceMeta 等路径。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报、推进 state 并发布自动化分支。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `526b91f095a7683a3d5bc315550758b2520cb24f`
- `last_processed_commit_date`: `2026-07-31 12:18:10 +0800`
- `last_report_date`: `2026-07-31`
- `last_report_path`: `reports/paimon-master-daily/2026-07-31.md`

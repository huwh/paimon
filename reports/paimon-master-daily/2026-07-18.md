# Apache Paimon Master Daily Report - 2026-07-18

## Summary

范围：`a26016e99977e34affdca0a156b13c2736afffc8..7234e4c34e9a705af66a816f5c859afd55b8f7f4`，共 12 个 first-parent 提交。当前 Apache upstream `master` head 是 `7234e4c34e9a`（2026-07-18 13:35:37 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 89 个文件，`+6761/-593`。

本次主线包含 4 类：core/format correctness 修复、REST / MAP shared-shredding / Flink compaction 新能力、PyPaimon 兼容与聚合扩展，以及 global-index 相关诊断和 ESLib 构建隔离。明确 bugfix 包括 format table 正 limit 不再按 split 数裁剪、deletion-vector 表增量读不再跳过 level 0、CDC 复杂类型 malformed JSON 不再静默变空，以及 Parquet decimal filter pushdown 按文件 schema 选择 INT32/INT64/BINARY/FIXED_LEN_BYTE_ARRAY 表达式。

`git cherry` 逐提交对照 `upstream/release-1.4` 显示 12 个提交均为 `+`，没有 patch-id 等价提交已进入 `release-1.4`。明确的 1.4 直接候选是 `f005439e`（format table LIMIT split pruning）、`2414e915`（DV incremental scan level-0 skip）、`0b1d75fc`（TypeUtils / Flink CDC complex JSON fail-loudly）和 `8096a82a`（Parquet decimal pushdown）。`c2a2f043` 是 Python 3.7 / PyArrow 低版本兼容的可选候选；Daft、ESLib、MAP shared-shredding、REST catalog-managed partition、global-index 诊断和 `nested_update` 主要是 master 新能力或诊断增强。

所有 commit 都执行了 `git show --stat --name-status --patch --find-renames --format=fuller <commit>`，并补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 hunks。release-1.4 判断使用逐提交 `git cherry`、`git patch-id --stable`、`git cat-file`、`git ls-tree`、`git grep` 和 targeted `git show upstream/release-1.4:<path>` 对照生产路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core format / scan correctness | 2 | Format table positive LIMIT 保留所有未知行数 split；PK deletion-vector 表的 incremental startup 不再因为 `batch-scan-skip-level0` 丢历史 level-0 delta/changelog 文件。 | `f005439e`、`2414e915` 均为直接候选；1.4 存在同类代码路径且无等价 patch。 |
| REST catalog / format table partitions | 1 | REST API 新增 create/drop partitions request/response；RESTCatalog 接入 catalog-managed Format Table partition metadata，drop 只 unregister catalog metadata。 | 新能力；1.4 已有 catalog default create/drop partitions，但缺 REST create/drop partition API。 |
| MAP shared-shredding | 1 | 取消 append compaction/cluster rewrite 禁用，允许 PK 表和 rewrite；schema validation 改为禁止 postpone bucket、只限制 shared-shredding MAP value 内的 BLOB/VECTOR。 | master 功能栈；1.4 缺 MAP shared-shredding 相关路径。 |
| Common / Flink CDC | 1 | `TypeUtils` 对 ARRAY/MAP/ROW JSON 结构类型做显式校验，malformed MAP/ROW 不再返回 empty map/row；CDC corrupt-record policy 能接住复杂类型转换错误。 | `0b1d75fc` 是直接候选；1.4 仍有 silent empty MAP/ROW 行为。 |
| PyPaimon Daft | 1 | Native Daft reader 在 schema evolution 场景按 file schema/current schema 字段 id、名称、nullable 和类型兼容性判定，必要时 fallback 到 PyPaimon normalization；同时处理 Daft time32 表示。 | 不直接适用，1.4 缺 `paimon-python/pypaimon/daft/daft_datasource.py`。 |
| Global-index / ESLib | 2 | ESLib 升到独立 lucene9 artifact、加 JDK11 CI 和 int8_hnsw 参数支持；global-index scan 增加 metadata/lookup/coverage 耗时日志。 | ESLib 不在 1.4；诊断日志路径存在但不是 correctness bugfix。 |
| PyPaimon compatibility / aggregation | 2 | Python CI 覆盖 3.7，调整 PyArrow/fastavro/OSS/ORC 低版本兼容；新增 Python `nested_update` ARRAY<ROW> 聚合器、nested key/sequence/null strategy/count limit options。 | `c2a2f043` 可作为 Python 3.7 兼容候选；`nested_update` 在 1.4 无对应 aggregator 路径。 |
| Parquet format | 1 | Parquet reader 先读取 footer/file schema，再把 Paimon predicate 转成与实际 physical decimal primitive 匹配的 filter；覆盖 IN/NOT IN、fixed-len binary padding、scale/overflow 等测试。 | `8096a82a` 是直接候选；1.4 仍在打开 reader 前用 projected schema 转 filter。 |
| Flink compaction | 1 | 新增 `compaction.bucket-distribution-strategy=size-aware-batch`，bounded full compaction 可按 data file size 生成/分配 bucket split 并直接写提交。 | 新能力；默认仍是 linear，不是 1.4 bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `f005439e` | Format table positive LIMIT 不再用 `splits.subList(0, limit)` 裁剪未知行数 split，仅保留 zero-limit 快路径。 | patch-id `144f53ff`; `git cherry` 为 `+`。 | 1.4 `FormatTableScan` 仍在 `limit != null` 时按 split 数裁剪。 | 直接候选；header-only/empty split 排在前面时会跳过后续有效数据。 |
| `2414e915` | `batch-scan-skip-level0` 只在非 incremental startup 下跳过 level 0；incremental scans 必须读取历史 snapshot 的 delta/changelog level-0 文件。 | patch-id `90be27d5`; `git cherry` 为 `+`。 | 1.4 等价逻辑在 `DataTableBatchScan`，仍未检查 `options.startupMode() != INCREMENTAL`。 | 直接候选；DV 或 input changelog 表的增量读可能丢行。 |
| `0b1d75fc` | ARRAY/MAP/ROW JSON 类型不匹配或 malformed MAP/ROW 直接抛错，避免 CDC 复杂类型错误输入被写成 empty map/row。 | patch-id `fdc9869b`; `git cherry` 为 `+`。 | 1.4 `TypeUtils` 捕获 `JsonProcessingException` 后返回 `new GenericMap(Collections.emptyMap())` / `new GenericRow(0)`。 | 直接候选；影响 Flink CDC 复杂类型数据质量和 corrupt-record 策略。 |
| `52c0b51c` | Daft native parquet read 遇 schema evolution 不兼容文件 schema 时 fallback 到 PyPaimon normalization，避免按物理 schema 错读。 | patch-id `af841ae8`; `git cherry` 为 `+`。 | 1.4 缺 Daft datasource 文件。 | master Daft correctness；不直接适用 1.4。 |
| `c2a2f043` | Python 3.7 / 低 PyArrow 运行环境：ORC compression 参数按 PyArrow 版本判断；fastavro zstd patch 扩到 3.7；OSS/S3 path handling 按 PyArrow 16 能力分支。 | patch-id `24170adf`; `git cherry` 为 `+`。 | 1.4 存在 `local_file_io.py`、`pyarrow_file_io.py`、`fastavro_py36_compat.py` 和 `setup.py`，但无 3.7 CI 矩阵。 | 可选候选；若 1.4 需要 Python 3.7 支持，建议单独验证依赖上限。 |
| `8096a82a` | Parquet decimal filter pushdown 使用文件 footer schema 决定 decimal physical primitive，并对 literal scale/overflow/fixed-len padding 做校验。 | patch-id `96f18a94`; `git cherry` 为 `+`。 | 1.4 `ParquetFileFormat` 仍调用 `ParquetFilters.convert(filters)`，`ParquetReaderFactory` 在读取 file schema 前构造 filter。 | 直接候选；影响 decimal 字段 filter/dictionary pushdown，可能误过滤或禁用错误。 |
| `869e7253` | Global-index scan 增加 table/searchMode/metadata/lookup/coverage 和 per-index lookup 耗时日志。 | patch-id `8c9ca010`; `git cherry` 为 `+`。 | 1.4 有 `GlobalIndexScanner` / `DataEvolutionBatchScan`，但缺这些日志。 | 诊断增强；非 correctness bugfix。 |
| `d4a493d7` | Bounded full compaction 新增 size-aware bucket distribution，降低大 bucket 长尾。 | patch-id `a3936dcb`; `git cherry` 为 `+`。 | 1.4 有 Flink compaction source/assigner，但无该 option；默认行为不变。 | 新能力 / 性能选项；非直接 bugfix。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `f005439e` | Core format table | `FormatTableScan` 删除 positive limit 下的 `splits.subList(0, limit)`，新增 header-only CSV split 在前、数据 split 在后时 `withLimit(1)` 仍读到有效行的测试。 | 直接候选。 |
| 2 | `56e0fce4` | REST catalog | 新增 `CreatePartitionsRequest/Response`、`DropPartitionsRequest/Response`、REST path 和 server/client tests；managed format table list/drop 不再 fallback 到 filesystem discovery。 | 新能力，非 bugfix。 |
| 3 | `f8426582` | MAP shared-shredding | 移除 compaction/cluster rewrite 对 shared-shredding 的硬禁用；validation 从“只支持 append-only / bucket=-1”改为禁止 postpone bucket，并允许非 shared MAP 字段里的 BLOB/VECTOR。 | master 功能，1.4 不适用。 |
| 4 | `2414e915` | Core scan | `AbstractBatchTableScan` 的 level-0 skip 增加 `startupMode() != INCREMENTAL` 条件；新增 DV 表 incremental between 和 input changelog 测试。 | 直接候选，1.4 等价类是 `DataTableBatchScan`。 |
| 5 | `0b1d75fc` | Common / Flink CDC | `TypeUtils.castFromString` 对 JSON node 类型显式校验，并移除 malformed MAP/ROW 的 silent empty fallback；CDC test 覆盖 corrupt-record policy。 | 直接候选。 |
| 6 | `52c0b51c` | PyPaimon Daft | 增加 native read schema compatibility check、time32 promotion、fallback explain reason 和 Daft schema-evolution integration tests。 | 不直接适用，1.4 缺 Daft。 |
| 7 | `d82e90d6` | ESLib | 新增 `utcase-eslib.yml`，ESLib 依赖改到 `eslib-*-lucene9` 1.0.7；`int8_hnsw` 共享 HNSW 参数校验。 | 1.4 缺 `paimon-eslib`。 |
| 8 | `c2a2f043` | PyPaimon compatibility | CI 增加 Python 3.7；dev requirements 对 3.7 设 PyArrow 7-12；ORC/OSS/S3/fastavro 兼容逻辑改为按库版本判断。 | 可选 1.4 Python 3.7 候选。 |
| 9 | `869e7253` | Global-index diagnostics | `UnionGlobalIndexReader` 支持 duration callback；`DataEvolutionBatchScan` / `GlobalIndexScanner` 输出 search mode、lookup 和 coverage 耗时。 | 诊断增强，非 bugfix。 |
| 10 | `8096a82a` | Parquet | `ParquetReaderFactory` 先读 footer，`ParquetFilters.convert` 接收 file schema/case sensitivity；decimal literal 转换匹配真实 physical primitive。 | 直接候选。 |
| 11 | `d4a493d7` | Flink compaction | 新增 size-aware batch distribution option；source 生成带 total file size 的 compact bucket splits，sink 直接写 committable，测试覆盖 full compaction 分配。 | 新性能选项，非 bugfix。 |
| 12 | `7234e4c3` | PyPaimon aggregation | 新增 `nested_update` 聚合器和 nested key/null/sequence/count options，支持 ARRAY<ROW> nested record merge。 | 新功能，1.4 无对应路径。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master` 前进到 `7234e4c34e9a`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse a26016e99977e34affdca0a156b13c2736afffc8..upstream/master` | 12 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat a26016e99977e34affdca0a156b13c2736afffc8..upstream/master` | 89 files changed, 6761 insertions(+), 593 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <12 commits>` | 对 12 个 commit 均已执行，输出缓存到 `/private/tmp/paimon-daily-2026-07-18-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <12 commits>` | 对 12 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry upstream/release-1.4 <commit> <commit>^` | 12 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git patch-id --stable` | 对 12 个 commit 均记录 stable patch-id，用于 release-impact 对照。 |
| release-1.4 targeted checks | `git cat-file` / `git ls-tree` / `git grep` / `git show upstream/release-1.4:<path>` 确认 format table、DV incremental scan、TypeUtils/CDC、Parquet decimal 候选路径存在且缺对应修复；确认 Daft、ESLib、MAP shared-shredding、Python aggregator 路径缺失。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `7234e4c34e9a705af66a816f5c859afd55b8f7f4`
- `last_processed_commit_date`: `2026-07-18 13:35:37 +0800`
- `last_report_date`: `2026-07-18`
- `last_report_path`: `reports/paimon-master-daily/2026-07-18.md`

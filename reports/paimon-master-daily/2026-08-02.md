# Apache Paimon Master Daily Report - 2026-08-02

## Summary

范围：`5a82d2a6cf2a91bb998e2eae51e2823da96adf71..582d2d1f15d5048ce51735cdfa16a0cec87ee84d`，共 10 个 first-parent 提交，22 个文件变更，`+350/-54`。本轮 `upstream/master` 前进到 `582d2d1f15d5`（2026-08-02 10:37:37 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

主要变化集中在 PyPaimon predicate/simple-stats 正确性、PyPaimon 打包与 RAT 校验、release source/archive/workflow 维护、docs 依赖安全升级，以及一个 core schema 校验异常分类修正。所有提交相对 `upstream/release-1.4` 的 `git cherry` 结果均为 `+`，未发现等价补丁已合入。

按实际 patch、cherry 状态和 release-1.4 代码路径判断，本轮最值得 1.4 关注的是 `f0d15accad`：PyPaimon 1.4 仍直接按 predicate index 读取 `null_counts`、`min_values`、`max_values`，并在 stats evolution 中把缺失 null-count 当成 `0`，可能对 incomplete stats 产生错误裁剪或索引异常。其次是 `3c38b63759` 的 user-option 异常分类，以及 `8eb4e33c89`、`4dd745b48a`、`643fdfba79`、`e20a7d7295` 中与 1.4 release/package 流程同路径相关的修复。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core schema validation | 1 | `SchemaValidation.validateBucket` 将 dynamic bucket 下配置 `bucket-key` 的异常从 `RuntimeException` 改为 `IllegalArgumentException`，并新增覆盖该错误分类的单测。 | 1.4 同路径仍抛 `RuntimeException`；属于用户配置错误分类修复，非数据正确性缺陷。 |
| PyPaimon predicate / stats correctness | 1 | `Predicate.test_by_simple_stats` 对缺失 index、缺失/短 `null_counts`、短 min/max rows 改为保守返回；`SimpleStats` 允许 unknown null counts；`ProjectedRow.__len__` 改为返回 projection 长度。 | 1.4 同路径仍是旧逻辑，较可能受影响；这是本轮最明确的 1.4 runtime backport 候选。 |
| PyPaimon packaging / licensing / benchmark | 4 | sdist/wheel 禁止打包 tests 和 acceptance；benchmark 目录补 package marker 并修 lint；RAT 脚本改用数组命令、临时解包目录、trap cleanup 和生成文件 excludes。 | 1.4 `setup.py` 仍 `find_packages(include=["pypaimon*"])`，`MANIFEST.in` 仍递归包含全部 `pypaimon *.py`，tests/acceptance 存在；RAT 脚本也是旧实现。 |
| Release workflow / source archive maintenance | 3 | Java release 对 Javadoc JAR 改为 produced-where-available；source release 设置 `COPYFILE_DISABLE=1` 并用 Python tarfile 拒绝 unsafe path/macOS metadata；workflow action 版本升级，release branch/RC tag 文档更清晰；Spark common 跳过不可解析 shim 的 Javadoc。 | 1.4 release workflow/docs 不是同一套路径，但 `tools/releasing/create_source_release.sh` 和 `paimon-spark-common/pom.xml` 同路径存在，部分修复可按 1.4 形态回补。 |
| Docs dependency | 1 | `docs/yarn.lock` 将 `brace-expansion` 从 `1.1.16` 升到 `1.1.18`。 | docs-only；未看到 release-1.4 生产路径影响。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact / action |
| --- | --- | --- | --- | --- |
| `f0d15accad` | 修复 PyPaimon predicate pruning 遇到 incomplete simple stats、缺失 null-count 或 projected row 长度时的保守性；不能证明文件不匹配时返回 `True`。 | stable patch-id `a0cb7fc50021`；`git cherry` 为 `+`。 | 1.4 `predicate.py` 仍直接执行 `stat.null_counts[self.index]`、`stat.min_values.get_field(self.index)`、`stat.max_values.get_field(self.index)`；`simple_stats_evolution.py` 对缺失数组返回 `0`，对 schema-evolution missing field 使用 row count；`ProjectedRow.__len__` 仍返回底层 row 长度。 | 中高；可能导致 PyPaimon 读取 evolved/incomplete stats 时错误裁剪或抛索引异常。建议作为 1.4 runtime correctness backport 候选，需跑 PyPaimon predicate/simple_stats_evolutions 测试。 |
| `3c38b63759` | 将 invalid `bucket-key` + dynamic bucket 的异常分类改为 `IllegalArgumentException`。 | stable patch-id `2193ec7884f0`；`git cherry` 为 `+`。 | 1.4 `SchemaValidation.validateBucket` 同消息仍抛 `RuntimeException`。 | 低到中；影响 API/SQL 层对用户配置错误的分类和断言，不影响写入数据语义。可作为窄 backport。 |
| `8eb4e33c89` | PyPaimon package 排除 `pypaimon/tests` 和 `pypaimon/acceptance`，并在 workflow 中校验 sdist/wheel 不含测试内容。 | stable patch-id `82bad1840084`；`git cherry` 为 `+`。 | 1.4 `setup.py` 包含全部 `pypaimon*`，`MANIFEST.in` 递归包含全部 `pypaimon *.py`，且 1.4 分支存在大量 tests/acceptance 文件。 | 中；影响 1.4 PyPaimon 发布包内容大小和合规边界。若 1.4 还会发 PyPaimon 包，建议回补 exclude。 |
| `4dd745b48a` | PyPaimon RAT 校验改为可复现：source package 解到临时目录，命令数组避免 word-splitting，生成文件 excludes 更完整，统一 cleanup。 | stable patch-id `8a84b29cd6ee`；`git cherry` 为 `+`。 | 1.4 `check-licensing.sh` 仍在当前目录解包、字符串拼接 `RUN_RAT`，并缺少新增 generated metadata excludes。 | 中；release 验证可靠性修复，非 runtime。建议随 PyPaimon release hardening 一并评估。 |
| `643fdfba79` | source archive 创建和验证拒绝 unsafe path、`__MACOSX`、AppleDouble `._*`，并在 macOS 设置 `COPYFILE_DISABLE=1`。 | stable patch-id `88188717bc9a`；`git cherry` 为 `+`。 | 1.4 `tools/releasing/create_source_release.sh` 直接 `tar czf ... --no-xattrs`，没有 Python tarfile 检查，也没有 `COPYFILE_DISABLE`。 | 中；影响 1.4 source release 合规性，尤其是 macOS RM 环境。建议回补 archive metadata guard。 |
| `a672ba4f00` / `e20a7d7295` | Java release 不再要求每个 jar module 都有 Javadoc JAR；Spark common POM 设置 `maven.javadoc.skip=true`，workflow install 阶段跳过 Javadocs，release docs 澄清 RC tag 与 branch head。 | stable patch-id `31268368bc30` / `1e06b25cccf6`；`git cherry` 均为 `+`。 | 1.4 缺少 master 的 release workflow/docs 路径，但 `paimon-spark/paimon-spark-common/pom.xml` 同路径存在且没有 `maven.javadoc.skip`。 | 低到中；主要是 release packaging/maintenance 风险。若 1.4 release 构建遇到 Spark common Javadoc 解析 generated shim 失败，可按 1.4 构建链路回补 POM property。 |
| `ffa8603143` / `43ca75d468` / `582d2d1f15` | docs dependency security bump；benchmark package marker 和 lint。 | stable patch-id `a02fa7208fbd` / `7e8a8cce96e4` / `29e92699e139`；`git cherry` 均为 `+`。 | 1.4 未见同一 benchmark package 路径；docs dependency 不在生产路径。 | 低；不建议作为 1.4 backport 优先项。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `3c38b63759` | Core schema validation | `SchemaValidation.validateBucket` 中 `bucket == -1` 且显式配置 `bucket-key` 时，异常从 `RuntimeException` 改成 `IllegalArgumentException`；新增 `testBucketKeyWithDynamicBucket` 断言精确消息。 | 1.4 同路径仍抛 `RuntimeException`；可窄回补。 |
| 2 | `f0d15accad` | PyPaimon predicate / stats | `Predicate.test_by_simple_stats` 对 invalid index、缺失 `null_counts`、短 min/max rows 改保守；`SimpleStats.null_counts` 改为可选；stats evolution 用 `None` 表达 unknown；`ProjectedRow.__len__` 返回 projection 长度；新增 incomplete stats、unknown null-count、projected row 单测。 | 1.4 同路径旧逻辑存在；本轮最高优先级 runtime backport 候选。 |
| 3 | `a672ba4f00` | Release Java packaging / docs | `release-java.yml` 不再强制每个 jar 都产出 Javadoc JAR，而是缺失时记录；release docs 改为 source/Javadoc produced-where-available，并将 RC checkout 从 master 改成 release branch。 | 1.4 无同 release workflow/docs；概念上影响 release packaging，不是 runtime。 |
| 4 | `8eb4e33c89` | PyPaimon package contents | `release-python.yml` 校验 sdist/wheel 不含 `pypaimon/tests`、`pypaimon/acceptance`；`MANIFEST.in` 和 `setup.py` 同步排除 acceptance。 | 1.4 package 规则仍会包含 tests/acceptance；是 1.4 PyPaimon package 候选。 |
| 5 | `ffa8603143` | Docs dependency | `docs/yarn.lock` 仅升级 `brace-expansion@^1.1.7` 的版本、resolved URL 和 integrity。 | docs-only，未见 1.4 生产影响。 |
| 6 | `43ca75d468` | PyPaimon benchmark package | 新增 `paimon-python/pypaimon/benchmark/__init__.py`，内容只有 ASF license header。 | 1.4 未见同路径 benchmark package；无直接 backport 价值。 |
| 7 | `4dd745b48a` | PyPaimon RAT validation | `.rat-excludes` 增加 Python packaging metadata；`check-licensing.sh` 使用默认空 `SOURCE_PACKAGE`、trap cleanup、临时目录解包、数组式 `RUN_RAT`，并保留 RAT 结果到检查后再清理。 | 1.4 同脚本仍是旧实现；release validation 候选。 |
| 8 | `643fdfba79` | Source release archive safety | `create_source_release.sh` 设置 `COPYFILE_DISABLE=1`，打包后用 Python `tarfile` 拒绝 absolute/parent paths、`__MACOSX` 和 `._*`；release verification docs 增加同类检查。 | 1.4 同脚本缺少这些 guard；release source 合规候选。 |
| 9 | `e20a7d7295` | Release workflow maintenance / Spark POM | action pin 更新到 setup-maven v5.1、upload/download-artifact v6、setup-python v6；intermediate install 阶段跳过 Javadocs；docs 澄清 RC tag immutable、release branch 可继续前进；`paimon-spark-common` 跳过 Javadoc。 | 1.4 workflow/docs 不同，但 Spark common POM 同路径缺 property；仅在 1.4 release Javadoc 链路复现时回补。 |
| 10 | `582d2d1f15` | PyPaimon benchmark lint | 删除 `benchmark/__init__.py` 末尾空行，修 lint。 | 无 1.4 直接影响。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，开始时工作区干净，并与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch upstream master:refs/remotes/upstream/master release-1.4:refs/remotes/upstream/release-1.4` | 普通 HTTPS fetch 先因 SSL/recv timeout 失败；提权重试成功更新 `upstream/master`，`release-1.4` 无更新输出。 |
| `git rev-parse upstream/master` / `git rev-parse upstream/release-1.4` | `upstream/master=582d2d1f15d5048ce51735cdfa16a0cec87ee84d`；`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git log --first-parent --reverse 5a82d2a6cf..upstream/master` | 10 个 first-parent commit。 |
| `git diff --shortstat 5a82d2a6cf..upstream/master` | 22 files，`+350/-54`。 |
| `git cherry -v upstream/release-1.4 upstream/master 5a82d2a6cf...` | 10 个提交全部为 `+`，未发现 release-1.4 等价 patch。 |
| `git patch-id --stable` | 已为 10 个提交生成 stable patch-id，并在 bugfix impact 表记录相关值。 |
| `git show --stat --name-status --patch --find-renames <commit>` | 已覆盖 10 个提交；本地 Git 同时带 `--name-status` 时只输出 name-status，因此另用 patch-only 输出读取 hunks。 |
| `git show --stat --patch <commit>` | 已逐个检查 10 个提交的实际 patch hunks；`e20a7d7295` 因 partial-clone 缺 blob，先通过 SSH fetch commit 并逐个 `git cat-file -s` 补齐缺失 blob 后完成。 |
| release-1.4 targeted `git ls-tree` / `git grep` / `git show upstream/release-1.4:<path>` | 已核验 core/PyPaimon/release script/POM 同路径现状，以及 workflow/docs/benchmark 路径差异。 |
| `git diff --check -- reports/paimon-master-daily/2026-08-02.md reports/paimon-master-daily/state.json` | 通过，无 whitespace error。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报、推进 state 并发布自动化分支。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `582d2d1f15d5048ce51735cdfa16a0cec87ee84d`
- `last_processed_commit_date`: `2026-08-02 10:37:37 +0800`
- `last_report_date`: `2026-08-02`
- `last_report_path`: `reports/paimon-master-daily/2026-08-02.md`

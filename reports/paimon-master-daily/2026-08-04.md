# Apache Paimon Master Daily Report - 2026-08-04

## Summary

范围：`64a4ceb7a9832c49a94058b7bf5ac667af99f15f..4b275a4ef1a6aae03d40f9638e293325f657181b`，共 30 个 first-parent 提交，121 个文件变更，`+5666/-1947`。本轮 `upstream/master` 前进到 `4b275a4ef1a`（2026-08-04 11:38:29 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

本轮主要是 runtime correctness 和功能扩展混合：Parquet reader/predicate pushdown 修复了 schema widening、case-insensitive column spelling、row-index lazy consumption 和 lossy narrowing；core/data-evolution 修复了 chain table DV 行选择、materialize-DV compaction 与 merge-into 的 row-id 冲突检测，并新增 postpone fixed-bucket write builder；filesystem 覆盖 S3 SSE-C multipart、OBS loader/recursive listing、Jindo file status；Spark 修复 statistics 估算和 compact partition-entry 扫描；PyPaimon 修复 REST config merge，并扩展 native Rust scan planning。

`git cherry -v upstream/release-1.4 upstream/master 64a4ceb7a9` 对 30 个提交均为 `+`，未发现等价补丁已进入 `release-1.4`。按 patch-id、生产文件和 1.4 代码路径看，优先关注 `b59f966438`、`5722fbc85f`、`5aae9ddb63`、`35b8369a17`、`a777c1b2b4`、`b93b577753`、`99559570fe`、`a2b2338cc6`、`09a0258c7f`、`b35e19f38d` 以及 OBS/Jindo 相关修复；Vortex、MAP BLOB key、PyPaimon native planner、Flink row-level DELETE 和 sorted-index stream 属于 master-only 或较大功能路径，不建议作为直接 1.4 bugfix cherry-pick。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core write / commit / data evolution | 7 | 新增 `PostponeFixedBucketWriteBuilder`；strict-mode 区分数据文件与 index-only compact；merge-reader 同 sequence 时用 `isAdd` 打破平局；materialize-DV compaction 增加 row-id range conflict checker；补 privilege checks；修正 index manifest serializer 兼容性。 | chain-table DV、materialize-DV compaction conflict、privilege checks 有 1.4 生产路径；strict-mode/index manifest 改动依赖 master 新结构，需 1.4-specific 评估。 |
| Flink connector / data evolution / global index | 5 | Lookup join IT 覆盖迁移为直接单元测试；SQL DELETE for data-evolution tables 复用 `DataEvolutionDeleteSink` 并接入 row-level modification scan；JM-side coordinator restore 改为 recommit 后继续运行；sorted-index builder 暴露 committable stream。 | SQL DELETE、sorted-index stream 为功能扩展；`CommittingWriteOperatorCoordinator` 在 1.4 不存在，不能直接 cherry-pick coordinator recovery patch。 |
| Format readers / predicate pushdown / BLOB / Vortex | 6 | Parquet widening reader、case-insensitive pushdown、lossy predicate guard、lazy row-index consumption；ORC `TIME` predicate pushdown；MAP BLOB key 支持 boolean/decimal/date；Vortex ordered scan。 | Parquet 和 ORC 路径在 1.4 存在；MAP BLOB serializer 与 Vortex module 在 1.4 缺失，属于 master-only。 |
| Filesystems and packaging | 6 | S3 multipart upload 通过 S3A factory 保留 SSE-C part 参数；OSS NOTICE 跟随 SDK 升级；OBS 实现 `listFilesIterative` 并取消 access/secret 必填；docs 增加 `dls://`；Jindo `FileStatus` 返回 access time/owner。 | S3/OBS/Jindo 同名生产文件在 1.4 存在，是窄 backport 候选；OSS NOTICE 只在同步 OSS SDK 升级时需要。 |
| Spark | 3 | Postpone fixed-bucket write 接入新 builder；`ANALYZE TABLE` 的 `mergedRecordSize` 避免先 cast 导致 0；compact 在未配置 `partition_idle_time` 时跳过 partition entries 扫描。 | `mergedRecordSize` 和 compact partition scan 都直接影响 1.4 Spark 生产路径。 |
| Python / PyPaimon | 3 | REST `ConfigResponse` 解析 `overrides` 且按 defaults < client < overrides 合并；MAP BLOB key serializer 扩类型；native Rust planning 推下 filter/limit/projection/time-travel 并恢复 partition path。 | `ConfigResponse` 在 1.4 存在且旧 merge 逻辑相反；MAP BLOB/native planner 路径在 1.4 缺失或不完整。 |
| Docs / release process | 5 | 修复 procedure 参数名，新增 DLS 文件系统文档，明确 ASF release signing key 必须 RSA，并修正 GPG subkey fingerprint 命令。 | 文档可按需 backport；不属于 runtime bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact / action |
| --- | --- | --- | --- | --- |
| `b59f966438` | Parquet 文件物理 schema 与声明 schema 不一致时，reader 支持 `INT32 -> BIGINT`、unsigned int32、`FLOAT -> DOUBLE`，predicate pushdown 改用文件物理类型和真实列名，避免读失败或 case-insensitive pushdown 误裁剪。 | patch-id `e84e76e26d1c`；`git cherry` 为 `+`。 | 1.4 有 `FormatReadBuilder`、`ParquetVectorUpdaterFactory`、`ParquetFilters`；旧 `BigIntType`/`DoubleType` updater 直接使用 `LongUpdater`/`DoubleUpdater`，旧 predicate literal 不看文件物理列类型。 | 高；Format Table / 外部 Parquet / schema widening 场景可能读失败或静默丢行，建议作为 1.4 backport 候选。 |
| `5722fbc85f` | 在 Parquet predicate pushdown 中拒绝 lossy narrowing：`DOUBLE -> FLOAT`、`INT64/INT32 -> TINYINT/SMALLINT` 等读后会 round/wrap 的场景不再提前按物理值裁剪 row group。 | patch-id `030af9ca15b3`；`git cherry` 为 `+`。 | 1.4 同 `ParquetFilters` 路径存在；没有 `validateNarrowIntegerPushdown`，`acceptableTypes` 仍允许会改变 predicate 语义的物理类型组合。 | 高；与 `b59f966438` 同一 Parquet pushdown 正确性链路，建议一起评估。 |
| `5aae9ddb63` | `RowIndexGenerator` 改成 lazy iterator，跨 batch 或未完全消费 batch 时跳过 pending row indexes，避免 Parquet predicate/filter 后 DELETE/UPDATE 使用错误 row id。 | patch-id `f6acba8121b4`；`git cherry` 为 `+`。 | 1.4 `RowIndexGenerator.populateRowIndex` 仍直接把全局 iterator 交给 `ColumnarBatch.resetPositions`，没有 pending skip/remaining count。 | 高；row tracking、deletion vector、row-level update/delete 相关路径可能错删/错改，建议 backport 并迁移单元测试。 |
| `35b8369a17` | sort-merge reader 在 sequence 相等时用 `isAdd` 打破平局，避免 chain table + DV 读取时 delete/add 顺序不稳定导致静默丢行。 | patch-id `6452d23a5c15`；`git cherry` 为 `+`。 | 1.4 `SortMergeReaderWithLoserTree` 和 `SortMergeReaderWithMinHeap` 同名路径仍只比较 key / user sequence / sequence，不比较 `isAdd`。 | 高；1.4 有 chain table 和 deletion-vector 相关路径，建议窄 backport。 |
| `a777c1b2b4` | data-evolution materialize-DV compaction 提交时新增 row-id range conflict checker，防止并发 merge-into 更新被 compaction 的 delete file 恢复。 | patch-id `3a87cbd72ed9`；`git cherry` 为 `+`。 | 1.4 有 `DataEvolutionTableCompactSink`、Spark data-evolution compact 和 `rowIdCheckConflict`，但 compaction sink/ Spark commit 仍直接 `table.newCommit(...)`，没有 materialize-DV 专用冲突检查。 | 高但需适配；1.4 代码路径存在，建议做 1.4-specific backport，重点迁移 conflict checker 语义和 Flink/Spark compaction 触发点。 |
| `b93b577753` | PyPaimon REST `ConfigResponse` 解析 server `overrides`，merge 优先级改为 defaults < client options < overrides，并过滤 `None`。 | patch-id `590f8c5663d0`；`git cherry` 为 `+`。 | 1.4 `ConfigResponse` 只有 `defaults`，且 `merge` 是 client copy 后再 `defaults` 覆盖 client，无法处理 server-enforced `overrides`。 | 中高；REST catalog/server-enforced config（如 data-token）会受影响，建议 backport。 |
| `99559570fe` | S3 multipart 每个 part upload 通过 S3A request factory 创建 `UploadPartRequest`，保留 SSE-C customer key 参数。 | patch-id `ec0d22ec507f`；`git cherry` 为 `+`。 | 1.4 `S3MultiPartUpload.uploadPart` 手工 builder 只设置 bucket/key/uploadId/partNumber/contentLength，会丢 SSE-C 参数。 | 中高；使用 SSE-C 的 S3 写入 multipart part 会失败，建议窄 backport。 |
| `a2b2338cc6` | Spark `ANALYZE TABLE` 计算 `mergedRecordSize` 时先做浮点乘法再 cast，避免 `(mergedRecordCount / totalRecordCount).toLong` 截断成 0。 | patch-id `2300da1a65e3`；`git cherry` 为 `+`。 | 1.4 `PaimonAnalyzeTableColumnCommand` 仍是 `totalSize * (mergedRecordCount.toDouble / totalRecordCount).toLong`。 | 中；影响统计质量和优化器估算，建议 backport。 |
| `09a0258c7f` | Spark compact 在没有 `partition_idle_time` 时不再扫描 partition entries，也不再用 partition set 过滤所有 bucket entries。 | patch-id `eadadeca91db`；`git cherry` 为 `+`。 | 1.4 `CompactProcedure` 仍无条件调用 `getHistoryPartition(snapshotReader, partitionIdleTime)` 并扫描 `partitionEntries()`。 | 中；大分区表 compact 额外扫描明显，建议 backport。 |
| `b35e19f38d` | privilege wrapper 给 create/drop/alter partitions、consumer manager / clear/reset consumer 操作增加 insert 权限检查，避免绕过授权的写侧变更。 | patch-id `ffdb1e7b3991`；`git cherry` 为 `+`。 | 1.4 `PrivilegedCatalog` 只检查 `markDonePartitions`，未 override create/drop/alter partitions；`PrivilegedFileStoreTable` 没有 `consumerManager()` 权限门禁。 | 中；属于权限正确性/安全 hardening，建议评估 1.4 backport。 |
| `bc5723b163` | OBS Hadoop-compliant FileIO 实现 `listFilesIterative(path, recursive)`，使用 Hadoop `listFiles` 支持递归文件枚举。 | patch-id `31ebc42d5450`；`git cherry` 为 `+`。 | 1.4 OBS impl 同名文件存在，但只有 `listStatus`，没有 `listFilesIterative` override。 | 中；依赖递归枚举的清理/扫描路径会受影响，建议 backport。 |
| `74a493c6fe` | OBS loader 只要求 endpoint，不再把 access key / secret key 作为必填，使 ECS agency / security provider chain 可走 OBS plugin。 | patch-id `e31fba484142`；`git cherry` 为 `+`。 | 1.4 `OBSLoader.requiredOptions` 仍要求 endpoint、access key、secret key。 | 中；credential-less OBS 场景会被 loader gate 拦截，建议 backport。 |
| `6557de395d` | Jindo `HadoopFileStatus` 返回真实 access time 和 owner，而不是接口默认值。 | patch-id `8f6cf999c27`；`git cherry` 为 `+`。 | 1.4 Jindo `HadoopFileStatus` 只 override length/dir/path/modification time，没有 `getAccessTime` / `getOwner`。 | 低中；主要影响元数据可见性，窄 backport 风险低。 |
| `79d935bd07` | ORC predicate converter 将 `TIME_WITHOUT_TIME_ZONE` 映射为 `PredicateLeaf.Type.LONG`，恢复 TIME 类型 predicate pushdown。 | patch-id `3e4106a65c08`；`git cherry` 为 `+`。 | 1.4 `OrcPredicateFunctionVisitor.toOrcType` 没有 `TIME_WITHOUT_TIME_ZONE` 分支。 | 低中；性能/过滤下推覆盖提升，语义风险小，可按需求 backport。 |
| `2a5a256046` + `c01a675323` | strict-mode 对 index-only compact 的处理先放宽、后限定到 data-evolution，避免普通表错误忽略 index partition 冲突。 | patch-id `32b781258595` / `4ac61e8ee18d`；`git cherry` 均为 `+`。 | 1.4 `StrictModeChecker` 是旧实现，遇到其他用户 `COMPACT` / `OVERWRITE` 直接 fail，没有 master 的 index-manifest overlap 结构。 | 暂不建议直接 cherry-pick；若 1.4 复现 data-evolution index-only compact 误报，需要按 1.4 strict-mode 模型单独设计。 |
| `142f8239be` | index manifest serializer 恢复 version 1，并用 6-field `GlobalIndexMeta` 保持新写 manifest 可被 legacy schema reader 读取。 | patch-id `7ceaa1af0f18`；`git cherry` 为 `+`。 | 1.4 当前已经是 version 1 且 `GlobalIndexMeta` 只有 5 字段，没有 master 新增 `sourceMeta` 的 serializer version 2 路径。 | 当前 1.4 不直接受影响；只有在 backport `sourceMeta`/global-index manifest 扩展时才应同时带上兼容修复。 |
| `b193d0b24e` | JM-side Flink coordinator restore 不再故意 failover，直接 recommit restored committables 后继续运行。 | patch-id `fb71a93ecb8b`；`git cherry` 为 `+`。 | 1.4 没有 `CommittingWriteOperatorCoordinator`，仍是 `CommitterOperator` + `RestoreCommittableStateManager` / `RestoreAndFailCommittableStateManager`。 | 无直接 1.4 patch；如要处理同类问题，需要按 1.4 committer operator 恢复模型重做。 |
| `4a05a83396` | Vortex scan 强制 `ordered(true)`，保证 returned position 与 physical row order 对齐。 | patch-id `fe0b458f1f46`；`git cherry` 为 `+`。 | `release-1.4` 下没有 `paimon-vortex` module。 | master-only，无 1.4 backport。 |
| `c07f35f271` | MAP BLOB key 支持 boolean、decimal、date，并同步 Java/Python serializer 与文档。 | patch-id `554e6acf72e5`；`git cherry` 为 `+`。 | 1.4 没有 `MapBlobElementSerializer.java` 和 Python `map_blob_key_serializer.py`。 | master-only 功能扩展，无直接 1.4 backport。 |
| `4b275a4ef1` | PyPaimon native Rust scan planning 推下 predicate/limit/projection/time-travel，并恢复 partitioned table legacy path。 | patch-id `4c349402ab8f`；`git cherry` 为 `+`。 | 1.4 只有 `table_scan.py`，没有 `native_plan.py`。 | master-only / optional performance path，无直接 1.4 backport。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `2281336cc8` | Core / Flink / Spark postpone bucket | 新增 `PostponeFixedBucketWriteBuilder`，把 fixed-bucket runtime write、append conflict check、bucket-count commit message 写入收敛到 table builder；Flink/Spark 不再通过 `PostponeUtils.tableForFixBucketWrite/tableForCommit` 包装表。26 files，`+577/-184`。 | 功能/架构调整；不是窄 bugfix，1.4 不建议直接 cherry-pick。 |
| 2 | `8c53b2cd82` | Flink lookup tests | 大幅削减慢 SQL IT 参数化组合，把 blob-as-descriptor、dynamic partition loader 等覆盖迁移到直接单元测试。3 files，`+326/-982`。 | test-only，无 runtime backport。 |
| 3 | `2a5a256046` | Core strict mode | `StrictModeChecker` 拆分 data/index partition overlap；index-only compact 不再被当成数据冲突，DV-only overwrite 仍检查 index partition。2 files，`+91/-5`。 | 1.4 strict-mode 模型更旧，无直接 patch；需和 `c01a675323` 一起看。 |
| 4 | `b59f966438` | Parquet / format table | reader 增加 INT32/unsigned INT32/FLOAT widening updater；pushdown 以文件物理 schema 和真实列名表达 predicate；读失败错误带文件路径。5 files，`+859/-44`。 | 直接 1.4 runtime correctness 候选。 |
| 5 | `35b8369a17` | Core merge reader / chain table | loser-tree/min-heap merge reader 在 sequence 相等时增加 `isAdd` tie-break；新增 chain table DV read 回归测试。3 files，`+201/-3`。 | 直接 1.4 runtime correctness 候选。 |
| 6 | `5790aa6350` | Flink data evolution | 将 SQL DELETE 的 DV rewrite 拆为 `DataEvolutionDeleteSink`；新增 row-level modification scan context 和 Flink 1.16 API stub；source 为 data-evolution 表暴露 row-level scan ability。14 files，`+1098/-335`。 | 功能扩展，1.4 不建议作为 bugfix 直接 cherry-pick。 |
| 7 | `79d935bd07` | ORC predicate pushdown | `TIME_WITHOUT_TIME_ZONE` 映射为 ORC LONG predicate，并更新 converter 测试。2 files，`+2/-1`。 | 1.4 同路径存在，低风险优化/覆盖候选。 |
| 8 | `b93b577753` | PyPaimon REST config | `ConfigResponse` 解析 `overrides`；merge 改成 defaults < client < overrides，并过滤 `None`；测试覆盖 data-token override。2 files，`+73/-4`。 | 直接 1.4 PyPaimon REST 候选。 |
| 9 | `5aae9ddb63` | Parquet row tracking | `RowIndexGenerator` 实现 `LongIterator`，延迟跳过未消费 batch 的 row indexes；`ColumnarBatch` 暴露 `numRows()`；新增多批次/未消费回归测试。3 files，`+231/-2`。 | 直接 1.4 row-id correctness 候选。 |
| 10 | `8a0be2e008` | Docs procedures | 修正 Flink `compact_strategy` 和 Spark `replace_tag(tag => ...)` 示例参数名。2 files，`+2/-2`。 | docs only。 |
| 11 | `b193d0b24e` | Flink coordinator commit | `CommittingWriteOperatorCoordinator.recover` 改为 recommit restored committables 并继续运行，移除 `failoverAfterRecovery` 和故意异常路径。4 files，`+56/-136`。 | 1.4 缺少同类 coordinator，不能直接 cherry-pick。 |
| 12 | `99559570fe` | S3 filesystem | `S3MultiPartUpload.uploadPart` 改用 S3A request factory 构造 part request，保留 SSE-C 参数；新增离线 request test。2 files，`+124/-7`。 | 直接 1.4 S3 SSE-C 候选。 |
| 13 | `ac8fa2c90d` | OSS packaging | 更新 `paimon-oss-impl` bundled NOTICE 中 aliyun-sdk-oss 及传递依赖版本。1 file，`+6/-6`。 | 只在 1.4 同步 OSS SDK 升级时需要。 |
| 14 | `a2b2338cc6` | Spark statistics | `mergedRecordSize` 改为 `(totalSize.toDouble * mergedRecordCount / totalRecordCount).toLong`，避免比例先转 long 后为 0；新增 Spark analyze 测试。2 files，`+21/-1`。 | 直接 1.4 Spark statistics 候选。 |
| 15 | `a777c1b2b4` | Core / Flink / Spark data evolution | 新增 `RowIdConflictChecker` 抽象和 range checker；materialize-DV compaction 在 Flink/Spark 提交前检查被删除 row-id range 是否和历史 ADD 冲突。13 files，`+478/-21`。 | 1.4 有 data-evolution compaction 路径，建议 1.4-specific backport。 |
| 16 | `c07f35f271` | MAP BLOB / Python | Java/Python MAP BLOB key serializer 增加 boolean、decimal、date 支持，更新文件格式文档和跨语言测试。10 files，`+461/-45`。 | 1.4 缺少相关 BLOB serializer，master-only。 |
| 17 | `bc5723b163` | OBS filesystem | OBS Hadoop-compliant FileIO 增加 `listFilesIterative(path, recursive)`，用 Hadoop `listFiles` 返回 remote iterator。1 file，`+21/-0`。 | 直接 1.4 OBS 候选。 |
| 18 | `74a493c6fe` | OBS loader | `OBSLoader.requiredOptions` 只保留 `fs.obs.endpoint`，允许 security provider / agency credentials；新增 loader test。2 files，`+51/-2`。 | 直接 1.4 OBS 候选。 |
| 19 | `c9a7236300` | Docs filesystem | 在 supported filesystems 文档表中增加 `dls://`。1 file，`+1/-0`。 | docs only。 |
| 20 | `6557de395d` | Jindo filesystem | Jindo `HadoopFileStatus` override `getAccessTime()` 和 `getOwner()`，返回 Hadoop status 的真实值。1 file，`+10/-0`。 | 直接 1.4 Jindo 元数据候选。 |
| 21 | `09a0258c7f` | Spark compact | 未配置 `partition_idle_time` 时 `getPartitionsToCompact` 返回空集且不扫描 partition entries；bucket filter 只在按 idle-time 过滤时启用。2 files，`+50/-24`。 | 直接 1.4 Spark compact 性能候选。 |
| 22 | `b35e19f38d` | Privilege / procedures | `PrivilegedCatalog` 给 create/drop/alter partitions 加 insert 权限；consumer manager 走 `PrivilegedFileStoreTable.consumerManager()`，Flink/Spark clear/reset consumer 不再手造 manager 绕过权限。11 files，`+82/-45`。 | 直接 1.4 权限正确性候选。 |
| 23 | `c01a675323` | Core strict mode follow-up | `StrictModeChecker` 新增 `dataEvolutionEnabled`，index-only compact 放宽只限 data-evolution；普通表仍检查 index partition 冲突。3 files，`+26/-9`。 | 1.4 无 master overlap 结构；不建议单独 cherry-pick。 |
| 24 | `4a468bb370` | Flink sorted index | `SortedIndexTopoBuilder` 暴露 `buildIndexStream` 返回 `Optional<DataStream<Committable>>`，允许外部共用 committer。2 files，`+57/-9`。 | 功能 API 扩展，非 1.4 runtime bugfix。 |
| 25 | `142f8239be` | Index manifest compatibility | serializer 版本恢复为 1；读取/写入 6-field `GlobalIndexMeta` 时保持 legacy schema 可读；新增 legacy reader 测试。3 files，`+63/-30`。 | 1.4 当前已是 version 1/5-field，除非 backport `sourceMeta`，否则无直接影响。 |
| 26 | `5722fbc85f` | Parquet predicate correctness | 新增 `validateNarrowIntegerPushdown`，拒绝会 wrap/round 的 physical-to-declared predicate pushdown；测试覆盖 double->float、int64/int32->tiny/small 等场景。2 files，`+188/-3`。 | 直接 1.4 Parquet correctness 候选。 |
| 27 | `191370c05b` | Release docs | release 创建/验证文档要求 RSA 2048+ signing key，新 key 建议 RSA 4096，并要求检查实际 signing subkey。2 files，`+26/-5`。 | docs/process，可按需 backport。 |
| 28 | `64772a0a98` | Release docs | 修正 GPG subkey fingerprint 命令为 `gpg --with-subkey-fingerprint ...`。2 files，`+2/-3`。 | docs/process，可按需 backport。 |
| 29 | `4a05a83396` | Vortex format | Vortex reader scan options 增加 `ordered(true)`，保证多 scan partition 下 returned position 与物理行序一致；新增并发 worker 测试。2 files，`+75/-1`。 | `release-1.4` 无 `paimon-vortex`，无直接 backport。 |
| 30 | `4b275a4ef1` | PyPaimon native scan | native Rust planning 推下 filter/limit/projection/time-travel，支持 data-evolution/blob/partitioned table 并恢复 Python legacy partition path；失败仍由 TableScan fallback。4 files，`+423/-53`。 | 1.4 缺少 `native_plan.py`，master-only/optional performance path。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，并与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功刷新 fork 自动化分支。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master` 从 `64a4ceb7a9` 更新到 `4b275a4ef1`，`upstream/release-1.4` 保持 `05d1ba75a9`。 |
| `git rev-parse HEAD origin/codex/paimon-master-daily-report upstream/master upstream/release-1.4` | `HEAD=f7e78edd3015`；`origin/codex/paimon-master-daily-report=f7e78edd3015`；`upstream/master=4b275a4ef1a6`；`upstream/release-1.4=05d1ba75a97c`。 |
| `git log --first-parent --reverse 64a4ceb7a9..upstream/master` | 30 个 first-parent commits，已逐个列出并检查 patch。 |
| `git diff --shortstat 64a4ceb7a9..upstream/master` | 121 files changed，`+5666/-1947`。 |
| `git diff --name-status 64a4ceb7a9..upstream/master` | 已检查变更文件列表，覆盖 core、Flink、format、filesystem、Spark、Python、docs。 |
| `git show --stat --name-status --patch <commit>` | 对 30 个提交均已运行并缓存到 `/private/tmp/paimon-daily-2026-08-04-patches`；本环境 combined 输出只展示 stat/name-status 时，已追加 per-path `git show --patch --format=fuller` 检查 hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master 64a4ceb7a9` | 30 个提交均为 `+`，未发现 release-1.4 等价 patch。 |
| `git show <commit> --patch \| git patch-id --stable` | 已为 30 个提交计算 stable patch-id，并用于 release-1.4 impact 表。 |
| `git ls-tree` / `git show upstream/release-1.4:<path>` / `git grep` | 已针对 Parquet、merge reader、row-id conflict、REST config、S3/OBS/Jindo、Spark compact/analyze、privilege、Vortex、MAP BLOB、native plan 等路径确认 1.4 存在性和旧逻辑。 |
| Build / unit tests | 未运行 Maven、ScalaTest 或 Python 测试；本次是日报生成、state 推进和自动化分支发布。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `4b275a4ef1a6aae03d40f9638e293325f657181b`
- `last_processed_commit_date`: `2026-08-04 11:38:29 +0800`
- `last_report_date`: `2026-08-04`
- `last_report_path`: `reports/paimon-master-daily/2026-08-04.md`

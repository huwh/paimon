# Apache Paimon Master Daily Report - 2026-07-01

## Summary

范围：`d7c5d42c469d4d63a17114ac0883133635545aae..78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888`，共 34 个 first-parent 提交。当前 Apache upstream `master` head 是 `78ef07218ef9`（2026-07-01 13:38:19 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`。

本次主线集中在 PyPaimon global-index build 和写入路径扩展、data-evolution deletion vector、Blob/VectorType 通用行路径、manifest 提交扩展、format table partition pruning、Spark/Flink procedure 修复、local lookup 并发安全，以及 ORC 1.9.8 同步。

`release-1.4` 方面，34 个提交经 `git cherry -v upstream/release-1.4 upstream/master d7c5d42c469d4d63a17114ac0883133635545aae` 检查均为 `+`，未发现等价 patch 已 cherry-pick。逐项检查 production path 后，直接 bugfix 候选是 `6afa0dbfc`、`581f324b3`、`e886a3637`、`f26a23fdb`、`83ccce1ac`。PyPaimon overwrite retry 和 manifest rolling/compression 是条件候选，建议作为成组回补评估；`78ef07218` 对 `release-1.4` 不是直接候选，因为 release 分支没有 `blob-write-null-on-missing-file` 路径。

所有 commit 都先用 `git show --stat --name-status --patch <commit>` 查看文件状态；由于当前 Git 输出在带 `--name-status` 时不会显示 patch hunks，实际代码审查又补跑了相邻 first-parent tree 的 `git diff --patch <prev> <commit>`。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core scan / lookup / format | 5 | `eab772869` 放开 DV 表 TopN pushdown 并保护宽 DV split；`581f324b3` 用完整 predicate 做 OR/nested partition pruning；`e886a3637` 修复 local table lookup 并发；`83ccce1ac` 保护 ORC batch size 至少为 1；`8171884d0` 用 `computeIfAbsent` 修复 DV maintainer cache 竞争。 | `581f324b3`、`e886a3637`、`83ccce1ac` 是直接候选；DV 相关提交主要是 master 新路径。 |
| Manifest / commit metadata | 4 | `653175d8e` manifest sort 支持 external spill；`ea0a53984` PyPaimon manifest 按目标大小滚动；`be34a5a8b` PyPaimon manifest/list/index fastavro 写入压缩；`ff73dcfbd` 给 `sys.catalog_options` 增加 catalog option gate 并扩展 REST system table listing。 | PyPaimon manifest 两项是条件候选；`ff73dcfbd` 是行为/安全 hardening，不是无风险 bugfix。 |
| PyPaimon write / scan / writer | 6 | `2fc0e7e13`、`62e26b8f1`、`f660ba4df` 缓存 overwrite retry 的目标文件并按增量推进；`d208b698f` 在构造 `DataFileMeta` 前用 row-id range 过滤 manifest record；`c5a8c4ae6` 批量 mixed Java-write 测试；`4041b51a4` 透传 Mosaic writer options。 | overwrite retry 系列和 manifest early filter 可评估回补；mixed test 和 Mosaic 需看 release 功能前置。 |
| PyPaimon global index | 5 | `96458b5da` 修复 extra-field global index padding；`0abb9c3c0` 增加 global index lifecycle API；`a34c8a1ff`、`0c9811ad8`、`605b9dd26` 分别支持 vindex、bitmap、Tantivy full-text build。 | release 有基础 global-index 包，但 scanner 尚无本次 padding 修复依赖的多字段 reader 逻辑；除非先补齐前置能力，否则不是直接回补项。 |
| Spark / Flink procedure and runtime | 6 | `6afa0dbfc` 修复 CDC schema compatibility 的 `canConvert` 方向；`77811ea2a` 增加 `compact_manifest` dry run；`f26a23fdb` 修复 Spark procedure partition 字符串转换；`74018bd67` 支持 Chain Table stream read；`b4e556950` 支持 data-evolution 表 Spark V1 UPDATE；`78ef07218` 支持 Blob HTTP URL 缺失文件写 null。 | `6afa0dbfc`、`f26a23fdb` 是直接候选；`78ef07218` 的主功能开关在 release 缺失。 |
| Blob / vector / data evolution / format | 6 | `1aace6c15` 移除 blob external storage mode；`fb80f2f35` 支持 `VectorType` common row paths；`d2be7eace` 引入 data-evolution deletion vector；`ba66bcce6` 升级 ORC 1.9.8；`073e4d991` 同步 ORC copy fixes；`eea31a30a` 重构 shredding write plan。 | 多为 master feature/refactor 或依赖升级，不建议作为普通 1.4 bugfix 单独回补。 |
| Docs | 1 | `5a3d591d9` 将 chain table 文档改为 `.mdx` tab 结构。 | docs-only。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `6afa0dbfc` | `CdcActionCommonUtils.schemaCompatible` 原来只按一个方向调用 `UpdatedDataFieldsProcessFunction.canConvert`，会拒绝部分 source/Paimon 可兼容类型组合。 | patch-id `e07bfca1e80f`; cherry `+`。 | 1.4 同文件仍只有 `canConvert(field.type(), type, ...)` 单向检查。 | 高。Flink CDC schema evolution 生产路径，补丁面小。 |
| `581f324b3` | Format table scan 从 per-field predicate map 改成完整 predicate 判断，支持 OR/nested partition predicate，并修正 default partition null 和不存在目录路径处理。 | patch-id `5ce6485d0b95`; cherry `+`。 | 1.4 `FormatTableScan` 仍只识别 `DefaultPartitionPredicate`，并通过 `PredicateUtils.splitPartitionPredicate` 传 `Map<String, Predicate>` 给 `PartitionPathUtils`。 | 高。影响 format table 分区目录裁剪，云存储 list 成本和扫描范围可能明显放大；建议回补。 |
| `e886a3637` | `LocalTableQuery` / `LookupLevels` / `LookupFile` 改为线程安全 cache、文件锁、同步 key 序列化和 idempotent close，避免并发 lookup 重复建 reader、关闭竞态和计数器竞态。 | patch-id `22ecccb67a15`; cherry `+`。 | 1.4 `LocalTableQuery` 使用 `HashMap` 嵌套 cache；`LookupFile` 仍是普通 `long requestCount` / `boolean isClosed`，`close` 非幂等。 | 高。并发 local lookup 生产路径，回补 patch 较大但属于实际正确性/稳定性修复。 |
| `f26a23fdb` | Spark procedure 的 `partitions` 参数不再拼成 SQL `where` 字符串，而是校验 key 并按 partition schema 解析 literal，修复未加引号字符串 partition 等问题。 | patch-id `53aade078ff2`; cherry `+`。 | 1.4 `SparkProcedureUtils.toWhere` 仍生成 `key=value` 字符串，`CompactProcedure` 等再调用 `convertToPartitionPredicate`。 | 中。影响 compact/rescale/global-index procedure 的 partitioned table 操作；建议评估回补。 |
| `83ccce1ac` | ORC reader 创建 batch wrapper 时保护 `batchSize / numBatches` 至少为 1，避免小 batch size 下创建 0 容量 batch。 | patch-id `d7ab5ec89005`; cherry `+`。 | 1.4 `OrcReaderFactory.createPoolOfBatches` 仍直接传 `batchSize / numBatches`。 | 中。直接 runtime bugfix，patch 很小，优先级高于风险。 |
| `2fc0e7e13` + `62e26b8f1` + `f660ba4df` | PyPaimon overwrite commit retry 从每次重扫目标/基础文件，改为缓存初始目标并根据 retry 期间 ADD/DELETE 增量推进。 | patch-ids `1a1e5155a79b`、`ffa830aa20ab`、`69182a1abcc2`; cherry 均 `+`。 | 1.4 `file_store_commit.py` 仍直接用 `_generate_overwrite_entries`，retry 时没有 `OverwriteChangesProvider` / `RetryResult.base_data_files`。 | 中。主要是冲突重试性能和一致性风险，建议作为 Python 系列 patch 成组评估。 |
| `ea0a53984` + `be34a5a8b` | PyPaimon manifest 写入支持按目标大小滚动，并补齐 manifest/index/list 压缩选项。 | patch-ids `3d955cbfc98f`、`46af569eca58`; cherry 均 `+`。 | 1.4 PyPaimon manifest/list 仍单文件 `fastavro.writer`，未发现 `manifest.compression`。 | 中。影响 PyPaimon metadata 文件大小和压缩；不是 Java runtime 直接 bug。 |
| `96458b5da` | Master 上修复 PyPaimon extra-field global index padding 造成的 false-negative 和 `None` 结果误转命中问题。 | patch-id `61e032770304`; cherry `+`。 | 1.4 有 `extra_field_ids` 元数据字段，但 `GlobalIndexScanner` 仍只按 `index_field_id` 建 reader，缺少本 patch 修改的多字段 reader/padding 前置逻辑。 | 条件项。若要回补，需要先确认 global-index extra-field 功能前置是否同步到 release。 |
| `78ef07218` | Blob missing-file check 支持 HTTP URL，并只对 BLOB 字段索引执行存在性检查，避免普通 binary 字段被误判。 | patch-id `d7ffa16fb87b`; cherry `+`。 | 1.4 未发现 `blob-write-null-on-missing-file` 配置；`FlinkRowWrapper` 仍只是普通 row wrapper。 | 无直接影响。release 缺失该功能路径，不作为 1.4 bugfix。 |
| `ff73dcfbd` | `sys.catalog_options` 默认关闭，REST catalog 增加 prefix/paging system table listing。 | patch-id `e523cb800e32`; cherry `+`。 | 1.4 `CatalogUtils` 仍直接返回 `CatalogOptionsTable`，Flink IT 直接查询 `sys.catalog_options`。 | 产品/安全 hardening。改变 public system table 可见性，需 release 决策，不应当作普通 bugfix 静默回补。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `eab772869` | Core TopN / DV | `DataTableBatchScan.applyPushDownTopN` 去掉 DV 禁用条件，`TopNDataSplitEvaluator` 对 wide DV split 用 `tightBoundsAvailable` 保守保留，并补 DV TopN 测试。 | master DV 性能/功能修复；1.4 是否需要取决于 DV 前置。 |
| 2 | `2fc0e7e13` | PyPaimon overwrite | 新增 `OverwriteChangesProvider`，commit retry 时缓存 overwrite target 的已有文件，避免每次重扫。 | 条件候选，建议与 `62e26b8f1`、`f660ba4df` 成组评估。 |
| 3 | `653175d8e` | Manifest sort | 新增 `ManifestEntryExternalSort`，`FileStoreCommit` / `TableCommit` 可接收 IOManager，Flink committer 传入 task temp dirs 以支持 spillable sort。 | 性能/扩展能力，不是直接 1.4 bugfix。 |
| 4 | `62e26b8f1` | PyPaimon retry | `RetryResult` 携带 `base_data_files`，commit conflict 检查读取 incremental changes，减少 retry 全量扫描。 | 条件候选，需与 overwrite cache 系列一起回补。 |
| 5 | `5a3d591d9` | Docs | `chain-table.md` 改为 `chain-table.mdx`，修复文档 tab 渲染。 | docs-only。 |
| 6 | `1aace6c15` | Blob storage | 删除 blob external storage mode 相关 Java/Python writers、options、docs 和测试。 | 行为移除，不适合 1.4 bugfix。 |
| 7 | `fb80f2f35` | VectorType row path | 新增 `HeapVectorColumnVector`，`RowToColumnConverter` 和 `RowCompactedSerializer` 支持 vector common row path，并禁止 vector key。 | 新类型路径支持，不是直接 1.4 bugfix。 |
| 8 | `77811ea2a` | Flink / Spark procedure | 增加 `ManifestCompactDryRun`，Flink/Spark `compact_manifest(..., dry_run=>true)` 返回 dry-run 统计。 | 新 procedure 能力。 |
| 9 | `ea0a53984` | PyPaimon manifest | `ManifestFileManager.rolling_write` 按目标大小拆分 manifest，commit/compaction 清理多文件结果。 | PyPaimon metadata 条件候选。 |
| 10 | `6afa0dbfc` | Flink CDC | `schemaCompatible` 同时检查 source->Paimon 和 Paimon->source 可转换性，修复兼容类型误判。 | 直接 release 候选。 |
| 11 | `581f324b3` | Format table scan | `FormatTableScan` 将完整 predicate 传给 `PartitionPathUtils`，支持 OR/nested pruning 和 default partition null。 | 直接 release 候选。 |
| 12 | `e886a3637` | Local table query | lookup cache 改为 concurrent/read-write lock/striped file lock，并同步 key serialization 和 processor access。 | 直接 release 候选。 |
| 13 | `f660ba4df` | PyPaimon overwrite | overwrite cache 根据 retry 期间 ADD/DELETE 增量推进，包括目标 append/overwrite 和 whole-table append。 | 条件候选，需与前两项成组。 |
| 14 | `74018bd67` | Chain Table stream read | 新增 `ChainTableFileStoreTable`、`ChainTableStreamScan`，连续 enumerator/source builder 支持 chain table stream read。 | 新能力。 |
| 15 | `be34a5a8b` | PyPaimon manifest compression | 增加 `manifest.compression`，manifest/index/list fastavro writer 默认 zstd。 | PyPaimon metadata 条件候选。 |
| 16 | `ba66bcce6` | ORC dependency | `pom.xml` 将 ORC 升至 1.9.8。 | 依赖升级，不建议单独回补。 |
| 17 | `073e4d991` | ORC copied impl | 同步 ORC 1.9.8 copy fixes 到 `paimon-format/src/main/java/org/apache/orc/impl` 并补测试。 | 与 ORC 升级绑定。 |
| 18 | `eea31a30a` | Shredding write plan | 抽象 `ShreddingWritePlan` 框架，重接 ORC/Parquet map/shared/variant writer。 | 重构/feature，不是普通 bugfix。 |
| 19 | `f26a23fdb` | Spark procedure | 新增 partition 参数解析函数，按 partition schema 解析 literal 并校验 key，替代拼 SQL where。 | 直接 release 候选。 |
| 20 | `d208b698f` | PyPaimon manifest scan | 在 raw fastavro record 层做 row-id range early filter，减少非重叠 `DataFileMeta` 构造，同时保留 ADD/DELETE pair 语义。 | Python scan 性能候选。 |
| 21 | `83ccce1ac` | ORC reader | `OrcReaderFactory.createPoolOfBatches` 用 `Math.max(1, batchSize / numBatches)`。 | 直接 release 候选。 |
| 22 | `96458b5da` | PyPaimon global index | 按 primary field 组织 multi-column index group，并只在 real reader 有结果时应用 extra-field padding。 | 条件项，release 前置不足。 |
| 23 | `b4e556950` | Spark data evolution | Spark V1 UPDATE 对 data-evolution 表走 `_ROW_ID` partial-column MERGE，并拒绝更新 partition column。 | 新 data-evolution 能力。 |
| 24 | `d2be7eace` | Data evolution DV | 引入 data-evolution deletion vector、split deletion files 序列化、blob fallback 调整，并在 DV 下禁用 compaction。 | 大功能，不作为 1.4 普通 bugfix。 |
| 25 | `eb31fd813` | PyPaimon Daft blob | Daft blob File column 写入序列化 `IOConfig`，支持 REST/OSS/S3 credential 解析和 oss->s3 alias。 | 需看 release Daft blob 前置，默认不是直接候选。 |
| 26 | `0abb9c3c0` | PyPaimon global index API | 增加 create/drop BTree index、system tables、external path、commit index add/delete。 | 新 API。 |
| 27 | `a34c8a1ff` | PyPaimon vindex | 支持 vindex IVF global index build、shard splitting，并校验 unaware-bucket 和无 deletion vectors。 | 新能力。 |
| 28 | `0c9811ad8` | PyPaimon bitmap | 支持 bitmap global index build，抽出 index file utils，并补文档/测试。 | 新能力。 |
| 29 | `605b9dd26` | PyPaimon full-text | 支持 Tantivy full-text global index build/read，对 boost/fuzzy/demote 等参数做对齐。 | 新能力。 |
| 30 | `ff73dcfbd` | Catalog system table | 新增 `catalog-options-table.enabled=false` gate，并让 REST catalog 支持按 prefix/paging 返回 system tables。 | hardening，需 release 决策。 |
| 31 | `c5a8c4ae6` | PyPaimon tests | `run_mixed_tests.sh` 支持先批量 Java write 再跑 Python reads。 | test/dev only。 |
| 32 | `4041b51a4` | PyPaimon Mosaic writer | 透传 Mosaic stats columns、bucket 数、block size、zstd level 等 writer options，并限制非 zstd。 | 需看 release Mosaic 前置。 |
| 33 | `8171884d0` | DV maintainer cache | `AppendCompactCoordinator.dvMaintainer` 用 `cache.computeIfAbsent` 替代 get/synchronized/put。 | master DV 路径，release 前置不明确。 |
| 34 | `78ef07218` | Flink Blob HTTP | HTTP URL 增加 HEAD/range GET existence check，`FlinkRowWrapper` 只对 BLOB 字段做 missing-file null check。 | 1.4 缺少功能开关路径，无直接影响。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 当前分支为 `codex/paimon-master-daily-report`；开始时本地因上次自动化未推送已 ahead 1。 |
| `git fetch origin codex/paimon-master-daily-report` | 沙箱内 DNS 失败后提权重跑成功；`origin/codex/paimon-master-daily-report=33bdcee76cbe3c2f41d76d5d2bd898a06dc8920f`。 |
| `git fetch upstream master release-1.4` | 沙箱内 DNS 失败后提权重跑成功；`upstream/master=78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888`，`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git log --first-parent --reverse --format=... d7c5d42c469d4d63a17114ac0883133635545aae..upstream/master` | 34 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <commit>` | 对 34 个 commit 均已执行，用作文件状态和提交元数据检查入口。 |
| `git diff --patch --stat --find-renames <prev> <commit>` | 对 34 个 first-parent 相邻 tree 均已执行并检查实际 hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master d7c5d42c469d4d63a17114ac0883133635545aae` | 34 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对 34 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 `CdcActionCommonUtils`、`FormatTableScan`、`PartitionPathUtils`、`LocalTableQuery`、`LookupFile`、Spark procedure utils、`OrcReaderFactory`、PyPaimon manifest/commit/global-index、Blob wrapper 和 `CatalogOptionsTable` 等触点。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888`
- `last_processed_commit_date`: `2026-07-01 13:38:19 +0800`
- `last_report_date`: `2026-07-01`
- `last_report_path`: `reports/paimon-master-daily/2026-07-01.md`

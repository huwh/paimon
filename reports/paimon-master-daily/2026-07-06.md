# Apache Paimon Master Daily Report - 2026-07-06

## Summary

范围：`9ea8120590944a7a31af0a046ad7ec0db0ae5021..09d8c2d0cdd939537c3633b2d0e746bc5a334bea`，共 4 个 first-parent 提交。当前 Apache upstream `master` head 是 `09d8c2d0cdd9`（2026-07-05 22:14:51 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线主要是 full-text global index 的 native 化：从旧的 `paimon-tantivy` / Tantivy JNI 形态迁到 `paimon-full-text`，新增 CI 安装脚本和 native Java/Python 集成，后续又将 index type/API 收敛为 `full-text`、表字段名加 raw query JSON 的调用方式。Python 侧另有一个 Daft BLOB descriptor 解析 bugfix，以及 Ray data-evolution 表按 `_ROW_ID` 分布式读取的新能力。

release-1.4 方面，`git cherry` 显示 4 个提交均为 `+`，没有等价 patch 已进入 `upstream/release-1.4`。唯一明确 bugfix 是 `9a097c57` 的 Daft BLOB descriptor 校验，但 release-1.4 不存在 `paimon-python/pypaimon/daft/daft_blob.py` 路径；其共享 `BlobDescriptor.deserialize` 已有 version、magic、长度校验，所以该 bugfix 不直接影响 1.4 运行路径。full-text 两个提交是大范围功能迁移/接口演进，不是可独立摘取的 release bugfix。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时仍不稳定展开 hunks，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 并检查实际补丁。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Full-text global index / native module | 2 | `baaaa0edf` 引入外部 `paimon-full-text` 依赖、CI 安装脚本、Java/Python native reader/writer 集成，并删除旧 Tantivy JNI 子模块；`09d8c2d0` 进一步把模块/API 改名为 `paimon-full-text` / `full-text`，删除结构化 `FullTextQuery`，改为列名 + raw query JSON。 | 功能迁移和 API 演进。1.4 有 `FullTextReadImpl` raw query 路径，但没有这些新模块/包；不属于孤立 bugfix。 |
| PyPaimon Daft BLOB | 1 | `9a097c57` 将 Daft BLOB `File` 转换从手写 bytes 解析改为 `BlobDescriptor.deserialize`，并对 malformed descriptor 增加回归测试。 | 1.4 没有 Daft 路径，非直接适用。 |
| PyPaimon Ray data-evolution | 1 | `a9a3d927` 新增 `read_by_row_id`，按 `_ROW_ID` 将 Ray Dataset 路由到目标 data files，只读取匹配行和投影列，并覆盖 blob/空结果/foreign row id 等测试。 | 新 Python/Ray 能力；1.4 只有基础 Ray datasource/sink，非 bugfix。 |
| Docs / CI / build wiring | 2 | full-text 文档、SQL/Spark/PyPaimon 示例、workflow trigger、根 `pom.xml` 模块和版本号随 native full-text 迁移同步更新。 | 随功能迁移，不建议单独回补。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `9a097c57` | Daft BLOB File conversion 原来手写解析 serialized `BlobDescriptor` bytes，绕过共享 descriptor 校验；malformed bytes 可能泄露底层 `struct`/UTF-8 错误，unsupported version 或 invalid magic 可能未按统一语义处理。补丁改用 `BlobDescriptor.deserialize` 并将 `struct.error`、`UnicodeDecodeError` 包装为 `ValueError`。 | stable patch-id `5756e0f8157d`; cherry `+`。 | `release-1.4` 不存在 `paimon-python/pypaimon/daft/daft_blob.py`；仅有 `paimon-python/pypaimon/table/row/blob.py`，其中 `BlobDescriptor.deserialize` 已检查 version、magic、URI length、offset/length。 | 不直接适用。除非 1.4 另行引入 Daft BLOB conversion，否则没有对应运行路径需要回补。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `baaaa0edf` | Full-text native dependency | 49 files，`+1044/-4577`。新增 `.github/workflows/install-paimon-full-text.sh`，各 CI job 先构建/安装 `apache/paimon-full-text` Java/native/Python 依赖；根 `pom.xml` 增加 `paimon.full-text-index.version`；旧 `paimon-tantivy/paimon-tantivy-index` 和 `paimon-tantivy-jni` 删除或扁平化；Java `FullTextReadImpl` 对单列 structured query 直接下推给 native reader；Python tantivy reader/writer 改走 `paimon_ftindex`。 | cherry `+`，patch-id `10b562370257`。这是 full-text 实现迁移，不是 release-1.4 bugfix；回补需要整套 native full-text 依赖和 CI/build 形态。 |
| 2 | `9a097c57` | PyPaimon Daft BLOB | 2 files，`+49/-18`。`paimon-python/pypaimon/daft/daft_blob.py` 删除手写 version/magic/URI/offset/length 解析，复用 `BlobDescriptor.deserialize`；测试覆盖 empty、unknown version、bad magic、truncated URI/offset/length、invalid UTF-8 和正常 roundtrip。 | bugfix 但 1.4 无 Daft path；见上表。 |
| 3 | `a9a3d927` | PyPaimon Ray read_by_row_id | 5 files，`+656`。新增 `pypaimon.ray.read_by_row_id` API；planner pin 到 base snapshot，按 target file 的 first row id groupby，构造 `IndexedSplit` 仅读匹配 row ids；拒绝非 data-evolution、未启用 row-tracking、DV 表、空 projection、foreign row id 等场景；文档和 325 行测试覆盖。 | cherry `+`，patch-id `16e7798c1863`。新能力，非 release bugfix；1.4 只有基础 Ray datasource/sink。 |
| 4 | `09d8c2d0` | Full-text API/module rename | 72 files，`+1996/-5476`。workflow 从 Tantivy 改名 full-text；`paimon-tantivy` 重命名/迁移为 `paimon-full-text`；删除 Java/Python `FullTextQuery` structured DSL，`FullTextSearch` 改为 raw query string + column；native reader/writer/indexer/options 类迁到 `org.apache.paimon.fulltext.index`；Python 包从 `tantivy` 改为 `full_text`；Spark TVF 和 docs 同步使用 `full-text` index type。 | cherry `+`，patch-id `cdda929aa649`。大范围 API 演进；1.4 的 `FullTextReadImpl` 已是 raw queryText 形态，但缺少新 native module，不能作为独立 bugfix 回补。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | sandbox DNS 失败后用联网权限重跑成功。 |
| `git fetch upstream master release-1.4` | sandbox DNS 失败后用联网权限重跑成功；`upstream/master` 前进到 `09d8c2d0cdd9`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git rev-list --first-parent --count 9ea8120590944a7a31af0a046ad7ec0db0ae5021..upstream/master` | 4 个 first-parent commit，均已纳入报告。 |
| `git diff --stat --find-renames 9ea8120590944a7a31af0a046ad7ec0db0ae5021..upstream/master` | 范围共 108 个文件，3517 insertions / 9843 deletions。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <4 commits>` | 对 4 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <4 commits>` | 对 4 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 4 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对 4 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 Daft/Ray/full-text 路径存在性、`BlobDescriptor.deserialize`、`FullTextReadImpl` 和 root `pom.xml` 模块形态。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `09d8c2d0cdd939537c3633b2d0e746bc5a334bea`
- `last_processed_commit_date`: `2026-07-05 22:14:51 +0800`
- `last_report_date`: `2026-07-06`
- `last_report_path`: `reports/paimon-master-daily/2026-07-06.md`

# Apache Paimon Master Daily Report - 2026-07-02

## Summary

范围：`78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888..6768580c214b95dd90074c156c867e5162646f90`，共 11 个 first-parent 提交。当前 Apache upstream `master` head 是 `6768580c214b`（2026-07-02 13:35:07 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线集中在 data-evolution 和 multimodal 相关能力：Flink DataStream lineage、global index 增量规划、PyPaimon predicate update / merge_into / multimodal facade、PyPaimon data-evolution + deletion vector 读取、Spark data-evolution matched update / DELETE、Spark lateral vector search 列裁剪修复，以及 PyArrow / Lumina 依赖调整。

`release-1.4` 方面，11 个提交经 `git cherry -v upstream/release-1.4 upstream/master 78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888` 检查均为 `+`，未发现等价 patch 已 cherry-pick。逐项检查 production path 后，较明确的 1.4 候选是 `813616e3`（PyArrow 19.0.0 依赖回避）和 `26da9299`（Spark data-evolution matched update + global index 漏更新）。`b31acd09` 是 data-evolution + deletion vector 组合能力缺口，若 1.4 需要该组合可评估成组回补；`e5335fe3` 修复的是 master 新增的 lateral vector search 路径，`release-1.4` 没有同一路径，不是直接候选。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames <commit>` 查看提交元数据和文件状态；由于当前 Git 在带 `--name-status` 时不展开 hunks，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 并检查实际补丁。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Flink lineage | 1 | `63da5586` 为 DataStream source/sink 和 format-table sink 增加 `LineageVertexProvider`，lineage dataset 带 schema facet，并按 JDBC catalog key 解析 dataset name。 | 新能力；非 1.4 bugfix。 |
| Global index planning | 1 | `ec79aaca` 抽出 Java/Flink/Spark/Python 共用的 unindexed row-range / shard split 规划，global index 默认复用已建索引范围。 | 大功能/性能优化；不建议作为普通 bugfix 单独回补。 |
| PyPaimon data-evolution API | 3 | `fc99df74` 增加 predicate update；`dba5e030` 增加 PyPaimon `TableUpdate.merge_into`；`db7a9798` 增加 multimodal facade、search/merge/scan API 和文档。 | 主要是新 API；release 需先确认功能前置。 |
| PyPaimon reader / deletion vector | 1 | `b31acd09` 让 data-evolution merge reader、blob fallback、row-range filtering 和 deletion vector 按 row-id / local position 正确对齐。 | 条件候选：1.4 有 DV reader 和 data-evolution 代码，但没有本次 data-evolution + DV 对齐逻辑。 |
| Spark data-evolution / vector search | 3 | `26da9299` 修复 global index 下 matched update 漏扫新行；`7e9403ee` 支持 data-evolution + DV 表 DELETE；`e5335fe3` 修复 lateral vector search query vector 被列裁剪。 | `26da9299` 是直接候选；`7e9403ee` 是新能力；`e5335fe3` 对 1.4 无同一路径。 |
| Dependencies / CI | 2 | `813616e3` 排除 PyArrow 19.0.0；`6768580c` 将 `lumina-jni` 升级到 0.3.0；`dba5e030` 也调整 GitHub workflow push paths-ignore。 | `813616e3` 是低风险候选；Lumina 是依赖升级，不按 bugfix 处理。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `813616e3` | PyPaimon dev requirements 将 `pyarrow>=16,<20` 改为 `pyarrow>=16,!=19.0.0,<20`，避开 Parquet `SizeStatistics` regression。 | stable patch-id `1bf85b2f3a9d`; cherry `+`。 | 1.4 `paimon-python/dev/requirements.txt` 仍是 `pyarrow>=16,<20; python_version >= "3.8"`。 | 中。主要影响 Python dev/test/runtime 依赖解析，patch 面很小，建议回补。 |
| `26da9299` | Spark data-evolution `UPDATE` / `MERGE INTO` 在 matched row-id discovery 时强制 `global-index.search-mode=full`，并固定到解析后的 snapshot，避免创建 global index 后追加的行被 matched update 漏掉。 | stable patch-id `e1d6c3f2adb6`; cherry `+`。 | 1.4 `MergeIntoPaimonDataEvolutionTable` 仍直接使用 `v2Table` / `targetRelation`，没有 `withMatchedUpdateScanOptions`、`TimeTravelUtil.tryTravelOrLatest` 或 full-search 动态选项；1.4 还没有本次 master 的 `UpdatePaimonDataEvolutionTableCommand` 文件。 | 高。1.4 已有 Spark data-evolution merge 生产路径，global index 条件下可能漏更新新写入行；建议优先评估回补。 |
| `b31acd09` | PyPaimon 读取 data-evolution + deletion vector 表时，将 deletion vector 映射到 reader 返回位置，merge reader / blob fallback / limit pushdown 也按 data-evolution + DV 语义处理。 | stable patch-id `24402aef6c7a`; cherry `+`。 | 1.4 有 `ApplyDeletionVectorReader`、`DataEvolutionSplitRead` 和 DV 基础 reader，但 `ApplyDeletionVectorReader` 仍直接按 returned position bitmap 过滤，没有 `PositionMappedDeletionVector` / data-evolution anchor 对齐逻辑。 | 中，条件候选。若 1.4 支持或计划支持 data-evolution + DV 组合，应成组评估；否则属于 master 新能力链路。 |
| `e5335fe3` | Spark lateral vector search 在 logical plan 阶段去掉 `OuterReference`，让 query vector 列进入 `references`，避免列裁剪把 query vector 从左表计划中删掉。 | stable patch-id `9bf0feea9bd8`; cherry `+`。 | 1.4 只有普通 `vector_search` TVF，未发现 `LateralVectorSearch` / `LateralVectorSearchExec` / LATERAL 相关类或执行路径。 | 低 / 不适用。对 1.4 没有同一路径，不建议回补。 |
| `6768580c` | `lumina-jni` 0.2.0 -> 0.3.0。 | stable patch-id `39202b46abe5`; cherry `+`。 | 1.4 是否应升级依赖需要 release 决策，不能从 patch 判定为 bugfix。 | 不按 bugfix 回补；除非有明确 Lumina 0.3.0 修复需求。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `63da5586` | Flink lineage | `LineageUtils` 扩展 Iceberg option 白名单、dataset schema facet 和 JDBC catalog-key name 解析；source/sink wrapper 实现 `LineageVertexProvider`；新增 `PaimonDataStreamSource`、`PaimonDiscardingSink` 及 lineage tests。 | 新 Flink lineage 能力；非 1.4 bugfix。 |
| 2 | `ec79aaca` | Global index | `GlobalIndexBuilderUtils` 新增 `indexedRowRanges`、`unindexedRowRanges`、`createShardIndexedSplits`；Flink/Spark/Python global index builder 复用同一增量规划，避免重复构建已覆盖 row ranges。 | 大范围规划重构和性能优化；不作为单点 bugfix。 |
| 3 | `fc99df74` | PyPaimon update | `TableUpdate.update_by_predicate` 通过 predicate 先读 `_ROW_ID`，再走 row-id update；对 global index 强制 full search，并处理 tag/snapshot/time-travel scan options；文档改为 MDX tabs。 | 新 PyPaimon API；1.4 无同一 API，非直接候选。 |
| 4 | `813616e3` | Python dependency | `paimon-python/dev/requirements.txt` 排除 `pyarrow==19.0.0`。 | 直接候选，低风险。 |
| 5 | `26da9299` | Spark data evolution | `MergeIntoPaimonDataEvolutionTable` 在 matched action 存在时复制带 full global-index search 和 pinned snapshot 的 `SparkTable` / relation；`UpdatePaimonDataEvolutionTableCommand` 复用该 scan target；补 global-index 后追加行仍能 update/merge 的测试。 | 直接候选，高优先级。 |
| 6 | `dba5e030` | PyPaimon merge_into / CI | 新增 `pypaimon.table.data_evolution_merge_into`，支持 batch/stream `TableUpdate.merge_into`、matched update、not matched insert、literal/source/target assignment、重复 source match 校验；同时 CI push paths-ignore 增加 Python/docs 过滤。 | 新 PyPaimon API；非普通 bugfix。 |
| 7 | `b31acd09` | PyPaimon data-evolution DV | 新增 `PositionMappedDeletionVector`、data-evolution DV 单测和 Java/Python E2E；`DataEvolutionSplitRead` 在 normal/blob/vector bunch 中统一应用 row_ranges 与 deletion vector；data-evolution + DV 下禁用 limit pushdown。 | 条件候选，取决于 1.4 是否需要 data-evolution + DV 组合。 |
| 8 | `7e9403ee` | Spark DELETE / data evolution | Spark DELETE 对 data-evolution 表仅在未启用 DV 时拒绝；row-level command 根据 row-id 找 data-evolution anchor file 并写 DV；补 data-evolution + DV 表 DELETE 测试。 | 新能力，非 bugfix；回补风险高于 `26da9299`。 |
| 9 | `db7a9798` | PyPaimon multimodal | 新增 `pypaimon.multimodal` connection/table/query facade，默认开启 row-tracking、data-evolution、DV、Vortex，并支持 add/update/merge/scan/create_index/search/search_vectors/search_hybrid；where parser 从 CLI 移到 common。 | 新高层 API；非 1.4 bugfix。 |
| 10 | `e5335fe3` | Spark lateral vector search | logical `LateralVectorSearch` 构造时 strip `OuterReference`，执行层移除重复 strip；补 query vector 不在外层 SELECT 时仍不被列裁剪的测试。 | 1.4 无 lateral vector search 路径，不适用。 |
| 11 | `6768580c` | Lumina | `paimon-lumina/pom.xml` 将 `org.aliyun.lumina:lumina-jni` 从 0.2.0 升到 0.3.0。 | 依赖升级；需单独 release 决策。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 当前分支为 `codex/paimon-master-daily-report`，开始时工作区干净且本地跟踪 ref 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch --no-tags upstream refs/heads/master:refs/remotes/upstream/master` | sandbox DNS 失败后提权重跑成功；`upstream/master` 从 `78ef07218` 更新到 `6768580c`。 |
| `git fetch --no-tags upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 提权重跑成功；`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git log --first-parent --reverse --format=... 78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888..upstream/master` | 11 个 first-parent commit，均已纳入本报告。 |
| `git show --stat --name-status --patch --find-renames <commit>` | 对 11 个 commit 均已执行，用作文件状态和提交元数据检查入口。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 对 11 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master 78ef07218ef9a1dd1e9a3cbaf58b54093c2b4888` | 11 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对重点 bugfix / dependency commits 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 PyArrow requirement、Spark data-evolution merge/update、PyPaimon DV reader、Spark DELETE/data-evolution、Spark vector_search/lateral vector search 路径。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `6768580c214b95dd90074c156c867e5162646f90`
- `last_processed_commit_date`: `2026-07-02 13:35:07 +0800`
- `last_report_date`: `2026-07-02`
- `last_report_path`: `reports/paimon-master-daily/2026-07-02.md`

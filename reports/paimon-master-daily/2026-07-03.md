# Apache Paimon Master Daily Report - 2026-07-03

## Summary

范围：`6768580c214b95dd90074c156c867e5162646f90..30a13d69e6947419b2914276686d7c079454cf42`，共 16 个 first-parent 提交。当前 Apache upstream `master` head 是 `30a13d69e694`（2026-07-03 13:27:39 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线集中在 PyPaimon 可变写入和多模态 API、Python 读路径剪枝、Spark data-evolution 可靠性、Flink lookup BLOB 缓存，以及少量文档修正。最值得 1.4 关注的是 Spark data-evolution 表在已有 deletion vectors 时的 row-id gap 处理、PyPaimon 对 partition column 的 value-stats 剪枝误用，以及 Flink lookup join 对大 BLOB 表的 RocksDB 缓存膨胀问题。

`release-1.4` 方面，`git cherry -v upstream/release-1.4 upstream/master 6768580c214b95dd90074c156c867e5162646f90` 显示本次 16 个提交均为 `+`，未发现等价 patch 已 cherry-pick。逐项对照 release 分支代码后，直接候选是 `d435ec9b` 和 `bde536c1`；`30a13d69` 是面向 BLOB lookup 的条件候选；`bbe2d109` 是 Spark 3.4+ 迁移兼容选项，不改变默认行为，可按需求评估。`e39d8178`、`3145e051`、`d9599117` 在 release-1.4 缺少同一路径，`7ca1cd36` 对应的 data-evolution standalone UPDATE 在 1.4 仍被拒绝，不是直接候选。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commits...>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时不展开 hunks，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 并检查实际补丁。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core manifest sort | 1 | `e39d8178` 让 manifest sort 在 `manifest-sort.max-rewrite-size` 小于 `manifest.target-file-size` 时仍至少重写两个 manifest files，避免无进展。 | `release-1.4` 未发现 `ManifestFileSorter` / `manifest-sort` 路径，非直接候选。 |
| PyPaimon mutable / multimodal APIs | 4 | `6bb99183` 增加 data-evolution delete；`2844adf2` 增加 row-based BLOB write/upsert；`d7bae9ba` 增加 multimodal BlobStore；`97dbf29d` 增加 multimodal overwrite。 | 主要是新 API；`release-1.4` 只保留较早 PyPaimon update 路径，缺少 multimodal / data-evolution merge API。 |
| PyPaimon read and pruning | 5 | `9a468444` 增加并发 BLOB range read；`3145e051` 将 Daft `partition_filters` 推到 plan；`bde536c1` 从 value-stats predicate 排除 partition columns；`878a4b01` 在 manifest read 阶段提前按 partition 过滤；`d9599117` 增加 Mosaic row-group stats skipping。 | `bde536c1` 直接影响 1.4 PyPaimon scanner；`878a4b01` 是性能候选；Daft / Mosaic 路径在 1.4 不存在。 |
| Spark data-evolution | 2 | `7ca1cd36` 为 deterministic V1 UPDATE data-evolution 冲突加 retry；`d435ec9b` 为 data-evolution merge + deletion vectors 的 row-id gaps 填充 filler rows。 | `d435ec9b` 是直接候选；`7ca1cd36` 在 1.4 没有同一 UPDATE 命令路径。 |
| Spark timestamp compatibility | 1 | `bbe2d109` 增加 `spark.paimon.legacy-timestamp-mapping.enabled`，允许 Spark 3.4+ 将 Paimon TIMESTAMP 暴露为 Spark TIMESTAMP。 | 兼容选项，默认 false；可按 Spark 3.3 -> 3.4 迁移需求评估。 |
| Flink lookup BLOB | 1 | `30a13d69` 增加 `lookup.blob-as-descriptor`，lookup cache 将 BLOB 转成 BlobDescriptor bytes，避免 RocksDB 存完整 BLOB。 | 1.4 有 full-cache lookup 和 BLOB descriptor 基础能力，但无 lookup 专用选项；条件候选。 |
| Docs | 2 | `24e25d3d` 补充 bucketed append incremental clustering 文档；`642b6c39` 修正 Hive migration command 拼写。 | 文档-only；`642b6c39` 可低风险回补。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `d435ec9b` | Spark data-evolution merge 在表已有 deletion vectors 时，输出 row-id 可能有 gap；补丁用 filler rows 保持 row-id 连续，并避免 raw BLOB filler 触发 fallback。 | stable patch-id `7fcbf247ee5b`; cherry `+`。 | 1.4 有 `MergeIntoPaimonDataEvolutionTable` 和 `DataEvolutionTableDataWrite`；`PerFileWriter.write` 仍断言 `rowId == firstRowId + numWritten`，没有 `fillGapUntil` 或 filler row 逻辑。 | 高。1.4 已有 Spark data-evolution merge 和 DV 基础路径，删除后 merge update 可能断言失败或写出缺口；建议优先评估回补。 |
| `bde536c1` | PyPaimon scanner 的 value-stats predicate 排除 partition columns，避免 partition predicate 被拿去和 data-file stats 比较。 | stable patch-id `1daba65eb263`; cherry `+`。 | 1.4 `FileScanner.__init__` 直接设置 `predicate_for_stats = remove_row_id_filter(predicate)`，后续 append-only / PK DV 路径会用它做 value-stats 过滤；没有 `exclude_predicate_with_fields`。 | 高。partition columns 不在 data-file value stats 中，尤其 null partition / partition-only filter 容易误剪；建议回补。 |
| `30a13d69` | Flink lookup join 读取 BLOB 表时，新增 `lookup.blob-as-descriptor`，cache value serializer 将 BLOB 替换为 VARBINARY 并写入 BlobDescriptor bytes，避免 RocksDB 缓存完整 BLOB。 | stable patch-id `f3111ea7a0b7`; cherry `+`。 | 1.4 有 `FullCacheLookupTable` / `NoPrimaryKeyLookupTable` 和通用 `blob-as-descriptor`，但 lookup cache 仍用 `InternalSerializers.create(projectedType)`，没有 `BlobAsDescriptorRow` / `cacheValueRowType` / lookup 专用 option。 | 中高，条件候选。默认行为不变；若 1.4 用户用 Flink lookup join 查大 BLOB 表，可能遇到磁盘膨胀、page-size overflow 或 bootstrap 超时。 |
| `bbe2d109` | Spark 3.4+ 默认把 Paimon TIMESTAMP 暴露为 `TIMESTAMP_NTZ`；新增 opt-in legacy mapping 兼容 Spark 3.3 语义。 | stable patch-id `d011491eb98c`; cherry `+`。 | 1.4 common `TypeUtils.treatPaimonTimestampTypeAsSparkTimestampType()` 固定 false；Spark 3.2/3.3 shim 固定 true。 | 中，兼容选项。不是默认 bugfix，但对从 Spark 3.3 升到 3.4+ 且依赖 `unix_timestamp/from_unixtime` 旧语义的用户有价值。 |
| `878a4b01` | PyPaimon manifest read 在反序列化 `_FILE` 前先按 partition predicate 过滤，减少大量 partition `IN` / `isNull` 读取成本。 | stable patch-id `c98144d188f7`; cherry `+`。 | 1.4 `ManifestFileManager.read` 总是先构造 `DataFileMeta`，再由 `FileScanner._filter_manifest_entry` 过滤 partition。 | 低到中。主要是性能优化，不修正默认结果；可与 `bde536c1` 成组评估。 |
| `642b6c39` | Hive migration 文档命令从 `migrate_databse` 修为 `migrate_database`。 | stable patch-id `81f4c903d2ec`; cherry `+`。 | release 文档若仍有相同拼写，用户会复制到错误命令；未影响生产代码。 | 低。文档修正，回补风险极低。 |
| `7ca1cd36` | Spark deterministic V1 UPDATE data-evolution 冲突自动 retry。 | stable patch-id `05817846b9ed`; cherry `+`。 | 1.4 `PaimonUpdateTable` 在 `dataEvolutionEnabled()` 时直接抛出 "Update operation is not supported..."；没有 `UpdatePaimonDataEvolutionTableCommand`。 | 低 / 不直接适用。1.4 没有同一 standalone UPDATE data-evolution 路径。 |
| `e39d8178` | Core manifest sort 小 rewrite budget 不进展修复。 | stable patch-id `14d0fa7fcf2d`; cherry `+`。 | 1.4 未发现 `ManifestFileSorter`、`manifest-sort.max-rewrite-size` 或相关 test。 | 不适用。release-1.4 缺少同一路径。 |
| `3145e051` | Daft `partition_filters` 推到 Paimon plan-time pruning，避免全表 plan。 | stable patch-id `745a8a3fc057`; cherry `+`。 | 1.4 `paimon-python/pypaimon` 未发现 `daft` datasource 目录。 | 不适用。release-1.4 缺少同一路径。 |
| `d9599117` | Mosaic format reader 用 row-group stats 做 predicate skipping，失败时保持 fail-open。 | stable patch-id `d78b54e6e4ce`; cherry `+`。 | 1.4 未发现 `format_mosaic_reader.py` / Mosaic reader 路径。 | 不适用。release-1.4 缺少同一路径。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `e39d8178` | Core manifest sort | `splitSectionAndRewriteHead` 不再只接受 `headSize + file.fileSize() <= remainingBudget`，而是包含第一个越过预算的 manifest file，并保证至少两个文件进入 rewrite head；新增小 rewrite budget 仍会重写并保持排序的测试。 | 1.4 缺少同一路径，不适用。 |
| 2 | `6bb99183` | PyPaimon data-evolution delete | 新增 `TableDeleteByRowId`、`delete_by_predicate` / `delete_by_row_id`、matched delete merge branch、Ray delete branch；partition-only delete 改成提交 `deleted_files` 和 index deletes。 | 新 API / 新能力；非 1.4 直接 bugfix。 |
| 3 | `9a468444` | PyPaimon BLOB read | `FileIO` 增加 range coalescing / concurrent reads；`TableRead.to_arrow` 和 batch reader 增加 `blob_parallelism`；Daft 增加 `read_blob` / `open_blob`；补 negative length descriptor 读取测试。 | 主要是性能和新 API；1.4 缺少 Daft 路径，不作为直接候选。 |
| 4 | `3145e051` | PyPaimon Daft pruning | 在 `PaimonDataSource._read_pushdown_state` 中把 Daft `pushdowns.partition_filters` 转成 Paimon predicate 并与 row filters 合并，`isNull` partition filter 保留 post-filter 语义。 | 1.4 无 Daft datasource，不适用。 |
| 5 | `2844adf2` | PyPaimon row write / upsert | 新增 `TableWrite.write_row`、`TableUpdate.upsert_by_key(rows, keys)`、row key extraction、row utils、DedicatedFormatWriter row path，支持 streaming Blob 不 materialize。 | 新 Python API；非普通 1.4 bugfix。 |
| 6 | `24e25d3d` | Docs | bucketed append 文档说明 composite bucket-key 的 filter 要求；incremental clustering 文档补充 bucketed append 支持、`bucket-append-ordered=false` 和 DV 限制。 | 文档-only。 |
| 7 | `642b6c39` | Docs | Hive migration action command 从 `migrate_databse` 修为 `migrate_database`。 | 低风险文档候选。 |
| 8 | `7ca1cd36` | Spark data-evolution UPDATE | 新增公共 `ErrorMessages`，`UpdatePaimonDataEvolutionTableCommand` 对 deterministic UPDATE 的 row-id conflict 做可配置 retry，新增 retry 配置项和并发 UPDATE 测试。 | 1.4 data-evolution UPDATE 仍被 analyzer 拒绝，非直接候选。 |
| 9 | `d7bae9ba` | PyPaimon multimodal BlobStore | 新增 `BlobStore`，支持 S3-like `put/get/head/list/delete/update_columns`，支持 external descriptor、managed BLOB、range reads 和 table column projection。 | 新 multimodal API；1.4 缺少同一路径。 |
| 10 | `d435ec9b` | Spark data-evolution + DV | `DataEvolutionTableDataWrite.PerFileWriter` 放宽 row-id 必须连续的断言，用 copied filler row 填补已删除位置，并对 raw blob filler 写 NULL；新增多 Spark 版本 data-evolution deletion tests。 | 直接候选，高优先级。 |
| 11 | `97dbf29d` | PyPaimon multimodal overwrite | `MultimodalTable.overwrite` 封装 batch overwrite，支持未分区全表覆盖、dynamic partition overwrite 和 static partition overwrite；补文档和测试。 | 新 API；非直接 bugfix。 |
| 12 | `bde536c1` | PyPaimon value-stats pruning | 新增 `exclude_predicate_with_fields`，在 `FileScanner` 中从 `predicate_for_stats` 排除 partition keys，避免 partition predicate 被用到 data-file value stats。 | 直接候选，高优先级。 |
| 13 | `878a4b01` | PyPaimon manifest read | `ManifestFileManager.read_entries_parallel/read` 增加 `partition_filter`，在构造 `DataFileMeta` 前反序列化 partition 并过滤；explain mode 保持完整 funnel stats。 | 性能候选，可选回补。 |
| 14 | `bbe2d109` | Spark timestamp compatibility | 新增 `legacy-timestamp-mapping.enabled` option，common `TypeUtils` 改为读取配置；新增 Java/Scala 测试覆盖 Spark 3.4+ TIMESTAMP legacy mapping。 | 兼容选项，按迁移需求评估。 |
| 15 | `d9599117` | PyPaimon Mosaic | `FormatMosaicReader` 接收 Paimon predicate，读取 Mosaic row-group stats 并做 `test_by_simple_stats`；补 timestamp / decimal / primitive stats conversion 和 fail-open tests。 | 1.4 无 Mosaic reader，不适用。 |
| 16 | `30a13d69` | Flink lookup BLOB | 新增 `lookup.blob-as-descriptor`、`BlobAsDescriptorRow`、lookup bootstrap reader table copy 为 `blob-as-descriptor=true`，cache value row type 将 BLOB 替换为 VARBINARY；NoPrimaryKey lookup refresh/bootstrap 都写 descriptor bytes。 | 条件候选，适合有大 BLOB lookup join 需求的 1.4 用户。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净且本地跟踪 ref 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | sandbox DNS 失败后提权重跑成功。 |
| `git fetch upstream master release-1.4` | sandbox DNS 失败后提权重跑成功；`upstream/master` 从 `6768580c2` 前进到 `30a13d69e`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git rev-parse upstream/master upstream/release-1.4 origin/codex/paimon-master-daily-report HEAD` | 确认 master head、release head、origin branch 和本地 HEAD。 |
| `git log --first-parent --reverse --format=... 6768580c214b95dd90074c156c867e5162646f90..upstream/master` | 16 个 first-parent commit，均已纳入本报告。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <16 commits>` | 对本次 16 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 对 16 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master 6768580c214b95dd90074c156c867e5162646f90` | 16 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对重点 bugfix / behavior commits 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 Manifest sorter、Daft、Mosaic、PyPaimon scanner、Spark data-evolution UPDATE / MERGE、Spark timestamp mapping、Flink lookup BLOB 路径。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `30a13d69e6947419b2914276686d7c079454cf42`
- `last_processed_commit_date`: `2026-07-03 13:27:39 +0800`
- `last_report_date`: `2026-07-03`
- `last_report_path`: `reports/paimon-master-daily/2026-07-03.md`

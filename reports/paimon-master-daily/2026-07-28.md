# Apache Paimon Master Daily Report - 2026-07-28

## Summary

范围：`45cf5ffb003368ea2efb0a6a92fed8f27dc6dd0b..9ad20d997858c124ae33654f6a1fa694dc588211`，共 15 个 first-parent 提交，89 个文件变更，`+10560/-2011`。本轮 `upstream/master` 前进到 `9ad20d997858`（2026-07-28 12:07:45 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

主要变化集中在 LocalKvDb/lookup/cache/SST 性能与可靠性、Flink distributed vector search、Spark postpone bucket 写入 bucket 数推断、Hive metastore partition done 状态，以及 PyPaimon blob bunch 构造优化。按 patch、`git cherry`、patch-id 和 `release-1.4` 代码路径判断，本轮最值得关注的 1.4 候选是 `095a1d2ea` 的 `BlockIterator.seekTo` 耗尽后复用修正、`2e8f69a15` 的 SimpleLsm/LocalKv flush 失败不丢 memtable 语义、`0e3f4461c` 的 Hive partition done 状态回填，以及 `e818190e4` 的 global-index 线程数正数校验。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core LocalKvDb / lookup / cache / SST | 9 | `SimpleLsmKvDb` 重命名并扩展为 `LocalKvDb`；引入 `LsmLevels`、同步/异步 compaction、Bloom filter builder、off-heap cache、ZSTD 直接 block 解压、bulk-load writer、range iterator、TTL/merge compaction、LocalKv-backed lookup states。 | 1.4 仍有 `SimpleLsmKvDb`、`BlockIterator` 和 ZSTD 解压旧逻辑；其中 `BlockIterator` 与 flush 失败语义建议单独 backport 评估，LocalKv-backed state 属 master 功能。 |
| Flink vector search | 1 | `vector_search` procedure 支持 distributed vector search、`where`、`partitions`、score projection、snapshot pinning，并新增 Flink primary-key/data-evolution vector read 实现。 | 1.4 有旧 `VectorSearchProcedure`，但这轮主要是功能增强，不是直接 bugfix。 |
| Spark postpone bucket | 1 | Spark batch fixed-bucket 写入从输入数据量/行数推断 bucket 数，新增 `postpone.target-size-per-bucket`，overwrite 场景忽略旧 postpone 行数，并缓存输入保证统计与写入一致。 | 1.4 有旧 Spark postpone fixed-bucket 写入路径，但该提交以策略增强为主；若 1.4 用户依赖更合理 bucket 数，可单独功能回摘。 |
| Hive catalog / metastore partitions | 1 | `listPartitions` 在 metastore partitioned table 且 `partition.mark-done-action=mark-event` 时，从 HMS event 查询并回填 `Partition.done()`。 | 1.4 有相同 HiveCatalog/listPartitions 与 mark-event 配置旧路径，属于直接语义候选。 |
| PyPaimon data-evolution reader | 1 | `BlobBunch` 增加 `finish()`，把 row-range merge 从每次 `row_count()` 延后为一次性完成，并禁止 finish 后继续 add。 | 1.4 的 `BlobBunch` 已是不同的增量 row_count 结构，未命中同一热点，不建议直接回摘。 |
| Tests / stability | 1 | 把 unaware bucket split shuffle 测试从 `AppendOnlyTableITCase` 拆到独立 IT，增加 reader subtask 分布断言和 dangling job 清理。 | 测试结构调整，无生产影响。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `095a1d2ea` | `BlockIterator.seekTo` 在迭代器已经耗尽后重置状态，并只记录命中 position，避免耗尽后无法再次 seek 到已存在 key。 | `git cherry` 为 `+`；stable patch-id `368af439ebfa98d70b1dca61a9d1b4cfa7e60ab0`。 | 1.4 的 `paimon-common/.../BlockIterator.java` 仍使用 `BlockEntry polled`，`seekTo` 没有 exhausted 状态重置；测试也只覆盖 seek 到越界后 `hasNext=false`。 | 高；SST block reader 路径存在，建议优先做小范围 backport 和 `seek after exhausted` 测试。 |
| `2e8f69a15` | LocalKvDb flush 失败时不再先清空 memtable；写 SST 成功并登记到 levels 后才清空，异常时关闭 writer、删除半成品文件并保留内存数据供重试。 | `git cherry` 为 `+`；stable patch-id `aed4305092b7cea19f3ecb9d1ed59a67a162a203`。 | 1.4 的 `SimpleLsmKvDb.flush/close` 仍先 `memTable = new TreeMap`、`memTableSize = 0`，再 `writeMemTableToSst`；失败会丢失当前 memtable 状态。 | 高；不是原样 cherry-pickable，因为 master 已重构为 `LocalKvDb`，但 1.4 的 `SimpleLsmKvDb` 应做语义移植。 |
| `0e3f4461c` | Hive `listPartitions` 对 mark-event 分区表回填 `Partition.done()`，避免 HMS 已标记 load done 但 catalog 返回 false。 | `git cherry` 为 `+`；stable patch-id `4ce345c16644f7cdcc0367e6fadba8c38f122501`。 | 1.4 的 `HiveCatalog.listPartitions` 对 HMS/tag-to-partition 和 filesystem 分区都构造 `done=false`；`partitionMarkDoneActions()`、`MARK_EVENT` 和 HMS `isPartitionMarkedForEvent` 路径存在。 | 高；生产语义一致，建议评估直接/轻量适配 backport。 |
| `e818190e4` | `global-index.thread-num` 拒绝 0/负数，避免构造不可用或异常的全局索引读取线程池。 | `git cherry` 为 `+`；stable patch-id `b5deabf836f9ba3e677e016c0e4ea7b8e22cb8c1`。 | 1.4 没有 `GlobalIndexReadThreadPool`，但 `global-index.thread-num` 存在，`GlobalIndexScanner` 把配置传给 `ManifestReadThreadPool.getExecutorService`，该方法没有正数校验。 | 中；代码路径不同，需在 1.4 的 scanner/thread-pool 入口做语义校验。 |
| `6695d6c6f` | ZSTD block 解压从 `ZstdInputStream` 改为 `Zstd.decompressByteArray`，减少流包装和循环读取，并显式处理 ZSTD error/超大结果。 | `git cherry` 为 `+`；stable patch-id `c49c2110ee1b400feb57e8846c2c0a0b03d8179e`。 | 1.4 的 `ZstdBlockDecompressor` 仍是 `ZstdInputStream` 实现。 | 中；更偏性能优化，若 1.4 有 ZSTD 解压热点可回摘，但不是明确 correctness bug。 |
| `9956a067f` | PyPaimon `BlobBunch` 把 blob row-range merge 缓存到 `finish()`，避免多次 `row_count()` 重复合并。 | `git cherry` 为 `+`；stable patch-id `eef19c12537abf1898143aa43ffb7fefbafc4bd7`。 | 1.4 的 `BlobBunch` 已按 add 过程维护 `_row_count`，没有 master 旧 patch 中每次 `row_count()` 调 `Range.sort_and_merge_overlap` 的结构。 | 低；不建议直接回摘。 |
| `9ad20d997` | Spark postpone fixed-bucket 写入改为按输入数据量/行数推断 bucket 数，并修正超大 row-count 计算在 Int 转换前按 max parallelism 截断。 | `git cherry` 为 `+`；stable patch-id `49c0a15472e6f6fe9d7a6e38141c1db3f9f870ab`。 | 1.4 有旧 Spark postpone fixed-bucket 路径，但缺少 `postpone.target-size-per-bucket` 和数据量推断。 | 中低；主要是行为增强，除非 1.4 用户明确遇到 bucket 数不合理或超大行数问题。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `9956a067f` | PyPaimon blob reader | `BlobBunch` 增加 `_finished` 和 `finish()`；`DataEvolutionSplitRead` 在 append blob bunch 前显式 finish；新增 130 行测试覆盖 merge 一次、finish 后拒绝 add、gap/overlap/row-id pushdown。 | 1.4 BlobBunch 实现不同且已增量维护 row_count，低影响。 |
| 2 | `e818190e4` | Core global index | `CoreOptions` 文档标注 `global-index.thread-num` 必须大于 0；`GlobalIndexReadThreadPool.getExecutorService` 增加 `checkArgument(threadNum > 0)`；新增 0/-1 参数测试。 | 1.4 缺少该线程池类，但同名 option 仍进入 `ManifestReadThreadPool`，建议语义回摘。 |
| 3 | `5652c0023` | Flink test | 删除 `AppendOnlyTableITCase` 中不稳定的 unaware bucket 流读测试，新增独立 `UnawareBucketSplitShuffleITCase`，用 MiniCluster 指标确认 4 个 reader subtask 均有输入。 | 测试-only，无生产影响。 |
| 4 | `095a1d2ea` | LocalKvDb / SST / cache | 大规模重构 `SimpleLsmKvDb` 为 `LocalKvDb`；新增 async/sync compactor、`LsmLevels` 读写锁快照、Bloom filter dynamic/fixed builder、Caffeine-only cache、4KB block 默认值；`BlockIterator.seekTo` 改为 position-based，修复 exhausted 后 seek。 | `BlockIterator` 小修正和 SimpleLsm 失败语义值得关注；其余多数是 master 性能/架构演进。 |
| 5 | `881e92439` | Core cache | `CacheManager` 支持 off-heap `MemorySegment`，实现 `AutoCloseable` 并在 close 时 invalidate data/index cache；新增 off-heap cache 测试。 | 1.4 无 `createOffHeap` 调用，功能增强。 |
| 6 | `6695d6c6f` | Core compression | ZSTD 解压改用 `Zstd.decompressByteArray(dst, ...)`，捕获 `ZstdException`，检查 `Zstd.isError` 和超大返回值。 | 1.4 同文件仍为 stream 实现，性能候选。 |
| 7 | `dbc0c024b` | Core lookup state | 抽出 `StateUtils.createBulkLoadSorter`，`RocksDBState.createBulkLoadSorter` 标记 deprecated 并委托新工具。 | 重构-only，低影响。 |
| 8 | `2e8f69a15` | LocalKvDb reliability | `flushMemTable` 改为先写 SST 并 `levels.addLevelZeroFile`，成功后才清空 memtable；`writeMemTableToSst` 失败时关闭 writer、删除新文件、保留 memtable。 | 1.4 `SimpleLsmKvDb` 有同类失败丢数据风险，建议语义回摘。 |
| 9 | `59003b520` | LocalKvDb bulk load | 新增 streaming `BulkLoadWriter`，支持 unknown entry count、单 writer 活跃保护、sorted/entry-count 校验、失败清理已生成 SST；旧 `bulkLoad(iterator,numEntries)` 改为基于 writer。 | 1.4 旧 `bulkLoad(iterator)` 不同，属于增强；可结合 SimpleLsm 可靠性单独评估。 |
| 10 | `5f6876c02` | Flink vector search | `vector_search` procedure 增加 filter/partition/search score 输出校验；支持 distributed builder；新增 Flink primary-key 和 data-evolution vector read 实现及大量 IT/unit test。 | 1.4 旧 procedure 存在，但主要是 feature，不作为 bugfix 候选。 |
| 11 | `5c45919ae` | LocalKvDb range iterator | 新增 `rangeIterator/rangeScan/forEachInRange`，按 newest-wins 合并 memtable 与 SST，阻止 iterator 打开期间修改/关闭 DB；`LsmLevels` 增加 range snapshot。 | master 新能力，1.4 无 LocalKv-backed state 依赖。 |
| 12 | `e599991d8` | LocalKvDb merge / TTL | 新增 `MergeOperator`、`RecordCombiningWriter`、expired value predicate；compaction 支持 partial/full TTL 处理和逻辑记录合并。 | master 新能力，低直接影响。 |
| 13 | `0e3f4461c` | Hive catalog | `listPartitions` 先统一获得 partitions，再在 metastore partitioned + mark-event 时逐分区调用 HMS event 状态并复制 `Partition` 的 done 字段；新增 HiveCatalogTest。 | 1.4 同路径同语义缺口，高影响候选。 |
| 14 | `81c935eda` | LocalKv-backed lookup state | 新增 `LocalKvStateFactory`、value/list/set state、bulk loader、composite key、value codecs，并调整 tombstone merge；测试覆盖 cache、TTL、bulk load、list/set/value state。 | master 新存储后端能力，1.4 无同名 local state factory。 |
| 15 | `9ad20d997` | Spark postpone bucket | Spark writer 增加 `preparePostponeBucketAssignment`、按 partition 收集行数/序列化大小、按 row-count/size 推断 bucket 数、overwrite 忽略旧 postpone 行数、finally unpersist；新增覆盖 size/row-count/overwrite/cap 的 Spark UT。 | 1.4 有旧写入路径，功能回摘需产品判断；不属于直接 bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，本轮开始工作区干净。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功，fork 自动化分支已刷新。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | 成功，`upstream/master=9ad20d997858c124ae33654f6a1fa694dc588211`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功，`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git rev-list --first-parent --reverse 45cf5ffb0033..upstream/master` | 15 个 first-parent commit。 |
| `git diff --shortstat 45cf5ffb0033..upstream/master` | 89 files，`+10560/-2011`。 |
| `git show --stat --name-status --patch <commit>` / `git show --patch --stat <commit>` | 已为 15 个提交生成并检查 patch，缓存于 `/private/tmp/paimon-daily-2026-07-28-patches`。 |
| `git cherry -v upstream/release-1.4 upstream/master 45cf5ffb0033` | 15 个提交全部为 `+`，未发现 release-1.4 等价 patch。 |
| per-candidate `git cherry -v` / `git patch-id --stable` | 已对 7 个候选提交记录 cherry 状态和 stable patch-id。 |
| release-1.4 targeted `git ls-tree` / `git grep` / `git show` | 已核验候选生产文件存在性和旧分支代码路径。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报和推进 state。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `9ad20d997858c124ae33654f6a1fa694dc588211`
- `last_processed_commit_date`: `2026-07-28 12:07:45 +0800`
- `last_report_date`: `2026-07-28`
- `last_report_path`: `reports/paimon-master-daily/2026-07-28.md`

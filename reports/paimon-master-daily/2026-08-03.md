# Apache Paimon Master Daily Report - 2026-08-03

## Summary

范围：`582d2d1f15d5048ce51735cdfa16a0cec87ee84d..64a4ceb7a9832c49a94058b7bf5ac667af99f15f`，共 1 个 first-parent 提交，3 个文件变更，`+110/-3`。本轮 `upstream/master` 前进到 `64a4ceb7a983`（2026-08-03 13:13:29 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

实际 patch 集中在 core merge-tree compaction：`UniversalCompaction.forcePickL0` 在强制 L0 compact 时，先把紧邻的 L1 run 纳入初始候选，再用 L0+L1 的合计大小判断是否继续合并 L2，避免旧逻辑只按 L0 大小判断而提前输出到 L1。两个测试覆盖了抽象 compaction 策略和 primary-key table 的实际文件层级结果。

`git cherry` 对 `upstream/release-1.4` 的结果为 `+`，stable patch-id 为 `ce44426b290dbe0c387557214d39da8b4ea3c452`，未发现等价补丁已合入。`release-1.4` 的 `UniversalCompaction.forcePickL0` 仍是旧逻辑，`ForceUpLevel0Compaction` 仍调用同一路径；因此这是一个明确的 1.4 runtime correctness backport 候选，但影响只在启用 `compaction.force-up-level-0` 或相关强制 L0 compact 路径时触发。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core merge-tree compaction | 1 | `UniversalCompaction.forcePickL0` 将 L1 作为强制 L0 compact 的初始候选，让 size-ratio 能基于 L0+L1 判断是否继续选择 L2；避免 L0 很小、L1/L2 接近时只 compact 到 L1。 | 1.4 同名生产代码仍是旧逻辑，`ForceUpLevel0Compaction` 调用路径存在；建议评估窄 backport。 |
| Core compaction/table tests | 1 | `ForceUpLevel0CompactionTest` 新增 L0+L1 是否考虑 L2 的策略级用例；`PrimaryKeySimpleTableTest` 新增写入、full compact、普通 compact 后强制 L0 compact 的端到端层级断言。 | 1.4 缺少这些覆盖；若 backport，应同步迁移等价测试。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact / action |
| --- | --- | --- | --- | --- |
| `64a4ceb7a9` | 修复 forced L0 compaction 的 level selection：旧逻辑只把 L0 作为初始 candidate，导致 L0 过小时不能用 L0+L1 的合计大小触发与 L2 的 size-ratio 合并，可能输出到 L1 并留下可合并的 L2。 | stable patch-id `ce44426b290d`；`git cherry -v upstream/release-1.4 64a4ceb7a9 64a4ceb7a9^` 为 `+`。 | 1.4 `UniversalCompaction.forcePickL0` 仍直接 `pickForSizeRatio(numLevels - 1, runs, candidateCount, true)`；`ForceUpLevel0Compaction` 在无 `maxCompactInterval` 或计数触发时仍调用该方法。`git grep` 确认 1.4 测试和配置路径中存在 `compaction.force-up-level-0`。 | 中；这是生产 compaction 选择正确性问题，但只影响启用 forced L0 compact 的表或触发路径。建议作为 1.4 窄 backport 候选，至少迁移策略级测试；如测试依赖差异允许，再迁移 primary-key table 场景。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `64a4ceb7a9` | Core merge-tree compaction | `UniversalCompaction.forcePickL0` 先收集所有 L0 run；若下一个 run 是 L1，则把 L1 也计入初始 `candidateCount`，再调用 `pickForSizeRatio(..., forcePick=true)`。新增策略级用例验证 `(L0=1,L1=99,L2=100)` 会选择 3 个文件输出 L2，而 `(L2=102)` 只选择 L0+L1 输出 L1；新增 primary-key table 用例验证实际 compact 后只剩一个 L2 文件且 row count 为 2100。 | 1.4 同路径旧逻辑存在且未 cherry-pick。属于 runtime compaction correctness 候选；不涉及文件格式或 public API，但会改变触发场景下的 compact 输出层级和读/空间放大表现。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，开始时工作区干净，并与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功刷新 fork 自动化分支引用。 |
| `git fetch upstream master:refs/remotes/upstream/master release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/master` 从 `582d2d1f15` 更新到 `64a4ceb7a9`，`release-1.4` 无新输出。 |
| `git rev-parse HEAD origin/codex/paimon-master-daily-report upstream/master upstream/release-1.4` | `HEAD=50e8bf10d177`；`origin/codex/paimon-master-daily-report=50e8bf10d177`；`upstream/master=64a4ceb7a983`；`upstream/release-1.4=05d1ba75a97c`。 |
| `git log --first-parent --reverse 582d2d1f15..upstream/master` | 1 个 first-parent commit：`64a4ceb7a9`。 |
| `git show --shortstat --oneline 64a4ceb7a9` / `git show --numstat --format= 64a4ceb7a9` | 3 files，`+110/-3`；生产文件 `UniversalCompaction.java` 为 `+12/-3`，其余为测试新增。 |
| `git show --stat --name-status --patch --find-renames 64a4ceb7a9` | 已确认改动文件列表；另用 `git show --stat --patch --find-renames --format=fuller 64a4ceb7a9 -- <paths>` 检查完整 patch hunks。 |
| `git cherry -v upstream/release-1.4 64a4ceb7a9 64a4ceb7a9^` | `+ 64a4ceb7a9`，未发现 release-1.4 等价 patch。 |
| `git show 64a4ceb7a9 --patch \| git patch-id --stable` | stable patch-id `ce44426b290dbe0c387557214d39da8b4ea3c452`。 |
| `git show upstream/release-1.4:paimon-core/src/main/java/org/apache/paimon/mergetree/compact/UniversalCompaction.java` | 确认 1.4 `forcePickL0` 仍是旧逻辑。 |
| `git grep -n "forcePickL0\\|COMPACTION_FORCE_UP_LEVEL_0\\|compaction.force-up-level-0" upstream/release-1.4 -- paimon-core/src/main/java paimon-core/src/test/java` | 确认 1.4 存在 forced L0 compact 调用和配置/测试路径。 |
| Build / unit tests | 未运行 Maven 测试；本次只生成日报、推进 state 并发布自动化分支。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `64a4ceb7a9832c49a94058b7bf5ac667af99f15f`
- `last_processed_commit_date`: `2026-08-03 13:13:29 +0800`
- `last_report_date`: `2026-08-03`
- `last_report_path`: `reports/paimon-master-daily/2026-08-03.md`

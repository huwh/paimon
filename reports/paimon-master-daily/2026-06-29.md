# Apache Paimon Master Daily Report - 2026-06-29

## Summary

范围：`7e22e56330af73c3de7747f760fb6a4a8af42d1d..d7c5d42c469d4d63a17114ac0883133635545aae`，共 8 个 first-parent 提交。当前 Apache upstream `master` head 是 `d7c5d42c469d`（2026-06-27 15:33:27 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`。

本次主线重点集中在 vector/global-index 搜索能力、format table partition pruning、data evolution row sidecar、CDC IT 稳定性、`VectorType.defaultSize()` 溢出保护，以及一次 `LookupMergeFunction` 行为回滚。直接 bugfix 候选主要是 `9636197fd`、`7eaa78501`、`d6662001a`；`fab87e8ad` 是测试稳定性，`d7c5d42c4` 对 `release-1.4` 不需要回补，因为 release 分支没有引入被回滚的 `setSequenceComparator` 行为。

`release-1.4` 方面，8 个提交经 `git cherry -v upstream/release-1.4 upstream/master 7e22e56330af73c3de7747f760fb6a4a8af42d1d` 检查均为 `+`，未发现等价 patch 已 cherry-pick。逐项检查 production path 后，建议优先关注 format table 的 `AndPartitionPredicate` pruning、`VectorType.defaultSize()` overflow、vector search partition filter 覆盖问题；row sidecar / MRR / refine factor 属于 master 新能力，默认不改变 1.4 行为。

所有 commit 都先用 `git show --stat --name-status --patch <commit>` 查看文件状态；由于当前 Git 输出在带 `--name-status` 时不会显示 patch hunks，实际代码审查又补跑了相邻 first-parent tree 的 `git diff --patch <prev> <commit>`。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Vector / global-index search | 3 | `976396b39` 新增 hybrid search `mrr` ranker；`b1daaa197` 新增 `ivf.refine_factor` indexed candidate rerank；`d6662001a` 修复 vector search builder partition filter 被覆盖的问题。 | `d6662001a` 是 1.4 候选 bugfix；`mrr` / refine factor 是新能力。1.4 有 `VectorSearchBuilderImpl`，但没有 `BatchVectorSearchBuilderImpl`。 |
| Core format table scan | 1 | `9636197fd` 递归提取 `AndPartitionPredicate` 中的底层 `Predicate`，使 format table 无 manifest 场景也能组合 static pushdown 和 runtime/DPP filter 做目录裁剪；`PartitionPathUtils` 改为捕获 `FileNotFoundException`。 | 1.4 仍只处理 `DefaultPartitionPredicate`，bug 路径存在，建议评估回补。 |
| Core data evolution / row sidecar | 1 | `3c0fab6af` 新增 `data-evolution.row-sidecar.*` 配置、写入 `.row` sidecar、稀疏 row-id 读取时按阈值选择 sidecar，并同步 PyPaimon 读写。 | 可选性能/读路径新能力，默认关闭，不是 1.4 既有 bugfix。 |
| Flink CDC tests | 1 | `fab87e8ad` 调整 MySQL newly-added table IT 时序，等待期间检查 Flink job 是否已进入 terminal state，避免测试静默等超时。 | 只改测试代码，不影响 release runtime。 |
| API types | 1 | `7eaa78501` 将 `VectorType.defaultSize()` 从直接乘法改为 `MathUtils.multiplySafely`，避免 int overflow。 | 1.4 同文件仍是直接乘法，且 `MathUtils.multiplySafely` 已存在；是低频但直接的 API bugfix 候选。 |
| Core lookup merge | 1 | `d7c5d42c4` 回滚 `LookupMergeFunction` 用 `sequence.field` 选择 high-level record 的逻辑，恢复按最小 level 选择 high-level record，并补 Flink 聚合/DV 用例。 | 1.4 没有原始错误提交 `a84c3826b`，当前 release 代码已经是回滚后的行为；不需要回补。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `9636197fd` | Format table 在 `PartitionPredicate.and(...)` 组合静态 partition pushdown 与 runtime/DPP filter 时，原实现无法从 `AndPartitionPredicate` 提取底层 `Predicate`，会退化为从表根目录遍历。 | patch-id `5b50524fbfec`; cherry `+`。 | 1.4 `FormatTableScan.findPartitions()` 仍只识别 `DefaultPartitionPredicate`，`computeScanPathAndLevel` 参数仍是 `PartitionPredicate`；`PartitionPredicate.AndPartitionPredicate` 存在但无 `predicates()` 暴露。 | 高。影响 format table 的分区目录裁剪，云存储上可能明显放大 list 成本；建议回补。 |
| `fab87e8ad` | CDC newly-added table IT 在等待结果或表创建时，如果 job 已终止，改为立即带 job 异常失败；同时调整测试写入/等待顺序。 | patch-id `cbe68a69d5c1`; cherry `+`。 | 只触及 `paimon-flink-cdc/src/test/java`。 | 低。测试稳定性修复，不是 release runtime bugfix。 |
| `7eaa78501` | `VectorType.defaultSize()` 对 `elementType.defaultSize() * length` 做 safe multiply，溢出时返回 `Integer.MAX_VALUE`。 | patch-id `63d95ca4f223`; cherry `+`。 | 1.4 `VectorType.defaultSize()` 仍直接乘法；`MathUtils.multiplySafely` 已在 1.4 存在。 | 中。只在超大 vector length 下触发，但这是 public API 类型大小估算的直接 overflow 修复，回补成本低。 |
| `d6662001a` | Vector search 同时调用 `withPartitionFilter(...)` 和 `withFilter(...)` 时，原实现会用从 data predicate 提取出的 partition predicate 覆盖已有 partition filter；现在改为 AND 组合，并保留非分区 data filter。 | patch-id `6c79ca354dcf`; cherry `+`。 | 1.4 `VectorSearchBuilderImpl.withFilter()` 仍调用 `splitPartitionPredicate(...).ifPresent(value -> this.partitionFilter = value)` 覆盖旧值；`BatchVectorSearchBuilderImpl` 在 1.4 不存在。 | 中。影响 partitioned vector search 的过滤正确性；若 1.4 暴露 vector search，应按单 builder 路径适配回补。 |
| `d7c5d42c4` | 回滚 high-level record 按 `sequence.field` 选择的逻辑，避免 lookup changelog / aggregation 场景违背 level 语义。 | patch-id `e4919620a44b`; cherry `+`。 | 1.4 `LookupMergeFunction.pickHighLevel()` 已按最小 level 选择，`LookupChangelogMergeFunctionWrapper` 没有调用 `setSequenceComparator`；`git log upstream/release-1.4 --grep='LookupMergeFunction'` 未包含被回滚的 `a84c3826b`。 | 无需回补。release-1.4 当前已经保持回滚后的行为。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `976396b39` | Vector / hybrid search | `HybridSearchRanker` 增加 `MRR_RANKER`，按每 route 的 rank 贡献 `weight / (rank + 1)` 聚合；PyPaimon `HybridSearchBuilderImpl` 增加同等 `_mrr` 路径；Spark TVF 文档和测试接受 `"mrr"`。 | 新 ranker 能力；不是 1.4 bugfix。 |
| 2 | `b1daaa197` | Vector refine rerank | `AbstractVectorRead` 增加 `indexedSearchLimit` 与 `maybeRerankIndexedResult`，按 `refine_factor` 扩大 indexed top-k 后用 raw vectors 重排；Spark distributed path 在 global merge 后 rerank；PyPaimon 增加同等选项解析和 batch rerank；默认未配置时不启用。 | 新可选能力；默认行为不变，低优先级。 |
| 3 | `9636197fd` | Format table pruning | `FormatTableScan.extractPartitionPredicate` 递归展开 `DefaultPartitionPredicate` / `AndPartitionPredicate`，`computeScanPathAndLevel` 改用 `Optional<Predicate>`；测试覆盖 static filter + runtime filter 组合后只列出目标 partition。 | 直接 bugfix 候选，建议回补。 |
| 4 | `3c0fab6af` | Data evolution row sidecar | `CoreOptions` 增加 row sidecar 开关和 sparse-read 阈值；`RowDataFileWriter` 抽象 auxiliary writers 同时写 data file index 与 `.row` sidecar；`DataEvolutionSplitRead` 在 sparse row range 且阈值满足时读 sidecar；PyPaimon 同步写 extra file、读 sidecar、缺失字段补 null/system field。 | 新能力，默认关闭；不作为 1.4 bugfix。 |
| 5 | `fab87e8ad` | Flink CDC IT | `CdcActionITCaseBase.waitForResult` 接受 `JobClient` 并在循环中检测 terminal state；newly-added table IT 先写初始数据、等待源表可见，再创建新表并等待 Paimon 表创建。 | 测试稳定性，release runtime 无影响。 |
| 6 | `7eaa78501` | API VectorType | `VectorType.defaultSize()` 使用 `MathUtils.multiplySafely`；新增 `VectorTypeTest` 覆盖正常大小与 `Integer.MAX_VALUE` 维度。 | 直接 bugfix 候选，回补成本低。 |
| 7 | `d6662001a` | Vector search filters | `VectorSearchBuilderImpl` / `BatchVectorSearchBuilderImpl` 使用 `splitPartitionPredicatesAndDataPredicates`，将 partition predicate 与已有 partition filter 做 AND，非分区 predicate 单独留在 `filter`；测试覆盖调用顺序和 batch/single 路径。 | 1.4 单 vector builder 存在同类覆盖问题；batch 路径不存在，需适配。 |
| 8 | `d7c5d42c4` | Lookup merge revert | 删除 `LookupMergeFunction.setSequenceComparator` 和按 sequence 排序 merge 的逻辑，恢复 high-level record 只按最小 level 选择；新增 Flink lookup changelog aggregation / DV out-of-order sequence 用例。 | release 已是该行为，无需回补。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 成功；本地 `codex/paimon-master-daily-report` 与 `origin/codex/paimon-master-daily-report` 一致。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=d7c5d42c469d4d63a17114ac0883133635545aae`，`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git rev-list --first-parent --reverse 7e22e56330af73c3de7747f760fb6a4a8af42d1d..upstream/master` | 8 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 8 个 commit 均已执行，用于文件状态检查入口。 |
| `git diff --patch <prev> <commit>` | 对 8 个 commit 均按 first-parent 相邻 tree 补跑并检查实际 hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master 7e22e56330af73c3de7747f760fb6a4a8af42d1d` | 8 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对 8 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 `FormatTableScan`、`PartitionPredicate`、`VectorType`、`VectorSearchBuilderImpl`、`LookupMergeFunction`、`LookupChangelogMergeFunctionWrapper` 等触点。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `d7c5d42c469d4d63a17114ac0883133635545aae`
- `last_processed_commit_date`: `2026-06-27 15:33:27 +0800`
- `last_report_date`: `2026-06-29`
- `last_report_path`: `reports/paimon-master-daily/2026-06-29.md`

# Apache Paimon Master Daily Report - 2026-06-23

## Summary

范围：`84d5acbea9716ba9fa3e0edd87c7175f650b2be0..eae7d22ff4c7ad7e5885bf02d421aff340f79178`，共 17 个 first-parent 提交。当前 Apache upstream `master` head 是 `eae7d22ff4c7`（2026-06-23 14:00:07 +0800），`release-1.4` 对照 head 是 `53815aebfd8d`。

本次主线变化集中在 full-text/global-index 搜索能力、Parquet VECTOR 读写、format table 小文件 split packing、MAP shared-shredding 准备工作、Flink CDC 恢复进度、Iceberg metadata、PyPaimon CLI/写入/向量检索，以及几项文档和测试稳定化。

`release-1.4` 方面，17 个提交经 `git cherry -v upstream/release-1.4 upstream/master 84d5acbea9716ba9fa3e0edd87c7175f650b2be0` 检查均为 `+`，未发现等价 patch 已 cherry-pick。需要优先关注 7 个生产修复：Parquet VECTOR 支持、global-index top-k tie-breaking、Iceberg parent snapshot id、CDC schema event checkpoint、PyPaimon duplicate-key upsert、BTree empty-string key NPE、nested update agg count-limit。`a3863cc6a5b1` 的 hybrid full-text options fail-fast 在 `release-1.4` 缺少 `HybridSearchRoute` 路径，当前不直接适用。

所有 commit 都用 `git show --stat --name-status --patch` 查看文件状态，并补跑 `git show --stat --patch` 读取实际 hunks，因为当前 Git 输出在带 `--name-status` 时未显示 patch hunks。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Full-text / global-index / hybrid search | 4 | `f4c9efe2e` 支持 full/detail full-text raw fallback；`8b2911b5b` 修正 scored top-k tie-breaking；`8b2844ae2` 修正 btree/sorted meta empty string key NPE；`a3863cc6a` 禁止未消费的 full-text hybrid route options。 | top-k 和 btree empty-string 修复直接相关；full-text full/detail 与 hybrid route options 在 release-1.4 缺少前置新 API，需条件评估。 |
| Parquet / VECTOR | 1 | `6342ba880` 将 VECTOR 作为 Parquet list 读写，新增 `CastedVectorColumnVector`，并校验向量长度。 | release-1.4 已有 `VectorType`，但 Parquet converter/writer/reader 缺 VECTOR 分支；是明确兼容性修复候选。 |
| Format table scan | 1 | `8b589d42d` 让 `FormatDataSplit` 可包含多个 file/range，并由 `FormatTableScan` 按 target size packing，reader 用 `ConcatRecordReader` 串读。 | release-1.4 仍是一文件一 split；偏性能/调度优化，不是 correctness bugfix。 |
| MAP shared-shredding | 1 | `23fca3cd1` 新增 MAP storage layout option、shared-shredding schema/metadata 工具与 schema validation。 | release-1.4 无相关配置和工具；新能力，不建议小范围回补。 |
| Procedures | 1 | `25b7b0d20` 为 Flink/Spark `drop_global_index` 增加 `dry_run`，Spark 返回 dropped file count。 | release-1.4 无 dry_run；功能增强，可选回补。 |
| Iceberg metadata | 1 | `17bac206c` 在 `createMetadataWithoutBase` 中为非首个 snapshot 写 parent snapshot id。 | release-1.4 同路径仍写 `null`；明确 bugfix 候选。 |
| Flink CDC source | 1 | `fa57a21eb` 为 CDC split/checkpoint 增加 schema event progress，恢复后跳过已发出的 schema change events。 | release-1.4 同路径只保存 data row `recordsToSkip`；明确恢复语义修复候选。 |
| PyPaimon | 4 | `ca5d7d18f` 新增 branch CLI；`ed1c8fbff` duplicate-key upsert 更新所有匹配行；`f23855a8a` Lumina native batch vector search；`f4c9efe2e` Python full-text full/detail raw fallback fail-fast。 | duplicate-key upsert 直接相关；branch CLI/Lumina/full-text full-detail 多为新能力或缺前置路径。 |
| Docs / tests | 3 | `44489c296` 补 BloomFilter 类型/哈希说明；`b8726cf35` 明确空 partition 目录默认不删；`71b815d6f` 稳定 Spark row-tracking 并发测试 retry。 | docs/test-only；无 runtime backport 压力。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `6342ba880` | Parquet 读写 `VECTOR`，避免向量未单独存储时不识别/UnsupportedOperationException，并校验长度。 | patch-id `41bf31ad...`；cherry `+`。 | release-1.4 有 `VectorType`，但 `ParquetSchemaConverter` 只处理 ARRAY，`ParquetRowDataWriter` 只对 ARRAY 创建 list writer。 | 高。VECTOR + Parquet 用户会受影响，回补相对集中。 |
| `8b2911b5b` | `ScoredGlobalIndexResult.topK` 在同分边界按 rowId 升序稳定保留候选。 | patch-id `7b026e5d...`；cherry `+`。 | release-1.4 仍只按 score 建 min-heap，并只在新 score 严格更高时替换。 | 中。影响 vector/full-text scored index top-k 的确定性和边界正确性。 |
| `17bac206c` | Iceberg `createMetadataWithoutBase` 为非首个 snapshot 写 parent snapshot id。 | patch-id `7744b632...`；cherry `+`。 | release-1.4 同方法构造 `IcebergSnapshot` 时 parent snapshot id 仍为 `null`。 | 高。影响 Iceberg metadata lineage，建议回补。 |
| `fa57a21eb` | CDC source reader 独立 checkpoint schema change event progress，恢复后不重复发 schema events。 | patch-id `8cf7c0f9...`；cherry `+`。 | release-1.4 `TableAwareFileStoreSourceSplit` 无 schema event skip 字段，reader 只用 `recordsToSkip` seek data rows。 | 高。Flink CDC 作业在 schema event 后失败恢复可能重复发 schema changes。 |
| `ed1c8fbff` | PyPaimon `upsert_by_key` 对 append-only duplicate keys 更新所有匹配 `_ROW_ID`，而不是最后一个。 | patch-id `b247398e...`；cherry `+`。 | release-1.4 仍是 `key_to_row_id: Dict[_KeyTuple, int]` 单 row id 映射。 | 中到高。Python 写入 duplicate-key 表会留下旧值。 |
| `8b2844ae2` | btree/sorted file meta selector 对 null first/last key 安全比较，避免空字符串 key 被 zero-length metadata 误判后 NPE。 | patch-id `8080a1c1...`；cherry `+`。 | release-1.4 等价旧路径是 `BTreeFileMetaSelector`/`BTreeIndexMeta`；`BTreeIndexMeta.deserialize` 将 key length 0 读成 `null`，selector 直接 `deserialize(meta.getFirstKey())`。 | 高。STRING btree/global-index 空字符串 key 场景可触发查询异常。 |
| `eae7d22ff` | nested update agg 在 count limit 达到后仍允许按 nested key 更新已有 key，limit 只限制新 key。 | patch-id `2a582256...`；cherry `+`。 | release-1.4 `FieldNestedUpdateAgg.agg` 在 `acc.size() >= countLimit` 时直接返回，早于 keyed merge。 | 高。带 nested-key 和 count-limit 的 agg 会静默丢现有 key 更新。 |
| `a3863cc6a` | full-text hybrid route options 未被执行路径消费，改为 fail-fast。 | patch-id `247a9309...`；cherry `+`。 | release-1.4 缺 `HybridSearchRoute.java` 和相关 Spark/Python hybrid full-text route 新路径。 | 低/不适用。不是 release-1.4 当前代码路径的直接 bugfix。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `f4c9efe2e` | Full-text search | 拆分 `IndexFullTextSearchSplit`/`RawFullTextSearchSplit`，`FullTextScanImpl` 用 `GlobalIndexCoverage.unindexedRanges` 生成 raw ranges，`FullTextReadImpl` 合并 index 与 raw search；Python full/detail raw full-text 暂时 fail-fast；Tantivy searcher pool max size 0 时不复用 shared pool。 | 主要依赖 master 的 `global-index.search-mode` 和新 full-text raw fallback；release-1.4 缺 search-mode，整体条件性。 |
| 2 | `6342ba880` | Parquet / VECTOR | Parquet schema converter、reader factory、reader util、writer 均支持 `VectorType`，新增 readable vector column wrapper，并测试 read/write 和长度不匹配。 | 明确修复候选。 |
| 3 | `8b589d42d` | Format table | `FormatDataSplit` 从单文件改为 file list，scan 端按 open-file-cost/target-size packing，read 端串联多个 file reader，Spark split size/count 使用总量。 | 性能优化；可选。 |
| 4 | `23fca3cd1` | MAP shared-shredding | 新增 `map.storage-layout` 和 `map.shared-shredding.max-columns`，MAP<STRING,T> 校验，logical-to-physical schema 和 metadata 压缩/序列化工具。 | 新能力；不建议小回补。 |
| 5 | `8b2911b5b` | Global-index scored result | top-k heap comparator 改为 weakest-first：score 升序、rowId 降序，并用 comparator 判断替换。 | 直接修复候选。 |
| 6 | `44489c296` | Docs | BloomFilter 支持类型和 hash function 改为表格，并列出不支持类型。 | docs-only。 |
| 7 | `25b7b0d20` | Flink/Spark procedures | `drop_global_index` 增加 dry_run 参数；Flink 返回 preview message；Spark 返回 result + dropped_file_count 并避免空提交。 | 功能增强；可选。 |
| 8 | `b8726cf35` | Docs | 说明 snapshot expiration 默认不删空 partition/bucket 目录，需启用 `snapshot.clean-empty-directories`。 | docs-only。 |
| 9 | `17bac206c` | Iceberg | `createMetadataWithoutBase` 的 Iceberg snapshot parent id 从 `null` 改为首个 snapshot 为 null、其他为 `snapshotId - 1`。 | 直接修复候选。 |
| 10 | `71b815d6f` | Spark tests | Row-tracking 并发测试抽取有界 retry，收窄 snapshot missing 判断并保留最终异常。 | test-only。 |
| 11 | `fa57a21eb` | Flink CDC | split serializer v2 增加 `schemaChangeEventsToSkip` 和 legacy marker；新增 `CDCSourceSplitState`；reader 恢复时跳过已发 schema events。 | 直接修复候选。 |
| 12 | `ca5d7d18f` | PyPaimon CLI | 新增 `paimon branch create/list/delete/rename/fast-forward`，覆盖 JSON list、from-tag、错误映射等测试。 | 新 CLI 能力；release-1.4 仅有 catalog/db/table CLI。 |
| 13 | `a3863cc6a` | Hybrid search | Java/Python/Spark full-text hybrid route 对 non-empty options fail-fast，文档说明 options reserved。 | release-1.4 缺相关 route；不直接适用。 |
| 14 | `ed1c8fbff` | PyPaimon write | key 到 `_ROW_ID` 的映射从单值改为 list，并展开更新所有匹配行；保留 `update_cols` 语义。 | 直接修复候选。 |
| 15 | `8b2844ae2` | Global-index btree/sorted meta | selector 在 first/last key 为 null 时使用开边界语义，避免空字符串 key 反序列化 NPE。 | 直接修复候选，release 旧类名不同但问题等价。 |
| 16 | `f23855a8a` | PyPaimon vector search | 新增 `BatchVectorSearch`，Lumina reader 支持 native batch search，offset reader 和 table read 按 split 批量查询并合并 raw fallback。 | 新能力；release-1.4 缺 Python Lumina/native batch 路径。 |
| 17 | `eae7d22ff` | Core nested update agg | keyed path 改为 map merge：已有 key 总是可更新，count-limit 只拦新 key；no-key path 保持截断行为。 | 直接修复候选。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 成功，本地分支与 `origin/codex/paimon-master-daily-report` 一致。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=eae7d22ff4c7ad7e5885bf02d421aff340f79178`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git log --first-parent --reverse 84d5acbea9716ba9fa3e0edd87c7175f650b2be0..upstream/master` | 17 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 17 个 commit 均已执行，用于文件状态和 patch 检查入口。 |
| `git show --stat --patch <commit>` | 对 17 个 commit 均补跑并检查实际 hunks。 |
| `git patch-id --stable` | 已记录 17 个 commit 的 stable patch-id。 |
| `git cherry -v upstream/release-1.4 upstream/master 84d5acbea9716ba9fa3e0edd87c7175f650b2be0` | 17 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted code checks | 检查 Parquet VECTOR、ScoredGlobalIndexResult、IcebergCommitCallback、CDC split reader/state、PyPaimon upsert、BTreeFileMetaSelector/BTreeIndexMeta、FieldNestedUpdateAgg、HybridSearchRoute、format table、drop_global_index、PyPaimon CLI/Lumina 路径。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `eae7d22ff4c7ad7e5885bf02d421aff340f79178`
- `last_processed_commit_date`: `2026-06-23 14:00:07 +0800`
- `last_report_date`: `2026-06-23`
- `last_report_path`: `reports/paimon-master-daily/2026-06-23.md`

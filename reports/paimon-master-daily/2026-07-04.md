# Apache Paimon Master Daily Report - 2026-07-04

## Summary

范围：`30a13d69e6947419b2914276686d7c079454cf42..f2de0ee193e7991575f4418db44343f35fef92bb`，共 18 个 first-parent 提交。当前 Apache upstream `master` head 是 `f2de0ee193e7`（2026-07-04 12:45:47 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线集中在 PyPaimon multimodal / Ray 数据演进能力、Spark V2 写入和 data-evolution delete、Flink rollback procedure 与 scan.bucket、Core auth scan planning、BLOB metadata 防御、compaction 日志和 nested aggregation 配置。最值得 release-1.4 关注的是 `113b3118` 的 query-auth 扫描规划修复、`ce92c8a1` 的 Spark V2 dynamic overwrite 分区列顺序修复、`250d793a` 的 BlobDescriptor 反序列化防御，以及 `564542f5` 的 gzip 文件句柄泄漏修复。

`git cherry -v upstream/release-1.4 upstream/master 30a13d69e6947419b2914276686d7c079454cf42` 显示本次 18 个提交均为 `+`，未发现等价 patch 已 cherry-pick 到 release-1.4。逐项对照 release 分支代码后，直接候选是 `113b3118`、`ce92c8a1`、`250d793a` 的 BlobDescriptor 部分和 `564542f5`；`0474b87e` 只在 `rollback_to_as_latest` 功能被回补时才有 1.4 影响；`12c51d0d` 依赖 master 新增的 Flink `FunctionDefinitionFactory` 路径，release-1.4 当前没有同一路径。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时不展开 hunks，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 并检查实际补丁。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| PyPaimon multimodal / Ray | 4 | `cc4cd0dd` 增加 row-id scan/search/take API；`1c5743f6` 增加 scan BLOB 并发 range read；`7dc65b9f` 增加 Ray no-shuffle bucket join；`f2de0ee1` 增加 Ray `update_by_row_id`。 | 主要是新 API / 新能力；不作为普通 1.4 bugfix。 |
| Spark writes / data evolution | 2 | `ce92c8a1` 修复 V2 dynamic overwrite 的 Hive-style partition column order；`85dd958b` 为 data-evolution merge 增加 DELETE / NOT MATCHED BY SOURCE DELETE。 | `ce92c8a1` 是 1.4 直接候选；`85dd958b` 是功能扩展。 |
| Core scan / auth / BLOB metadata | 3 | `b2eba89c` 增加 `scan.bucket`；`250d793a` 防御 malformed BlobDescriptor / BlobViewStruct；`113b3118` 将 query-auth row filter 推入 scan planning 并修正 limit/topN。 | `113b3118` 直接影响 1.4 query-auth；`250d793a` 的 BlobDescriptor 部分直接适用。 |
| Flink procedure / function path | 3 | `1d717f01` 增加 `rollback_to_as_latest`；`0474b87e` 为该 procedure 创建保护 tag 防止过期清理；`12c51d0d` 修复 master 新 Flink function factory 的内置函数 classloader。 | rollback procedure 在 1.4 缺失，条件候选；`12c51d0d` 非直接适用。 |
| Core compaction / aggregation / utility | 5 | `992f0b5f` 增加 compaction duration log；`c852c1a6` 优化日志实现；`652ebc88` 修复 CompactTask 构造编译；`1fc7506c` 增加 nested-key-null-strategy；`564542f5` 用 try-with-resources 修复 gzip 资源泄漏。 | `564542f5` 是低风险直接候选；其余主要是新配置、日志或 master 内部修正。 |
| Docs | 1 | `8a04723e` 增加 Flink Kubernetes Operator 运行 MySQL CDC action 示例。 | 文档-only，可按文档需要低风险回补。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `113b3118` | query-auth row filter 原本只在 read-time 包装 split，scan planning 阶段不会按授权 partition filter 裁剪，且 limit / TopN pushdown 可在 read-time auth 之前丢掉可见行；补丁新增 auth partition filter slot、FieldRef by-name remap，并在非 partition auth 下禁止 scan-side limit / TopN。 | stable patch-id `3d52309f`; cherry `+`。 | 1.4 已有 `query-auth.enabled`、`TableQueryAuthResult.convertPlan`、`AbstractDataTableScan.plan()`、`DataTableBatchScan.withLimit/withTopN` 和 read-time `doAuth`；当前 1.4 同样是 plan 后再 `convertPlan`。 | 高。REST Catalog / Flink / Spark 使用 query-auth 的 partition 表、schema evolution 后 field-id 不等于 position、或 limit/topN 查询都可能出现漏行或误剪；建议优先评估回补。 |
| `ce92c8a1` | Spark V2 dynamic overwrite 遇到 `INSERT OVERWRITE ... PARTITION (dt, hr)` 且 SELECT 输出为 Hive-style 分区列后置 / 混合静态分区时，原 resolver 按表列顺序解析，可能拒绝或写错列；补丁在 parser 标记动态分区列，并按 Hive-style 输出重排。 | stable patch-id `3841a9de`; cherry `+`。 | 1.4 有 `spark.paimon.write.use-v2-write`、`PaimonDynamicPartitionOverwrite`、`OverwritePartitionsDynamic` 和 dynamic overwrite tests，但 parser rules 没有 `MarkHiveDynamicPartitionWrite`，analysis 仍走旧 `resolveQueryColumns`。 | 中高。开启 Spark V2 write 且使用 dynamic partition overwrite 时可触发，尤其数据列和分区列输出顺序不等于表 schema 顺序。 |
| `250d793a` | BLOB metadata 反序列化原来信任 length 字段，malformed bytes 可能抛出 `BufferUnderflowException` / `NegativeArraySizeException` 等低层异常；补丁增加最小长度、负长度、剩余 payload 完整性校验。 | stable patch-id `4af40054`; cherry `+`。 | 1.4 有 `BlobDescriptor.deserialize` 且同样直接读 buffer 和 length；但 1.4 缺少 `BlobViewStruct`，只适用 BlobDescriptor 部分。 | 中。生产读 BLOB descriptor 或外部 descriptor 输入时会得到更清晰失败，避免低层异常；建议以 BlobDescriptor 子集回补。 |
| `564542f5` | `CompressUtils.gzipCompressFile` 原来只在正常 EOF 时关闭 `fis` / `gzipOs`，中途 read/write/close 异常会泄漏文件句柄；补丁改为 try-with-resources。 | stable patch-id `a19cd40a`; cherry `+`。 | 1.4 `CompressUtils` 方法与修复前一致，仍手动 close。 | 中低。影响使用 gzip 压缩工具的异常路径；回补简单、风险低。 |
| `0474b87e` | `rollback_to_as_latest` 若从旧 snapshot 恢复文件，后续 snapshot expiration 可能清掉恢复文件；补丁为 snapshot_id rollback 创建 non-expiring protection tag，并在失败时清理未提交 tag。 | stable patch-id `0d15a333`; cherry `+`。 | 1.4 有 `TableCommitImpl` / expiration 机制，但没有 `RollbackToAsLatestProcedure` 和 `TableCommitImpl.rollbackToAsLatest`。 | 条件影响。若准备把 `1d717f01` 的 rollback_to_as_latest 回补到 1.4，应同时带上该修复；否则当前 1.4 无直接路径。 |
| `12c51d0d` | master 新 `FunctionDefinitionFactory` 路径实例化内置函数时使用 context classloader，可能找不到 Paimon connector 内置函数类；补丁对 `BuiltInFunctions` 改用 `FlinkCatalog.class.getClassLoader()`。 | stable patch-id `51431b0b`; cherry `+`。 | release-1.4 `FlinkCatalog` 没有 `getFunctionDefinitionFactory` / `UserDefinedFunctionHelper.instantiateFunction` 路径，内置函数仍通过 `getFunction` 返回 class name。 | 不直接适用。只有先回补 master 的 function factory 路径时才需要该修复。 |
| `652ebc88` | `c852c1a6` 改了 `CompactTask` 构造签名后，append / clustering compact task 未同步传参；补丁补上空 bucketInfo 并修正 `FileRewriteCompactTask` super 调用。 | stable patch-id `e82b5ad4`; cherry `+`。 | 1.4 没有本次 `992f0b5f` / `c852c1a6` 的构造签名变化。 | 不单独适用。只有回补 compaction log 改造时才需要。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `cc4cd0dd` | PyPaimon multimodal row-id | `ScanQuery.with_row_id()` 将 `_ROW_ID` 加入 projection；`MultimodalTable.scan/search/take_row_ids` 支持 snapshot/tag time travel；新增 row-id manifest / take tests。 | 新 API；非直接 1.4 bugfix。 |
| 2 | `ce92c8a1` | Spark V2 dynamic overwrite | `PaimonAnalysis` 新增 Hive dynamic partition marker、分区列输出重排和 analyzer transform 调整；parser rule 标记 `InsertIntoStatement` 的动态分区列；测试覆盖 V2 dynamic overwrite 分区列乱序。 | 直接候选。 |
| 3 | `12c51d0d` | Flink function classloader | `CatalogAwareFunctionDefinitionFactory.createFunctionDefinition` 对 `BuiltInFunctions.FUNCTIONS` 使用 `FlinkCatalog.class.getClassLoader()`。 | release-1.4 缺同一路径，非直接适用。 |
| 4 | `8a04723e` | Docs / CDC | MySQL CDC 文档新增 Flink Kubernetes Operator 运行 action jar 的 image / `FlinkDeployment` 示例，并提醒 Secrets / Hadoop Hive 配置。 | 文档-only。 |
| 5 | `250d793a` | Common BLOB metadata | `BlobDescriptor` / `BlobViewStruct` deserialize 增加 payload 长度、magic、identifier/URI length、offset/rowId 完整性校验；新增 malformed payload tests。 | BlobDescriptor 部分直接候选；BlobViewStruct 在 1.4 缺失。 |
| 6 | `564542f5` | Core utility | `gzipCompressFile` 从手动 close 改成 try-with-resources，异常路径自动关闭 input/output/gzip stream。 | 直接低风险候选。 |
| 7 | `992f0b5f` | Core compaction log | `CompactTask` 增加 start/finish/failure 日志和 duration/input/output metrics；write restore 错误加 partition/bucket context；Flink coordinator 错误信息一度增加 partition info。 | 主要可观测性；非必要 1.4 bugfix。 |
| 8 | `1d717f01` | Flink rollback procedure | 新增 core `rollbackToAsLatest`，通过 delta files materialize target snapshot 为新 latest；Flink `rollback_to_as_latest` procedure 支持 snapshot/tag；保留 later snapshots/tags。 | 新功能；若回补需同时考虑 `0474b87e`。 |
| 9 | `b2eba89c` | Core / Flink scan.bucket | 新增 `scan.bucket` option，在 batch/stream scan 对 fixed-bucket PK 表指定单 bucket manifest pruning，并校验非 PK、dynamic/postpone/out-of-range 场景。 | 新 option；非 bugfix。 |
| 10 | `c852c1a6` | Core compaction log | 将 compaction log info 改为构造期 `bucketInfo`，移除 `WriteRestore` context overload 和 Flink coordinator 临时 partitionInfo；测试构造同步调整。 | 后续优化；非直接 1.4 bugfix。 |
| 11 | `652ebc88` | Core compile fix | 为 append compact、append cluster、clustering anonymous CompactTask 补 `bucketInfo` 参数，并修正 `FileRewriteCompactTask` super 调用。 | 只随 compaction log 改造适用。 |
| 12 | `85dd958b` | Spark data-evolution delete | `MergeIntoPaimonDataEvolutionTable` 支持 matched DELETE 和 not matched by source DELETE，生成/合并 deletion vectors；`PaimonRowLevelCommand` 复用 data-evolution DV 构建；新增删除语义测试。 | 功能扩展；非小型 bugfix。 |
| 13 | `1c5743f6` | PyPaimon scan BLOB reads | `scan().read_blobs/stream_blobs` 以 `blob-as-descriptor=true` 读取 descriptor，通过 `FileIO.read_ranges_coalesced` 并发读取 bytes，支持 row-aligned scalar table 和 stream reader close。 | 新 Python API / 性能能力。 |
| 14 | `7dc65b9f` | PyPaimon Ray bucket join | 新增 `bucket_join`，校验 fixed bucket、bucket-key、bucket-function、key type、partitioned table 限制，按 bucket 分发 Ray task 并避免全局 shuffle。 | 新 Ray API。 |
| 15 | `1fc7506c` | Core nested aggregation | 新增 `fields.<field>.nested-key-null-strategy=<merge|ignore|error>`，在 `FieldNestedUpdateAgg` agg/retract 时处理 nested key null，并强化 nested-sequence-field 依赖校验。 | 新配置；默认 `merge` 保持旧行为。 |
| 16 | `0474b87e` | Core/Flink rollback protection | `rollback_to_as_latest` snapshot_id 路径创建 non-expiring protection tag；`TableCommitImpl.rollbackToAsLatest` 改接收 `Tag`；失败未提交时删除临时 tag；补 expiration 测试。 | 条件候选，需随 rollback feature。 |
| 17 | `113b3118` | Core query-auth scan planning | 将 auth predicate by-name remap 后拆分 partition/data predicate，partition 部分推入 manifest filter；非 partition auth 禁止 scan-side limit/topN；ReadBuilder 在 auth 下 read-side limit；补 REST/Flink/Spark tests。 | 直接高优先级候选。 |
| 18 | `f2de0ee1` | PyPaimon Ray update_by_row_id | 新增 `update_by_row_id`，要求 data-evolution + row-tracking，拒绝 DV/blob/partition column/table-name source，按 `_ROW_ID` 定位文件并只重写相关文件；planner pin base snapshot。 | 新 Ray API；非普通 1.4 bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净且本地跟踪 ref 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | sandbox DNS 失败后提权重跑成功。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | sandbox DNS 失败后提权重跑成功；`upstream/master` 从 `30a13d69e` 前进到 `f2de0ee19`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 提权重跑成功；`release-1.4` 保持 `05d1ba75a97c`。 |
| `git rev-parse upstream/master upstream/release-1.4 origin/codex/paimon-master-daily-report HEAD` | 确认 master head、release head、origin branch 和本地 HEAD。 |
| `git rev-list --first-parent --count 30a13d69e6947419b2914276686d7c079454cf42..upstream/master` | 18 个 first-parent commit。 |
| `git log --first-parent --reverse --format=... 30a13d69e6947419b2914276686d7c079454cf42..upstream/master` | 18 个 commit 均已纳入本报告。 |
| `git diff --stat --find-renames` / `git diff --name-status --find-renames` | 范围共 69 个文件，6600 insertions / 439 deletions。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <18 commits>` | 对本次 18 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 对 18 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master 30a13d69e6947419b2914276686d7c079454cf42` | 18 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对重点 bugfix / conditional commits 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 Spark V2 overwrite、query-auth、BlobDescriptor、CompressUtils、Flink function factory、rollback procedure 等路径存在性和当前实现。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `f2de0ee193e7991575f4418db44343f35fef92bb`
- `last_processed_commit_date`: `2026-07-04 12:45:47 +0800`
- `last_report_date`: `2026-07-04`
- `last_report_path`: `reports/paimon-master-daily/2026-07-04.md`

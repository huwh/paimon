# Apache Paimon Master Daily Report - 2026-07-16

## Summary

范围：`dd2a065b64ea6ad888964f0e7af5512ea41b23ef..eb797e11119be30b56d975c1c3618c707eb3e7c3`，共 38 个 first-parent 提交。当前 Apache upstream `master` head 是 `eb797e11119b`（2026-07-16 13:47:39 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 204 个文件，`+14880/-2469`。

本次主线集中在 primary-key global index：补 primary-key batch scan 的索引谓词规划，新增 full-text archive / search primitives，继续扩展 primary-key full-text、vector、sorted index 的维护、查询、level alignment、parallel / batch search 与 deletion-vector aware ANN build。跨模块变化包括 PyPaimon REST query auth、Ray zero-block overwrite、BLOB fallback read 并行化、Hive/S3/service/VFS/Lance/Lumina/Spark/Flink 多个 bugfix，以及 object table 在 JDBC / Hive catalog 中的支持。

`git cherry -v upstream/release-1.4 upstream/master dd2a065b64ea6ad888964f0e7af5512ea41b23ef` 显示 38 个提交均为 `+`，没有 patch-id 等价提交已进入 `release-1.4`。明确的 1.4 直接候选包括 `bbf4045b`（Hive `column.name.delimiter` typo）、`9a302e7d`（PyPaimon Ray zero-block overwrite）、`dfc2cd79`（service connection race leak）、`75f3b939`（VFS read statistics double count）、`e41543b6`（S3 credential-less auth 被 required options 拦截）、`ce9987c9`（Lance MULTISET validation）、`d339a4d0`（Lumina metric metadata normalization）、`efa3294b`（Spark 4 Scala 2.13 dependency resolution）、`55433db9`（Flink lookup join debug log IndexOutOfBounds）和 `d04db12e`（Hive 3.1 pre-epoch timestamp）。`0939062a` 是 BLOB materialization 性能/安全候选；`4584230d`、`99741c25`、`f9557b1f` 是低风险诊断或日志文本修复。

所有 commit 都先执行 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；同时对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch hunks。release-1.4 判断使用 `git cherry`、`git cat-file -e`、`git grep` 和 targeted `git show upstream/release-1.4:<path>` 对照生产路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core primary-key index/search/BLOB | 11 | 修 `PrimaryKeyBatchScan` 只提取索引字段谓词；新增 full-text archive/search 文件、ranker、split/read/scan；managed BLOB 写入加固；vector search parallel/batch、DV-aware ANN build、index level alignment。 | 多数依赖 master 的 `PrimaryKeyBatchScan`、`pkfulltext`、`pkvector`、managed BLOB 路径，1.4 不直接适用。 |
| PyPaimon / Ray / REST auth | 4 | Ray zero-block overwrite 触发 empty commit；新增 REST query auth、predicate JSON parser、row filter/column masking reader、write-side auth guard；`listagg`；fallback BLOB reads 并行化。 | `9a302e7d` 直接适用；query auth / BLOB fallback 并行化是 master 新栈。 |
| Hive / catalog / object table | 4 | 修 Hive SerDe delimiter typo、Hive 3.1 pre-epoch timestamp nanos、client pool error message；JDBC/Hive catalog 支持 object table。 | `bbf4045b`、`d04db12e` 直接适用；`4584230d` 为低风险诊断；object table 是功能扩展。 |
| Flink / Spark integration | 4 | SortedIndexTopoBuilder 使用 configured commit user；Spark vector residual filter 下推到 `SparkPrimaryKeyVectorRead`；Spark 4 modules pin Scala 2.13；Flink lookup debug log 使用 projected row 类型。 | `efa3294b`、`55433db9` 直接候选；sorted/vector 路径依赖 master 新功能。 |
| Service/network | 2 | `NetworkClient.sendRequest` 用 `computeIfAbsent` 消除首连竞态泄漏；KvQueryServer shutdown failure 打完整堆栈。 | `dfc2cd79` 直接候选；`99741c25` 为诊断改进。 |
| Filesystems / VFS / S3 | 3 | VFS table-root Javadoc；`VFSInputStream.read()` 不重复统计 bytes；S3 loader 不再要求 access/secret key，补 MinIO/STSes/default-provider 测试。 | `75f3b939`、`e41543b6` 直接候选。 |
| Format / index engines / common | 7 | Avro array optimization revert；Vortex nested ARRAY validation；Lance MULTISET validation；Lumina metric metadata normalization；Parquet heap-buffer byte read 优化；`BlobRef.toData()` 按已知 length 直接读取；Iceberg REST log/Javadoc typo。 | `ce9987c9`、`d339a4d0`、`0939062a` 可回补；Vortex/Avro revert 不直接适用；Parquet 是性能优化。 |
| Docs / test util / deps | 3 | 修 generated docs compile output；删除 dead `cache_docker_images.sh` ref；docs `websocket-driver` 0.7.5。 | docs/test/dependency-only，无 Paimon runtime 影响。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `855de94e` | primary-key global-index scan 只对 indexed BTREE/BITMAP fields 提取谓词，拒绝非 split index result，并避免已有 global index result 时重复规划。 | `git cherry` 为 `+`。 | 1.4 缺 `PrimaryKeyBatchScan.java`。 | master 新 global-index scan 路径；不直接适用。 |
| `c335a950` | generated docs compile output 修正。 | `git cherry` 为 `+`。 | 只改 `docs/generated/core_configuration.html`。 | 文档-only。 |
| `bbf4045b` | HiveSchema 将 `column.name.delimite` typo 改为 `column.name.delimiter`，自定义列名分隔符才会生效。 | `git cherry` 为 `+`。 | 1.4 同文件仍使用 typo key。 | 直接候选；影响 Hive SerDe properties 中自定义 delimiter 的 schema extraction。 |
| `9a302e7d` | Ray input 无 blocks 时不会调用 `on_write_start`，overwrite 空数据现在会在 `on_write_complete` 懒启动 writer 并提交空 commit。 | `git cherry` 为 `+`。 | 1.4 `ray_datasink.py` 有相同 writer lifecycle。 | 直接候选；影响 PyPaimon Ray zero-block overwrite 清空表语义。 |
| `83cd9965` | managed BLOB 引用写入去重/flush 失败关闭流；postpone bucket 写入也 externalize BLOB 并在 abort 时清理 managed BLOB。 | `git cherry` 为 `+`。 | 1.4 缺 `ManagedBlobReferenceCollector` / `PrimaryKeyBlobExternalizer`。 | 依赖 master managed BLOB 新栈，不直接适用。 |
| `f7ec93b3` | SortedIndexTopoBuilder committer 使用 `CoreOptions.createCommitUser(userOptions)`，不再随机 UUID。 | `git cherry` 为 `+`。 | 1.4 缺 `SortedIndexTopoBuilder.java`。 | 不直接适用。 |
| `e0960f66` | Spark distributed vector search 将 residual filter 传入 `SparkPrimaryKeyVectorRead`，先过滤再 top-k。 | `git cherry` 为 `+`。 | 1.4 缺 Spark primary-key vector read 路径。 | 不直接适用。 |
| `fde4de55` | primary-key ANN build 从 `BucketedDvMaintainer` snapshot deletion vectors，排除已删除 physical row positions，并支持 zero-row segment。 | `git cherry` 为 `+`。 | 1.4 缺 `pkvector` ANN maintainer 路径。 | 不直接适用。 |
| `92e5c16d` | revert Avro array raw-byte optimization，删除 `AvroBytesArray` 和 writer fast path。 | `git cherry` 为 `+`。 | 1.4 不存在 `AvroBytesArray.java`。 | 不直接适用。 |
| `dfc2cd79` | `NetworkClient.sendRequest` 用 `connections.computeIfAbsent` 原子创建连接，避免并发首连创建多个 channel 后泄漏。 | `git cherry` 为 `+`。 | 1.4 仍是 `get` 后 `put` 的非原子逻辑。 | 直接候选；影响 service client 并发首个请求。 |
| `63c352de` | VFSTableRootIdentifier Javadoc 修正。 | `git cherry` 为 `+`。 | Javadoc-only。 | 无 runtime 影响。 |
| `75f3b939` | `VFSInputStream.read()` 不再在调用 `read(byte[],off,len)` 后重复 `incrementBytesRead`。 | `git cherry` 为 `+`。 | 1.4 `read()` 仍在 bulk read 已统计后再统计一次。 | 直接候选；影响 PVFS/Hadoop statistics bytes read。 |
| `ff118501` | Vortex ARRAY 校验递归检查 element type，嵌套 unsupported MAP 会失败而非放过。 | `git cherry` 为 `+`。 | 1.4 缺 `paimon-vortex` 模块。 | 不直接适用。 |
| `e41543b6` | `S3Loader.requiredOptions()` 清空，不再要求 `s3.access.key` / `s3.secret.key`，允许 IAM/default provider/session token 路径。 | `git cherry` 为 `+`。 | 1.4 `S3Loader` 仍要求 access/secret key。 | 直接候选；影响无显式 AK/SK 的 S3A credential provider 使用。 |
| `ce9987c9` | Lance schema validation 对 MULTISET 抛 unsupported，而不是返回 null 后让 native 侧晚失败。 | `git cherry` 为 `+`。 | 1.4 `visit(MultisetType)` 仍 `return null`。 | 直接候选；影响 Lance file format schema validation。 |
| `d339a4d0` | Lumina 将 enum-form metric（如 `L2`、`INNER_PRODUCT`）规范化为 native name 写入 metadata，避免 read path 解析失败。 | `git cherry` 为 `+`。 | 1.4 `buildLuminaOptions` 仍直接写原始 option value。 | 直接候选；影响 Lumina vector index metadata round-trip。 |
| `0939062a` | `BlobRef.toData()` 对已知 length 直接分配并一次读满，过大 length 在开流前失败；未知 length 仍走 grow buffer。 | `git cherry` 为 `+`。 | 1.4 `BlobRef.toData()` 仍总是 `IOUtils.readFully(newInputStream(), true)`。 | 可选回补；性能/安全改进，降低大 BLOB materialization 风险。 |
| `f9557b1f` | Iceberg REST committer factory Javadoc / log typo。 | `git cherry` 为 `+`。 | 生产文件存在，但只改文本。 | 低风险日志/文档修复。 |
| `efa3294b` | Spark 4.0/4.1/common 模块本地 pin `scala.binary.version=2.13`，避免 inactive spark4 profile 时继承 parent 2.12。 | `git cherry` 为 `+`。 | 1.4 Spark 4 modules artifactId 是 `_2.13`，但未设置本地 `scala.binary.version`。 | 直接候选；影响 Spark 4 Maven dependency resolution/build。 |
| `99741c25` | KvQueryServer shutdown failure log 输出完整 stack trace。 | `git cherry` 为 `+`。 | 1.4 只打印 exception message。 | 诊断改进；非功能 bugfix。 |
| `55433db9` | Flink lookup debug log 的 field getter 从 original projection 改为 projected row 类型，并捕获 debug log 异常，避免追加 PK 列时 `IndexOutOfBoundsException`。 | `git cherry` 为 `+`。 | 1.4 `FileStoreLookupFunction` 仍按 `projection.length` 与 `rowType.getTypeAt(projection[i])` 构造 getter。 | 直接候选；影响 DEBUG 日志开启时 lookup join 稳定性。 |
| `d04db12e` | Hive 3.1 timestamp inspector 对 pre-epoch fractional millis 使用 `Math.floorMod` 计算 nanos，避免负 nano-of-second。 | `git cherry` 为 `+`。 | 1.4 仍使用 `millis % 1000`。 | 直接候选；影响 1970 前带小数秒 timestamp。 |
| `4584230d` | Hive client pool unknown key error message 从 `"Unknown key element %s" + trimmed` 改为正常拼接。 | `git cherry` 为 `+`。 | 1.4 同文件存在相同 malformed message。 | 低风险诊断修复。 |
| `57201293` | PyPaimon fallback BLOB selected values 并发 materialize，只读取 merge 后最终可见 BLOB。 | `git cherry` 为 `+`。 | 1.4 未发现 `BlobFallbackBatchReader` / `blob_parallelism` 路径。 | master BLOB read-path 优化，不直接适用。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `855de94e` | Core global index | `PrimaryKeyBatchScan` 缓存 BTREE/BITMAP definitions 和 field ids，使用 `PredicateProjectionConverter` 提取 index-only predicate；已有 `GlobalIndexSplitResult` 时跳过 post-process，并新增未索引 filter 不触发 index planning 的测试。 | 不直接适用。 |
| 2 | `31a3e0a` | Core full-text/search primitives | 新增 `pkfulltext` bucket state、data-file reader、index builder/file，新增 `PrimaryKeyIndexPositionReader`、scored result、ranker/search position，并调整 vector result/position reader。 | 新功能栈。 |
| 3 | `803ed5c8` | Core full-text maintenance | `CoreOptions` / schema validation / primary-key index definitions 支持 full-text index；新增 `BucketedFullTextIndexMaintainer` 并接入 `KeyValueFileStore`。 | 新功能栈。 |
| 4 | `c335a950` | Docs | 修 generated core configuration HTML。 | 文档-only。 |
| 5 | `16af7fd1` | Core full-text search | 新增 `PrimaryKeyFullTextBucketSearch`、`PrimaryKeyFullTextRead/Scan/SearchSplit`，将原 full-text scan/read 重命名为 data-evolution 版本。 | 新功能栈。 |
| 6 | `bbf4045b` | Hive | HiveSchema property key typo 修为 `column.name.delimiter`，新增 custom delimiter 单测。 | 直接候选。 |
| 7 | `9a302e7d` | PyPaimon Ray | Ray zero-block overwrite 在 `on_write_complete` 中懒调用 `on_write_start`，并覆盖 unpartitioned overwrite / write_complete without start。 | 直接候选。 |
| 8 | `83cd9965` | Core managed BLOB | managed BLOB descriptor URI 去重、flush 失败关闭流、postpone bucket BLOB externalization、abort cleanup 和 tests。 | 不直接适用。 |
| 9 | `f7ec93b3` | Flink sorted index | SortedIndexTopoBuilder committer 使用 configured commit user。 | 不直接适用。 |
| 10 | `4fbb308a` | Flink/Spark full-text | docs、Flink `FullTextSearchProcedure`、Spark SQL full-text/hybrid tests 和 service factory exposure。 | 新功能栈。 |
| 11 | `e0960f66` | Spark vector | distributed vector search 将 residual filter 传入 Spark read，测试 filter before top-k。 | 不直接适用。 |
| 12 | `110985f3` | Core vector search | primary-key vector search 并行化，调整 ANN segment searcher/read docs/tests。 | 新功能栈。 |
| 13 | `fde4de55` | Core vector DV | ANN build 捕获 deletion-vector snapshot，排除 deleted positions，并支持 empty segment。 | 不直接适用。 |
| 14 | `5f700f4a` | Docs hotfix | 删除两个临时 design docs。 | 文档清理。 |
| 15 | `b7df2b48` | Core batch vector | 支持 batch primary-key vector search，新增 `PrimaryKeyBatchVectorRead` 并调整 Spark strategy。 | 新功能栈。 |
| 16 | `92e5c16d` | Avro format | revert Avro array optimization，删除 `AvroBytesArray`、benchmark 和 fast writer path。 | 不直接适用。 |
| 17 | `8b298182` | Core PK index levels | primary-key index maintenance 与 data levels 对齐，调整 sorted/vector/full-text maintainer、state、source meta 与 validation/tests。 | 新功能栈。 |
| 18 | `07c257d9` | PyPaimon query auth | REST catalog auth endpoint、predicate JSON parser、query-auth split/reader、extra filter fields、write-side permission guard 和大量 tests。 | 功能扩展；非独立 1.4 bugfix。 |
| 19 | `dfc2cd79` | Service network | `NetworkClient` 首连用 `computeIfAbsent`，并新增并发首请求只建一个 connection 的测试。 | 直接候选。 |
| 20 | `19ca961d` | Test util | 删除 `DockerImageVersions` 中 dead `cache_docker_images.sh` ref。 | test-util only。 |
| 21 | `63c352de` | VFS docs | 修 VFSTableRootIdentifier Javadoc。 | Javadoc-only。 |
| 22 | `75f3b939` | PVFS/VFS | `read()` 移除重复 bytes read 统计，新增 single-byte/bulk/EOF stats 测试。 | 直接候选。 |
| 23 | `ff118501` | Vortex | ARRAY validation 递归检查 element type，新增 nested MAP failure / nested ARRAY pass tests。 | 1.4 缺模块。 |
| 24 | `e41543b6` | S3 filesystem | `S3Loader.requiredOptions()` 改为空，新增 MinIO、session token、default credentials provider 测试。 | 直接候选。 |
| 25 | `ce9987c9` | Lance | MULTISET validation 改为显式 unsupported。 | 直接候选。 |
| 26 | `d339a4d0` | Lumina | `LuminaVectorIndexOptions` 持久化 native metric name，并覆盖 enum/native form tests。 | 直接候选。 |
| 27 | `f3da8fda` | Parquet format | `VectorizedPlainValuesReader.readBytes` 对 heap buffer 直接数组访问，direct buffer 保持原逻辑。 | 性能优化，非 bugfix。 |
| 28 | `9be80341` | PyPaimon aggregation | 将 `listagg` 加入 supported aggregation functions。 | 新功能/兼容补充。 |
| 29 | `0939062a` | Common BLOB | `BlobRef.toData()` 对已知 length 直接读定长数组并检查最大数组大小。 | 可选性能/安全候选。 |
| 30 | `f9557b1f` | Iceberg | REST committer factory Javadoc 和 log typo。 | 低风险文本修复。 |
| 31 | `efa3294b` | Spark build | Spark 4.0/4.1/common 模块 pin Scala 2.13，避免依赖解析到 2.12。 | 直接候选。 |
| 32 | `99741c25` | Service diagnostics | KvQueryServer shutdown failure log 打完整 exception。 | 诊断改进。 |
| 33 | `402f5675` | Docs dependency | docs `websocket-driver` 0.7.4 -> 0.7.5。 | docs dependency only。 |
| 34 | `55433db9` | Flink lookup | lookup debug logging 使用 projected row getters，并捕获日志异常；新增追加 PK 列 debug log 测试。 | 直接候选。 |
| 35 | `d04db12e` | Hive 3.1 | pre-epoch timestamp nanos 用 `Math.floorMod`，新增前后 epoch 和 null tests。 | 直接候选。 |
| 36 | `4584230d` | Hive diagnostics | unknown client pool cache key error message 修正。 | 低风险诊断修复。 |
| 37 | `57201293` | PyPaimon BLOB | fallback BLOB reads 支持并行 materialize selected scalar/array values，避免读被覆盖的旧 BLOB。 | 1.4 缺对应路径。 |
| 38 | `eb797e11` | Catalog object table | `AbstractCatalog` 抽出 `createObjectTable` / fixed schema builder；JDBC/Hive catalog 创建 object table 并保留 custom path。 | 功能扩展。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master` 前进到 `eb797e11119b`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse dd2a065b64ea6ad888964f0e7af5512ea41b23ef..upstream/master` | 38 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat dd2a065b64ea6ad888964f0e7af5512ea41b23ef..upstream/master` | 204 files changed, 14880 insertions(+), 2469 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <38 commits>` | 对 38 个 commit 均已执行，输出缓存到 `/private/tmp/paimon-daily-2026-07-16-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <38 commits>` | 对 38 个 commit 均已执行并检查实际 patch hunks。 |
| `git cherry -v upstream/release-1.4 upstream/master dd2a065b64ea6ad888964f0e7af5512ea41b23ef` | 38 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted checks | `git cat-file -e` / `git grep` / `git show upstream/release-1.4:<path>` 确认 Hive delimiter、Ray datasink、NetworkClient、VFSInputStream、S3Loader、Lance、Lumina、Spark 4 pom、Flink lookup、Hive timestamp 等候选路径存在且缺对应修复。 |
| Build / unit tests | 未运行 Maven 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `eb797e11119be30b56d975c1c3618c707eb3e7c3`
- `last_processed_commit_date`: `2026-07-16 13:47:39 +0800`
- `last_report_date`: `2026-07-16`
- `last_report_path`: `reports/paimon-master-daily/2026-07-16.md`

# Apache Paimon Master Daily Report - 2026-07-08

## Summary

范围：`0a695abf813c579d9ea994852407734be8660768..5b797ef63fba5e98bf4fd285fb5a34877c91ee81`，共 14 个 first-parent 提交。当前 Apache upstream `master` head 是 `5b797ef63fba`（2026-07-07 23:23:05 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线集中在 PyPaimon BLOB / data-evolution 读写修复、core data-evolution scan 与 conflict detection、FieldListagg 聚合空白值处理、RTAS manifest/stat 继承修复、Flink shuffle 中 BLOB descriptor 保留、Spark vector search meta-only 优化、MAP shared-shredding 前置校验、postpone compact 按行数算 bucket，以及 master 版本号切到 `2.0-SNAPSHOT`。范围共 140 个文件，`+3005/-270`。

release-1.4 方面，`git cherry -v upstream/release-1.4 <commit> <commit>^` 显示 14 个提交均为 `+`，没有等价 patch 已进入 `upstream/release-1.4`。较明确的 1.4 bugfix/backport 候选是：`622e67d8` FieldListagg 忽略空白 token、`dd0bbf6a` data-evolution 混合 `_ROW_ID OR 普通列条件` 不安全 pushdown、`b18c14d6` BLOB descriptor 在 Flink shuffle 序列化中丢失、以及 `5b797ef63` `DataEvolutionBatchScan.withShard` 保持 wrapper 链式调用。`e194867b` conflict diagnostics 与 `5b1a4606` PyPaimon update-by-row-id 文件清理也有 1.4 路径风险，但需要按 1.4 的旧 writer / dedicated file API 适配。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时仍不展开 hunk，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>`，并用 `git cherry`、stable patch-id、`git grep` / `git show upstream/release-1.4:<path>` 核对 release 分支代码路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| PyPaimon BLOB / update-by-row-id | 3 | `bd9b2510` 让 descriptor bytes 可显式使用 upstream `UriReader` 或 table `FileIO`；`933421aa` 让 BlobView 解析保留 upstream table FileIO 并按 file_io 分组并发读取；`5b1a4606` 在 update-by-row-id prepare 后失败时 abort 已写 data/blob 文件。 | 1.4 有 BLOB descriptor 和 update-by-row-id，但没有 master 的 `Blob.from_bytes(...file_io...)` / BlobView prescan 结构，且 `FileStoreWrite.abort()` 缺失；需要适配而非整 commit 回补。 |
| Core data-evolution scan / conflict | 3 | `e194867b` 将 normal data files 与 dedicated blob/vector files 的 row-id conflict 分开报告；`dd0bbf6a` 保留完整 read-time filter，只把 row-id-safe residual 下推给 stats/global-index；`5b797ef6` 修复 `withShard()` 返回底层 batch scan 导致丢失 data-evolution wrapper。 | 1.4 三个核心文件路径均存在；`dd0bbf6a` 和 `5b797ef6` 是直接正确性候选，`e194867b` 需要按 1.4 dedicated-file 范围适配。 |
| Core commit / aggregate correctness | 2 | `622e67d8` 用 `StringUtils.isBlank` 跳过空白 input/token，避免 listagg 拼出空白值或前导 delimiter；`d7f3d04c` 在 RTAS append-after-truncate 时重置 inherited manifest/index/stat state。 | FieldListagg 旧逻辑在 1.4 原样存在，直接候选；1.4 未命中 `REPLACE_TABLE_AS_SELECT` / `CREATE_OR_REPLACE_TABLE_AS_SELECT` operation，RTAS 修复不直接适用。 |
| Common / Flink BLOB serialization | 1 | `b18c14d6` 在 `AbstractBinaryWriter.writeBlob` 和 `BlobSerializer` 中对 `BlobRef` 写 descriptor bytes，而不是强制 `toData()`；反序列化恢复 `BlobRef`。 | 1.4 的 serializer 仍直接 `blob.toData()`，且 Flink `path_to_descriptor` 已存在；直接候选。 |
| Spark vector search | 1 | `41376112` 对只请求 `_row_id` / `__paimon_search_score` 的 vector search 直接从 index result 构造 local rows，lateral vector search 同步跳过 table lookup。 | 1.4 有 vector_search，但没有 `__paimon_search_score` metadata；偏功能/优化，非直接 bugfix。 |
| MAP shared-shredding | 1 | `428c18fa` 添加 MAP shared-shredding 配置校验、field dict compression 元数据、RowToColumnConverter element converter，并拒绝 PK/fixed bucket、非 parquet/orc、非 none/lz4/zstd、nested file-index、Variant/BLOB/MULTISET/VECTOR 等组合。 | 1.4 没有 `map.storage-layout=shared-shredding` 代码路径；功能前置校验，不直接影响 1.4。 |
| Postpone compact | 1 | `b9d0a461` 新增 `postpone.target-row-num-per-bucket`，Flink compact action 和 Spark procedure 根据 postpone active row count 计算首次 compact bucket 数。 | 1.4 有 postpone compact 路径，但这是新能力，不是 bugfix。 |
| Release / build docs | 2 | `cd871ab6` 将全仓 Maven version 从 `1.5-SNAPSHOT` 切到 `2.0-SNAPSHOT`；`25598ee2` 更新 AGENTS.md，要求 Spark/Flink 测试显式加 `-Pspark3/-Pspark4`、`-Pflink1/-Pflink2`。 | 不进入 release-1.4 bugfix 判断。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `bd9b2510` | `Blob.from_bytes` 解析 descriptor bytes 时不应固定使用错误 FileIO；补丁允许显式 `uri_reader_factory`，否则用 table `FileIO` 创建 `UriReader.from_file`。 | stable patch-id `1a1788ea735a`; cherry `+`。 | 1.4 的 `blob.py` 只有 `BlobData.from_bytes(cls, data)`，没有 master 的 `Blob.from_bytes(data, file_io, uri_reader_factory, ...)` 静态 API。 | 不直接适用；若 backport master BLOB descriptor API，需要联动此修复。 |
| `933421aa` | BlobView 字段默认读应使用 upstream table FileIO，不应落到 downstream table FileIO；并发读取时按 upstream file_io 分组。 | stable patch-id `81d85c55799d`; cherry `+`。 | 1.4 对 `blob-view` / `BlobViewStruct` 无命中，只存在 `blob-descriptor-field` 和简单 `BlobDescriptorConvertReader`。 | 不直接适用；仅在 BlobView 能力回补时需要一起带上。 |
| `5b1a4606` | PyPaimon update-by-row-id 在 prepare/metadata assignment 之后失败时，旧逻辑只 close writer，会留下未提交 data/blob 文件；补丁失败时 abort writer。 | stable patch-id `d72e43057de6`; cherry `+`。 | 1.4 有 `table_update_by_row_id.py`，但 `FileStoreWrite` 只有 `close()`，没有 `abort()`，且 update path 没有 master 的 `blob_writers` 变量。 | 需要适配的候选。1.4 可能仍有 data-file orphan 风险，但不能直接 cherry-pick。 |
| `622e67d8` | FieldListagg 聚合应忽略空白字符串和 distinct 模式中的空白 token，避免输出空白值或前导 delimiter。 | stable patch-id `488d306e38ca`; cherry `+`。 | 1.4 `FieldListaggAgg` 仍用 `getSizeInBytes() <= 0`，不能跳过 `" "`、`\t`、全角空格等。 | 直接 bugfix 候选。 |
| `d7f3d04c` | RTAS truncate 后 append snapshot 不应继承 truncate snapshot 的 manifest/index/stat state，避免 incompatible nested type replace 后 latest read 看到旧状态。 | stable patch-id `66f61d6b1795`; cherry `+`。 | 1.4 没有 `REPLACE_TABLE_AS_SELECT` / `CREATE_OR_REPLACE_TABLE_AS_SELECT` operation 命中。 | 不直接适用；相关 RTAS operation 不在当前 1.4 upstream 路径。 |
| `e194867b` | Data-evolution conflict detection 将 normal data files 与 blob/vector dedicated files 分开检查，错误信息能指出 dedicated file 是否跨多个 data-file range。 | stable patch-id `9972eda8fe5d`; cherry `+`。 | 1.4 `ConflictDetection.checkRowIdRangeConflicts` 仍把所有 row-id entries 一起 merge，再只排除 blob file 做 `areAllRangesSame`。 | 需要适配的候选；更偏诊断/保护语义，建议和 data-evolution + BLOB/DV 场景测试一起回补。 |
| `dd0bbf6a` | 混合谓词如 `_ROW_ID = 1 OR f0 > 5` 不能把去掉 `_ROW_ID` 后的普通列谓词推进 stats/global-index/limit pruning，否则会缩窄 scan 语义。 | stable patch-id `403c474890b2`; cherry `+`。 | 1.4 `DataEvolutionBatchScan.withFilter` 仍先 `removeRowIdFilter(predicate)` 再把残余 predicate 传给 `batchScan.withFilter`；`SnapshotReader` 没有 full filter / pushdown filter 分离接口。 | 直接正确性候选，但需要同步接口改动并跑 data-evolution/global-index/limit 测试。 |
| `b18c14d6` | BLOB descriptor 经 Flink shuffle 序列化时不能被 `toData()` 展开成真实 bytes，否则 sink 端失去 descriptor 语义。 | stable patch-id `adcbd228d42b`; cherry `+`。 | 1.4 `AbstractBinaryWriter.writeBlob` 和 `BlobSerializer.serialize` 仍直接 `blob.toData()`；`path_to_descriptor` / `blob-descriptor-field` 已存在。 | 直接 bugfix 候选，优先级高。 |
| `5b797ef6` | `DataEvolutionBatchScan.withShard` 返回底层 `batchScan` 会破坏 fluent chain，后续 plan 不再走 data-evolution wrapper。 | stable patch-id `9d10a1c91b6a`; cherry `+`。 | 1.4 同方法仍是 `return batchScan.withShard(indexOfThisSubtask, numberOfParallelSubtasks);`。 | 直接 one-line bugfix 候选。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `bd9b2510` | PyPaimon BLOB | 2 files，`+70/-4`。`Blob.from_bytes` 增加 `uri_reader_factory` 参数；descriptor bytes 默认通过 table `FileIO` 构造 file-backed reader，显式 factory 时按 descriptor uri 创建 reader；新增两个 unit tests 覆盖 factory 与 file_io 强制路径。 | cherry `+`，patch-id `1a1788ea735a`。1.4 API 形态不同，不直接适用。 |
| 2 | `933421aa` | PyPaimon BlobView read | 3 files，`+195/-22`。BlobView prescan 后不仅替换成 descriptor bytes，还保存每行 upstream file_io；`_resolve_descriptor_fields` 对 descriptor fields 和 view fields 分开处理，view fields 可按 file_io 并发读；`BlobViewLookup` 缓存 upstream `UriReader` 并提供 `resolve_file_io`。 | cherry `+`，patch-id `81d85c55799d`。1.4 缺 BlobView 路径，不直接适用。 |
| 3 | `428c18fa` | MAP shared-shredding | 9 files，`+1095/-29`。新增 reusable `RowToColumnConverter.ElementConverter`，修复 CHAR/VARCHAR/BINARY/VARBINARY 与 Variant row append；shared-shredding metadata 持久化 dict compression；SchemaValidation 拒绝当前未支持的表模式、文件格式、压缩、nested file-index 和数据类型组合。 | cherry `+`，patch-id `c45d348a7d54`。1.4 无对应功能路径。 |
| 4 | `41376112` | Spark vector search | 9 files，`+216/-25`。抽出 `PaimonBaseScan.evalVectorSearch`；新增 `VectorSearchResultUtils`，当 required schema 只含 vector search metadata 时直接返回 `PaimonLocalScan`；lateral vector search meta-only 子查询直接从 index result 生成 rows。 | cherry `+`，patch-id `4d175766d3a3`。1.4 缺 score metadata，非直接 bugfix。 |
| 5 | `5b1a4606` | PyPaimon update-by-row-id | 2 files，`+66/-4`。`_write_group` 增加 `success` 标记，成功时 close，失败时 abort `FileStoreWrite` 和 blob writers；新增测试在 metadata assignment 抛错后确认 table files 不增加。 | cherry `+`，patch-id `d72e43057de6`。1.4 需要先补 abort 能力。 |
| 6 | `622e67d8` | Core aggregate | 2 files，`+94/-11`。`FieldListaggAgg.agg` 对 input/accumulator 和 distinct token 使用 `StringUtils.isBlank`，测试覆盖空字符串、空格、tab/newline、全角空格和 first non-blank 无前导 delimiter。 | cherry `+`，patch-id `488d306e38ca`。直接候选。 |
| 7 | `d7f3d04c` | Core/Spark RTAS | 3 files，`+135/-17`。`FileStoreCommitImpl` 新增 RTAS-after-truncate 判断，命中时清空 inherited manifests、old index manifest 和 stats 继承；测试覆盖 index/stat reset 与 Spark incompatible nested type RTAS read。 | cherry `+`，patch-id `66f61d6b1795`。1.4 缺对应 operation，不直接适用。 |
| 8 | `b9d0a461` | Postpone compact | 9 files，`+334/-13`。新增 `postpone.target-row-num-per-bucket` option；`PostponeUtils` 汇总 postpone bucket active row counts 并按 `ceil(row_count/target)` 算 bucket；Flink compact action 与 Spark procedure 共用逻辑，测试覆盖不同 partition 算出 1/3 buckets。 | cherry `+`，patch-id `a15fc0d250f5`。新能力，非 bugfix。 |
| 9 | `cd871ab6` | Release version | 83 files，`+84/-84`。全仓 parent/module POM version 从 `1.5-SNAPSHOT` 切到 `2.0-SNAPSHOT`，root `project.build.outputTimestamp` 更新到 2026-07-07。 | cherry `+`，patch-id `e5b40f077e6b`。release 版本推进，不进入 1.4。 |
| 10 | `e194867b` | Data-evolution conflict | 4 files，`+285/-29`。Java/Python conflict detection 分离 normal data files 与 dedicated blob/vector files；data file range 先独立 merge；dedicated file 必须被单个 data-file range 覆盖，否则报告跨多个 data-file range 或未覆盖。 | cherry `+`，patch-id `9972eda8fe5d`。1.4 有旧逻辑，需适配验证。 |
| 11 | `dd0bbf6a` | Data-evolution scan pushdown | 11 files，`+305/-26`。新增 `rowIdSafeResidualFilter`：含 `_ROW_ID` 的 OR 不再作为 stats/global-index residual；`SnapshotReader.withFilter` 分离 full read-time filter 与 pushdown predicate；Python stats predicate 同步排除 `_ROW_ID`。 | cherry `+`，patch-id `403c474890b2`。直接正确性候选。 |
| 12 | `b18c14d6` | Common/Flink BLOB | 3 files，`+97/-3`。`writeBlob` 和 `BlobSerializer` 对 `BlobData` 仍写 raw data，对 `BlobRef` 写 `Blob.serializeBlob(blob)`；反序列化走 `Blob.fromBytes`；新增 Flink streaming/batch shuffle 写 descriptor tests。 | cherry `+`，patch-id `adcbd228d42b`。直接候选。 |
| 13 | `25598ee2` | Build docs | 1 file，`+14/-2`。AGENTS.md 中 Java surefire 示例显式 `-DwildcardSuites=none`，并记录 Spark/Flink UT 必须带 `-Pspark3/-Pspark4` 或 `-Pflink1/-Pflink2`，避免模块未进入 reactor 而跑到 stale jar。 | cherry `+`，patch-id `5836f320e3a2`。文档更新。 |
| 14 | `5b797ef6` | Data-evolution scan shard | 2 files，`+15/-1`。`DataEvolutionBatchScan.withShard` 改为先调用底层 `batchScan.withShard(...)`，再 `return this`；测试验证返回对象仍是 wrapper。 | cherry `+`，patch-id `9d10a1c91b6a`。直接 one-line 候选。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | sandbox DNS 失败后用联网权限重跑成功。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | sandbox DNS 失败后用联网权限重跑成功；`upstream/master` 前进到 `5b797ef63fba`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | sandbox DNS 失败后用联网权限重跑成功；`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse ...` | 14 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat 0a695abf813c..5b797ef63fba` | 140 files changed, 3005 insertions(+), 270 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <14 commits>` | 对 14 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <14 commits>` | 对 14 个 commit 均已执行并检查实际 patch hunks；超大 MAP / release version patch 按生产文件和统计拆看。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 14 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show <commit> \| git patch-id --stable` | 对 14 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 PyPaimon BLOB/update-by-row-id、FieldListagg、RTAS operation、ConflictDetection、DataEvolutionBatchScan/SnapshotReader、BlobSerializer、Spark vector metadata、MAP shared-shredding、postpone compact 等路径存在性和旧代码片段。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `5b797ef63fba5e98bf4fd285fb5a34877c91ee81`
- `last_processed_commit_date`: `2026-07-07 23:23:05 +0800`
- `last_report_date`: `2026-07-08`
- `last_report_path`: `reports/paimon-master-daily/2026-07-08.md`

# Apache Paimon Master Daily Report - 2026-07-20

## Summary

范围：`7234e4c34e9a705af66a816f5c859afd55b8f7f4..cd39bd70567eb27909142523c0b4faace2c8b540`，共 11 个 first-parent 提交，47 个文件变更，`+3101/-488`。本轮 `upstream/master` 推进到 `cd39bd70567e`（2026-07-20 13:37:00 +0800），`upstream/release-1.4` 仍是 `05d1ba75a97c`。

已逐个提交缓存并检查 `git show --stat --name-status --patch --find-renames --format=fuller` 的实际 patch。主要变化集中在 PyPaimon native planner / aggregation / global index / Daft / BLOB 读取，Java global index union 语义修正，chain table streaming 起始阶段 merge-snapshot 能力，以及 Flink lookup 可观测性和测试稳定性。

`release-1.4` 影响上，唯一明确的直接 bugfix 候选是 `d5d49952e`：Java `UnionGlobalIndexReader` 在 union 多个 global-index reader 时，如果任一 reader 返回 unsupported，以前会跳过该结果并继续用部分 index 结果裁剪；新 patch 改为整体 unsupported，避免错误裁剪。`git cherry` 显示本轮 11 个 patch 相对 `upstream/release-1.4` 均未等价合入，且 `release-1.4` 的 Java `UnionGlobalIndexReader` 仍存在跳过 unsupported 的旧逻辑。PyPaimon `cd39bd70` 是同类语义修正，但 `release-1.4` 没有对应 `union_global_index_reader.py` 文件，不是直接 1.4 代码路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| PyPaimon read / planner / global index / Daft / BLOB / aggregation | 6 | 新增 `collect` 聚合器；BLOB coalesced read 支持 zero-copy `memoryview`；Daft source 反序列化时恢复 IOConfig；新增 `scan.native-plan.enabled`、Rust split planner 接入和 SplitSerializer v1 解码；修正 PyPaimon union global-index 语义。 | 多数为 master PyPaimon 新路径或功能增强；Daft/native-plan/union_global_index_reader 在 `release-1.4` 不存在，未列为直接 1.4 候选。 |
| Java core global index | 1 | `UnionGlobalIndexReader` union scalar 结果时传播 unsupported，新增 BTree global index 回归测试。 | 明确 1.4 候选：生产文件存在、旧逻辑仍跳过 unsupported、未有等价 cherry。 |
| Chain table streaming / Flink IT | 1 | 新增 `chain-table.streaming.merge-snapshot`，起始阶段可用 anchor-based merge 处理跨分支 delete/update；抽出 `ChainTableUtils.buildChainSplits`；补充 Flink chain table merge-snapshot IT。 | `release-1.4` 没有 `ChainTableStreamScan` / `ChainGroupReadTable` 相关路径，按 master 功能栈处理。 |
| Flink local partial lookup metrics | 1 | 为 local partial lookup 增加 request count 与 remote-access count，`LookupLevels` 透传 `LookupContext`，Flink lookup function 接入 `FlinkMetricRegistry`。 | 1.4 有 lookup 基础路径，但该提交是可观测性增强，不是 bugfix；可选 backport。 |
| Test stabilization | 2 | CDC MySQL 多 shard IT 延长 timeout 并把 `JobClient` 传给 wait；Sink savepoint IT 限制 savepoint/restart 轮数并对中断快速失败。 | 测试路径变更，无生产运行影响。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `d5d49952e` | Java core `UnionGlobalIndexReader`：任一子 reader 不支持 predicate 时，整体 union 应返回 unsupported，而不是继续 union 其他 reader 的部分结果。 | `git cherry -v upstream/release-1.4 upstream/master 7234e4c34e9a...` 标记 `+`，未等价合入。 | `release-1.4` 存在 `paimon-common/src/main/java/org/apache/paimon/globalindex/UnionGlobalIndexReader.java`，当前 `union(...)` 仍对 `!current.isPresent()` 执行 `continue`；`git log upstream/release-1.4 --grep='Propagate unsupported'` 无结果。 | 直接 1.4 correctness 候选。涉及 global index 过滤裁剪，混合 supported/unsupported 子索引时可能返回过窄结果，建议评估 backport。 |
| `cd39bd70` | PyPaimon union global-index：标量 predicate unsupported 需要传播；batch vector fan-out 改为非阻塞 future；close 应关闭所有 child 后传播首个异常。 | `git cherry` 标记 `+`，未等价合入。 | `release-1.4` 有 `global_index_reader.py` 接口，但没有 `paimon-python/pypaimon/globalindex/union_global_index_reader.py`；不存在同名修复提交。 | 非直接 1.4 路径。若 1.4 后续引入 PyPaimon union reader，需要同步语义。 |
| `e8c21786` | Daft source 反序列化后重新构造 storage config；REST catalog 读取可刷新 token，显式 IOConfig 可被保留。 | `git cherry` 标记 `+`，未等价合入。 | `release-1.4` 不存在 `paimon-python/pypaimon/daft/*`。 | 非直接 1.4 路径。 |
| `6596efd1` | MySQL CDC `testSyncMultipleShards` 稳定性：timeout 120s -> 240s，等待时传入 `JobClient`。 | `git cherry` 标记 `+`，未等价合入。 | 只改 `paimon-flink-cdc` 测试文件。 | 测试稳定性，可选 backport，不影响生产运行。 |
| `679aa019` | PyPaimon 并发 update 测试稳定性：用每个 worker 的 `commit.user-prefix` 和最新 snapshot 的 commit user 判定真实 winner。 | `git cherry` 标记 `+`，未等价合入。 | 只改 `paimon-python/pypaimon/tests/table_update_test.py`。 | 测试稳定性，无生产影响。 |
| `7c93b030` | Sink savepoint IT 稳定性：限制 savepoint round，避免 timeout 中断被吞，终态非 FINISHED 时快速失败。 | `git cherry` 标记 `+`，未等价合入。 | 只改 `paimon-flink-common` 测试文件。 | 测试稳定性，可选 backport，不影响生产运行。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `35ca2c04` | PyPaimon aggregation | 在 `merge_engine_support.py` 放开 `collect`；新增 `FieldCollectAgg`，支持 array 类型、distinct/non-distinct、retract 和 nested element 测试；原 unsupported 测试改用 `hll_sketch`。 | 功能增强，不是 1.4 bugfix。 |
| 2 | `6596efd1` | Flink CDC test | `MySqlSyncDatabaseActionITCase.testSyncMultipleShards` timeout 加到 240s；`runActionWithDefaultEnv` 返回 `JobClient` 并传给多次 `waitForResult`。 | 测试稳定性，无生产影响。 |
| 3 | `679aa019` | PyPaimon test | 并发 update 测试为每个 worker 复制 table 并设置 `commit.user-prefix`，用最新 snapshot 的 `commit_user` 判断最终写入者，而不是线程返回顺序。 | 测试稳定性，无生产影响。 |
| 4 | `b7a688c5` | PyPaimon BLOB / Daft | `FileIO` 新增 `read_ranges_coalesced_views`，返回共享 backing buffer 的 `memoryview`，并用 retained amplification 避免稀疏 span 长时间持有过大 buffer；Daft BLOB 读取直接构造 Arrow binary array。 | 性能/内存优化；Daft 路径不在 1.4，非直接 bugfix。 |
| 5 | `d5d49952` | Java core global index | `UnionGlobalIndexReader` union 多个 reader 时，遇到 `Optional.empty()` 立即返回 unsupported；新增 BTree global index 混合 fallback 回归测试，覆盖一段 index 可用、另一段需要 fallback 的场景。 | 明确 1.4 correctness 候选。 |
| 6 | `bbecc0fc` | Chain table streaming / Flink | 新增 `chain-table.streaming.merge-snapshot` 配置；stream starting phase 可按 group 合并最新 snapshot partition 和后续 delta partition；抽出 `ChainTableUtils.buildChainSplits`；文档说明 lightweight/merge trade-off；新增多组 Flink IT。 | master chain table 能力；1.4 缺少对应 streaming chain table 路径，非直接候选。 |
| 7 | `e8c21786` | PyPaimon Daft | `_build_storage_config` 改为接收 table 和显式 IOConfig bytes；source 反序列化后重新加载 table，再用 REST token 刷新后的 catalog options 或显式 IOConfig 构造 storage config；新增 REST 和 native task 序列化测试。 | Daft 路径不在 1.4，非直接候选。 |
| 8 | `7c93b030` | Flink sink test | `SinkSavepointITCase.testRecoverFromSavepoint` 限制最多 10 次 stop-with-savepoint/recover；达到上限后等待作业完成；中断异常直接抛出，终态失败提前报错。 | 测试稳定性，无生产影响。 |
| 9 | `09fc333a` | Flink lookup / metrics | `LookupLevels` 新增每次 lookup 的 `LookupContext`，远端创建 lookup file 时标记 remote accessed；新增 `PartialLookupMetrics` 和测试；Flink lookup function 在 local partial lookup fallback 中注册 metrics。 | 可观测性增强；1.4 有 lookup 路径但不是 bugfix，可选 backport。 |
| 10 | `875cfa77` | PyPaimon native planner | 新增 `scan.native-plan.enabled`；通过 `pypaimon_rust.datafusion.PaimonCatalog` 规划 split，解码 SplitSerializer v1 为 Python `DataSplit` / `IndexedSplit`；plain full-snapshot scan 才走 native，auth、time-travel、incremental、DV、data evolution、partitioned table、stale schema、unsupported catalog 等均 fallback；explain 标记 native planner；CI Python 3.11 lane 安装指定 `paimon-rust` rev。 | 大功能增强；1.4 不具备该 native-plan 栈，非直接 bugfix。 |
| 11 | `cd39bd70` | PyPaimon global index | PyPaimon union reader 对标量 predicate 传播 unsupported；默认 batch-vector fan-out 改为 `_collect_futures` 非阻塞收集并保留异步异常；`close()` 关闭所有 child 后再抛首个错误；新增回归测试。 | 语义修正但 1.4 无对应 union reader 文件，非直接候选。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master` 从 `7234e4c34e9a` 推进到 `cd39bd70567e`，`upstream/release-1.4` 保持 `05d1ba75a97c`。 |
| `git ls-remote upstream refs/heads/master refs/heads/release-1.4` | 确认远端 `master=cd39bd70567eb27909142523c0b4faace2c8b540`，`release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git rev-list --count --first-parent 7234e4c34e9a705af66a816f5c859afd55b8f7f4..upstream/master` | `11`。 |
| `git log --first-parent --reverse --format=... 7234e4c34e9a... .. upstream/master` | 列出并审查 11 个 first-parent commit。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <commit>` | 已对 11 个 commit 执行，patch 缓存在 `/private/tmp/paimon-daily-2026-07-20-patches`。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 已对 11 个 commit 执行，用于补充本地 Git 在 `--name-status` 场景下可能省略的 hunk。 |
| `git cherry -v upstream/release-1.4 upstream/master 7234e4c34e9a705af66a816f5c859afd55b8f7f4` | 本轮 11 个 commit 均为 `+`，对 `release-1.4` 未发现等价 patch。 |
| `git ls-tree` / `git grep` / `git show upstream/release-1.4:<path>` | 已核对 Java global index、PyPaimon union、Daft、chain table streaming、lookup metrics 等 release-1.4 路径。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只新增日报和 state 游标更新。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `cd39bd70567eb27909142523c0b4faace2c8b540`
- `last_processed_commit_date`: `2026-07-20 13:37:00 +0800`
- `last_report_date`: `2026-07-20`
- `last_report_path`: `reports/paimon-master-daily/2026-07-20.md`

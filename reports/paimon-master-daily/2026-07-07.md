# Apache Paimon Master Daily Report - 2026-07-07

## Summary

范围：`09d8c2d0cdd939537c3633b2d0e746bc5a334bea..0a695abf813c579d9ea994852407734be8660768`，共 12 个 first-parent 提交。当前 Apache upstream `master` head 是 `0a695abf813c`（2026-07-07 11:03:39 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。

本次主线集中在 PyPaimon data-evolution/Ray 能力、OSS server-side encryption、Spark vector/search 与 streaming offset JSON、Flink lineage/Chain Table lookup、Iceberg REST catalog 并发提交、data-evolution + deletion vectors compaction，以及 core Split 的 versioned binary serializer。范围共 64 个文件，`+4245/-387`。

release-1.4 方面，`git cherry -v upstream/release-1.4 <commit> <commit>^` 显示 12 个提交均为 `+`，没有等价 patch 已进入 `upstream/release-1.4`。较明确的 1.4 bugfix/backport 候选是：`92e39e10` Iceberg REST namespace 并发创建竞态、`85096ccc` PyPaimon data-evolution split planning O(n^2)、`a39e3e8e` Spark streaming offset 避免使用 unshaded Jackson、以及 `984f3590` 中可独立摘取的 `ChainKeyValueFileReaderFactory` missing `return` 修复。`2e81be99` 对 DV-enabled data-evolution compaction 也有 release 代码路径风险，但需要按 1.4 代码适配和验证，不能直接整 commit 回补。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时仍不展开 hunk，实际代码审查又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>`，并对超大 patch 按生产文件分路径复查。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| PyPaimon Ray / data-evolution read & write | 4 | `08fbfd98` 给 `read_by_row_id` 增加 `dynamic_options`、time-travel 和 lazy source 行为；`3952b562` 在 Ray `merge_into` 最终 commit 前失败时 abort 已准备但未提交的 commit messages；`85096ccc` 用 `RangeHelper` 将 data-evolution split planning 从 O(n^2) 降到 O(n log n)；`076db7e2` 将 PyPaimon 默认文件格式改为 Parquet，并移除 multimodal table 的 `file.format=vortex` 覆盖。 | `read_by_row_id.py`、`data_evolution_merge_into.py`、`multimodal/connection.py` 在 1.4 缺失；但 `data_evolution_split_generator.py` 和旧 O(n^2) 路径存在，`85096ccc` 是实际候选。 |
| OSS filesystem | 1 | `38392b21` 新增 `fs.oss.server-side-encryption`、`fs.oss.server-side-encryption-key-id`、`fs.oss.server-side-data-encryption` 三个配置，解析 AES256/KMS/SM4，并在 PutObject、CopyObject、multipart init 写入 OSS SSE headers。 | 1.4 的 `OSSFileIO` 存在，但没有 SSE 配置路径；这是能力增强，不是 bugfix。 |
| Spark vector search / streaming source | 2 | `b09282e7` 让 vector search 输出 `_row_id` 与 search score，`ScoreRecordIterator` 传递 row id，Spark reader/TVF/lateral path 同步；`a39e3e8e` 删除 Spark 自有 `JsonUtils`，改用 Paimon shaded `JsonSerdeUtil` 读写 streaming offset JSON。 | vector search row-id 是功能增强且 1.4 Spark TVF 形态较旧；`a39e3e8e` 的旧 unshaded Jackson 路径在 1.4 存在，适合评估回补。 |
| Flink lineage | 1 | `e70d047d` 新增 `lineage-use-catalog-key-as-identifier` catalog option，让 JDBC metastore 下 Table API lineage 可显式使用 `catalog-key.db.table`。 | 1.4 没有该 option；配置能力增强，非 bugfix。 |
| Iceberg REST catalog | 1 | `92e39e10` 让 `IcebergRestMetadataCommitter.createDatabase()` 捕获 `AlreadyExistsException`，处理多个 sink 首次提交同一 namespace 的 check-then-act 竞态。 | 1.4 同文件仍直接 `restCatalog.createNamespace(...)`；直接 bugfix 候选。 |
| Flink Chain Table lookup | 1 | `984f3590` 增加 Chain Table lookup join 支持：Chain stream scan pinned snapshot、bucket filter、delta branch refresh、lookup cache 限制、join key 校验和大量 IT case；同时修复 `ChainKeyValueFileReaderFactory.getDataSchema` 当前 branch 分支漏 `return`。 | 整体是 master 新功能，且 1.4 缺 `ChainTableStreamScan`；但 missing `return` 在 1.4 原样存在，可作为独立小修。 |
| Core data-evolution + deletion vectors compaction | 1 | `2e81be99` 新增 `DataEvolutionCompactDeletionVectorRewriter`，在 data-evolution compaction 后把旧 anchor file 的 DV 重写/搬迁到新 anchor file；Flink compact sink 和 Spark compact procedure 在 commit 前追加 DV index-only messages。 | 1.4 有部分 compaction 路径但缺 rewriter/operator/helper；如果 1.4 允许 DV 表 data-evolution compaction，需要适配验证。 |
| Core split serialization | 1 | `0a695abf` 新增 `SplitSerializer` v1 binary protocol，覆盖 Data/Incremental/Indexed/Chain/QueryAuth/Fallback splits，增加 golden files，并让 QueryAuth/Fallback Java serialization 走 versioned serializer。 | 跨 runtime 协议能力；非 release bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `08fbfd98` | `read_by_row_id` 原先会在调用时执行 lazy source 的 schema/empty 检查；补丁改为 lazy Dataset，并支持 `dynamic_options` time-travel/blob descriptor。 | stable patch-id `517de2b32405`; cherry `+`。 | `release-1.4` 不存在 `paimon-python/pypaimon/ray/read_by_row_id.py`。 | 不直接适用；对应 API 不在 1.4。 |
| `3952b562` | Ray `merge_into` 在 update/delete/insert prepare 后、最终 commit 前失败时会留下 orphan files；补丁跟踪 pending messages 并在 commit 未开始时 abort。 | stable patch-id `15573c191044`; cherry `+`。 | `release-1.4` 不存在 `paimon-python/pypaimon/ray/data_evolution_merge_into.py`。 | 不直接适用；1.4 没有该 Ray merge path。 |
| `85096ccc` | PyPaimon `DataEvolutionSplitGenerator._split_by_row_id` 用每个文件扫描所有 merged ranges 的 O(n^2) 分组；补丁改用 `RangeHelper.merge_overlapping_ranges` 并 fail fast 检查 missing row id。 | stable patch-id `e32abf98d624`; cherry `+`。 | `release-1.4` 存在旧 `_split_by_row_id` O(n^2) 实现，且 `pypaimon/utils/range_helper.py` 已存在；`chunk_shuffle_split_generator.py` 在 1.4 缺失。 | 直接/部分候选。建议回补 `data_evolution_split_generator.py` 和 `DataFileMeta.non_null_row_id_range` 相关部分，跳过 1.4 不存在的 chunk-shuffle path。 |
| `a39e3e8e` | Spark streaming source offset JSON 依赖 `org.apache.paimon.spark.util.JsonUtils`，内部使用 unshaded Jackson Scala module；补丁改用 Paimon shaded `JsonSerdeUtil` 手动读写字段并删除 Spark util。 | stable patch-id `931dd4408976`; cherry `+`。 | 1.4 仍有 `PaimonSourceOffset` 导入 `JsonUtils`，`JsonUtils.scala` 使用 unshaded `com.fasterxml.jackson.*`，`paimon-api` 已有 `org.apache.paimon.utils.JsonSerdeUtil`。 | 直接候选。适合回补以降低 Spark classpath/Jackson 冲突风险。 |
| `92e39e10` | 两个 Iceberg REST catalog sink 并发首次提交同一 namespace 时，双方都可能观察到 namespace 不存在并调用 `createNamespace`，输家因 `AlreadyExistsException` 失败；补丁将该异常视为 benign race。 | stable patch-id `ea1b2d65d1a`; cherry `+`。 | 1.4 的 `IcebergRestMetadataCommitter.createDatabase()` 仍直接调用 `restCatalog.createNamespace(Namespace.of(...))`，没有 catch。 | 直接 bugfix 候选。建议优先回补。 |
| `984f3590` | 大部分是 Chain Table lookup join 新功能；其中 `ChainKeyValueFileReaderFactory.getDataSchema` 对 current branch 调 `super.getDataSchema(fileMeta)` 后漏 `return`，会继续走 branch schema managers。 | stable patch-id `c2f531059d55`; cherry `+`。 | 1.4 同文件同方法仍有漏 `return`；但 1.4 缺 `ChainTableStreamScan`，整 commit 不可直接回补。 | 建议只摘取 one-line `return super.getDataSchema(fileMeta);`。整 commit 是功能 backport，不适合作为 bugfix 直接进 1.4。 |
| `2e81be99` | data-evolution compaction 在 deletion-vectors-enabled 表上需要把旧 anchor file 的 DV 重写到 compact 后的新 anchor file；补丁新增 rewriter 并接入 Flink/Spark compact commit 前流程。 | stable patch-id `b92b75037962`; cherry `+`。 | 1.4 存在 `DataEvolutionCompactCoordinator`、`DataEvolutionCompactTask`、Flink compact sink、Spark compact procedure，但缺 `DataEvolutionCompactDeletionVectorRewriter`、`DataEvolutionUtils` helper 和 Flink rewriter operator。 | 需要适配验证的正确性候选。如果 1.4 实际允许 DV-enabled data-evolution compaction，缺 rewriter 可能导致 DV index 指向旧文件；不建议不经测试整 commit 回补。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `08fbfd98` | PyPaimon Ray read_by_row_id | 3 files，`+196/-15`。`read_by_row_id` 增加 `dynamic_options`，拒绝覆盖 data-evolution/row-tracking/DV invariants；支持 `scan.snapshot-id`/`scan.tag-name` 解析到目标 snapshot；schema 检查改为 `fetch_if_missing=False`，避免调用时执行 lazy source；非空 target 下不再先做 empty-source precheck，而是返回 lazy result union typed empty block 保留 schema。 | cherry `+`，patch-id `517de2b32405`。1.4 无该 API，非直接回补。 |
| 2 | `3952b562` | PyPaimon Ray merge_into | 2 files，`+181/-35`。`_execute_and_commit` 用 `pending_msgs` 跟踪 update/delete/insert 已准备的 commit messages；在最终 commit 未开始前捕获异常并 `abort` pending messages；commit 对象在 `finally` close，close/abort 失败只 warning；测试覆盖 duplicate action row id、later branch failure、insert failure 和 commit-started 后不 abort。 | cherry `+`，patch-id `15573c191044`。1.4 无该 Ray merge path，非直接适用。 |
| 3 | `38392b21` | OSS filesystem | 3 files，`+337/-83`。`OSSFileIO` 新增 SSE config parser，支持 AES256/KMS/SM4、CMK key id、KMS+SM4 data encryption；通过替换 OSS object/multipart operation 在 PutObject、CopyObject、InitiateMultipartUpload 加 header；atomic write 只在 Paimon SSE 未设置时保留 hadoop native SSE fallback；测试覆盖 header stamping、copy wire headers、非法配置。 | cherry `+`，patch-id `4519ad2e8d59`。1.4 没有 SSE 配置能力；功能增强，不是 bugfix。 |
| 4 | `85096ccc` | PyPaimon data-evolution split planning | 5 files，`+156/-37`。新增 `DataFileMeta.non_null_row_id_range`；`DataEvolutionSplitGenerator._split_by_row_id` 和 chunk-shuffle DE grouping 改用 `RangeHelper` 一次性合并 overlapping row-id ranges；新增随机等价测试锁定原 O(n^2) 行为。 | cherry `+`，patch-id `e32abf98d624`。1.4 有旧 split planning path，适合部分回补。 |
| 5 | `b09282e7` | Spark vector search | 6 files，`+109/-21`。`ScoreRecordIterator` 新增 `returnedRowId()` 并透传 transform/filter；`IndexedSplitRecordReader` 从 row id column 记录 returned row id；Spark reader 允许 vector search metadata 同时包含 `_row_id` 和 `__paimon_search_score`；TVF output、lateral vector search read fields 和 IT case 同步。 | cherry `+`，patch-id `cd79d5391912`。功能增强；1.4 vector search 形态较旧，不作为 bugfix。 |
| 6 | `076db7e2` | PyPaimon multimodal defaults | 3 files，`+1/-3`。`CoreOptions.FILE_FORMAT` 默认值从 ORC 改为 Parquet，并删除 multimodal connection 中强制 `file.format=vortex`；vector file format 仍保留 `vortex`。 | cherry `+`，patch-id `1e316d120c0d`。行为默认值变更，不是 release bugfix。 |
| 7 | `a39e3e8e` | Spark streaming offset JSON | 3 files，`+17/-46`。删除 Spark 自建 `JsonUtils.scala`；`PaimonSourceOffset.json()` 改用 `JsonSerdeUtil.OBJECT_MAPPER_INSTANCE` 创建 ObjectNode，parse 时手动读取 `snapshotId`、`index`、`scanSnapshot`；`paimon-spark/pom.xml` 注释改为明确依赖 shaded Jackson。 | cherry `+`，patch-id `931dd4408976`。1.4 有同样 unshaded JsonUtils path，直接候选。 |
| 8 | `e70d047d` | Flink lineage | 4 files，`+52/-8`。新增 `FlinkCatalogOptions.LINEAGE_USE_CATALOG_KEY`；`LineageUtils.resolveNameByMetastore` 在 defaultName 为 null 或该 flag 为 true 且 JDBC metastore 时使用 `catalog-key.table.fullName`，否则保留显式 Flink catalog name；生成文档和单测同步。 | cherry `+`，patch-id `e5da09447110`。配置能力增强，非 bugfix。 |
| 9 | `92e39e10` | Iceberg REST catalog | 2 files，`+36/-2`。`createDatabase()` 加 `AlreadyExistsException` catch 并记录 info 日志；测试通过连续两次 `createDatabase()` 复现 namespace 已存在时不失败。 | cherry `+`，patch-id `ea1b2d65d1a`。1.4 同旧竞态路径存在，直接 bugfix 候选。 |
| 10 | `984f3590` | Flink Chain Table lookup | 12 files，`+1756/-46`。新增 Chain Table lookup join 文档和 IT；Chain stream scan pinned snapshot 增加 `scan.mode=from-snapshot`、bucket filter、incremental read merge-engine 校验；lookup table 为 chain table 改用 delta branch snapshot manager 刷新并限制 cache/join key；同时修复 `ChainKeyValueFileReaderFactory` 当前 branch schema path 漏 `return`。 | cherry `+`，patch-id `c2f531059d55`。整 commit 是功能 backport；1.4 只建议摘取 missing-return 小修。 |
| 11 | `2e81be99` | Data-evolution compaction with DVs | 9 files，`+602/-82`。新增 `DataEvolutionCompactDeletionVectorRewriter`：对 compact messages 的 before/after normal files 校验 row-id range，读取旧 anchor file DVs，按新 anchor file 重写/合并 DV index changes；Flink compact sink 在 DV 表上插入 single-parallelism rewriter operator；Spark compact procedure commit 前追加 rewriter messages；测试覆盖 DV merge、same-range rename、only existing DVs、blob compact keeps DVs。 | cherry `+`，patch-id `b92b75037962`。1.4 路径部分存在但 helper/operator 缺失；需要适配验证。 |
| 12 | `0a695abf` | Split serialization | 12 files，`+802/-9`。新增 `SplitSerializer` v1 magic/version/type-id binary protocol，覆盖 DataSplit、IncrementalSplit、IndexedSplit、ChainSplit、QueryAuthSplit、Fallback split；QueryAuthSplit 允许 null authResult 且 Java serialization 走 versioned serializer；新增 roundtrip/golden tests 和 compatibility resources。 | cherry `+`，patch-id `475550ea9548`。跨 runtime 协议功能，不是 release bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | sandbox DNS 失败后用联网权限重跑成功。 |
| `git fetch upstream master release-1.4` | 用联网权限成功；`upstream/master` 前进到 `0a695abf813c`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git rev-list --first-parent --count 09d8c2d0cdd939537c3633b2d0e746bc5a334bea..upstream/master` | 12 个 first-parent commit，均已纳入报告。 |
| `git diff --shortstat --find-renames 09d8c2d0cdd939537c3633b2d0e746bc5a334bea..upstream/master` | 64 files changed, 4245 insertions(+), 387 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <12 commits>` | 对 12 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <12 commits>` | 对 12 个 commit 均已执行并检查实际 patch hunks；大 patch 按生产文件分路径复查。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 12 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show --patch --format=medium <commit> \| git patch-id --stable` | 对 12 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 Python/Ray/DE、Spark JsonUtils、Iceberg REST、OSS SSE、Chain Table、data-evolution DV compaction、SplitSerializer 相关路径存在性和旧代码片段。 |
| Build / unit tests | 未运行 Maven 或 Python 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `0a695abf813c579d9ea994852407734be8660768`
- `last_processed_commit_date`: `2026-07-07 11:03:39 +0800`
- `last_report_date`: `2026-07-07`
- `last_report_path`: `reports/paimon-master-daily/2026-07-07.md`

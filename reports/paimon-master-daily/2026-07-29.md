# Apache Paimon Master Daily Report - 2026-07-29

## Summary

范围：`9ad20d997858c124ae33654f6a1fa694dc588211..a4bfec1bd2242847510c09ff7faf017a9ee2efbb`，共 28 个 first-parent 提交，191 个文件变更，`+15021/-2884`。本轮 `upstream/master` 前进到 `a4bfec1bd224`（2026-07-29 13:55:19 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

主要变化集中在 data-evolution row-id/manifest 扫描优化、LocalKvDb 取代 RocksDB lookup state、shared-shredding MAP selected-key pushdown、OSS blob presigned URL、PyPaimon 读写与聚合能力、common serializer/statistics 修复，以及 Spark postpone merge-on-read 指标。按实际 patch、`git cherry`、stable patch-id 和 release-1.4 代码路径判断，本轮最值得关注的 1.4 候选是 `d27d85ad0` 的 Flink lookup AUTO fallback scan mode 修复、`eb986103c` 的 BINARY/VARBINARY compacted row comparator 修复、`120b10bb6` 的 BinaryRow page-size fail-fast 保护，以及 `28f54f9c1` 的 binary column min/max stats 语义补齐。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core data-evolution row-id / manifest | 7 | 降低 row-id reassignment planning 内存；新增 `DataEvolutionRowIdAssignmentPlanner`、`BinaryDataFileMeta`、`BinaryManifestEntry`；用 primitive ranges / binary entries 优化 manifest scan/sort；移除危险 partition view 复用；用 projected scan 优化 deleted/expire entry 读取。 | 多数依赖 master 新引入的 `BinaryManifestEntry` 和 append data-evolution manifest pipeline；release-1.4 缺少 `BinaryManifestEntry`，不建议原样回摘。 |
| LocalKvDb / RocksDB lookup / Flink lookup | 3 | lookup/full-cache/global-index state 从 RocksDB 迁到 LocalKvDb；删除 RocksDB lookup state 代码和配置；修复 AUTO cache fallback 到 full cache 后仍用 AUTO 判断 lookup stream scan mode。 | LocalKvDb 替换是 master 架构演进；`d27d85ad0` 修复路径在 release-1.4 存在且旧逻辑一致。 |
| Shared-shredding MAP pushdown | 1 | core read plan、schema mapping 和 Spark optimizer 支持 MAP selected-key pushdown，并新增 Spark 3.2-4.1 E2E 覆盖。 | 功能增强；release-1.4 可独立评估需求，但不是 bugfix。 |
| Blob / FileIO / presigned URL | 1 | `FileIO` 增加 blob presign 能力；OSS/Jindo/REST/cache/resolving FileIO 适配；Flink/Spark 注册 descriptor-to-presigned-url UDF。 | 新功能；无直接 1.4 bugfix 信号。 |
| Common serializer / statistics / Vortex | 4 | BINARY/VARBINARY compacted-row comparator 改用 unsigned binary comparator；BinaryRow fixed part 超过 page size 时 fail fast；binary column min/max stats 支持 byte[]；Vortex 正确读取 PK format 物理 `_SEQUENCE_NUMBER`。 | 前三条 common 生产路径在 release-1.4 存在；Vortex module 在 release-1.4 缺失。 |
| PyPaimon read/write/functions | 8 | data-evolution append 表支持 row-count file rolling；支持 pypaimon-rust 0.4；chunk_shuffle 按 live rows 处理 DV；`$manifests` 渲染 min/max_partition_stats；新增 `rbm32` aggregator；补充 `product` 边界测试；修复 Python CI；Parquet row-id range 读按 row group pruning。 | 多数是 master Python 新能力或性能优化；`chunk_shuffle_split_generator.py` 在 release-1.4 不存在。 |
| Spark scan/metrics/tests | 2 | improve postpone merge-on-read metrics；补充 DV MOR level-0 count 测试。 | Spark 指标/测试增强，无直接 1.4 bugfix。 |
| Scalar global index defaults | 1 | `scalar-index.search-mode` 默认值从 `full` 改为 `fast`，并同步 Java/Python 测试和文档。 | 行为变更；release-1.4 未发现同名 scalar option，且不适合作为稳定分支 bugfix 直接回摘。 |
| Flink CDC test stability | 1 | `testSpecifyKeys` 等待时间从 30s 调整为 60s，降低 flaky timeout。 | 测试-only，无生产影响。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `d27d85ad0` | AUTO lookup cache fallback 到 full cache 后，把 table option 复制为 `lookup.cache-mode=FULL` 再创建 `FullCacheLookupTable.Context`，避免 scan mode 仍按 AUTO + PK join 走 `CONTINUOUS_FROM_SNAPSHOT` 之类旧判断。 | `git cherry` 为 `+`；stable patch-id `f361bc18262192eaa385adcf6055b9fee4e2a445`。 | release-1.4 存在 `FileStoreLookupFunction` 和 `LookupFileStoreTable`；旧代码在 `lookupTable == null` 时直接传原 table，没有 fallback option 覆写。 | 高；Flink lookup AUTO fallback 场景可达，建议优先 backport 并保留新增测试语义。 |
| `eb986103c` | `RowCompactedSerializer.createSliceComparator` 读取 BINARY/VARBINARY 字段得到 `byte[]` 时改用 `SortUtil.compareBinary`，避免把 `byte[]` 强转 `Comparable`。 | `git cherry` 为 `+`；stable patch-id `1b84c5bc48880e4ac6f14357ce4a0113f5193f53`。 | release-1.4 同文件仍直接 `((Comparable) o1).compareTo(o2)`，无 byte[] 分支。 | 高；common compacted row comparator 稳定路径，建议小范围 backport。 |
| `120b10bb6` | `BinaryRowSerializer.serializeToPages` 在 fixed-length part 大于 page size 时抛出清晰 `IllegalArgumentException`，避免跨 segment 写入导致 offset 腐化和后续异常。 | `git cherry` 为 `+`；stable patch-id `39ee9dd75936daa16f238793b7d8de7da8b57c52`。 | release-1.4 同文件只检查当前 segment 剩余空间，不检查 fixed part 本身是否大于 segment size。 | 高；common page serializer 稳定路径，建议 backport fail-fast guard 和测试。 |
| `28f54f9c1` | binary columns 的 full/truncate simple stats 开始用 unsigned binary comparator 维护 min/max，`SimpleColStats` 对 byte[] 用 deep equals/hash/toString。 | `git cherry` 为 `+`；stable patch-id `64c6028652ac3e2e094a10f65637519445ef7cac`。 | release-1.4 的 `FullSimpleColStatsCollector` 对非 `Comparable` 直接跳过，byte[] 无 min/max；同类 truncation 逻辑也无 byte[] 分支。 | 中；属于 stats correctness/feature 补齐，若 1.4 依赖 binary stats/pruning 可回摘。 |
| `9c71ad1f4` | Vortex reader 不再把 primary-key format 物理 `_SEQUENCE_NUMBER` 当成合成 row-tracking 字段跳过。 | `git cherry` 为 `+`；stable patch-id `bc46f1a14280792b831770bd5b4190b374bd6a4e`。 | release-1.4 缺少 `paimon-vortex/paimon-vortex-format/.../VortexRecordsReader.java`。 | 低；master-only Vortex 路径，不适用于 1.4。 |
| `1bc2588b5` | PyPaimon chunk_shuffle 按 live-row count 切分并附带 deletion files，避免 DV 删除行被计入 chunk 行数。 | `git cherry` 为 `+`；stable patch-id `228cad0b8b50313e10d7828da5dd7d51571fc7a8`。 | release-1.4 缺少 `read/scanner/chunk_shuffle_split_generator.py`，只有较旧 split generator 架构。 | 低；master Python chunk_shuffle 新路径，不建议直接回摘。 |
| `903aa197a` | `BinaryManifestEntry.partition()` 不再复用可变 partition view，避免 replace 后旧 view 指向新 partition。 | `git cherry` 为 `+`；stable patch-id `e9354be92e5f6131decd974a221f9e6f4050abb9`。 | release-1.4 缺少 `BinaryManifestEntry`。 | 低；master-only binary manifest entry 修复。 |
| `4bf72abde` / `75d53ac1e` | Flink flaky test timeout 与 Python CI 检查修复。 | `git cherry` 均为 `+`；stable patch-id 分别为 `78bebc5e93787dfd2191766bfde3e5cd3d8d4299`、`28bd976ce9d67e25d0e21d709d7d791de06f5f83`。 | 只改测试/CI 文件。 | 无生产影响。 |
| `a4bfec1bd` | PyPaimon Parquet row-id range read 按 row group pruning，并避免把大范围展开成 Python row index list。 | `git cherry` 为 `+`；stable patch-id `950cd7dc51e9eb166e4268e77f4e321d1e7e4547`。 | release-1.4 有 `FormatPyArrowReader`/`DataFileBatchReader`，但 IndexedSplit row_ranges 仍通过 `RowIdFilterRecordBatchReader` 后置过滤；本 patch 是性能优化和新 reader plumbing。 | 中低；不是 correctness bug，可按 Python 1.4 性能需求独立评估。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `15261fa0c` | Core data evolution | `DataEvolutionRowIdReassigner` 和 `RowRangeMappingIndex` 改为更紧凑的 row-range mapping；测试新增 legacy reassigner 对比，覆盖大规模文件/删除场景内存规划。 | master data-evolution row-id planning 优化；未见直接 1.4 bugfix。 |
| 2 | `ee771269f` | LocalKv / lookup | lookup/full-cache/global-index state 使用 LocalKvDb-backed list/set/value state；调整 full/no-PK/PK lookup table close、managed-memory 文案和 benchmark。 | 架构替换；release-1.4 仍是 RocksDB lookup state，不建议原样回摘。 |
| 3 | `bcf2c2b27` | LocalKv / RocksDB removal | 删除 `paimon-core/.../lookup/rocksdb` 生产类、RocksDB 配置文档和相关依赖；lookup factory/table 测试迁到 LocalKv。 | master cleanup；1.4 仍依赖 RocksDB 路径，无 bugfix。 |
| 4 | `6fa106eb6` | Core manifest / row-id | 新增 `DataEvolutionRowIdAssignmentPlanner`、`BinaryDataFileMeta`、`BinaryManifestEntry`，把 row-id assignment manifest 处理改为 binary/projection 读取。 | 依赖 master 新类，release-1.4 缺少 `BinaryManifestEntry`。 |
| 5 | `877c9e42e` | Core / Spark MAP | shared-shredding MAP 支持 selected-key pushdown；`FormatReaderMapping`、schema manager factory 注册和 Spark optimizer/extension 增加 key path 透传。 | 功能增强，不是 1.4 bugfix。 |
| 6 | `e039421b2` | Blob / FileIO / Flink / Spark | `FileIO` 增加 `generatePresignedUrl`；OSS/Jindo/REST/cache/resolving FileIO 适配；Flink/Spark 新增 descriptor-to-presigned-url 函数和测试。 | 新功能，不建议作为稳定分支 bugfix。 |
| 7 | `ac5ab5159` | Core manifest / row-id | 新增 primitive byte-array key/range/list utils 和 `CurrentRowIdEntries`，减少 row-id assignment manifest 扫描对象分配，并补充 binary manifest 可复用 identifier 测试。 | master 优化；release-1.4 缺少关键 binary manifest 路径。 |
| 8 | `d27d85ad0` | Flink lookup | AUTO cache fallback 时复制 table option 为 FULL 后创建 full-cache context，并暴露 lookup scan mode helper 供测试验证。 | 高影响 1.4 候选。 |
| 9 | `9c71ad1f4` | Vortex | `physicalReadRowType/physicalFieldMapping` 用 data schema 判断 row-tracking 字段是否为合成字段，避免 PK 物理 `_SEQUENCE_NUMBER` 被映射为 -1。 | release-1.4 无 Vortex module，低影响。 |
| 10 | `eb986103c` | Common serializer | compacted row slice comparator 对 byte[] 改用 `SortUtil.compareBinary`，测试覆盖 unsigned byte order。 | 高影响 1.4 候选。 |
| 11 | `3120ac76f` | PyPaimon write | 新增 `data-evolution.row-count.target-file-size` Python option，append data-evolution writer 按行数 rolling，并补充 row rolling 测试。 | Python 功能增强；不是 bugfix。 |
| 12 | `2ce4f09fe` | PyPaimon build | Python checks workflow 支持 Rust 0.4 feature，`setup.py` 放宽 `pypaimon-rust<0.5`。 | 依赖/CI 适配；无生产代码 bugfix。 |
| 13 | `28f54f9c1` | Common statistics | full/truncate stats collector 支持 byte[] min/max，Parquet extractor 返回 binary stats，`SimpleColStats` 正确比较/打印 byte[]。 | 中等 1.4 候选，取决于 binary stats 需求。 |
| 14 | `1bc2588b5` | PyPaimon scanner | chunk_shuffle split generator 读取 deletion vectors，按 live rows 切片，维护 exact merged row count，并让 append/data-evolution split 附带 deletion files。 | release-1.4 缺少该 generator，低影响。 |
| 15 | `93089092f` | PyPaimon system table | `$manifests` system table 用 `row_to_string` 渲染 min/max partition stats，新增 row-to-string helper 和 system table 测试。 | Python system table 展示增强；不是 bugfix。 |
| 16 | `120b10bb6` | Common serializer | `BinaryRowSerializer` 检查 fixed part 是否大于 page size，失败时给出包含 page-size/numFields 的异常；更新 Flink writer 测试 page-size。 | 高影响 1.4 候选。 |
| 17 | `125d4e9b8` | PyPaimon aggregation | 新增 `rbm32` field aggregator，校验 VARBINARY/BYTES 类型并用 RoaringBitmap OR 合并。 | 新功能。 |
| 18 | `d98a14741` | PyPaimon tests | 仅补充 `product` aggregator 的 BIGINT 边界、DECIMAL precision/scale/除法精度测试。 | 测试-only。 |
| 19 | `4bf72abde` | Flink CDC tests | MySQL CDC `testSpecifyKeys` 等待从 30s 调到 60s，降低 flaky timeout。 | 测试-only。 |
| 20 | `0a48a1687` | Core row-id | `CurrentRowIdEntries` 重命名为 `LiveFileRowIdRangeCollector`，澄清 live-file row-id range collection 语义并调整测试。 | master 重命名/clarify，低影响。 |
| 21 | `bbcac771f` | Core manifest sorting | manifest sorting 使用 `BinaryManifestEntry` 和 binary `DataFileMeta`，`DeletedIdentifierSet` 移入 manifest 包，并新增 sorter benchmark。 | master 性能优化；release-1.4 缺少 binary manifest path。 |
| 22 | `bf6934a22` | Spark metrics | postpone merge-on-read scan metrics 增加 input splits/bytes/count time/scan time，并调整 physical exec metrics 暴露。 | Spark 可观测性增强。 |
| 23 | `a3121b7a1` | Spark tests | 增加 DV merge-on-read level-0 files 的 count aggregate pushdown 测试。 | 测试-only。 |
| 24 | `903aa197a` | Core manifest | `BinaryManifestEntry.partition()` 改为每次 deserialize binary row，避免复用 partition view 导致旧引用被 replace 改写。 | master-only 修复，release-1.4 无该类。 |
| 25 | `75d53ac1e` | PyPaimon CI tests | 调整 Python 3.6 REST append-only read/write 测试和 shard table updator 测试，修复 CI 检查。 | 测试-only。 |
| 26 | `dd5ce17c2` | Core manifest scan | `readDeletedEntries` 先过滤 `numDeletedFiles>0` 并用 `BinaryManifestEntry.DELETE_ENTRY_PROJECTION` scan；`readExpireFileEntries` 改为 projected scan 只读过期所需 file meta 字段。 | master manifest scan 性能优化。 |
| 27 | `6c9c05726` | Core / Python global index | Java/Python `scalar-index.search-mode` 默认从 FULL 改 FAST，测试改为默认 drop unindexed rows，full mode 需显式配置。 | 行为变更；release-1.4 未发现同名 option。 |
| 28 | `a4bfec1bd` | PyPaimon Parquet reader | Parquet row-id range read 选择相交 row groups 并按 compact ranges slice batch；`DataFileBatchReader` 支持 range cursor，避免大范围展开 row indices。 | 性能候选；非直接 bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，本轮开始工作区干净且与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功，fork 自动化分支已刷新。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功，`upstream/master=a4bfec1bd2242847510c09ff7faf017a9ee2efbb`；`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| `git log --first-parent --reverse 9ad20d997858..upstream/master` | 28 个 first-parent commit。 |
| `git diff --shortstat 9ad20d997858..upstream/master` | 191 files，`+15021/-2884`。 |
| `git show --stat --name-status --patch --format=fuller --find-renames <commit>` | 已为 28 个提交逐个生成并检查 patch，缓存于 `/private/tmp/paimon-master-daily-20260729/patches`。 |
| `git cherry -v upstream/release-1.4 upstream/master 9ad20d997858` | 28 个提交全部为 `+`，未发现 release-1.4 等价 patch。 |
| per-candidate `git patch-id --stable` | 已记录 10 个候选/疑似 bugfix 的 stable patch-id。 |
| release-1.4 targeted `git cat-file` / `git show upstream/release-1.4:<path>` / `rg` | 已核验 Flink lookup、RowCompactedSerializer、BinaryRowSerializer、binary stats、Python reader、Vortex/BinaryManifestEntry/chunk_shuffle 路径存在性和旧逻辑。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报和推进 state。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `a4bfec1bd2242847510c09ff7faf017a9ee2efbb`
- `last_processed_commit_date`: `2026-07-29 13:55:19 +0800`
- `last_report_date`: `2026-07-29`
- `last_report_path`: `reports/paimon-master-daily/2026-07-29.md`

# Apache Paimon Master Daily Report - 2026-07-15

## Summary

范围：`4fe3cfdd131af7799259d6c4307d89a884c4b011..dd2a065b64ea6ad888964f0e7af5512ea41b23ef`，共 23 个 first-parent 提交。当前 Apache upstream `master` head 是 `dd2a065b64ea`（2026-07-15 10:19:19 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 226 个文件，`+19948/-1688`。

本次主线集中在三块：一是 global index 扩展，新增 `paimon-eslib` Lucene/Elasticsearch index engine，并修复多字段 global index 的 full-text / scalar filter 覆盖、ESLib packaging/query/vector score；二是 BLOB 能力，支持 `ARRAY<BLOB>`，并新增 primary-key table managed BLOB 外部化、引用文件和 orphan cleanup；三是 primary-key sorted index，补齐 LSM maintenance、global-index 文档、Flink/Spark IT。其他变化包括 Flink migrate/action/help 修复、FLIP-510 key-only deletes、Parquet/ORC/OffsetSeekableInputStream 读取修复、OBS FileSystem authority cache、Python `listagg` 聚合和 Lumina JNI 升级。

`git cherry -v upstream/release-1.4 upstream/master 4fe3cfdd131af7799259d6c4307d89a884c4b011` 显示 23 个提交均为 `+`，没有 patch-id 等价提交已进入 `release-1.4`。明确的 release-1.4 直接 bugfix 候选包括 `34bd5a16`（Flink migrate action 未传 `--parallelism` 崩溃）、`2f36427b`（bounded bulk read 越界/0 长度语义）、`8852d82f`（OBS FileSystem 按 authority 缓存）、`9a5661e5`（Parquet INT64 窄化读 TINYINT/SMALLINT）和 `6211b651`（ORC `isNaN` filter pushdown 应跳过而不是失败）。`c2e3a761` 与 `0a084375` 是 1.4 可回补的诊断/帮助文本修复。`1a44193e` 是 global-index correctness 候选，但 master 补丁依赖多字段 extra-field / `IndexFullTextSearchSplit` 等 1.4 不完整路径，需要按 1.4 代码形态拆分适配。

所有 commit 都先执行 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在带 `--name-status` 时不展开 hunk，又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch。release-1.4 判断使用 `git cherry`、`git patch-id --stable`、`git cat-file`/`git ls-tree` 和 targeted `git show upstream/release-1.4:<path>` 对照生产路径。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Global index / ESLib / vector search | 5 | `d37e503d` 新增 `paimon-eslib` 模块、Lucene-backed reader/writer/options/E2E；`1a44193e` 修 full-text row-range selection、multi-field scalar coverage、`IS NOT NULL` pruning；`a910c615` 改 ESLib thin packaging、query compatibility 和 vector score metric；`045d8f0e` 限制 vector live-row planning 只扫描 indexed ranges；`dd2a065b` 升级 `lumina-jni` 到 0.4.0。 | ESLib 与 vector live-row master 路径在 1.4 缺失；`1a44193e` 仅部分 global-index evaluator 思路可能适配；Lumina 是依赖升级非 bugfix。 |
| BLOB / managed BLOB | 3 | `ee865220` 支持 `ARRAY<BLOB>`，新增 `BlobArrayPlaceholder`、schema/comment directive、Java/Flink/Spark/Python reader/writer 流程；`18fcff65` 关闭 Python exhausted blob fallback readers；`db2c6183` 新增 PK table managed BLOB reference collector/file/externalizer、writer 接线和 orphan cleanup。 | `ARRAY<BLOB>`、Python blob fallback、managed PK BLOB 均依赖 master 新 BLOB 栈；不是 1.4 直接 bugfix。 |
| Primary-key sorted index | 2 | `b524bae1` 为 sorted PK index 新增 LSM maintenance、`PrimaryKeyIndexLevels`、bucket state、payload builder 和 bitmap format 调整；`711481be` 补 global-index 文档、redirect/sidebar、batch scan 接线和 Flink/Spark sorted-index 测试。 | 新功能栈；1.4 无对应 `pksorted` 生产路径。 |
| Flink connector / actions | 4 | `0a084375` 修 action help 参数名；`34bd5a16` 允许 migrate action 省略 parallelism；`c2e3a761` 修 `scan.partitions` 错误消息 placeholder；`558765aa` 新增 FLIP-510 key-only deletes opt-in。 | `34bd5a16` 直接运行时候选；`0a084375`/`c2e3a761` 是低风险诊断修复；`558765aa` 是新能力。 |
| Format / common read path | 3 | `2f36427b` 修 `OffsetSeekableInputStream.read(byte[],off,len)` 的 null/bounds/0 长度和 EOF 语义；`9a5661e5` 为 Parquet INT64 -> TINYINT/SMALLINT 增加 updater；`6211b651` 让 ORC `isNaN` 返回 `Optional.empty()` 跳过 pushdown。 | 三个都是 1.4 直接候选，生产路径存在且 release 代码仍缺对应 guard/updater/visitor。 |
| Filesystems / docs / tests | 4 | `8852d82f` 将 OBS Hadoop `FileSystem` cache 从单实例改为 authority map；`bafcd970`/`298f031d` 修 OBS/S3 loader Javadoc；`8ba25a80` 修 e2e result sink 列名标签。 | `8852d82f` 是 OBS 运行时直接候选；其余为文档或测试-only。 |
| Python aggregation | 1 | `67779ac5` 新增 PyPaimon `listagg` aggregate function、options 和单测覆盖。 | 新功能，非 bugfix。 |
| Spark test stability | 1 | `e425fb42` 让 `DeletionVectorTest` 在 V2 delta path 下跳过 split-scan assertion，按 `SparkTable.supportsV2DeltaOps` 判断。 | 测试-only；不影响 1.4 runtime。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `1a44193e` | Multi-field global index 的 full-text range selection / scalar extra-field coverage 修复，并避免 AND 中冗余 `IS NOT NULL` 让 evaluator 误判。 | `git cherry` 为 `+`；patch-id `89e0b2542467...`。 | 1.4 有 `GlobalIndexEvaluator` / `FullTextScanImpl`，但扫描仍按 `indexFieldId == textColumn.id()`，且缺 master 的 `IndexFullTextSearchSplit`、extra-field range selection 和 vector live-row 新路径。 | Correctness 候选但不能直接 cherry-pick；若 1.4 要修 global-index predicate 覆盖，需要按单字段/旧 scanner 形态拆分适配并补测试。 |
| `e425fb42` | Spark `DeletionVectorTest` 兼容 V2 delta write path。 | `git cherry` 为 `+`。 | 只改 `paimon-spark-ut` 测试断言。 | 测试-only，无 release runtime 影响。 |
| `a910c615` | ESLib thin packaging、legacy query `column` hint 兼容、vector score metric 转换。 | `git cherry` 为 `+`。 | 1.4 无 `paimon-eslib` 模块。 | master 新模块修复，不适用于 1.4。 |
| `18fcff65` | Python blob fallback batch reader 在 selected ranges 耗尽后关闭底层 reader，减少资源占用。 | `git cherry` 为 `+`；patch-id `ef2113ef4f84...`。 | 1.4 的 `concat_batch_reader.py` 只有通用 reader / data-evolution merge reader，缺 master 的 `BlobFallbackBatchReader`。 | 不直接适用 1.4。 |
| `0a084375` | Flink action help 中补/删实际参数名，如 `time_retained`、`tag_name`。 | `git cherry` 为 `+`；patch-id `a87131a046ba...`。 | 1.4 有对应 action factory help 文本。 | 可回补的帮助文本修复；不影响作业运行语义。 |
| `34bd5a16` | `migrate_database` / `migrate_table` 不传 `--parallelism` 时不再 `Integer.parseInt(null)` 崩溃，改为 `null` 默认值。 | `git cherry` 为 `+`；patch-id `c250153b2474...`。 | 1.4 对应两个 factory 仍直接 `Integer.parseInt(params.get(PARALLELISM))`。 | 直接候选；影响未显式指定 parallelism 的 Hive migrate action。 |
| `c2e3a761` | `scan.partitions` 非分区表错误消息 placeholder 从 `{}` 改为 `%s`。 | `git cherry` 为 `+`；patch-id `85be37f96919...`。 | 1.4 `PartitionLoader` 仍使用 `{}`，会打印未替换占位符。 | 可回补的诊断修复；不改变执行结果。 |
| `bafcd970` / `298f031d` | OBS/S3 loader Javadoc 指向修正。 | `git cherry` 均为 `+`。 | 仅 Javadoc。 | 文档-only。 |
| `8ba25a80` | e2e 测试 result sink 列名标签修正。 | `git cherry` 为 `+`。 | 仅 e2e 测试。 | 测试-only。 |
| `2f36427b` | Bounded `OffsetSeekableInputStream` bulk read 对 null/bounds/0 长度按 `InputStream` 语义处理，并在 `remaining <= 0` 时返回 `-1`，避免 seek past end 后负 len 传到底层。 | `git cherry` 为 `+`；patch-id `b5f686983076...`。 | 1.4 同文件仍用 `len = min(len, length - getPos())` 且只判断 `len == 0`。 | 直接候选；影响 bounded stream 的 EOF/zero-length/bounds 语义，可能触发底层读异常。 |
| `8852d82f` | OBS Hadoop `FileSystem` 不再全局单实例缓存，改为按 URI authority 缓存。 | `git cherry` 为 `+`；patch-id `43ef26a214de...`。 | 1.4 仍只有 `protected transient volatile FileSystem fs` 单实例缓存。 | 直接候选；同一 FileIO 访问不同 OBS authority 时避免复用错误 FileSystem。 |
| `045d8f0e` | Vector search 只对 indexed ranges 规划 live rows，避免未索引范围的 deletion-vector 文件影响 indexed search。 | `git cherry` 为 `+`；patch-id `294a5923685d...`。 | 1.4 缺 `AbstractVectorRead` / `GlobalIndexLiveRowFilter` master 路径。 | 不直接适用；属于 master vector search 栈修复。 |
| `9a5661e5` | Parquet vectorized reader 允许用 INT64 physical type 窄化读取 TINYINT/SMALLINT，新增 `ByteFromLongUpdater` / `ShortFromLongUpdater`。 | `git cherry` 为 `+`；patch-id `3021d0b8835b...`。 | 1.4 `ParquetVectorUpdaterFactory` 对 TINYINT/SMALLINT 仍固定 `ByteUpdater` / `ShortUpdater`。 | 直接候选；影响 schema evolution 或外部 Parquet 中 BIGINT 文件按 tiny/smallint 读取。 |
| `6211b651` | ORC filter converter 对 `isNaN` 返回 `Optional.empty()`，跳过 SearchArgument pushdown，避免 unsupported visitor 失败。 | `git cherry` 为 `+`；patch-id `32736f1aeccf...`。 | 1.4 `OrcPredicateFunctionVisitor` 缺 `visitIsNaN` override。 | 直接候选；影响 ORC 表 float/double `isNaN` 谓词。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `d37e503d` | ESLib global index | 新增 `paimon-eslib` 模块：`ESIndexGlobalIndexReader/Writer/Options/FileMeta`、service registration、README 和 E2E/options/query parser 测试，并接入 root `pom.xml`。 | 新模块；1.4 无 `paimon-eslib`。 |
| 2 | `1a44193e` | Global index correctness | `GlobalIndexEvaluator` 增加 null-rejecting sibling pruning；`GlobalIndexScanner` union dedicated/extra-field readers；`FullTextScanImpl` 按 index identity 和 row-range 边界切分 split；`FullTextReadImpl` 按 split 自身 meta 建 reader。 | 需适配候选，非直接 cherry-pick。 |
| 3 | `e425fb42` | Spark tests | `DeletionVectorTest` 用 `SparkTable.supportsV2DeltaOps` 判断 V2 delta path，避免强制要求 `PaimonSplitScan`。 | 测试-only。 |
| 4 | `ee865220` | ARRAY<BLOB> | `BlobType` 支持 array element display；新增 `BlobArrayPlaceholder`；schema/comment directive、Java/Python/Flink/Spark row wrapper、blob format reader/writer、data-evolution split read 全面接入 array blob。 | 新 BLOB 能力，非直接。 |
| 5 | `a910c615` | ESLib fixes | `paimon-eslib` 去除 shade packaging，改 GitHub raw repository；ES reader 接受 legacy query `column` hint；vector score 按 metric 转 Paimon score；indexer 实现 `VectorGlobalIndexer.metric()`。 | 依赖新模块，非 1.4。 |
| 6 | `b524bae1` | PK sorted index LSM | 增加 `PrimaryKeyIndexLevels`、sorted bucket state/build/maintainer、bitmap format reader/writer 调整和 schema validation；维护 sorted payload 与 active source files 的 LSM lifecycle。 | 新功能栈。 |
| 7 | `711481be` | PK sorted index completion | 用 `global-index.mdx` 替代 `vector-index.md`，补 redirect/sidebar；`PrimaryKeyBatchScan` 接入 sorted index；新增 Flink IT 和 Spark SQL sorted-index 测试。 | 新功能栈。 |
| 8 | `18fcff65` | Python BLOB | `BlobFallbackBatchReader` 在 selected ranges 耗尽时调用 `_close_state_reader`，并让 overlap check 持有实例方法以便关闭 reader。 | 1.4 缺对应 reader。 |
| 9 | `0a084375` | Flink action help | 修多处 action help 参数名：补 `--time_retained`，删除 expire partitions 的错误 `--tag_name`，统一 timestamp/watermark/snapshot action 参数。 | 可回补但低优先级。 |
| 10 | `34bd5a16` | Flink migrate actions | `MigrateDatabaseActionFactory` / `MigrateTableActionFactory` 对 `PARALLELISM` 使用 `params.has`，缺省传 `null`，并在 help 中标出可选参数。 | 直接 1.4 候选。 |
| 11 | `c2e3a761` | Flink lookup diagnostics | `PartitionLoader` 的 `Preconditions.checkArgument` format 从 `{}` 改 `%s`，新增非分区表 `scan.partitions` 单测。 | 可回补诊断修复。 |
| 12 | `bafcd970` | OBS docs | `OBSLoader` Javadoc 从 Alibaba OSS 改为 Huawei OBS Storage。 | 文档-only。 |
| 13 | `8ba25a80` | E2E tests | `FileStoreBatchE2eTest` 修 result sink column 标签。 | 测试-only。 |
| 14 | `2f36427b` | Common FS stream | `OffsetSeekableInputStream.read(byte[],off,len)` 增加 null/bounds/zero-length 检查，`remaining <= 0` 返回 `-1`，并避免负长度下传。 | 直接 1.4 候选。 |
| 15 | `8852d82f` | OBS FileIO | `HadoopCompliantFileIO` 从单 `FileSystem fs` 改为 `ConcurrentHashMap<authority, FileSystem>`，无 authority 使用 `DEFAULT`。 | 直接 1.4 候选。 |
| 16 | `298f031d` | S3 docs | `S3Loader` Javadoc 从 OSS 改为 S3。 | 文档-only。 |
| 17 | `045d8f0e` | Vector live rows | `AbstractVectorRead` 从 index splits 提取 row ranges，传给 `GlobalIndexLiveRowFilter.liveRows`，SnapshotReader 用 `withRowRanges` 只读 indexed ranges。 | master vector 路径，非直接。 |
| 18 | `67779ac5` | PyPaimon aggregation | 新增 `listagg` option 和 `FieldListaggAgg`，覆盖 bool/listagg 聚合单测。 | 新功能。 |
| 19 | `9a5661e5` | Parquet reader | `ParquetVectorUpdaterFactory` 对 TINYINT/SMALLINT 遇到 INT64 physical type 时使用 long reader/dictionary decoder 后窄化。 | 直接 1.4 候选。 |
| 20 | `6211b651` | ORC filter | `OrcPredicateFunctionVisitor.visitIsNaN` 返回 `Optional.empty()`，让 engine 层过滤而不是 ORC SARG pushdown。 | 直接 1.4 候选。 |
| 21 | `558765aa` | Flink key-only deletes | 新增 `sink.key-only-deletes.enabled`，在 PK sink 且 merge/changelog 条件允许时通过 Flink 2.1 `ChangelogMode` API 启用 key-only deletes，并在不适用时 warn。 | 新能力，非 bugfix。 |
| 22 | `db2c6183` | Managed BLOB for PK tables | 新增 managed blob reference collector/file/externalizer；`KeyValueDataFileWriter` 外部化 BLOB 并提交 extra files；orphan cleanup 排除/清理 managed blob references；Flink/Spark procedure 接线。 | 新 BLOB 能力，非直接。 |
| 23 | `dd2a065b` | Vector dependency | `paimon-lumina` 将 `lumina-jni` 从 0.3.0 升到 0.4.0。 | 依赖升级；1.4 仍为 0.1.0，非 bugfix。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master` 前进到 `dd2a065b64ea`，`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse 4fe3cfdd131af7799259d6c4307d89a884c4b011..upstream/master` | 23 个 first-parent commit，均已纳入报告。 |
| `git diff --stat --shortstat --find-renames 4fe3cfdd131af7799259d6c4307d89a884c4b011..upstream/master` | 226 files changed, 19948 insertions(+), 1688 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <23 commits>` | 对 23 个 commit 均已执行，输出缓存到 `/private/tmp/paimon-daily-2026-07-15-patches` 作为审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <23 commits>` | 对 23 个 commit 均已执行并检查实际 patch hunks；大 patch 使用 targeted file checks 补充。 |
| `git cherry -v upstream/release-1.4 upstream/master 4fe3cfdd131af7799259d6c4307d89a884c4b011` | 23 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted checks | `git cat-file` / `git ls-tree` / `git show upstream/release-1.4:<path>` 确认 Flink migrate、OffsetSeekableInputStream、OBS FileIO、Parquet updater、ORC visitor 等候选生产路径存在且缺对应修复。 |
| Build / unit tests | 未运行 Maven 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `dd2a065b64ea6ad888964f0e7af5512ea41b23ef`
- `last_processed_commit_date`: `2026-07-15 10:19:19 +0800`
- `last_report_date`: `2026-07-15`
- `last_report_path`: `reports/paimon-master-daily/2026-07-15.md`

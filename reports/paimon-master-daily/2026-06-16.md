# Apache Paimon Master Daily Report - 2026-06-16

## Summary

范围：`575ce805ed069ab380d762ee6d9556f8393c6e16..cd5460321378175dfb9aa704e40093761a7bab59`，共 12 个 first-parent 提交。当前 Apache upstream `master` head 是 `cd5460321378`（2026-06-16 14:05:49 +0800），`release-1.4` 对照 head 仍是 `53815aebfd8d`。

本次最大变化集中在 Python 读写与提交路径：新增 manifest minor compaction、blob update 与并发 COMPACT 的冲突检测、大字段 Arrow RecordBatch 拆分，以及 earliest snapshot 并发过期重试。Java core/common 侧有两个明显 release-relevant bugfix：二进制 predicate 比较按无符号字节排序、earliest snapshot 重试窗口从 3 扩到 300 并改为失败显式报错。Vector/Lumina 侧主要是全局向量索引配置能力与 VECTOR 类型维度修正；Spark 和 docs 是依赖升级。

本地 shell `git fetch upstream master release-1.4` 被沙箱网络拦截且提升审批超时；实际 upstream 数据通过 GitHub commit/compare API 获取，并逐个读取 commit diff。`release-1.4` 影响判断使用本地已有 `upstream/release-1.4`（head 与 GitHub compare 返回一致为 `53815aebfd8d`）检查生产文件存在性和关键修复 hunk。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Paimon Python read/write/snapshot | 4 | `pypaimon` 增加 manifest minor compaction；blob row-id update 检测 COMPACT 删除的同类 anchor file；大 STRING/BYTES 读取在 pyarrow 返回 `ChunkedArray` 时拆成多个 `RecordBatch`；earliest snapshot 查找向前重试。 | 多数生产文件在 `release-1.4` 存在且未含关键修复 hunk；Python 用户受影响时应评估 backport。 |
| Core snapshot expiration / time travel | 2 | `SnapshotManager` 抽出 `retryEarliestSnapshot`，重试窗口 3 -> 300；snapshot/changelog expiration 规划和删除改为并行执行，并去重 manifest/index/stat deletion。 | earliest snapshot bugfix 对 `release-1.4` 明确相关；expiration 并行化偏性能/重构，非必须 backport。 |
| Common catalog / predicate | 2 | `CatalogContext` 在 Hadoop class 缺失时允许无 Hadoop configuration 的 context；`CompareUtils` 将 byte 比较改为 unsigned。 | `CompareUtils` 修复明确影响 `release-1.4`；no-Hadoop catalog 属兼容性增强。 |
| Vector / Lumina global index | 2 | Vector global index 支持 `fields.<field>.<option>` 覆盖；Lumina 对 `VectorType` 使用 schema length 作为 dimension，并拒绝显式冲突。 | `paimon-vector` 文件在 `release-1.4` 缺失；Lumina 文件存在但无 VECTOR dimension 修复，只有使用 Lumina VECTOR 时相关。 |
| Spark / docs dependencies | 3 | Spark 4 module 从 4.0.2 升到 4.0.3；docs `yarn.lock` 升级 `@babel/core` 和 `launch-editor`。 | 主要是依赖维护；除 Spark 4.0.3 需求外，release 产品影响低。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | Touched production files on release-1.4 | Likely release-1.4 impact |
| --- | --- | --- | --- | --- |
| `7487409ac0` | `CompareUtils.compare(byte[])` 从 signed subtraction 改为 `Byte.toUnsignedInt`，修复 high-bit binary/bytes predicate ordering。 | 未发现等价修复 hunk；`release-1.4` 仍为 `first[x] - second[x]`。 | `paimon-common/src/main/java/org/apache/paimon/predicate/CompareUtils.java` 存在。 | 高。BYTES/BINARY 范围谓词、manifest/value stats pruning 或过滤路径可能误判，建议 backport。 |
| `45895a719e` | earliest snapshot/changelog 被并发过期时，Java/Python 向前重试更大窗口，并在耗尽时显式报错。 | 未发现等价修复 hunk；`release-1.4` Java 仍是 `EARLIEST_SNAPSHOT_DEFAULT_RETRY_NUM = 3` 且无 `retryEarliestSnapshot`。 | Java `SnapshotManager`、`TimeTravelUtil` 存在；Python `snapshot_manager.py` 存在但无新 retry 常量/报错。 | 中高。并发 snapshot expiration + time travel / earliest scan 容易触发，建议 backport。 |
| `c834ed39b7` | Python blob row-id update 在 stale snapshot 遇到 COMPACT 删除 anchor blob file 时拒绝提交，避免后续读失败或表损坏。 | 未发现等价修复 hunk；release 分支存在相关 Python commit scanner/conflict detection 文件但无 raw DELETE entry 读取和 `_compact_conflicts_with_delta`。 | `paimon-python/pypaimon/write/commit/conflict_detection.py`、`commit_scanner.py` 存在。 | 中高。若 `release-1.4` 暴露 Python blob update / row-id update，建议 backport；否则影响限于未启用功能。 |
| `4c0d1ae58c` | Python read path 遇到单列超过 pyarrow 32-bit offset 上限时，将 rows 递归拆成多个 `RecordBatch`，避免 `ChunkedArray` 转换异常。 | 未发现等价修复 hunk；`release-1.4` `table_read.py` 无 `_emit_overflow_safe_batches`。 | `paimon-python/pypaimon/read/table_read.py` 存在。 | 中。大 STRING/BYTES 读取场景会受影响，建议按 Python 使用强度评估 backport。 |
| `274d1de9c8` | Lumina VECTOR 字段用 schema length 解析 dimension，并拒绝 `lumina.index.dimension` 与 VECTOR length 冲突。 | 未发现等价修复 hunk；release Lumina 文件存在但无 `validateAndResolveDimension` / explicit dimension conflict 检查。 | `paimon-lumina/src/main/java/org/apache/paimon/lumina/index/*` 存在。 | 中。仅影响 Lumina + `VectorType` 用户；ARRAY<FLOAT> 路径基本不变。 |
| `c3dd46447e` | `CatalogContext` 在 Hadoop class 不存在时不强制初始化 Hadoop configuration，`hadoopConf()` 缺失时 fail loud。 | 未发现等价修复 hunk；release 文件存在但无 nullable Hadoop conf/loading guard。 | `paimon-common/src/main/java/org/apache/paimon/catalog/CatalogContext.java` 存在。 | 中低。主要影响无 Hadoop classpath 且使用自定义 FileIO/catalog 的部署。 |
| `a23ac58d22` | Spark 4 module 依赖 4.0.2 -> 4.0.3。 | release 文件存在且仍为 4.0.2；这是版本 bump，不是核心逻辑 bugfix。 | `paimon-spark/paimon-spark-4.0/pom.xml` 存在。 | 低到中。仅 Spark 4.0.3 对齐需求相关。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `c3dd46447e` | Common catalog | `CatalogContext.hadoopConf` 改为 nullable；构造时捕获 Hadoop `NoClassDefFoundError` 并允许无 Hadoop context；`hadoopConf()` 在不可用时抛明确 `IllegalStateException`；新增无 Hadoop classloader 测试。 | 兼容性修复。release 文件存在但无修复 hunk；按 no-Hadoop 部署需求评估。 |
| 2 | `fb5b3436a3` | Python manifest commit | 新增 Python `manifest.target-file-size` / `manifest.merge-min-count` 选项、`ManifestFileMerger` minor compaction、`write_with_meta`，commit 时合并 base manifests 并清理失败准备阶段产生的新 manifest。 | 偏功能/性能；release Python manifest 文件存在但未含新 merger。不是紧急 bugfix。 |
| 3 | `c834ed39b7` | Python blob update conflict | 新增 Java/Python E2E 覆盖 blob compact conflict；commit scanner 支持读取 raw DELETE entries；conflict detection 对 COMPACT 删除的同类 blob anchor + row-id range + write columns 重叠返回错误；兼容 pyarrow `sort_by`/`pc.is_in` API 差异。 | release-relevant bugfix；见上表。 |
| 4 | `4c0d1ae58c` | Python table read | `_convert_rows_to_arrow_batch_with_row_kind` 改为可 yield 多个 batch；新增 `_emit_overflow_safe_batches` 递归拆分 pyarrow auto chunk；单行仍超限时抛清晰错误；新增单测模拟 `ChunkedArray`。 | release-relevant bugfix；见上表。 |
| 5 | `45895a719e` | Core/Python snapshot | Java `TimeTravelUtil` 复用 `SnapshotManager.retryEarliestSnapshot`；默认重试数 3 -> 300；耗尽后抛 `Cannot find earliest snapshot`；Python `SnapshotManager` 对齐 stop id/retry 逻辑；新增 Java/Python 单测。 | release-relevant bugfix；见上表。 |
| 6 | `7487409ac0` | Common predicate | `CompareUtils.compare(byte[])` 改用 unsigned byte 差值；新增 predicate tests 覆盖 `0x80`、`0xFF` 与 range/between。 | release-relevant bugfix；建议 backport。 |
| 7 | `4276e0cdd7` | Vector global index | `VectorGlobalIndexerFactory.nativeOptions` 增加 field name，先收集 index-type options，再以 `fields.<field>.<option>` 覆盖；文档新增 per-field vector options；测试覆盖 exact field、override、与 core field options 共存。 | `paimon-vector` release 文件缺失，release-1.4 直接影响低。 |
| 8 | `274d1de9c8` | Lumina vector index | `LuminaVectorGlobalIndexWriter` 解析 `VectorType#getLength()` 为实际 dimension；显式 dimension 冲突时报错；PQ `m` 按最终 dimension cap；新增 writer 单测。 | release Lumina 存在但无修复；仅 Lumina VECTOR 用户相关。 |
| 9 | `55f2be7386` | Core expiration | `FileDeletionBase` 将数据/manifest/index/stat 删除拆成 planning + parallel execution；manifest read 走 `ManifestReadThreadPool.sequentialBatchedExecute`；删除集合去重并支持 thread num 0；大量 expire 并发/失败/慢 IO 测试。 | 性能/重构为主。release 核心文件存在但不建议无需求大范围 backport。 |
| 10 | `a23ac58d22` | Spark 4 | `paimon-spark-4.0` 的 `spark.version` 从 4.0.2 升到 4.0.3，并同步注释。 | 低到中，仅 Spark 4.0.3 对齐相关。 |
| 11 | `6e1b5930b4` | Docs dependency | `docs/yarn.lock` 升级 `@babel/core` 7.29.0 -> 7.29.7 及其 Babel 子依赖。 | 无核心 release 影响。 |
| 12 | `cd54603213` | Docs dependency | `docs/yarn.lock` 升级 `launch-editor` 2.13.2 -> 2.14.1，并将 `shell-quote` range 调整到 `^1.8.4`。 | 无核心 release 影响。 |

## Checks Run

| Check | Result |
| --- | --- |
| GitHub compare `575ce805ed069ab380d762ee6d9556f8393c6e16..master` | `ahead_by=12`, current head `cd5460321378175dfb9aa704e40093761a7bab59`。 |
| GitHub commit fetch for all 12 commits | 每个提交读取了 diff/files，报告按实际 patch 归类。 |
| Local `upstream/release-1.4` key hunk checks | 确认 release head `53815aebfd8d`；关键 bugfix hunk 未出现在 release 分支。 |
| Local shell `git fetch upstream master release-1.4` | 受沙箱网络限制失败，提升审批超时；已用 GitHub API 数据补足。 |

## State Update

`reports/paimon-master-daily/state.json` 可前移到：

- `last_processed_commit`: `cd5460321378175dfb9aa704e40093761a7bab59`
- `last_processed_commit_date`: `2026-06-16 14:05:49 +0800`
- `last_report_date`: `2026-06-16`
- `last_report_path`: `reports/paimon-master-daily/2026-06-16.md`

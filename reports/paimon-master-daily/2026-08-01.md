# Apache Paimon Master Daily Report - 2026-08-01

## Summary

范围：`526b91f095a7683a3d5bc315550758b2520cb24f..5a82d2a6cf2a91bb998e2eae51e2823da96adf71`，共 6 个 first-parent 提交，23 个文件变更，`+832/-362`。本轮 `upstream/master` 前进到 `5a82d2a6cf2a`（2026-07-31 23:16:53 +0800）；`upstream/release-1.4` 仍为 `05d1ba75a97c`。

主要变化集中在 release workflow、release 文档、PyPaimon 版本/打包、Java Maven repository image 生成与签名/上传脚本，以及 Spark Hive UDF 测试资源去二进制化。所有提交相对 `upstream/release-1.4` 的 `git cherry` 结果均为 `+`，未发现等价补丁已合入。按实际 patch、patch-id、cherry 状态和 release-1.4 代码路径判断，本轮没有运行时生产 bugfix；最值得关注的 1.4 候选是 `5a82d2a6cf` 的 release package 内容修复，因为 1.4 仍跟踪 Spark 测试 JAR、`.gitignore` 仍允许 `resources/**/*.jar`，`create_source_release.sh` 也没有二进制文件阻断，且 PyPaimon sample data 存在但未被 `MANIFEST.in` 纳入。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Release workflow orchestration | 4 | `release.yml` 校验 PyPaimon 与 Maven 版本一致；把 shared release version 传给 PyPaimon package/publish；移除 Java workflow 对 Python 发布的 gating；Java release workflow 生成三条 JDK lane 的 Maven repository image 并合并。 | 1.4 没有这些 `.github/workflows/release*.yml` 路径；不能机械回摘。 |
| Release scripts | 2 | 新增 `sign_maven_repository.sh` 和 `deploy_maven_repository.sh`；删除三个按 JDK 直接 deploy 的旧 staging 脚本；source release 脚本新增 `.jar`/`.class` 检查。 | 1.4 有旧 deploy scripts 和 `create_source_release.sh`；repository image 流程是流程改造，source binary 检查是 1.4 候选。 |
| Release documentation | 5 | release 文档改成单一 Paimon/PyPaimon 版本、Java repository image 签名和 Nexus 上传流程；去掉跨环境 JAR checksum 必须一致的说明；候选验证文档同步。 | 1.4 缺少 `docs/docs/project/creating-a-release.md` 和 `verifying-a-release-candidate.md`；docs 不直接影响生产。 |
| PyPaimon packaging | 4 | `setup.py` 版本推进到 `2.1.dev`；PyPaimon workflow 用传入 release version 构造 RC/final 包；`MANIFEST.in` 纳入 sample data；workflow 校验 sdist/wheel 中 sample data。 | 1.4 有 `paimon-python/setup.py` 和 `MANIFEST.in`，sample data 未被 manifest 包含；属于 release/package 候选。 |
| Spark test resources | 1 | 删除跟踪的 `hive-test-udfs.jar`；新增 Java source `UDFExampleAdd2`，`FunctionResources.scala` 在测试运行时生成临时 JAR。 | 1.4 仍跟踪 3.4、3.5、4.0、ut 的 test JAR；source release 内容候选，非运行时生产路径。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `5a82d2a6cf` | 修复 release package 内容：PyPaimon sdist/wheel 必须包含 sample data；source release 发现 `.jar`/`.class` 立即失败；移除仓库中跟踪的 Spark Hive UDF 测试 JAR，改为测试运行时从 Java class 生成临时 JAR。 | `git cherry` 为 `+`；stable patch-id `8d6c56eaaa1a63ae4af84185d46411c17ea5b0ee`，未在 1.4 等价合入。 | 1.4 的 `.gitignore` 仍有 `!**/resources/**/*.jar`；仍跟踪 `paimon-spark-3.4/3.5/4.0` 和 `paimon-spark-ut` 下的 `hive-test-udfs.jar`；`tools/releasing/create_source_release.sh` 没有 binary guard；`paimon-python/MANIFEST.in` 未包含 `pypaimon/sample/data/*.jsonl`。 | 中高；影响 1.4 后续 RC/source/PyPaimon 包内容合规性，非运行时生产缺陷。建议若 1.4 还会切 RC，按 1.4 路径回补 binary guard、test JAR 去仓库化和 sample data manifest。 |
| `f1f8c3326b` | 把 Java release workflow 从上传 lane package 改为生成/合并 unsigned Maven repository image，并新增本地签名和 Nexus staging 上传脚本。 | `git cherry` 为 `+`；stable patch-id `11022d30075d8648eb7601bfcd0d5e47415e6c73`。 | 1.4 有旧 `deploy_staging_jars*.sh`，但没有 master 的 release workflow/doc 路径；脚本流程也从直接 deploy 改成 repository image 后本地签名上传。 | 中；是 release 流程可靠性改造，不是小型 bugfix。若 1.4 需要同流程，应做 1.4-specific 方案而非直接 cherry-pick。 |
| `9f65053657` / `c8987fc021` | 强制 Paimon 与 PyPaimon 使用同一 release version，并通过 workflow input 传递给 package/publish。 | `git cherry` 均为 `+`；patch-id 分别为 `5bcb994668f2193beadc2849005b0b79edcba893`、`31366f220a87e1d06a93ee2cb7f8aa25857e64c3`。 | 1.4 没有这些 release workflows；`paimon-python/setup.py` 当前是 `1.4.dev`，是否同 Maven release version 属于切版流程问题。 | 低到中；release 流程一致性候选，非生产 bugfix。 |
| `dd667cc49a` | Python 发布不再等待 Java packaging lane；Java workflow 仅在 RC tag 运行；文档说明 CI 不是本地 Java staging 的自动 gate。 | `git cherry` 为 `+`；patch-id `128ffd332f85893ea2a332b0e62d8706f87e8c78`。 | 1.4 缺少同名 release workflows/docs。 | 低；master release automation 行为调整。 |
| `b85955cb71` | 文档移除跨环境 JAR checksum 必须一致的要求，改为检查 manifest/artifact inventory。 | `git cherry` 为 `+`；patch-id `2e6d0ac8a7f6647c4a245fec852c7208c7e13ee3`。 | 1.4 缺少对应 release docs 路径。 | 低；docs-only。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `b85955cb71` | Release docs | `creating-a-release.md` 删除本地 JAR 与 CI package checksum 必须一致的校验步骤，改成检查 manifest、artifact inventory、lane/version/commit/JDK/module scope，并说明 archive order/build metadata 会导致 byte-level 差异。 | 1.4 没有该 docs 路径，docs-only。 |
| 2 | `9f65053657` | Release validation / PyPaimon version | `release.yml` 从 `paimon-python/setup.py` 读取 `VERSION` 并要求等于 Maven `project.version`；文档改成 Paimon/PyPaimon 共享版本；`setup.py` 从 `1.5.dev` 推到 `2.1.dev`。 | 1.4 无 release workflow；`setup.py` 同路径存在但此提交是 master 版本推进/流程校验。 |
| 3 | `c8987fc021` | Release workflow / docs | `release-python.yml` 和 `release-python-publish.yml` 增加 `release_version` input；RC/final PyPaimon 包版本由 Paimon release version 统一派生；release docs/verification docs 全部把 `PYPAIMON_VERSION` 替换为 `PAIMON_VERSION`。 | 1.4 无同 workflow/docs 路径；非运行时。 |
| 4 | `dd667cc49a` | Release gating | `release-java.yml` 增强 RC number 校验和 checksum 生成目录；`release.yml` 让 Java job 只依赖 tag/ref 条件，Python publish 只依赖 validation + python-package；文档说明 Java workflow artifact 是额外证据而非自动 gate。 | 1.4 无同 workflow/docs 路径。 |
| 5 | `f1f8c3326b` | Java release repository image | Java workflow 对 JDK8/11/17 lane 使用 `mvn deploy` 到 runner-local Maven repository，解析 effective POM 校验 deploy-enabled 项目、POM/JAR/source/javadoc 完整性、版本路径和无签名状态；合并三条 lane 并拒绝冲突坐标；新增签名与 Nexus staging 上传脚本，删除旧按 JDK 直接 deploy scripts。 | 1.4 有旧 scripts 但缺 workflow/docs；流程变更大，需 1.4-specific backport。 |
| 6 | `5a82d2a6cf` | Release package contents / Spark tests / PyPaimon | PyPaimon workflow 校验 sample data 进入 sdist/wheel；`MANIFEST.in` include `pypaimon/sample/data/*.jsonl`；`.gitignore` 删除 resources JAR 例外；Spark test UDF 改由 Java source 和 Scala helper 运行时生成临时 JAR；source release 脚本在打包前拒绝 `.jar`/`.class`。 | 中高；1.4 有同类 tracked test JAR、`.gitignore` 例外和未加 binary guard 的 source release 脚本，且 PyPaimon sample data 未进 manifest。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 位于 `codex/paimon-master-daily-report`，开始时工作区干净，并与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功，fork 自动化分支已刷新。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | 普通/full fetch 两次因 HTTPS recv timeout 失败；随后 `git fetch --depth=10 --no-tags --filter=blob:none upstream ...` 成功更新 `upstream/master`，再用 `git fetch --depth=10 --no-tags upstream ...` 补齐当前范围 patch 所需 blob。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功；`upstream/release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`。 |
| GitHub API ref/commit fallback | 在初始 fetch 超时后，用 GitHub API 确认 `master=5a82d2a6cf2a91bb998e2eae51e2823da96adf71`、`release-1.4=05d1ba75a97cc889b0c7988167464b691bd59173`，并预取 first-parent commit 列表；最终本地浅 fetch 成功后以本地 `git show` 为准。 |
| `git log --first-parent --reverse 526b91f095..upstream/master` | 6 个 first-parent commit。 |
| `git diff --shortstat 526b91f095..upstream/master` | 23 files，`+832/-362`。 |
| `git cherry -v upstream/release-1.4 upstream/master 526b91f095...` / per-commit `git cherry -v` | 6 个提交全部为 `+`，未发现 release-1.4 等价 patch。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <commit>` | 已为 6 个提交逐个执行；本地组合参数只输出 name-status，按执行规范另行读取 patch-only 输出。 |
| `git show --patch --stat --find-renames --format=fuller <commit>` | 已为 6 个提交逐个检查实际 patch hunks。 |
| `git patch-id --stable < <commit>.patch` | 已为 6 个提交生成 stable patch-id。 |
| release-1.4 targeted `git cat-file` / `git ls-tree` / `git show upstream/release-1.4:<path>` | 已核验 release workflow/docs 路径缺失、旧 deploy scripts 存在、`create_source_release.sh` 无 binary guard、`.gitignore` 仍允许 resources JAR、Spark test JAR 仍被跟踪、PyPaimon sample data 未进 `MANIFEST.in`。 |
| Build / unit tests | 未运行 Maven/Python 测试；本次只生成日报、推进 state 并发布自动化分支。 |

## State Update

`reports/paimon-master-daily/state.json` 更新为：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `5a82d2a6cf2a91bb998e2eae51e2823da96adf71`
- `last_processed_commit_date`: `2026-07-31 23:16:53 +0800`
- `last_report_date`: `2026-08-01`
- `last_report_path`: `reports/paimon-master-daily/2026-08-01.md`

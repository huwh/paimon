# Apache Paimon Master Daily Report - 2026-07-12

## Summary

范围：`1886d2f4e297cbb3e2f72b041387037488aebba7..fb3484df6e0053a94ed281f43e2dad09d04cf704`，共 3 个 first-parent 提交。当前 Apache upstream `master` head 是 `fb3484df6e00`（2026-07-12 07:37:36 +0800），`release-1.4` 对照 head 是 `05d1ba75a97c`（2026-06-27 08:49:02 +0800）。范围共 53 个文件，`+3518/-31`。

本次主线全部围绕 primary-key vector index：先引入 `pk-vector.index.columns` 配置、schema validation、ANN segment 文件和 source metadata；再添加 bucket-local 维护器，从 compact 输出数据文件恢复/维护 ANN payload；最后把维护器接入 primary-key 写入、Flink coordinator restore 和 compact increment 发布路径。写入路径只把 `FileSource.COMPACT && level > 0` 的完整 compact 文件作为 ANN source，并强制 level-0 rewrite，保证 vector payload 与 compact 数据文件转换在同一个 commit message 中发布。

`git cherry -v upstream/release-1.4 <commit> <commit>^` 显示 3 个提交均为 `+`，没有等价 patch 已进入 `upstream/release-1.4`。但这些提交是新功能栈，不是针对既有 1.4 生产路径的 bugfix；`release-1.4` 没有 `pkvector` 包、`pk-vector` 配置、`GlobalIndexMeta.sourceMeta()` 或 Flink write-restore 的 vector-index payload 扫描参数。因此本次没有明确的 `release-1.4` 直接 bugfix 回补项。

所有 commit 都先用 `git show --stat --name-status --patch --find-renames --format=fuller <commit>` 检查提交元数据和文件状态；由于当前 Git 在同时带 `--name-status` 时不展开 hunk，又对每个提交补跑 `git show --patch --stat --find-renames --format=fuller <commit>` 检查实际 patch。release-1.4 判断使用了 per-commit `git cherry`、stable patch-id、`git ls-tree`、`git grep` 和 targeted `git show upstream/release-1.4:<path>`。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core primary-key vector index foundation | 1 | `21d8e0ca` 新增 `pk-vector.index.columns` 及 field-scoped index type/options/metric 解析；限制首版单列 `VECTOR<FLOAT>`、primary-key table、fixed bucket、DV enabled、deduplicate/partial-update、禁止 DV merge-on-read 和 pk-clustering override；扩展 `GlobalIndexMeta` 为 source metadata 预留字段，并保持旧 5-field global-index row 兼容读取；新增 ANN segment build/search、source metadata 和 schema rename 防护。 | 1.4 缺 `pk-vector` 配置和 `sourceMeta`，不是直接 bugfix。 |
| Core bucket-level ANN maintenance | 1 | `50b0f704` 新增 `BucketedVectorIndexMaintainer`、bucket state、data-file vector reader 和 source policy；从 active compact files 与 active ANN payload 恢复 bucket state，删除引用失效 source 的 ANN segment，为未覆盖 compact source 构建新 ANN segment；`IndexManifestFileHandler` 允许 source-backed ANN segment 的 file-local row ranges 重叠。 | 1.4 缺 `pkvector` 包和 source-backed ANN payload；功能新增。 |
| Core/Flink write publication | 1 | `fb3484df` 在 `KeyValueFileStore` 创建 vector maintainer factory；`AbstractFileStoreWrite.prepareCommit` 把 vector index 增删挂到 append/compact increment；writer restore 扫描 vector payloads；Flink coordinator request/response 增加 `scanVectorIndexPayloads`；lookup compact 在 pk-vector index enabled 时强制 rewrite level-0 到高层 compact 文件。 | 1.4 写入 restore 接口只有 dynamic bucket index / deletion vector index 参数；无直接适配路径。 |
| Tests / generated docs | 3 | 新增 pk-vector option/validation/serializer/manifest/maintainer/data-reader/write/Flink coordinator 测试，更新 generated core configuration。 | 测试覆盖新功能栈；可作为未来 backport 参考，不是 1.4 bugfix。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | release-1.4 evidence | Likely impact |
| --- | --- | --- | --- | --- |
| `21d8e0ca` | 新增 primary-key vector index 基础能力和 metadata 兼容读取；不是修复 1.4 已有运行路径。 | stable patch-id `220e69821da5`; cherry `+`。 | `release-1.4` 的 `GlobalIndexMeta` 仍是 5-field schema，无 `sourceMeta()`；`git grep` 未找到 `pk-vector` / `primaryKeyVector`。 | 无直接 1.4 bugfix；如未来 backport pk-vector feature，需要从基础能力开始整体引入。 |
| `50b0f704` | 新增 bucket-local ANN 维护器；`IndexManifestFileHandler` 对 source-backed ANN segment 放宽 row-range overlap 校验。 | stable patch-id `6405622645f0`; cherry `+`。 | `release-1.4` 没有 `paimon-core/src/main/java/org/apache/paimon/index/pkvector`，也没有 source-backed ANN payload metadata。 | 无直接 1.4 bugfix；只在 backport pk-vector ANN payload 时相关。 |
| `fb3484df` | 激活 primary-key vector index 写入发布和 Flink coordinator restore；完整 compact L1+ 文件成为 ANN source，payload 增删与 compact increment 一起提交。 | stable patch-id `7dc604326494`; cherry `+`。 | `release-1.4` 的 `WriteRestore.restoreFiles` 只有 `scanDynamicBucketIndex` 和 `scanDeleteVectorsIndex`，没有 vector payload restore；Flink coordinator 也无对应 request/response 字段。 | 无直接 1.4 bugfix；这是 master 新功能发布路径。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `21d8e0ca` | Core pk-vector foundation | 21 files，`+1812/-11`。`CoreOptions` 新增 `PK_VECTOR_INDEX_COLUMNS`，支持 field-scoped algorithm options JSON、distance metric 规范化和单列解析；`SchemaValidation` 校验 primary-key、fixed bucket、DV enabled、merge engine、`VECTOR<FLOAT>`、metric；`GlobalIndexMeta` / serializers 增加 `_SOURCE_META` 并兼容旧 row；新增 `PkVectorAnnSegmentFile` 将 source-file row position 写为 ANN id，`PkVectorAnnSegmentSearcher` 将 ANN ordinal 映射回 source file/row position 并过滤 DV；新增 source metadata、schema rename 防护和测试。 | cherry `+`，patch-id `220e69821da5`。1.4 无配置、metadata 和 pkvector 类，非直接 bugfix。 |
| 2 | `50b0f704` | Core bucket maintenance | 11 files，`+1182/-0`。`BucketedVectorIndexMaintainer` 从 restored data files 和 active ANN payloads 构造 bucket state，拒绝 append files 作为 index source，只读取 `FileSource.COMPACT && level > 0` 的文件；compact 前后对比删除旧 ANN、为未覆盖 source 构建新 ANN，并验证 source row count 与 coverage；`PkVectorDataFileReader` 只投影 vector value 且不应用 deletion-vector filtering，保留物理 row position；manifest handler 允许带 source metadata 的 ANN segment 在同一 field 上有重叠 row ranges。 | cherry `+`，patch-id `6405622645f0`。1.4 缺 source-backed ANN payload 和维护器，不直接适用。 |
| 3 | `fb3484df` | Core/Flink write publication | 22 files，`+524/-20`。`KeyValueFileStore` 为 enabled pk-vector table 创建 maintainer factory；`AbstractFileStoreWrite` restore 时取回 vector payloads，prepareCommit 时把 maintainer 产生的 index add/delete 挂入 append/compact increment；`FileSystemWriteRestore` 和 Flink `CoordinatedWriteRestore` / `TableWriteCoordinator` 支持扫描 source-backed vector index payloads；`LookupMergeTreeCompactRewriter` 在 pk-vector enabled 时强制 level-0 rewrite，确保 source 文件是完整 compact 输出；新增 write、restore、Flink coordinator、compact rewriter 测试。 | cherry `+`，patch-id `7dc604326494`。1.4 写入/Flink restore 接口无 vector payload 参数，功能新增。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git status --short --branch` | 开始时位于 `codex/paimon-master-daily-report`，工作区干净，本地与 `origin/codex/paimon-master-daily-report` 对齐。 |
| `git remote -v` | `origin=https://github.com/huwh/paimon.git`，`upstream=https://github.com/apache/paimon.git` 已存在。 |
| `git fetch origin refs/heads/codex/paimon-master-daily-report:refs/remotes/origin/codex/paimon-master-daily-report` | 成功。 |
| `git fetch upstream refs/heads/master:refs/remotes/upstream/master` | 成功；`upstream/master` 前进到 `fb3484df6e00`。 |
| `git fetch upstream refs/heads/release-1.4:refs/remotes/upstream/release-1.4` | 成功；`release-1.4` 保持 `05d1ba75a97c`。 |
| `git log --first-parent --reverse 1886d2f4e297..upstream/master` | 3 个 first-parent commit，均已纳入报告。 |
| `git diff --stat --shortstat 1886d2f4e297..upstream/master` | 53 files changed, 3518 insertions(+), 31 deletions(-)。 |
| `git show --stat --name-status --patch --find-renames --format=fuller <3 commits>` | 对 3 个 commit 均已执行，作为文件状态和命令审计入口。 |
| `git show --patch --stat --find-renames --format=fuller <3 commits>` | 对 3 个 commit 均已执行并检查实际 patch hunks；关键路径补充 targeted `git show`。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 3 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| `git show <commit> \| git patch-id --stable` | 对 3 个 commit 计算 stable patch-id。 |
| release-1.4 targeted code checks | 检查 `pkvector` 包、`pk-vector` 配置、`GlobalIndexMeta.sourceMeta()`、`WriteRestore` / Flink coordinator vector payload restore 路径；release-1.4 均缺失。 |
| Build / unit tests | 未运行 Maven 测试；本次只新增自动化报告和 state 更新，验证重点是 git 数据收集、patch 审查和 release 路径检查。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `release_impact_commit`: `05d1ba75a97cc889b0c7988167464b691bd59173`
- `last_processed_commit`: `fb3484df6e0053a94ed281f43e2dad09d04cf704`
- `last_processed_commit_date`: `2026-07-12 07:37:36 +0800`
- `last_report_date`: `2026-07-12`
- `last_report_path`: `reports/paimon-master-daily/2026-07-12.md`

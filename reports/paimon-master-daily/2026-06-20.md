# Apache Paimon Master Daily Report - 2026-06-20

## Summary

范围：`04282834bb4962387e476f8f789739b98aa70aa9..cca01af0fdda277171212a27c16ba2311850428d`，共 12 个 first-parent 提交。当前 Apache upstream `master` head 是 `cca01af0fdda`（2026-06-20 13:11:20 +0800），`release-1.4` 对照 head 是 `53815aebfd8`。

本次主线变化集中在写入/提交正确性、Python global-index 读取能力、Flink lineage namespace、OSS 原子写、以及 vector/vindex 模块重组。明确的 bugfix 包括 lookup changelog overwrite 生成孤儿 changelog 文件、write-only snapshot sequence bucket rescale 检查、row-tracking compaction 后 manifest sequence number 错误、Flink lineage namespace 使用 table path、Python REST catalog 读 current schema 触发 schema manager 访问、OSS `tryToWriteAtomic`、commit retry 重复 manifest merge 等。

`release-1.4` 方面，12 个提交经 `git cherry -v upstream/release-1.4 <commit> <commit>^` 检查均为 `+`，未发现等价 patch 已 cherry-pick。优先关注 `7b27a4c25e`、`022ad03332`、`98afc10300`、`cca01af0fd`：release-1.4 仍有对应旧生产路径。`64380bbf8a` 与 `16f0d42161` 也在 release-1.4 存在同类旧逻辑，影响取决于是否依赖 Flink lineage 或 Python REST catalog 读路径。`926459dd77` 需要额外验证 release 的 write-only snapshot sequence bucket-rescale 行为，因为 release 代码路径和 master patch 位置不同。

本次本地分支先通过 `git rebase origin/codex/paimon-master-daily-report` 对齐了上次 connector 发布的等价提交，然后 fetch `origin`、`upstream master`、`upstream release-1.4`。所有 commit 都用 `git show --stat --name-status --patch` 查看文件状态，并补跑 `git show --stat --patch` 读取实际 hunks 后分类。

## Module Summary

| Module | Commits | Actual patch focus | release-1.4 note |
| --- | ---: | --- | --- |
| Core write / commit correctness | 5 | lookup changelog overwrite 禁用 lookup path；snapshot sequence append 纳入 bucket rescale check；row-tracking file writer 记录实际 min/max sequence；commit retry 复用或跳过 manifest merge；新增 ListUtils 子列表替换工具。 | `7b27a4c25e`、`022ad03332`、`cca01af0fd` 在 release-1.4 有明显旧路径；`926459dd77` 需用目标测试确认。 |
| Filesystem / OSS | 1 | `PluginFileIO` 转发 `tryToWriteAtomic`，`OSSFileIO` 使用 OSS `x-oss-forbid-overwrite` 的 `putObject` 做原子写，并处理已存在对象。 | release-1.4 OSS 实现没有该 override，高优先级回补候选。 |
| Python global index / read path | 3 | Python 支持 Java 写出的 bitmap global index；抽象 sorted index reader/meta selector；SplitRead/FileScanner/manifest 读取 current schema 时短路内存 schema，避免 REST catalog 访问失败。 | bitmap/vindex 多为新能力；`16f0d42161` 是 release-1.4 可见的 Python 读路径 bugfix。 |
| Vector index / vindex | 1 | `paimon-vector` 合并旧 `paimon-vector-index` 与 `paimon-vector-jni` 子模块，切到 `paimon-vector-index-java`；Python 通过 `paimon-vindex` 读 vindex vector index。 | 大型能力/依赖重组，不适合作为小 bugfix 回补。 |
| Flink lineage | 1 | lineage namespace 从 `paimon://<table path>` 改为 catalog warehouse；config facet 只加入安全 allowlist 的 catalog options。 | release-1.4 仍用 table path namespace；对 lineage 消费方有中等影响。 |
| Docs dependencies | 2 | `docs/yarn.lock` 升级 `webpack-dev-server` 5.2.4 -> 5.2.5、`undici` 7.25.0 -> 7.28.0。 | docs-only 依赖更新，非 Paimon runtime。 |
| Hybrid search ranking | 1 | RRF route-level ranking 对同分结果按 rowId 升序打破平局，和 final topK 规则一致。 | release-1.4 未见 `HybridSearchRanker`，不构成直接回补项。 |

## Bugfix release-1.4 Impact

| Commit | Bugfix / behavior | Patch-id / cherry status | Touched production files on release-1.4 | Likely release-1.4 impact |
| --- | --- | --- | --- | --- |
| `7b27a4c25e` | Primary-key table 在 `changelog-producer=lookup` 下执行 overwrite 时，不再走 lookup changelog path，避免生成未被最新 table data 引用的孤儿 `changelog-*` 文件。 | `+`，未 cherry-pick。 | release-1.4 中 `MergeTreeCompactManagerFactory` 仍直接使用 `options.needLookup()`；`BatchWriteBuilderImpl.newWrite()` 仍用 `staticPartition != null` 判断 `ignorePreviousFiles`。 | 高。release-1.4 有相同 lookup changelog/overwrite 路径，建议优先评估 backport。 |
| `926459dd77` | write-only 且 `write.sequence-number-init-mode=snapshot` 的 APPEND commit 也检查 bucket 数一致性，阻止 `INSERT INTO` 在 rescale 前写入新 bucket 布局。 | `+`，未 cherry-pick。 | release-1.4 没有 master 的 `FileStoreCommitImpl.shouldCheckSameBucket` 方法；但存在 write-only、snapshot sequence、bucket mismatch 相关 writer/commit 逻辑。 | 中。代码路径不完全相同，建议先在 release-1.4 跑新增 `testSnapshotSequenceInsertIntoCheckSameBucketAndInsertOverwriteRescale` 形态的验证，再决定回补方式。 |
| `bf9333ea6` | Hybrid RRF route-level ranking 对 score tie 使用 rowId 升序，结果更稳定。 | `+`，未 cherry-pick。 | release-1.4 未命中 `HybridSearchRanker`，只存在较底层 `ScoredGlobalIndexResult.topK`。 | 低。主线 hybrid search 新能力相关，release-1.4 无直接代码路径。 |
| `022ad03332` | row-tracking compaction 后，manifest 中 data file 的 min/max sequence number 改为从实际 `_SEQUENCE_NUMBER` 字段统计；混合旧记录和更新记录时 max 可正确归到当前 snapshot。 | `+`，未 cherry-pick。 | release-1.4 `RowDataFileWriter` 仍用全局 `seqNumCounter` 推导 min/max；`RowTrackingCommitUtils.assignSnapshotId` 只处理 `minSequenceNumber == 0`。 | 高。release-1.4 有 row-tracking 生产路径，compaction/update 混合文件可能产生错误 sequence range，建议 backport。 |
| `64380bbf8a` | Flink lineage dataset namespace 改为 catalog warehouse，而不是具体 table path；config facet 增加 `type=paimon` 与安全 catalog option。 | `+`，未 cherry-pick。 | release-1.4 `LineageUtils` 仍用 `paimon://` + table physical path 作为 namespace。 | 中。影响 lineage 标识稳定性和上游系统聚合，建议按 lineage 使用情况评估。 |
| `16f0d42161` | Python `SplitRead`、`FileScanner`、manifest 解析在 schema id 等于 current table schema 时直接使用内存 schema，避免 REST catalog 读当前 schema 文件时 403。 | `+`，未 cherry-pick。 | release-1.4 Python 仍多处直接 `schema_manager.get_schema(schema_id)`，包括 `manifest_file_manager.py`、`file_scanner.py`、`split_read.py`。 | 中到高。若 release-1.4 支持 Python REST catalog 读取，这是明确 bugfix。 |
| `98afc10300` | OSS `tryToWriteAtomic` 改用带 `x-oss-forbid-overwrite=true` 的 atomic `putObject`，并让 `PluginFileIO` 正确转发该方法。 | `+`，未 cherry-pick。 | release-1.4 `OSSFileIO` 没有 `tryToWriteAtomic` override；`PluginFileIO` 也没有该转发。 | 高。OSS 上依赖 atomic create 的 commit/lock/metadata 写入更容易受非原子 rename 或默认实现影响，建议评估回补。 |
| `cca01af0fd` | commit retry 时复用第一次 manifest merge 结果；若前次 merge 结果无法安全替换，则跳过 retry 中重复 merge，以保留 manifest 顺序和降低冲突重试开销。 | `+`，未 cherry-pick。 | release-1.4 `FileStoreCommitImpl` retry 路径每次仍调用 `ManifestFileMerger.merge`；`RetryCommitResult` 不保存 manifest merge result。 | 中到高。对高并发 commit、manifest full compaction、冲突重试场景有 correctness/performance 价值，建议 backport 前跑新增 retry tests。 |
| `de0e1bcbe` | docs `webpack-dev-server` lockfile bump。 | `+`，未 cherry-pick。 | docs lockfile only。 | 低，非 runtime。 |
| `c4d26da4a` | docs `undici` lockfile bump。 | `+`，未 cherry-pick。 | docs lockfile only。 | 低，非 runtime。 |

## Commit Details

| # | Commit | Module | Patch-based details | release-1.4 assessment |
| ---: | --- | --- | --- | --- |
| 1 | `7b27a4c25e` | Core lookup changelog / overwrite | `AbstractFileStoreWrite` 把 `ignorePreviousFiles` 传入 writer 创建；`KeyValueFileStoreWrite` 计算 `lookupEnabled = !ignorePreviousFiles && options.needLookup()`；compact manager factory 接收 `lookupEnabled` 并只在非 overwrite 时启用 lookup compaction/changelog；Spark IT 断言 `INSERT OVERWRITE` 不新增 changelog 文件。 | 高优先级 bugfix，release 有旧路径。 |
| 2 | `926459dd77` | Core bucket rescale guard | `FileStoreCommitImpl.shouldCheckSameBucket` 从仅检查 unordered write-only append 扩展为也检查 write-only snapshot sequence append；新增 core 和 Flink IT 覆盖 `INSERT INTO` 拒绝 rescaled bucket，`INSERT OVERWRITE` 允许重写布局。 | 中，需在 release 路径复现确认。 |
| 3 | `bf9333ea6` | Hybrid search ranking | `HybridSearchRanker` 在 route score 相同的时候按 rowId 升序排序；新增测试构造非自然 bitmap iteration order，验证 RRF topK 稳定。 | 低，release 未见该 ranker。 |
| 4 | `76e32fc68f` | Python bitmap global index | 新增 Python bitmap index reader、lazy filtered bitmap reader、shared sorted index meta/serializer/input；BTree reader 复用 shared sorted path；Java/Python E2E 覆盖 Java 写 bitmap、Python 读 equal/null/in/range/string predicate。 | 新能力为主，不建议单独回补。 |
| 5 | `022ad03332` | Core row tracking sequence | 新增 `RowDataFileSequenceNumberTracker`；`RowDataFileWriter` 从行内 `_SEQUENCE_NUMBER` 统计实际 min/max；`RowTrackingCommitUtils` 对 `maxSequenceNumber == 0` 的混合文件只把 max 归到当前 snapshot；Spark row-tracking 测试覆盖 compact/update 后 sequence range。 | 高优先级 bugfix，release 有旧路径。 |
| 6 | `64380bbf8a` | Flink lineage | `LineageUtils` 从 `FileStoreTable`/`FormatTable` 提取 `CatalogContext`；namespace 优先 catalog warehouse，fallback table path；config facet 只加入 warehouse/metastore allowlist，避免泄漏其他 catalog option。 | 中，按 lineage 使用情况评估。 |
| 7 | `f88febf0c5` | Vector / Python vindex | 删除旧 `paimon-vector-index` 与 `paimon-vector-jni` 子模块，把 `paimon-vector` 变成单模块并引入 `paimon-vector-index-java`；Java native vector 类迁移到 `org.apache.paimon.vector.index`；Python 新增 `VindexVectorGlobalIndexReader` 和 mixed tests。 | 大型能力/依赖重组，非 release 小修。 |
| 8 | `de0e1bcbe` | Docs dependency | `docs/yarn.lock` 中 `webpack-dev-server` 从 5.2.4 升到 5.2.5。 | docs-only。 |
| 9 | `c4d26da4a` | Docs dependency | `docs/yarn.lock` 中 `undici` 从 7.25.0 升到 7.28.0。 | docs-only。 |
| 10 | `16f0d42161` | Python read/schema lookup | `ManifestFileManager`、`FileScanner`、`SplitRead` 对 current schema id 短路为 `table.table_schema`，只有历史 schema 才访问 `schema_manager.get_schema`；新增 FileScanner schema fields tests。 | 中到高，Python REST catalog 用户相关。 |
| 11 | `98afc10300` | OSS atomic write | `PluginFileIO.tryToWriteAtomic` 透传底层 FileIO；`OSSFileIO.tryToWriteAtomic` 直接用 OSSClient `putObject`，设置 forbid-overwrite header、content length 和可选 SSE，已存在对象返回 false。 | 高，OSS atomic write 回补候选。 |
| 12 | `cca01af0fd` | Core commit retry manifest merge | `RetryCommitResult` 保存 `ManifestMergeResult`；`FileStoreCommitImpl` 在 retry 时尝试用 `ListUtils.tryReplace` 复用前次 merge 结果，无法复用时跳过重复 merge；新增多种冲突/空表/delete order 测试。 | 中到高，commit retry/manifest compaction 场景相关。 |

## Checks Run

| Check | Result |
| --- | --- |
| `git fetch origin codex/paimon-master-daily-report` | 成功；远端分支更新到上次 connector commit `853bafa5a`。 |
| `git rebase origin/codex/paimon-master-daily-report` | 成功；跳过本地等价旧提交 `f4feb3e84`。 |
| `git fetch upstream master release-1.4` | 成功；`upstream/master=cca01af0fdda277171212a27c16ba2311850428d`，`upstream/release-1.4=53815aebfd8d2d661be0c0bc5d7b54c6249bdec5`。 |
| `git log --first-parent --reverse 04282834bb4962387e476f8f789739b98aa70aa9..upstream/master` | 12 个 commit，均已纳入本报告。 |
| `git show --stat --name-status --patch <commit>` | 对 12 个 commit 均已执行，用于文件状态和 patch 检查入口。 |
| `git show --stat --patch <commit>` | 对 12 个 commit 均补跑并检查实际 hunks，因为本 Git 版本中 `--name-status` 会覆盖 patch 输出。 |
| `git cherry -v upstream/release-1.4 <commit> <commit>^` | 12 个 commit 均为 `+`，未发现等价 patch 已在 release-1.4。 |
| release-1.4 targeted code checks | 检查 lookup changelog、bucket rescale、row tracking、lineage、Python schema lookup、OSS atomic write、manifest retry 等关键生产路径。 |

## State Update

`reports/paimon-master-daily/state.json` 前移到：

- `last_processed_commit`: `cca01af0fdda277171212a27c16ba2311850428d`
- `last_processed_commit_date`: `2026-06-20 13:11:20 +0800`
- `last_report_date`: `2026-06-20`
- `last_report_path`: `reports/paimon-master-daily/2026-06-20.md`
